/******/ (function() { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ 5117:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "html,body,ui-root,.app,ui-presenter{width:100%;height:100%}html{color:#555}*{box-sizing:border-box;transition-property:all;transition-timing-function:linear;font-family:\"Segoe UI\", Tahoma, Geneva, Verdana, sans-serif;text-decoration:none;color:inherit;cursor:inherit;margin:0;padding:0;user-select:none;outline:none}input,textarea{outline:none;font-size:24px;padding:3px 5px}a:visited{color:inherit}::-webkit-scrollbar{width:10px}::-webkit-scrollbar-track{background:#f1f1f1}::-webkit-scrollbar-thumb{background:#888}::-webkit-scrollbar-thumb:hover{background:#555}.text{user-select:text}h1,h2,h3,h4{font-weight:400;color:#202122}h1{font-size:47px}h2{font-size:37px}button{display:inline-block;background-color:#fff;border:1px solid #e1e1e1;text-transform:uppercase;padding:5px 7px;cursor:pointer}button:hover{border-color:aquamarine}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 83097:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "main-app{width:100%;height:100%;display:block}main-app>.container{width:100%;height:100%;display:block}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 80764:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "app-editable-text span{cursor:pointer;padding:5px}app-editable-text span:hover{background-color:rgba(255,255,255,0.5)}app-editable-text input{display:none;border:none}app-editable-text.editing span{display:none}app-editable-text.editing input{display:block}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 47961:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "app-loader{display:flex;position:absolute;top:0;left:0;width:100%;height:100%;background-color:rgba(255,255,255,0.5);align-items:center;justify-content:center}app-loader .loader{border:8px solid #f3f3f3;border-top:8px solid #3498db;border-radius:50%;width:120px;height:120px;animation:spin 2s linear infinite}@keyframes spin{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 6636:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "@keyframes placeHolderShimmer{0%{background-position:100% 0}100%{background-position:-100% 0}}loading-content{background-color:#e1e1e1;animation-duration:1s;animation-fill-mode:forwards;animation-iteration-count:infinite;animation-name:placeHolderShimmer;animation-timing-function:ease-in;background:#f6f6f6;background:linear-gradient(to right, transparent 8%, #fff 100%, transparent 33%);background-size:200%;width:100%;height:100%}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 25745:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "mark-down{display:block}mark-down .model{display:inline-block;position:relative;width:fit-content}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 32575:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "content-summary{display:block;top:20px;padding:0 10px;padding-right:30px;min-width:200px;text-align:right}content-summary header{padding:5px;cursor:pointer}content-summary header h3{font-size:18px;font-weight:300}content-summary header:hover{background-color:#f9fafb;border-radius:10px}content-summary .sections{margin-top:7px}content-summary .sections .section{font-weight:400;font-size:17px;border-right:1px solid #e1e1e1;padding:5px 10px;cursor:pointer}content-summary .sections .section:hover,content-summary .sections .section.current{color:cornflowerblue;border-right:3px solid cornflowerblue;background-color:rgba(225,225,225,0.3)}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 58363:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "tool-tip{display:none;position:absolute;z-index:10;background-color:#e1e1e1;border:1px solid rgba(225,225,225,0.5);width:max-content;padding:5px;top:0;left:calc(100% + 10px)}tool-tip.show{display:block}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 53095:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "wheel-select{display:block;position:relative;overflow:hidden;height:40px}wheel-select .options{transition-duration:0.2s;position:absolute;z-index:99999;top:0;left:0;height:40px;background-color:#fff;border:1px solid #e1e1e1}wheel-select .options .option{height:40px;border-bottom:1px solid #e1e1e1;padding:5px;cursor:pointer}wheel-select .options .option.current{color:red}wheel-select .options .option:hover{background-color:aquamarine}wheel-select:hover{overflow:visible}wheel-select:hover .options{height:auto}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 57778:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "empty-layout{display:block;width:100%;height:100%}empty-layout>.container{width:100%;height:100%}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 6380:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "app-layout-main{width:100%;height:100%;display:flex;flex-direction:column}app-layout-main>.top-bar{height:50px;width:100%;padding-right:30px;display:flex;flex-direction:row;align-items:center;border-bottom:1px solid #e1e1e1;background-color:#ecf0f1}app-layout-main>.top-bar .burger{margin-left:20px;cursor:pointer;transform:rotate(0deg);transition-duration:0.2s}app-layout-main>.top-bar .burger:hover{color:red;transition-duration:0.2}app-layout-main>.top-bar h3{margin-left:20px}app-layout-main>.top-bar .search{flex:1;margin-left:50px}app-layout-main>.row{width:100%;flex:1;display:flex;flex-direction:row;overflow:hidden}app-layout-main>.row>.left-bar{width:175px;height:100%;border-right:1px solid #e1e1e1;background-color:#ecf0f1;overflow:hidden;transition-duration:0.2s}app-layout-main>.row>.left-bar .menu .link{display:flex;flex-direction:row;width:200px;cursor:pointer;padding:5px}app-layout-main>.row>.left-bar .menu .link .image{width:17px;height:17px}app-layout-main>.row>.left-bar .menu .link .image svg{width:100%;height:100%}app-layout-main>.row>.left-bar .menu .link label{margin-left:20px;flex:1}app-layout-main>.row>.left-bar .menu .link:hover,app-layout-main>.row>.left-bar .menu .link.active{background-color:#3498db;color:#fff}app-layout-main>.row>.container{flex:1}app-layout-main.open>.top-bar>.burger{transform:rotate(90deg);transition:transform ease 0.2s}app-layout-main.open>.row .left-bar{width:27px}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 17960:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "mo-dal{display:none;border:1px solid #e1e1e1;background-color:#fff;min-height:200px;min-width:350px;padding:15px}mo-dal>header{position:relative;display:flex;flex-direction:row;align-items:center;height:30px}mo-dal>header .title{display:block;font-size:23px;flex:1}mo-dal .actions{position:absolute;top:0;right:0}mo-dal .actions div{display:flex;flex-direction:column;align-items:center;justify-content:center;width:30px;height:30px;cursor:pointer}mo-dal .actions div:hover{background-color:#e1e1e1}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 38802:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "modal-container{position:absolute;width:100%;height:100%;top:0;left:0;display:none;flex-direction:column;align-items:center;justify-content:center}modal-container.has-modal{display:flex;background-color:rgba(128,128,128,0.5)}modal-container mo-dal{display:block !important}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 99665:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "entity-card{display:block;background-repeat:no-repeat;background-size:contain;background-position:center;background-color:rgba(225,225,225,0.5);position:relative;overflow:hidden;width:250px;height:150px}entity-card header{display:flex;flex-direction:row;align-items:center;padding:5px 10px;background-color:rgba(255,255,255,0.9);color:dimgrey;border:1px solid #e1e1e1}entity-card header img{width:20px;height:20px}entity-card header label{flex:1;margin-left:10px}entity-card .content{overflow-y:scroll;height:150px;background-color:#fff;position:absolute;top:100%;transition-duration:0.2s;font-size:13px;padding:10px;text-align:justify;border:1px solid #e1e1e1}entity-card .content::-webkit-scrollbar{width:3px}entity-card:hover .content{transition-duration:0.2s}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 96592:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "app-map{display:block;width:100%;height:100%}app-map .outer{display:block;width:100%;height:100%;position:relative;overflow:hidden}app-map .inner{position:absolute;cursor:all-scroll;transition-duration:0.1s;transition-property:transform;position:absolute}app-map .inner .layers .layer{position:absolute;transform:translateX(-50%) translateY(-50%);transition-duration:0.2ms;border-radius:100%}app-map .inner .layers .layer svg{overflow:visible}app-map .inner .layers .layer svg circle{cursor:pointer;fill:rgba(52,152,219,0.1);transition-duration:0.2s}app-map .inner .layers .layer svg circle:hover{stroke:aquamarine}app-map .inner .object{top:0px;left:0px;position:absolute;width:35px;height:35px;background-color:#e1e1e1;border-radius:100%;transform:translateX(-50%) translateY(-50%);display:flex;flex-direction:row;align-items:center;justify-content:center;transition-duration:0.5s;transition-property:all;border:1px solid #a1a1a1;font-size:22px;text-transform:uppercase;font-weight:400;cursor:pointer}app-map .inner .object:hover{background-color:aquamarine}app-map .inner .object label{position:absolute;top:-70%;left:120%;font-weight:500}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 35618:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 447:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 99275:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "app-system-card{display:block;background-size:cover;position:relative;overflow:hidden;width:250px;height:150px}app-system-card header{display:flex;flex-direction:row;align-items:center;padding:5px 10px;background-color:rgba(255,255,255,0.9);color:dimgrey;border:1px solid #e1e1e1}app-system-card header img{height:100%}app-system-card header label{flex:1}app-system-card .content{overflow-y:scroll;height:150px;background-color:#fff;position:absolute;top:100%;transition-duration:0.2s;font-size:13px;padding:10px;text-align:justify;border:1px solid #e1e1e1}app-system-card .content::-webkit-scrollbar{width:3px}app-system-card:hover .content{transition-duration:0.2s}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 4350:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "organization-little{display:flex;flex-direction:row;align-items:center;border:1px solid #e1e1e1;background-color:rgba(255,255,255,0.7);padding:3px 5px;width:fit-content}organization-little .color{padding:5px;border-radius:5px}organization-little label{margin-left:5px;font-size:11px;font-weight:500}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 17155:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "organization-row .icon{padding:5px;border-radius:100%}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 83098:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "app-notifications-list{position:absolute;top:15%;left:75%;width:25%;overflow:hidden}app-notifications-list .notifications{display:flex;flex-direction:column}app-notifications-list .notifications .notification{display:flex;flex-direction:row;background-color:rgba(255,255,255,0.8);margin-bottom:10px;cursor:pointer;white-space:nowrap;padding:8px;border:1px solid #e1e1e1}app-notifications-list .notifications .notification label{margin-left:10px}app-notifications-list .notifications .notification.closed{margin-left:1000px;transition-duration:0.5s}app-notifications-list .notifications .notification.info{background-color:blue;color:white}app-notifications-list .notifications .notification.success{background-color:green;color:white}app-notifications-list .notifications .notification.error{background-color:red;color:white}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 16734:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, ".action{background-color:#ecf0f1;color:#a1a1a1}.action.warning{background-color:#c55;color:#fff}.action.active,.action:hover{background-color:#3599db;color:#fff}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 50368:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "context-menu{display:none;position:absolute;min-width:200px;background:#fff;border:1px solid #e1e1e1;border-bottom:none;box-shadow:10px 5px 5px #e1e1e1;transform:translateY(100%)}context-menu.open{display:flex;flex-direction:column}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 12986:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "context-menu-item{display:block;width:100%;padding:5px;border-bottom:1px solid #e1e1e1;cursor:pointer}context-menu-item:hover,context-menu-item.selected{background-color:rgba(52,152,219,0.3)}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 69688:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "editable-grid{display:block;width:100%;height:100%}editable-grid.hover .object{pointer-events:none}editable-grid .grid-panel>.content{overflow:auto}editable-grid .root{width:100%;height:100%}editable-grid .root .column{height:100%;display:flex;flex-direction:column}editable-grid .root .column .row{width:100%;height:100%;display:flex;flex-direction:row;flex:1}editable-grid .root .column .row:not(:last-child){border-bottom:1px solid #e1e1e1}editable-grid .root .column .row .cell{flex:1}editable-grid .root .column .row .cell:not(:last-child){border-right:1px solid #e1e1e1}editable-grid .root .column .row.hover.left::before{content:\" \";background-color:aquamarine;width:50px;height:100%}editable-grid .root .column .row.hover.right::after{content:\" \";background-color:aquamarine;width:50px;height:100%}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 8881:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, ".grid-panel{display:flex;width:100%;height:100%;flex-direction:column}.grid-panel:not(:last-child){border-right:1px solid #e1e1e1}.grid-panel>header{height:35px;display:flex;flex-direction:row;border-bottom:1px solid #e1e1e1}.grid-panel .content{flex:1;position:relative}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 75958:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "grid-tab-panel>header .tabs{display:flex;flex-direction:row}grid-tab-panel>header .tabs .tab{padding:5px 10px;display:flex;flex-direction:row;align-items:center;border-right:1px solid #e1e1e1;cursor:pointer}grid-tab-panel>header .tabs .tab.current{background-color:#e1e1e1}grid-tab-panel>header .tabs .tab.selected.current,grid-tab-panel>header .tabs .tab:hover{background-color:aquamarine}grid-tab-panel>header .tabs .tab:hover>div,grid-tab-panel>header .tabs .tab.selected>div{visibility:visible}grid-tab-panel>header .tabs .tab>div{padding:5px;display:flex;visibility:hidden;align-items:center;justify-content:center;margin-left:10px}grid-tab-panel>header .tabs .tab>div:hover{background-color:rgba(255,255,255,0.3)}grid-tab-panel>.content>.container{width:100%;height:100%}grid-tab-panel>.content>.container .object{width:100%;height:100%}grid-tab-panel>.content>.container .hover{display:none;position:absolute;transition-duration:0.1s;background:rgba(127,255,212,0.3);pointer-events:none}grid-tab-panel>.content>.container .hover.left{left:0;top:0;width:50%;height:100%}grid-tab-panel>.content>.container .hover.right{left:50%;top:0;width:50%;height:100%}grid-tab-panel>.content>.container .hover.top{left:0;top:0;width:100%;height:50%}grid-tab-panel>.content>.container .hover.bottom{left:0;top:50%;width:100%;height:50%}grid-tab-panel>.content>.container .hover.full{left:0;top:0;width:100%;height:100%}grid-tab-panel>.content>.container .hover.visible{display:block}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 83570:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "ui-root{width:100%;height:100%}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 98223:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "short-cut{cursor:pointer;padding:7px 10px;font-size:15px;width:100%;display:flex;flex-direction:row;align-items:center}short-cut:hover{background-color:#e1e1e1}short-cut .key-container{flex:1}short-cut .key-container .key{width:fit-content;display:flex;flex-direction:row;align-items:center;justify-content:center;background-color:#e1e1e1;padding:5px 10px;text-transform:uppercase;outline:2px outset #b1b1b1;font-weight:700;font-size:16px}short-cut .transclude{margin-left:10px;text-transform:uppercase;font-weight:600}short-cut:not(:last-child){border-bottom:1px solid #e1e1e1}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 67269:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "body.shortcut short-cuts{display:flex}short-cuts{display:none;flex-direction:column;align-items:start;position:absolute;border:1px solid #e1e1e1;top:0;left:calc(100% + 10px);background-color:#fff;z-index:11}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 77925:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "user-menu.has-user{position:relative}user-menu.has-user .logged-in{display:block}user-menu.has-user .logged-out{display:none}user-menu .logged-in{display:none}user-menu .logged-in.is-open .list{display:flex;flex-direction:column;align-items:center;margin-top:10px;width:100%}user-menu .logged-in .list{display:none;position:absolute}user-menu .logged-in .list a{padding-bottom:5px}user-menu .logged-in .list a:hover{background-color:rgba(85,85,204,0.1);font-weight:bold;cursor:pointer}user-menu .logged-in header{border:2px solid #55c;background-color:#fff;padding:5px 10px;display:flex;align-items:center;justify-content:center;transition-duration:0.1s;cursor:pointer}user-menu .logged-in header label{position:relative;text-transform:uppercase;font-weight:500;font-size:20px}user-menu .logged-in header:hover{background-color:rgba(85,85,204,0.1)}user-menu .logged-out{display:block;background-color:#55c;color:#fff}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 68987:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 21445:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "model-branch{display:flex;flex-direction:row;align-items:center}model-branch.loading .preview{display:flex}model-branch.hide{display:none}model-branch .circle{border:1px solid #e1e1e1;border-radius:100%;width:fit-content}model-branch .circle .inner{background-color:#e1e1e1;border-radius:100%;margin:3px;padding:5px}model-branch .preview{display:flex;flex-direction:row;align-items:center;display:none;flex-direction:row;align-items:center}model-branch .preview::before{display:block;content:\" \";width:17px;background-color:#e1e1e1;height:0.5px}model-branch .preview .app-row{width:150px;padding:10px}model-branch .list{display:flex;flex-direction:row;align-items:center}model-branch .list .model{display:flex;flex-direction:row;align-items:center}model-branch .list .model::before{display:block;content:\" \";width:17px;background-color:#e1e1e1;height:0.5px}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 43379:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, ".model-card{position:relative;display:flex;flex-direction:column;width:250px;height:150px;background-color:#ecf0f1;align-items:stretch;cursor:pointer}.model-card.selected header{background-color:aquamarine}.model-card header{flex:0 0 auto;display:flex;flex-direction:row;align-items:center;width:100%;padding:5px 10px;background-color:rgba(255,255,255,0.3)}.model-card header label{text-transform:uppercase;font-weight:500}.model-card .content{width:100%;height:121px}.model-card .content img{width:100%;height:100%;object-fit:cover;background-color:black}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 75187:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "model-search{display:block}model-search input{width:100%}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 9427:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, ".model-row{cursor:pointer;padding:7px;padding-left:5px;display:flex;flex-direction:row;align-items:center;min-height:30px;background-color:#ecf0f1;border-radius:2px;font-size:12px;font-weight:600;text-transform:uppercase;height:30px;width:fit-content;min-width:150px;border-left:5px solid transparent}.model-row.focus{border-left-color:aquamarine}.model-row.selected,.model-row:hover{background-color:aquamarine}.model-row img,.model-row svg,.model-row .icon{margin-right:7px}.model-row img,.model-row svg{height:100%}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 6009:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "app-search{position:relative;display:block;width:fit-content}app-search .search-bar{display:flex;flex-direction:row;align-items:stretch;width:fit-content;cursor:pointer}app-search .search-bar .search-icon,app-search .search-bar .informations{display:flex;align-items:center}app-search .search-bar .search-icon{padding:5px 10px;background-color:#b1b1b1}app-search .search-bar input{font-size:18px;border:none;padding:2px;min-width:300px}app-search .search-bar input:hover{border-color:#55c}app-search .search-bar .x{background-color:#fff;display:flex;align-items:center;padding:5px 7px}app-search .search-bar .x.empty svg{visibility:hidden}app-search .search-bar .x:not(.empty):hover{background-color:#c55;color:#fff}app-search .search-bar .x svg{width:20px;height:20px}app-search .search-bar .informations{padding:0 14px;background-color:#55c}app-search .search-bar .informations svg{color:#fff}app-search .search-bar:hover .search-icon svg{color:#55c}app-search .searchables{display:none;z-index:1;position:absolute;top:100%;border-top:1px solid #e1e1e1;min-width:100%}app-search .searchables .list{display:flex;flex-direction:column;flex-wrap:wrap;width:100%;max-height:500px}app-search .searchables .list .searchable{width:100%;background-color:#e1e1e1}app-search .searchables .list .searchable.hide{display:none}app-search .searchables .list .searchable.error header{background-color:rgba(204,85,85,0.5)}app-search .searchables .list .searchable.error header .icon{display:block}app-search .searchables .list .searchable header{padding:3px 5px;display:flex;flex-direction:row;align-items:center}app-search .searchables .list .searchable header label{font-size:18px;text-transform:capitalize;flex:1}app-search .searchables .list .searchable header .icon{position:relative;display:none;color:#c55;margin-right:15px;cursor:pointer}app-search .searchables .list .searchable .results .model-row{width:100%}app-search .searchables .list .searchable .results>*{display:block;margin:2px}app-search.open .searchables{display:flex}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 18856:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 10910:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 79269:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, ".field.disabled{display:none}.field.hidden{display:none}.field>header{display:flex;flex-direction:row;align-items:center}.field>header>label{display:block;font-size:18px;font-weight:500;text-transform:uppercase}.field.hidden{display:none}.field.required>header{display:flex;flex-direction:row;align-items:center}.field.required>header>label::after{margin-left:5px;content:\"*\";color:red}.field>.errors .error{background-color:rgba(204,85,85,0.3);color:#c55;padding:5px;border-radius:5px;margin-top:5px}.field>.messages .message{background-color:rgba(85,85,204,0.3);color:#55c;padding:5px;border-radius:5px;margin-top:5px;cursor:pointer}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 93871:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "markdown-field{flex:100% !important}markdown-field textarea{width:100%;height:400px}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 85263:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "number-field input{width:100%}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 17831:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "text-field input{width:100%}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 9723:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "child-model-form{width:100%}child-model-form>form>.bottom{display:flex;flex-direction:row;width:fit-content;margin-left:auto;margin-right:0}child-model-form>form>.bottom .submit{display:block}child-model-form>form>.bottom .edit{display:none}child-model-form>form>.bottom .cancel{display:none}child-model-form>form>.bottom .delete{display:none}child-model-form>form>.bottom button{font-size:26px;padding:5px;margin-left:5px}child-model-form.read>form>.bottom .submit{display:none}child-model-form.editable.read>form>.bottom .edit{display:block}child-model-form.editable.edit>form>.bottom .cancel{display:block}child-model-form.deletable>form>.bottom .delete{display:block}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 77544:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "model-field{display:block}model-field .inner{position:relative;display:flex;flex-direction:row;align-items:center;height:40px}model-field .inner .input{display:flex;flex:1;flex-direction:row;align-items:center;border:1px solid #e1e1e1;background-color:#fff;height:100%;padding:5px}model-field .inner .input .value{width:100%}model-field .inner .input .value .model-row{width:100%}model-field .inner .input input{display:none;border:none;height:100%;width:100%}model-field .inner .input .close{margin:2px;cursor:pointer;padding:4px 7px}model-field .inner .input .close:hover{background-color:rgba(204,85,85,0.4)}model-field .inner .create{padding:4px 12px;height:100%;font-size:19px}model-field .suggestions{position:absolute;top:100%;width:100%;background-color:#fff;border:1px solid #e1e1e1;z-index:9999}model-field .suggestions .suggestion{margin:5px}model-field.empty .inner .input .value,model-field.edit .inner .input .value{display:none}model-field.empty .inner .input input,model-field.edit .inner .input input{display:block}model-field.empty .inner .input .close,model-field.edit .inner .input .close{display:none}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 31485:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "model-form{width:100%}model-form.disable-nested .breadcrumb{display:none}model-form .breadcrumb{display:flex;flex-direction:row;align-items:center}model-form .breadcrumb div label{display:inline-block;background-color:rgba(225,225,225,0.5);cursor:pointer;padding:5px}model-form .breadcrumb div label:hover{background-color:aquamarine}model-form .breadcrumb div:not(:last-child)::after{font-size:22px;font-weight:bold;content:\">\";margin:5px}model-form .forms child-model-form:not(:last-child){display:none}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 54909:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "object-fieldset{display:block;width:100%;flex:100% !important}object-fieldset:not(.empty)>header select{display:block}object-fieldset:not(.empty)>header .create{display:none}object-fieldset:not(.empty)>header .delete{display:block}object-fieldset:not(.empty)>.main>fieldset{display:flex}object-fieldset.required>header .delete{display:none}object-fieldset.one-option.required>header select{display:none}object-fieldset>header select{display:none;margin-left:10px;cursor:pointer;-webkit-appearance:none;-moz-appearance:none;border:none;padding:5px;background:none;font-size:17px;border:1px solid transparent}object-fieldset>header select:hover,object-fieldset>header select:focus{border-color:#e1e1e1;background-color:#fff;-webkit-appearance:menulist;-moz-appearance:menulist}object-fieldset>header .create,object-fieldset>header .delete{margin-left:5px;display:flex;cursor:pointer;align-items:center;justify-content:center;padding:5px;background-color:#fff;border:1px solid #e1e1e1}object-fieldset>header .create:hover,object-fieldset>header .delete:hover{background-color:rgba(127,255,212,0.3)}object-fieldset>header .delete{display:none}object-fieldset>.main>fieldset{display:none;background-color:rgba(161,161,161,0.2);margin-top:5px;padding:15px;flex-direction:row;align-items:flex-start;justify-content:space-between;flex-wrap:wrap;gap:12px}object-fieldset>.main>fieldset>*{flex:1 1 450px}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 21092:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, ".object-form{width:100%}.object-form>form>.bottom{display:flex;flex-direction:row;width:fit-content;margin-left:auto;margin-right:0}.object-form>form>.bottom .submit{display:block}.object-form.read>form>.bottom .submit{display:none}.object-form.editable.read>form>.bottom .edit{display:block}.object-form.editable.edit>form>.bottom .cancel{display:block}.object-form.deletable>form>.bottom .delete{display:block}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 30108:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 66103:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "file-field input{display:none}file-field .area{width:250px;height:150px;border:4px dashed #e1e1e1;background-color:rgba(255,255,255,0.3);display:flex;flex-direction:column;align-items:center;justify-content:center;font-size:35px;cursor:pointer}file-field .area svg{background-color:#e1e1e1;padding:10px;border-radius:100%}file-field .area:hover,file-field .area.hover{border-color:aquamarine}file-field .value{display:none;flex-direction:row;align-items:start}file-field .value img{width:250px}file-field .value .actions{display:flex;flex-direction:column;border:1px solid #d4d4d4;border-left:none}file-field .value .actions div{display:flex;flex-direction:column;align-items:center;justify-content:center;padding:8px;background-color:#e1e1e1;font-size:17px;cursor:pointer}file-field .value .actions div:not(:last-child){border-bottom:1px solid #d4d4d4}file-field .value .actions div:hover{background-color:aquamarine}file-field .progress-bar{width:100%;height:10px;background-color:#fff}file-field .progress-bar .bar{height:100%;background-color:green}file-field:not(.empty) .value{display:flex}file-field:not(.empty) .area{display:none}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 37541:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 6785:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "folder-card .content{display:flex;align-items:center;justify-content:center}folder-card svg{color:#f1c40f;font-size:85px}folder-card svg.open{display:none}folder-card.open svg{display:none}folder-card.open svg.open{display:block}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 6361:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "folder-row svg{color:#f1c40f}folder-row svg.open{display:none}folder-row.open svg{display:none}folder-row.open svg.open{display:block}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 43812:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "folder-tree{display:block;width:fit-content;position:relative;height:100%}folder-tree>.root{height:100%;overflow-y:auto;padding:10px}folder-tree .folder>.row{display:flex;flex-direction:row;align-items:center}folder-tree .folder>.row folder-row{flex:1}folder-tree .folder>.folders{display:none;margin-left:13px;width:fit-content;border-left:2px dashed #e1e1e1}folder-tree .folder>.folders>.folder>.row::before{content:\" \";width:10px;border-bottom:2px dashed #e1e1e1;margin-top:2px}folder-tree .folder>.folders>.folder>.row>folder-row{margin-top:5px}folder-tree .folder.active>.folders{display:block;border-color:aquamarine !important}folder-tree .folder.active>.row::before{border-color:aquamarine !important}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 93914:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "image-card img{width:100%;height:100%;object-fit:cover}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 10707:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, "", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 30822:
/***/ (function(module, exports, __webpack_require__) {

// Imports
var ___CSS_LOADER_API_IMPORT___ = __webpack_require__(23645);
exports = ___CSS_LOADER_API_IMPORT___(false);
// Module
exports.push([module.id, ".imageable-image{height:100%}\n", ""]);
// Exports
module.exports = exports;


/***/ }),

/***/ 23645:
/***/ (function(module) {

"use strict";


/*
  MIT License http://www.opensource.org/licenses/mit-license.php
  Author Tobias Koppers @sokra
*/
// css base code, injected by the css-loader
// eslint-disable-next-line func-names
module.exports = function (useSourceMap) {
  var list = []; // return the list of modules as css string

  list.toString = function toString() {
    return this.map(function (item) {
      var content = cssWithMappingToString(item, useSourceMap);

      if (item[2]) {
        return "@media ".concat(item[2], " {").concat(content, "}");
      }

      return content;
    }).join('');
  }; // import a list of modules into the list
  // eslint-disable-next-line func-names


  list.i = function (modules, mediaQuery, dedupe) {
    if (typeof modules === 'string') {
      // eslint-disable-next-line no-param-reassign
      modules = [[null, modules, '']];
    }

    var alreadyImportedModules = {};

    if (dedupe) {
      for (var i = 0; i < this.length; i++) {
        // eslint-disable-next-line prefer-destructuring
        var id = this[i][0];

        if (id != null) {
          alreadyImportedModules[id] = true;
        }
      }
    }

    for (var _i = 0; _i < modules.length; _i++) {
      var item = [].concat(modules[_i]);

      if (dedupe && alreadyImportedModules[item[0]]) {
        // eslint-disable-next-line no-continue
        continue;
      }

      if (mediaQuery) {
        if (!item[2]) {
          item[2] = mediaQuery;
        } else {
          item[2] = "".concat(mediaQuery, " and ").concat(item[2]);
        }
      }

      list.push(item);
    }
  };

  return list;
};

function cssWithMappingToString(item, useSourceMap) {
  var content = item[1] || ''; // eslint-disable-next-line prefer-destructuring

  var cssMapping = item[3];

  if (!cssMapping) {
    return content;
  }

  if (useSourceMap && typeof btoa === 'function') {
    var sourceMapping = toComment(cssMapping);
    var sourceURLs = cssMapping.sources.map(function (source) {
      return "/*# sourceURL=".concat(cssMapping.sourceRoot || '').concat(source, " */");
    });
    return [content].concat(sourceURLs).concat([sourceMapping]).join('\n');
  }

  return [content].join('\n');
} // Adapted from convert-source-map (MIT)


function toComment(sourceMap) {
  // eslint-disable-next-line no-undef
  var base64 = btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap))));
  var data = "sourceMappingURL=data:application/json;charset=utf-8;base64,".concat(base64);
  return "/*# ".concat(data, " */");
}

/***/ }),

/***/ 20793:
/***/ (function(module) {

// Module
var code = "<self> <div as=\"container\" class=\"container\"></div> <app-notifications-list></app-notifications-list> <modal-container></modal-container> </self> ";
// Exports
module.exports = code;

/***/ }),

/***/ 8240:
/***/ (function(module) {

// Module
var code = "<self ::class=\"{{this.@editing ? 'editing' : ''}}\"> <span :on-click=\"this.edit()\"> {{this.@value}} </span> <input as=\"input\" :on-blur=\"this.editing = false\" :value=\"this.@value\"> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 97946:
/***/ (function(module) {

// Module
var code = "<self> <div class=\"loader\" :style.width=\"this.@size\" :style.height=\"this.@size\"> </div> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 11661:
/***/ (function(module) {

// Module
var code = "<self> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 25232:
/***/ (function(module) {

// Module
var code = "<self> <div class=\"sections\" :v-for=\"section of this.@sections\"> <div :class=\"`section ${this.@current == section ? 'current' :''}`\" :on-click=\"this.focus(section)\"> <label>{{section.title}}</label> </div> </div> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 76298:
/***/ (function(module) {

// Module
var code = "<self :class=\"`${this.@show ? 'show' : ''}`\" slot> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 11633:
/***/ (function(module) {

// Module
var code = "<self> <div :on-wheel=\"this.onWheel(event)\" :style.top=\"`-${this.@index * 40}px`\" class=\"options\" :v-for=\"option of this.@options\"> <div :class=\"`option ${option === this.@option ? 'current' : ''} `\" :on-click=\"this.setOption(option)\"> <label>{{option.label}}</label> </div> </div> <div style=\"clear:both\"></div> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 5911:
/***/ (function(module) {

// Module
var code = "<self> <div class=\"container\" as=\"container\"> </div> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 48631:
/***/ (function(module) {

// Module
var code = "<self :class=\"`${ this.@open && 'open' || '' }`\"> <div class=\"top-bar\"> <div class=\"burger\" :on-click=\"this.open = !this.open\"> <i class=\"fa-solid fa-bars\"></i> </div> <h3>Star citizen universe</h3> <div class=\"search\"> <app-search></app-search> </div> <user-menu></user-menu> </div> <div class=\"row\"> <div class=\"left-bar\"> <div class=\"menu\" :v-for=\"link of menu.links\"> <a class=\"link\" :v-link=\"link.url\"> <div class=\"image\"> <i :class=\"link.class\"></i> </div> <label>{{ link.label }}</label> </a> </div> </div> <div :v-loader=\"!this.@contentReady size '50px'\" class=\"container\" as=\"container\"></div> </div> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 52609:
/***/ (function(module) {

// Module
var code = "<self :v-exit=\"this.close() when this.@isOpened\"> <header> <h3 class=\"title\">{{this.@title}}</h3> <div class=\"actions\"> <div :on-click=\"this.close()\"> <i class=\"fa-solid fa-xmark\"></i> </div> </div> </header> <div class=\"transclude\" slot></div> </self> ";
// Exports
module.exports = code;

/***/ }),

/***/ 77215:
/***/ (function(module) {

// Module
var code = "<self :class=\"`${this.@modal ? 'has-modal' : ''}`\"> {{= this.@modal }} </self> ";
// Exports
module.exports = code;

/***/ }),

/***/ 49283:
/***/ (function(module) {

// Module
var code = "<super> <slot name=\"content\"> <img :src=\"this.@model.@image?.@path\">  </slot></super>";
// Exports
module.exports = code;

/***/ }),

/***/ 79332:
/***/ (function(module) {

// Module
var code = "<self> <div class=\"outer\" :on-mousedown=\"this.onMouseDown()\" :on-mouseup=\"this.onMouseUp()\" :on-mousemove=\"this.onMove(event)\" :on-wheel=\"this.onWheel(event)\"> <div class=\"inner\" :style.left=\"`${this.camera.@x}px`\" :style.top=\"`${this.camera.@y}px`\" :style.transform=\"`scale(${this.camera.@zoom})`\"> <div class=\"layers\" :v-for=\"layer of this.@layers?.slice().reverse()\"> <div class=\"layer\"> <svg :style.height=\"`${layer.size}px`\" :style.width=\"`${layer.size}px`\"> <circle cx=\"50%\" cy=\"50%\" r=\"50%\" stroke=\"#fff\" stroke-width=\"3\"/> </svg> </div> </div> <div class=\"objects\" :v-for=\"mapObject of this.@mapObjects\"> <a class=\"object\" :on-click=\"this.object = mapObject.object\" :style.left=\"`${mapObject.@x}px`\" :style.top=\"`${mapObject.@y}px`\" :style.background-color=\"mapObject.color\"> <label>{{mapObject.name}}</label> </a> </div> </div> </div></self>";
// Exports
module.exports = code;

/***/ }),

/***/ 54990:
/***/ (function(module) {

// Module
var code = "<super> {{this.model?.@name || ''}} </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 24572:
/***/ (function(module) {

// Module
var code = "<super> <label>{{this.model.@name}}</label> </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 33566:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

// Imports
var ___HTML_LOADER_GET_SOURCE_FROM_IMPORT___ = __webpack_require__(27091);
var ___HTML_LOADER_IMPORT_0___ = __webpack_require__(95405);
// Module
var ___HTML_LOADER_REPLACEMENT_0___ = ___HTML_LOADER_GET_SOURCE_FROM_IMPORT___(___HTML_LOADER_IMPORT_0___);
var code = "<super> <img src=\"" + ___HTML_LOADER_REPLACEMENT_0___ + "\"/> <label>{{this.model.@name}}</label> </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 39174:
/***/ (function(module) {

// Module
var code = "<self> <div class=\"color\" :style.background-color=\"this.@model?.@color\"></div> <label>{{this.@model?.@code}}</label> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 1333:
/***/ (function(module) {

// Module
var code = "<super> <div class=\"icon\" :style.background-color=\"this.@model?.@color\"></div> <label>{{this.@model?.@name}}</label> </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 68989:
/***/ (function(module) {

// Module
var code = "<self> <div class=\"notifications\" :v-for=\"notification of service.notifications\"> <div class=\"notification\" :on-click=\"notification.close()\" :class=\"`${notification.type.name} ${ notification.@isClosed ? 'closed' : ''}`\"> <div :inner-html=\"notification.type.icon\"></div> <label>{{notification.message}}</label> </div> </div> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 7394:
/***/ (function(module) {

// Module
var code = "<self slot :class=\"`${this.@open && 'open'}`\" :v-exit=\"this.open = false\" :style.left=\"`${this.@left}px`\" :style.top=\"`${this.@top}px`\" :v-selectable-root> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 22474:
/***/ (function(module) {

// Module
var code = "<self :on-click=\"this.onSelected()\" :v-selectable=\"this.click()\" slot> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 92395:
/***/ (function(module) {

// Module
var code = "<self :class=\"`${this.@dragInfos && 'hover'}`\"> <div as=\"initSlot\" slot></div> <template as=\"columnTemplate\"> <div class=\"column\" :v-for=\"row of column\"> <div :class=\"`row ${ this.@hoverRow === node && 'hover'} ${this.@hoverMode}`\" :v-for=\"cell of row\" :on-dragover=\"this.onRowDragOver(row, node, event)\" :on-drop=\"this.onRowDrop(row)\"> <div class=\"cell\"> {{= this.cell(cell, scope) }} </div> </div> </div> </template> <div class=\"root\"> {{= this.cell(this.column, scope) }} </div> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 13970:
/***/ (function(module) {

// Module
var code = "<self :class=\"'grid-panel'\"> <header slot=\"header\"> </header> <div slot class=\"content\"> </div> </self> ";
// Exports
module.exports = code;

/***/ }),

/***/ 80982:
/***/ (function(module) {

// Module
var code = "<super :class=\"`${this.grid.@dragInfos && 'hover'}`\" :on-click=\"this.grid.focusPanel = this\"> <slot name=\"header\"> <div class=\"tabs\" :v-for=\"object of this.objects\"> <div draggable=\"true\" :class=\"`tab ${object === this.@currentObject && 'current'} ${this.grid.@focusPanel === this && 'selected'}`\" :on-dragstart=\"this.drag(object)\" :on-dragend=\"this.dragEnd()\" :on-click=\"this.currentObject = object\"> <label>{{ object.data.name }}</label> <div :on-click.stop=\"this.removeObject(object)\"> <i class=\"fa-solid fa-xmark\"></i> </div> </div> </div> </slot> <slot> <div class=\"container\" :on-dragleave=\"this.hover = null\" :on-drop=\"this.onDrop()\" :on-dragover=\"this.onDragOver(node, event)\"> <div class=\"object\"> {{= this.@currentObject.element }} </div> <div :class=\"`hover ${this.@hover && 'visible'} ${this.@hover}`\"> </div> </div> </slot> </super> ";
// Exports
module.exports = code;

/***/ }),

/***/ 80725:
/***/ (function(module) {

// Module
var code = "<self :on-click=\"this.trigger()\"> <div class=\"key-container\"> <div class=\"key\" :inner-html=\"this.@keyHtml\"> </div> </div> <div slot class=\"transclude\"></div> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 6937:
/***/ (function(module) {

// Module
var code = "<self slot></self>";
// Exports
module.exports = code;

/***/ }),

/***/ 28142:
/***/ (function(module) {

// Module
var code = "<self :class=\"`${context.@user ? 'has-user' : ''}`\"> <div :class=\"`logged-in ${this.@isOpen ? 'is-open' : '' }`\"> <header :on-click=\"this.isOpen = !this.isOpen\"> <label>{{context.@user?.@username}}</label> </header> <div class=\"list\"> <a :v-link=\"`/user/${context.@user?.username}`\">My profile</a> <a :v-link=\"'/logout'\">Logout</a> </div> </div> <div class=\"logged-out\"> <a :v-link=\"'/login'\"> Login </a> </div> </self> ";
// Exports
module.exports = code;

/***/ }),

/***/ 13219:
/***/ (function(module) {

// Module
var code = "<super> <i class=\"fa-solid fa-user\"></i> {{this.model?.@username || ''}} </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 83152:
/***/ (function(module) {

// Module
var code = "<self :class=\"`${this.@loading ? 'loading' : ''} ${this.branch.@length ? '' : 'hide'}`\"> <div class=\"circle\"> <div class=\"inner\"></div> </div> <div class=\"preview\"> <div class=\"model-row\"> <loading-content> </loading-content></div> </div> <div :v-for=\"model of this.branch\" class=\"list\"> <a class=\"model\" :v-link=\"model.@url\"> <model-row :model=\"model\"></model-row> </a> </div> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 42296:
/***/ (function(module) {

// Module
var code = "<self :class=\"'model-card'\"> <header slot=\"header\"> <label> {{this.model.@name}} </label> </header> <div class=\"content\" slot> </div> <context-menu slot=\"context-menu\"> <div :v-if=\"mixer.is(this.model, Pageable)\"> <context-menu-item :on-selected=\"navigator.navigate(this.model.url)\"> Open </context-menu-item> <context-menu-item :on-selected=\"console.log(this.model, '_blank')\"> Open in a new tab </context-menu-item> <context-menu-item :on-selected=\"navigator.navigate(this.model.url + '/edit')\"> Edit </context-menu-item> </div> <context-menu-item :on-selected=\"this.delete()\" :v-if=\"this.model.canDelete()\"> Delete </context-menu-item> </context-menu> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 28944:
/***/ (function(module) {

// Module
var code = "<self> <div class=\"search-bar\"> <input as=\"input\" :on-change=\"this.onChange()\"/> </div> <div class=\"models\" :v-for=\"model of this.models with (m) => this.template(m)\"> </div> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 44871:
/***/ (function(module) {

// Module
var code = "<self :class=\"`model-row`\" slot> {{this.model?.@title || ''}} </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 21458:
/***/ (function(module) {

// Module
var code = "<self :class=\"`${this.@open ? 'open' : ''}`\" :v-exit=\"this.stop()\" :v-selectable-root> <div class=\"search-bar\"> <div class=\"search-icon\"> <i class=\"fa-solid fa-magnifying-glass\"></i> </div> <input as=\"input\" placeholder=\"Search ...\" :on-input=\"this.length = this.input.value.length\" :v-input-delay=\"this.search()\" :on-focus=\"this.start()\"> <div class=\"x\" :class=\"`${this.@length ? '' : 'empty'}`\" :on-click=\"this.empty()\"> <i class=\"fa-solid fa-xmark\"></i> </div> <div class=\"informations\"> <i class=\"fa-solid fa-info\"></i> <tool-tip> <span><a href=\"https://en.wikipedia.org/wiki/Regular_expression\">Regex</a> are supported</span> </tool-tip> </div> </div> <div class=\"searchables\"> <div class=\"list\" :v-for=\"searchable of this.searchables\"> <div class=\"searchable\" :class=\"`${searchable.@error ? 'error' : ''} ${searchable.@results?.length ? '' : 'hide'}`\"> <header> <label>{{searchable.type.definition.pluralName}}</label> <div class=\"icon\"> <i class=\"fa-solid fa-triangle-exclamation\"></i> <tool-tip> {{ searchable.@error || '' }} </tool-tip> </div> </header> <div :v-loader=\"searchable.@loading size '50px'\" class=\"results\" :v-for=\"result of searchable.@results\"> <a :on-click=\"this.stop()\" :v-link=\"result.@url\"> <model-row :model=\"result\" :v-selectable=\"node.click()\"> </model-row> </a> </div> </div> </div> <short-cuts> <short-cut :key=\"'ArrowUp'\">Previous</short-cut> <short-cut :key=\"'ArrowDown'\">Next</short-cut> <short-cut :key=\"'shift+tab'\">Previous</short-cut> <short-cut :key=\"'tab'\">Next</short-cut> </short-cuts> </div> <short-cuts> <short-cut :key=\"'alt+x'\" :callback=\"() => this.empty()\" :target=\"this.input\">Erase</short-cut> <short-cut :key=\"'alt+f'\" :callback=\"() => this.input.focus()\" :target=\"window\">Focus</short-cut> </short-cuts> </self>";
// Exports
module.exports = code;

/***/ }),

/***/ 89418:
/***/ (function(module) {

// Module
var code = "<super> <input :name=\"this.@name\" :type=\"this.@type || 'text'\" :value=\"this.@value\" :required=\"this.@required\" :on-change=\"console.log('ok')\">  </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 60175:
/***/ (function(module) {

// Module
var code = "<super> <input :name=\"this.@name\" type=\"date\" :value=\"this.@value\" :required=\"this.@required\" :on-change=\"this.value = node.value\">  </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 61510:
/***/ (function(module) {

// Module
var code = "<self :class=\"`field ${this.state.@required && 'required'} ${!this.state.@value && 'empty'} ${this.state.@disabled && 'disabled'} ${this.state.@hidden && 'hidden'}`\"> <header slot=\"header\"> <label :style.display=\"this.state.property.name ? null : 'none'\"> {{this.state.property.name || ''}} </label> </header> <div class=\"main\" slot></div> <div class=\"errors\" :v-for=\"error of this.state.@errors\" :style.display=\"this.@touched === true ? null : 'none'\"> <div class=\"error\">{{error}}</div> </div> <div class=\"messages\" :v-for=\"message of this.state.@messages\"> <div class=\"message\" :on-click=\"this.state.messages.tryRemove(message)\"> {{message.text}} </div> </div> </self> ";
// Exports
module.exports = code;

/***/ }),

/***/ 66278:
/***/ (function(module) {

// Module
var code = "<super> <textarea :name=\"this.state.property.name\" :value=\"this.state.@value || ''\" :on-change=\"this.setValue(node.value)\">\n  </textarea> </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 71617:
/***/ (function(module) {

// Module
var code = "<super> <input :name=\"this.state.property.name\" type=\"number\" :value=\"this.state.@value\" :step=\"this.state.@step || 1\" :on-change=\"this.setValue(parseFloat(node.value))\">  </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 73406:
/***/ (function(module) {

// Module
var code = "<super> <input :name=\"this.state.property.name\" :type=\"this.type\" :value=\"this.state.@value || ''\" :disabled=\"this.state.@readOnly\" :on-change=\"this.setValue(node.value)\">  </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 24119:
/***/ (function(module) {

// Module
var code = "<super :class=\"`${this.@mode} ${this.@editable ? 'editable' : ''}  ${this.@deletable && this.model ? 'deletable' : ''}`\"> <slot name=\"bottom\"> <button :on-click=\"this.mode = 'read'\" class=\"cancel\" type=\"button\"> Cancel </button> <super-slot></super-slot> <button :on-click=\"this.mode = 'edit'\" class=\"edit\" type=\"button\"> Edit </button> <button :on-click=\"this.delete()\" class=\"delete\" type=\"button\"> Delete </button> </slot> </super> ";
// Exports
module.exports = code;

/***/ }),

/***/ 85251:
/***/ (function(module) {

// Module
var code = "<super :class=\"`${this.@mode}`\" :v-selectable-root> <div class=\"inner\" :v-exit=\"this.default(true)\"> <div class=\"input\"> <div class=\"value\" :on-click=\"this.edit()\"> {{= this.templateSuggestion(this.state.@value) }} </div> <input as=\"input\" :on-focus=\"this.onFocus()\" :v-input-delay=\"this.search(node.value)\"/> <div class=\"close\" :on-click=\"this.selectSuggestion(null)\"> <i class=\"fa-solid fa-xmark\"></i> </div> </div> <button class=\"create\" type=\"button\" :on-click=\"this.create()\"> <i class=\"fa-solid fa-plus\"></i> </button> <div class=\"suggestions\" :style.display=\"!this.@suggestionsOpen && 'none' || null\" :v-for=\"suggestion of this.@suggestions\"> <div class=\"suggestion\" :on-click=\"this.selectSuggestion(suggestion)\"> <model-row :model=\"suggestion\" :v-selectable=\"node.click()\"> </model-row> </div> </div> </div>  </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 49998:
/***/ (function(module) {

// Module
var code = "<self :class=\"`${this.disableNested ? 'disable-nested' : ''}`\"> <div class=\"breadcrumb\" :v-for=\"form of this.forms\"> <div> <label :on-click=\"this.target(form)\">{{ form.label || form.@type?.definition.name }}</label> </div> </div> <child-model-form :form=\"this\" in=\"forms\" :type=\"this.@type\" :label=\"this.@model?.@title\" :on-saved=\"this.onSaved(event)\" :mode=\"this.@mode\" :editable=\"this.@editable\" :deletable=\"this.@deletable\" :disable-nested=\"this.@disableNested\" :states=\"this.@states\" :object=\"this.@model\"> </child-model-form> <div class=\"forms\" :v-for=\"form of this.forms with (f) => f\"></div> </self> ";
// Exports
module.exports = code;

/***/ }),

/***/ 13698:
/***/ (function(module) {

// Module
var code = "<super :class=\"`${this.@types.length === 1 ? 'one-option' : ''}`\"> <slot name=\"header\"> <super-slot></super-slot> <select :v-for=\"option of this.@types\"> <option :option-value=\"option\" :selected=\"this.@types.find((t)=> this.value === t.value || (t.value && this.state.@value instanceof t.value))\"> {{ option.definition.name }} </option> </select> <div class=\"create\" :on-click=\"this.create()\"> <i class=\"fa-solid fa-plus\"></i> </div> <div class=\"delete\" :on-click=\"this.state.value = null\"> <i class=\"fa-solid fa-xmark\"></i> </div> </slot> <slot> <fieldset :v-for=\"field of Object.values(this.@fields) with (f) => f\"> </fieldset> </slot>  </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 37551:
/***/ (function(module) {

// Module
var code = "<self :class=\"`object-form ${this.@mode} ${this.@editable ? 'editable' : ''}  ${this.@deletable && this.model ? 'deletable' : ''}`\"> <form :on-submit=\"this.onSubmit(event)\"> <object-fieldset as=\"fieldset\" :child-form=\"this\" :form=\"this.@form\" :state=\"this.state\" :on-changed=\"this.onFieldsetChanged()\"> </object-fieldset> <div class=\"bottom\" slot=\"bottom\"> <button class=\"submit\" type=\"submit\"> Save </button> </div> </form> </self> ";
// Exports
module.exports = code;

/***/ }),

/***/ 12020:
/***/ (function(module) {

// Module
var code = "<super> <img :src=\"this.@imgSrc\"> </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 60901:
/***/ (function(module) {

// Module
var code = "<super> <div class=\"inner\" :on-dragenter=\"this.onDragEnter(event)\" :on-dragover=\"this.onDragOver(event)\" :on-drop=\"this.onDrop(event)\"> <div class=\"progress-bar\" :style.display=\"this.progress != null ? 'block' : 'none'\"> <div class=\"bar\" :style.width=\"`${this.@progress}%`\"></div> </div> <div class=\"value\"> <img :src=\"this.state.@value?.@path\"/> <div class=\"actions\"> <div :on-click=\"this.onClick()\"> <i class=\"fa-solid fa-pen-to-square\"></i> </div> <div> <i class=\"fa-solid mark\"></i> </div> </div> </div> <div class=\"area\" :class=\"`${this.@hover ? 'hover' : ''}`\" :on-click=\"this.onClick()\"> <i class=\"fa-solid fa-plus\"></i> </div> </div> <input as=\"input\" type=\"file\" :on-change=\"this.onInputChanged()\"/> <mo-dal dettach style=\"width:50%\" :title=\"'Select a file'\" as=\"modal\"> <file-selector as=\"fileSelector\" :type=\"this.type\" :accept=\"this.state.property.type.accept\" :on-selected=\"this.fileSelected()\"> </file-selector> </mo-dal> </super> ";
// Exports
module.exports = code;

/***/ }),

/***/ 68496:
/***/ (function(module) {

// Module
var code = "<super> </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 11377:
/***/ (function(module) {

// Module
var code = "<super> <i class=\"fa-solid fa-folder\"></i> <i class=\"fa-regular fa-folder-open open\"></i> </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 77790:
/***/ (function(module) {

// Module
var code = "<super> <i class=\"fa-solid fa-folder\"></i> <i class=\"fa-regular fa-folder-open open\"></i> <label> {{ this.@model.@name }} </label> </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 92051:
/***/ (function(module) {

// Module
var code = "<self :v-loader=\"this.@loading size '50px'\"> <template as=\"treeTemplate\"> <div :class=\"`folder ${$.@active && 'active'}`\"> <vars :active=\"this.@folder === folder || this.folder.branch.indexOf(folder) !== -1\"></vars> <div class=\"row\"> <folder-row :model=\"folder\" :class=\"`${ $.@active && 'focus open'}`\" :on-click=\"this.select(folder)\"> </folder-row> </div> <div class=\"folders\" :v-for=\"folder of folder.children.filterLink((f)=>f instanceof Folder) with this.treeTemplate\"> </div> </div> </template> <div class=\"root\"> {{= scope.renderTemplate(this.treeTemplate, { folder: this.@root || this.@calculatedRoot }) }} </div> </self> ";
// Exports
module.exports = code;

/***/ }),

/***/ 93733:
/***/ (function(module) {

// Module
var code = "<super> <img :src=\"`${this.@imgSrc}?width=250&height=119`\"> </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 70716:
/***/ (function(module) {

// Module
var code = "<super> </super>";
// Exports
module.exports = code;

/***/ }),

/***/ 27091:
/***/ (function(module) {

"use strict";


module.exports = function (url, options) {
  if (!options) {
    // eslint-disable-next-line no-param-reassign
    options = {};
  } // eslint-disable-next-line no-underscore-dangle, no-param-reassign


  url = url && url.__esModule ? url.default : url;

  if (typeof url !== 'string') {
    return url;
  }

  if (options.hash) {
    // eslint-disable-next-line no-param-reassign
    url += options.hash;
  }

  if (options.maybeNeedQuotes && /[\t\n\f\r "'=<>`]/.test(url)) {
    return "\"".concat(url, "\"");
  }

  return url;
};

/***/ }),

/***/ 53787:
/***/ (function(module, exports, __webpack_require__) {

var __WEBPACK_AMD_DEFINE_RESULT__;;/*! showdown v 2.1.0 - 21-04-2022 */
(function(){
/**
 * Created by Tivie on 13-07-2015.
 */

function getDefaultOpts (simple) {
  'use strict';

  var defaultOptions = {
    omitExtraWLInCodeBlocks: {
      defaultValue: false,
      describe: 'Omit the default extra whiteline added to code blocks',
      type: 'boolean'
    },
    noHeaderId: {
      defaultValue: false,
      describe: 'Turn on/off generated header id',
      type: 'boolean'
    },
    prefixHeaderId: {
      defaultValue: false,
      describe: 'Add a prefix to the generated header ids. Passing a string will prefix that string to the header id. Setting to true will add a generic \'section-\' prefix',
      type: 'string'
    },
    rawPrefixHeaderId: {
      defaultValue: false,
      describe: 'Setting this option to true will prevent showdown from modifying the prefix. This might result in malformed IDs (if, for instance, the " char is used in the prefix)',
      type: 'boolean'
    },
    ghCompatibleHeaderId: {
      defaultValue: false,
      describe: 'Generate header ids compatible with github style (spaces are replaced with dashes, a bunch of non alphanumeric chars are removed)',
      type: 'boolean'
    },
    rawHeaderId: {
      defaultValue: false,
      describe: 'Remove only spaces, \' and " from generated header ids (including prefixes), replacing them with dashes (-). WARNING: This might result in malformed ids',
      type: 'boolean'
    },
    headerLevelStart: {
      defaultValue: false,
      describe: 'The header blocks level start',
      type: 'integer'
    },
    parseImgDimensions: {
      defaultValue: false,
      describe: 'Turn on/off image dimension parsing',
      type: 'boolean'
    },
    simplifiedAutoLink: {
      defaultValue: false,
      describe: 'Turn on/off GFM autolink style',
      type: 'boolean'
    },
    excludeTrailingPunctuationFromURLs: {
      defaultValue: false,
      describe: 'Excludes trailing punctuation from links generated with autoLinking',
      type: 'boolean'
    },
    literalMidWordUnderscores: {
      defaultValue: false,
      describe: 'Parse midword underscores as literal underscores',
      type: 'boolean'
    },
    literalMidWordAsterisks: {
      defaultValue: false,
      describe: 'Parse midword asterisks as literal asterisks',
      type: 'boolean'
    },
    strikethrough: {
      defaultValue: false,
      describe: 'Turn on/off strikethrough support',
      type: 'boolean'
    },
    tables: {
      defaultValue: false,
      describe: 'Turn on/off tables support',
      type: 'boolean'
    },
    tablesHeaderId: {
      defaultValue: false,
      describe: 'Add an id to table headers',
      type: 'boolean'
    },
    ghCodeBlocks: {
      defaultValue: true,
      describe: 'Turn on/off GFM fenced code blocks support',
      type: 'boolean'
    },
    tasklists: {
      defaultValue: false,
      describe: 'Turn on/off GFM tasklist support',
      type: 'boolean'
    },
    smoothLivePreview: {
      defaultValue: false,
      describe: 'Prevents weird effects in live previews due to incomplete input',
      type: 'boolean'
    },
    smartIndentationFix: {
      defaultValue: false,
      describe: 'Tries to smartly fix indentation in es6 strings',
      type: 'boolean'
    },
    disableForced4SpacesIndentedSublists: {
      defaultValue: false,
      describe: 'Disables the requirement of indenting nested sublists by 4 spaces',
      type: 'boolean'
    },
    simpleLineBreaks: {
      defaultValue: false,
      describe: 'Parses simple line breaks as <br> (GFM Style)',
      type: 'boolean'
    },
    requireSpaceBeforeHeadingText: {
      defaultValue: false,
      describe: 'Makes adding a space between `#` and the header text mandatory (GFM Style)',
      type: 'boolean'
    },
    ghMentions: {
      defaultValue: false,
      describe: 'Enables github @mentions',
      type: 'boolean'
    },
    ghMentionsLink: {
      defaultValue: 'https://github.com/{u}',
      describe: 'Changes the link generated by @mentions. Only applies if ghMentions option is enabled.',
      type: 'string'
    },
    encodeEmails: {
      defaultValue: true,
      describe: 'Encode e-mail addresses through the use of Character Entities, transforming ASCII e-mail addresses into its equivalent decimal entities',
      type: 'boolean'
    },
    openLinksInNewWindow: {
      defaultValue: false,
      describe: 'Open all links in new windows',
      type: 'boolean'
    },
    backslashEscapesHTMLTags: {
      defaultValue: false,
      describe: 'Support for HTML Tag escaping. ex: \<div>foo\</div>',
      type: 'boolean'
    },
    emoji: {
      defaultValue: false,
      describe: 'Enable emoji support. Ex: `this is a :smile: emoji`',
      type: 'boolean'
    },
    underline: {
      defaultValue: false,
      describe: 'Enable support for underline. Syntax is double or triple underscores: `__underline word__`. With this option enabled, underscores no longer parses into `<em>` and `<strong>`',
      type: 'boolean'
    },
    ellipsis: {
      defaultValue: true,
      describe: 'Replaces three dots with the ellipsis unicode character',
      type: 'boolean'
    },
    completeHTMLDocument: {
      defaultValue: false,
      describe: 'Outputs a complete html document, including `<html>`, `<head>` and `<body>` tags',
      type: 'boolean'
    },
    metadata: {
      defaultValue: false,
      describe: 'Enable support for document metadata (defined at the top of the document between `«««` and `»»»` or between `---` and `---`).',
      type: 'boolean'
    },
    splitAdjacentBlockquotes: {
      defaultValue: false,
      describe: 'Split adjacent blockquote blocks',
      type: 'boolean'
    }
  };
  if (simple === false) {
    return JSON.parse(JSON.stringify(defaultOptions));
  }
  var ret = {};
  for (var opt in defaultOptions) {
    if (defaultOptions.hasOwnProperty(opt)) {
      ret[opt] = defaultOptions[opt].defaultValue;
    }
  }
  return ret;
}

function allOptionsOn () {
  'use strict';
  var options = getDefaultOpts(true),
      ret = {};
  for (var opt in options) {
    if (options.hasOwnProperty(opt)) {
      ret[opt] = true;
    }
  }
  return ret;
}

/**
 * Created by Tivie on 06-01-2015.
 */

// Private properties
var showdown = {},
    parsers = {},
    extensions = {},
    globalOptions = getDefaultOpts(true),
    setFlavor = 'vanilla',
    flavor = {
      github: {
        omitExtraWLInCodeBlocks:              true,
        simplifiedAutoLink:                   true,
        excludeTrailingPunctuationFromURLs:   true,
        literalMidWordUnderscores:            true,
        strikethrough:                        true,
        tables:                               true,
        tablesHeaderId:                       true,
        ghCodeBlocks:                         true,
        tasklists:                            true,
        disableForced4SpacesIndentedSublists: true,
        simpleLineBreaks:                     true,
        requireSpaceBeforeHeadingText:        true,
        ghCompatibleHeaderId:                 true,
        ghMentions:                           true,
        backslashEscapesHTMLTags:             true,
        emoji:                                true,
        splitAdjacentBlockquotes:             true
      },
      original: {
        noHeaderId:                           true,
        ghCodeBlocks:                         false
      },
      ghost: {
        omitExtraWLInCodeBlocks:              true,
        parseImgDimensions:                   true,
        simplifiedAutoLink:                   true,
        excludeTrailingPunctuationFromURLs:   true,
        literalMidWordUnderscores:            true,
        strikethrough:                        true,
        tables:                               true,
        tablesHeaderId:                       true,
        ghCodeBlocks:                         true,
        tasklists:                            true,
        smoothLivePreview:                    true,
        simpleLineBreaks:                     true,
        requireSpaceBeforeHeadingText:        true,
        ghMentions:                           false,
        encodeEmails:                         true
      },
      vanilla: getDefaultOpts(true),
      allOn: allOptionsOn()
    };

/**
 * helper namespace
 * @type {{}}
 */
showdown.helper = {};

/**
 * TODO LEGACY SUPPORT CODE
 * @type {{}}
 */
showdown.extensions = {};

/**
 * Set a global option
 * @static
 * @param {string} key
 * @param {*} value
 * @returns {showdown}
 */
showdown.setOption = function (key, value) {
  'use strict';
  globalOptions[key] = value;
  return this;
};

/**
 * Get a global option
 * @static
 * @param {string} key
 * @returns {*}
 */
showdown.getOption = function (key) {
  'use strict';
  return globalOptions[key];
};

/**
 * Get the global options
 * @static
 * @returns {{}}
 */
showdown.getOptions = function () {
  'use strict';
  return globalOptions;
};

/**
 * Reset global options to the default values
 * @static
 */
showdown.resetOptions = function () {
  'use strict';
  globalOptions = getDefaultOpts(true);
};

/**
 * Set the flavor showdown should use as default
 * @param {string} name
 */
showdown.setFlavor = function (name) {
  'use strict';
  if (!flavor.hasOwnProperty(name)) {
    throw Error(name + ' flavor was not found');
  }
  showdown.resetOptions();
  var preset = flavor[name];
  setFlavor = name;
  for (var option in preset) {
    if (preset.hasOwnProperty(option)) {
      globalOptions[option] = preset[option];
    }
  }
};

/**
 * Get the currently set flavor
 * @returns {string}
 */
showdown.getFlavor = function () {
  'use strict';
  return setFlavor;
};

/**
 * Get the options of a specified flavor. Returns undefined if the flavor was not found
 * @param {string} name Name of the flavor
 * @returns {{}|undefined}
 */
showdown.getFlavorOptions = function (name) {
  'use strict';
  if (flavor.hasOwnProperty(name)) {
    return flavor[name];
  }
};

/**
 * Get the default options
 * @static
 * @param {boolean} [simple=true]
 * @returns {{}}
 */
showdown.getDefaultOptions = function (simple) {
  'use strict';
  return getDefaultOpts(simple);
};

/**
 * Get or set a subParser
 *
 * subParser(name)       - Get a registered subParser
 * subParser(name, func) - Register a subParser
 * @static
 * @param {string} name
 * @param {function} [func]
 * @returns {*}
 */
showdown.subParser = function (name, func) {
  'use strict';
  if (showdown.helper.isString(name)) {
    if (typeof func !== 'undefined') {
      parsers[name] = func;
    } else {
      if (parsers.hasOwnProperty(name)) {
        return parsers[name];
      } else {
        throw Error('SubParser named ' + name + ' not registered!');
      }
    }
  }
};

/**
 * Gets or registers an extension
 * @static
 * @param {string} name
 * @param {object|object[]|function=} ext
 * @returns {*}
 */
showdown.extension = function (name, ext) {
  'use strict';

  if (!showdown.helper.isString(name)) {
    throw Error('Extension \'name\' must be a string');
  }

  name = showdown.helper.stdExtName(name);

  // Getter
  if (showdown.helper.isUndefined(ext)) {
    if (!extensions.hasOwnProperty(name)) {
      throw Error('Extension named ' + name + ' is not registered!');
    }
    return extensions[name];

    // Setter
  } else {
    // Expand extension if it's wrapped in a function
    if (typeof ext === 'function') {
      ext = ext();
    }

    // Ensure extension is an array
    if (!showdown.helper.isArray(ext)) {
      ext = [ext];
    }

    var validExtension = validate(ext, name);

    if (validExtension.valid) {
      extensions[name] = ext;
    } else {
      throw Error(validExtension.error);
    }
  }
};

/**
 * Gets all extensions registered
 * @returns {{}}
 */
showdown.getAllExtensions = function () {
  'use strict';
  return extensions;
};

/**
 * Remove an extension
 * @param {string} name
 */
showdown.removeExtension = function (name) {
  'use strict';
  delete extensions[name];
};

/**
 * Removes all extensions
 */
showdown.resetExtensions = function () {
  'use strict';
  extensions = {};
};

/**
 * Validate extension
 * @param {array} extension
 * @param {string} name
 * @returns {{valid: boolean, error: string}}
 */
function validate (extension, name) {
  'use strict';

  var errMsg = (name) ? 'Error in ' + name + ' extension->' : 'Error in unnamed extension',
      ret = {
        valid: true,
        error: ''
      };

  if (!showdown.helper.isArray(extension)) {
    extension = [extension];
  }

  for (var i = 0; i < extension.length; ++i) {
    var baseMsg = errMsg + ' sub-extension ' + i + ': ',
        ext = extension[i];
    if (typeof ext !== 'object') {
      ret.valid = false;
      ret.error = baseMsg + 'must be an object, but ' + typeof ext + ' given';
      return ret;
    }

    if (!showdown.helper.isString(ext.type)) {
      ret.valid = false;
      ret.error = baseMsg + 'property "type" must be a string, but ' + typeof ext.type + ' given';
      return ret;
    }

    var type = ext.type = ext.type.toLowerCase();

    // normalize extension type
    if (type === 'language') {
      type = ext.type = 'lang';
    }

    if (type === 'html') {
      type = ext.type = 'output';
    }

    if (type !== 'lang' && type !== 'output' && type !== 'listener') {
      ret.valid = false;
      ret.error = baseMsg + 'type ' + type + ' is not recognized. Valid values: "lang/language", "output/html" or "listener"';
      return ret;
    }

    if (type === 'listener') {
      if (showdown.helper.isUndefined(ext.listeners)) {
        ret.valid = false;
        ret.error = baseMsg + '. Extensions of type "listener" must have a property called "listeners"';
        return ret;
      }
    } else {
      if (showdown.helper.isUndefined(ext.filter) && showdown.helper.isUndefined(ext.regex)) {
        ret.valid = false;
        ret.error = baseMsg + type + ' extensions must define either a "regex" property or a "filter" method';
        return ret;
      }
    }

    if (ext.listeners) {
      if (typeof ext.listeners !== 'object') {
        ret.valid = false;
        ret.error = baseMsg + '"listeners" property must be an object but ' + typeof ext.listeners + ' given';
        return ret;
      }
      for (var ln in ext.listeners) {
        if (ext.listeners.hasOwnProperty(ln)) {
          if (typeof ext.listeners[ln] !== 'function') {
            ret.valid = false;
            ret.error = baseMsg + '"listeners" property must be an hash of [event name]: [callback]. listeners.' + ln +
              ' must be a function but ' + typeof ext.listeners[ln] + ' given';
            return ret;
          }
        }
      }
    }

    if (ext.filter) {
      if (typeof ext.filter !== 'function') {
        ret.valid = false;
        ret.error = baseMsg + '"filter" must be a function, but ' + typeof ext.filter + ' given';
        return ret;
      }
    } else if (ext.regex) {
      if (showdown.helper.isString(ext.regex)) {
        ext.regex = new RegExp(ext.regex, 'g');
      }
      if (!(ext.regex instanceof RegExp)) {
        ret.valid = false;
        ret.error = baseMsg + '"regex" property must either be a string or a RegExp object, but ' + typeof ext.regex + ' given';
        return ret;
      }
      if (showdown.helper.isUndefined(ext.replace)) {
        ret.valid = false;
        ret.error = baseMsg + '"regex" extensions must implement a replace string or function';
        return ret;
      }
    }
  }
  return ret;
}

/**
 * Validate extension
 * @param {object} ext
 * @returns {boolean}
 */
showdown.validateExtension = function (ext) {
  'use strict';

  var validateExtension = validate(ext, null);
  if (!validateExtension.valid) {
    console.warn(validateExtension.error);
    return false;
  }
  return true;
};

/**
 * showdownjs helper functions
 */

if (!showdown.hasOwnProperty('helper')) {
  showdown.helper = {};
}

/**
 * Check if var is string
 * @static
 * @param {string} a
 * @returns {boolean}
 */
showdown.helper.isString = function (a) {
  'use strict';
  return (typeof a === 'string' || a instanceof String);
};

/**
 * Check if var is a function
 * @static
 * @param {*} a
 * @returns {boolean}
 */
showdown.helper.isFunction = function (a) {
  'use strict';
  var getType = {};
  return a && getType.toString.call(a) === '[object Function]';
};

/**
 * isArray helper function
 * @static
 * @param {*} a
 * @returns {boolean}
 */
showdown.helper.isArray = function (a) {
  'use strict';
  return Array.isArray(a);
};

/**
 * Check if value is undefined
 * @static
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is `undefined`, else `false`.
 */
showdown.helper.isUndefined = function (value) {
  'use strict';
  return typeof value === 'undefined';
};

/**
 * ForEach helper function
 * Iterates over Arrays and Objects (own properties only)
 * @static
 * @param {*} obj
 * @param {function} callback Accepts 3 params: 1. value, 2. key, 3. the original array/object
 */
showdown.helper.forEach = function (obj, callback) {
  'use strict';
  // check if obj is defined
  if (showdown.helper.isUndefined(obj)) {
    throw new Error('obj param is required');
  }

  if (showdown.helper.isUndefined(callback)) {
    throw new Error('callback param is required');
  }

  if (!showdown.helper.isFunction(callback)) {
    throw new Error('callback param must be a function/closure');
  }

  if (typeof obj.forEach === 'function') {
    obj.forEach(callback);
  } else if (showdown.helper.isArray(obj)) {
    for (var i = 0; i < obj.length; i++) {
      callback(obj[i], i, obj);
    }
  } else if (typeof (obj) === 'object') {
    for (var prop in obj) {
      if (obj.hasOwnProperty(prop)) {
        callback(obj[prop], prop, obj);
      }
    }
  } else {
    throw new Error('obj does not seem to be an array or an iterable object');
  }
};

/**
 * Standardidize extension name
 * @static
 * @param {string} s extension name
 * @returns {string}
 */
showdown.helper.stdExtName = function (s) {
  'use strict';
  return s.replace(/[_?*+\/\\.^-]/g, '').replace(/\s/g, '').toLowerCase();
};

function escapeCharactersCallback (wholeMatch, m1) {
  'use strict';
  var charCodeToEscape = m1.charCodeAt(0);
  return '¨E' + charCodeToEscape + 'E';
}

/**
 * Callback used to escape characters when passing through String.replace
 * @static
 * @param {string} wholeMatch
 * @param {string} m1
 * @returns {string}
 */
showdown.helper.escapeCharactersCallback = escapeCharactersCallback;

/**
 * Escape characters in a string
 * @static
 * @param {string} text
 * @param {string} charsToEscape
 * @param {boolean} afterBackslash
 * @returns {XML|string|void|*}
 */
showdown.helper.escapeCharacters = function (text, charsToEscape, afterBackslash) {
  'use strict';
  // First we have to escape the escape characters so that
  // we can build a character class out of them
  var regexString = '([' + charsToEscape.replace(/([\[\]\\])/g, '\\$1') + '])';

  if (afterBackslash) {
    regexString = '\\\\' + regexString;
  }

  var regex = new RegExp(regexString, 'g');
  text = text.replace(regex, escapeCharactersCallback);

  return text;
};

/**
 * Unescape HTML entities
 * @param txt
 * @returns {string}
 */
showdown.helper.unescapeHTMLEntities = function (txt) {
  'use strict';

  return txt
    .replace(/&quot;/g, '"')
    .replace(/&lt;/g, '<')
    .replace(/&gt;/g, '>')
    .replace(/&amp;/g, '&');
};

var rgxFindMatchPos = function (str, left, right, flags) {
  'use strict';
  var f = flags || '',
      g = f.indexOf('g') > -1,
      x = new RegExp(left + '|' + right, 'g' + f.replace(/g/g, '')),
      l = new RegExp(left, f.replace(/g/g, '')),
      pos = [],
      t, s, m, start, end;

  do {
    t = 0;
    while ((m = x.exec(str))) {
      if (l.test(m[0])) {
        if (!(t++)) {
          s = x.lastIndex;
          start = s - m[0].length;
        }
      } else if (t) {
        if (!--t) {
          end = m.index + m[0].length;
          var obj = {
            left: {start: start, end: s},
            match: {start: s, end: m.index},
            right: {start: m.index, end: end},
            wholeMatch: {start: start, end: end}
          };
          pos.push(obj);
          if (!g) {
            return pos;
          }
        }
      }
    }
  } while (t && (x.lastIndex = s));

  return pos;
};

/**
 * matchRecursiveRegExp
 *
 * (c) 2007 Steven Levithan <stevenlevithan.com>
 * MIT License
 *
 * Accepts a string to search, a left and right format delimiter
 * as regex patterns, and optional regex flags. Returns an array
 * of matches, allowing nested instances of left/right delimiters.
 * Use the "g" flag to return all matches, otherwise only the
 * first is returned. Be careful to ensure that the left and
 * right format delimiters produce mutually exclusive matches.
 * Backreferences are not supported within the right delimiter
 * due to how it is internally combined with the left delimiter.
 * When matching strings whose format delimiters are unbalanced
 * to the left or right, the output is intentionally as a
 * conventional regex library with recursion support would
 * produce, e.g. "<<x>" and "<x>>" both produce ["x"] when using
 * "<" and ">" as the delimiters (both strings contain a single,
 * balanced instance of "<x>").
 *
 * examples:
 * matchRecursiveRegExp("test", "\\(", "\\)")
 * returns: []
 * matchRecursiveRegExp("<t<<e>><s>>t<>", "<", ">", "g")
 * returns: ["t<<e>><s>", ""]
 * matchRecursiveRegExp("<div id=\"x\">test</div>", "<div\\b[^>]*>", "</div>", "gi")
 * returns: ["test"]
 */
showdown.helper.matchRecursiveRegExp = function (str, left, right, flags) {
  'use strict';

  var matchPos = rgxFindMatchPos (str, left, right, flags),
      results = [];

  for (var i = 0; i < matchPos.length; ++i) {
    results.push([
      str.slice(matchPos[i].wholeMatch.start, matchPos[i].wholeMatch.end),
      str.slice(matchPos[i].match.start, matchPos[i].match.end),
      str.slice(matchPos[i].left.start, matchPos[i].left.end),
      str.slice(matchPos[i].right.start, matchPos[i].right.end)
    ]);
  }
  return results;
};

/**
 *
 * @param {string} str
 * @param {string|function} replacement
 * @param {string} left
 * @param {string} right
 * @param {string} flags
 * @returns {string}
 */
showdown.helper.replaceRecursiveRegExp = function (str, replacement, left, right, flags) {
  'use strict';

  if (!showdown.helper.isFunction(replacement)) {
    var repStr = replacement;
    replacement = function () {
      return repStr;
    };
  }

  var matchPos = rgxFindMatchPos(str, left, right, flags),
      finalStr = str,
      lng = matchPos.length;

  if (lng > 0) {
    var bits = [];
    if (matchPos[0].wholeMatch.start !== 0) {
      bits.push(str.slice(0, matchPos[0].wholeMatch.start));
    }
    for (var i = 0; i < lng; ++i) {
      bits.push(
        replacement(
          str.slice(matchPos[i].wholeMatch.start, matchPos[i].wholeMatch.end),
          str.slice(matchPos[i].match.start, matchPos[i].match.end),
          str.slice(matchPos[i].left.start, matchPos[i].left.end),
          str.slice(matchPos[i].right.start, matchPos[i].right.end)
        )
      );
      if (i < lng - 1) {
        bits.push(str.slice(matchPos[i].wholeMatch.end, matchPos[i + 1].wholeMatch.start));
      }
    }
    if (matchPos[lng - 1].wholeMatch.end < str.length) {
      bits.push(str.slice(matchPos[lng - 1].wholeMatch.end));
    }
    finalStr = bits.join('');
  }
  return finalStr;
};

/**
 * Returns the index within the passed String object of the first occurrence of the specified regex,
 * starting the search at fromIndex. Returns -1 if the value is not found.
 *
 * @param {string} str string to search
 * @param {RegExp} regex Regular expression to search
 * @param {int} [fromIndex = 0] Index to start the search
 * @returns {Number}
 * @throws InvalidArgumentError
 */
showdown.helper.regexIndexOf = function (str, regex, fromIndex) {
  'use strict';
  if (!showdown.helper.isString(str)) {
    throw 'InvalidArgumentError: first parameter of showdown.helper.regexIndexOf function must be a string';
  }
  if (regex instanceof RegExp === false) {
    throw 'InvalidArgumentError: second parameter of showdown.helper.regexIndexOf function must be an instance of RegExp';
  }
  var indexOf = str.substring(fromIndex || 0).search(regex);
  return (indexOf >= 0) ? (indexOf + (fromIndex || 0)) : indexOf;
};

/**
 * Splits the passed string object at the defined index, and returns an array composed of the two substrings
 * @param {string} str string to split
 * @param {int} index index to split string at
 * @returns {[string,string]}
 * @throws InvalidArgumentError
 */
showdown.helper.splitAtIndex = function (str, index) {
  'use strict';
  if (!showdown.helper.isString(str)) {
    throw 'InvalidArgumentError: first parameter of showdown.helper.regexIndexOf function must be a string';
  }
  return [str.substring(0, index), str.substring(index)];
};

/**
 * Obfuscate an e-mail address through the use of Character Entities,
 * transforming ASCII characters into their equivalent decimal or hex entities.
 *
 * Since it has a random component, subsequent calls to this function produce different results
 *
 * @param {string} mail
 * @returns {string}
 */
showdown.helper.encodeEmailAddress = function (mail) {
  'use strict';
  var encode = [
    function (ch) {
      return '&#' + ch.charCodeAt(0) + ';';
    },
    function (ch) {
      return '&#x' + ch.charCodeAt(0).toString(16) + ';';
    },
    function (ch) {
      return ch;
    }
  ];

  mail = mail.replace(/./g, function (ch) {
    if (ch === '@') {
      // this *must* be encoded. I insist.
      ch = encode[Math.floor(Math.random() * 2)](ch);
    } else {
      var r = Math.random();
      // roughly 10% raw, 45% hex, 45% dec
      ch = (
        r > 0.9 ? encode[2](ch) : r > 0.45 ? encode[1](ch) : encode[0](ch)
      );
    }
    return ch;
  });

  return mail;
};

/**
 *
 * @param str
 * @param targetLength
 * @param padString
 * @returns {string}
 */
showdown.helper.padEnd = function padEnd (str, targetLength, padString) {
  'use strict';
  /*jshint bitwise: false*/
  // eslint-disable-next-line space-infix-ops
  targetLength = targetLength>>0; //floor if number or convert non-number to 0;
  /*jshint bitwise: true*/
  padString = String(padString || ' ');
  if (str.length > targetLength) {
    return String(str);
  } else {
    targetLength = targetLength - str.length;
    if (targetLength > padString.length) {
      padString += padString.repeat(targetLength / padString.length); //append to original to ensure we are longer than needed
    }
    return String(str) + padString.slice(0,targetLength);
  }
};

/**
 * POLYFILLS
 */
// use this instead of builtin is undefined for IE8 compatibility
if (typeof (console) === 'undefined') {
  console = {
    warn: function (msg) {
      'use strict';
      alert(msg);
    },
    log: function (msg) {
      'use strict';
      alert(msg);
    },
    error: function (msg) {
      'use strict';
      throw msg;
    }
  };
}

/**
 * Common regexes.
 * We declare some common regexes to improve performance
 */
showdown.helper.regexes = {
  asteriskDashAndColon: /([*_:~])/g
};

/**
 * EMOJIS LIST
 */
showdown.helper.emojis = {
  '+1':'\ud83d\udc4d',
  '-1':'\ud83d\udc4e',
  '100':'\ud83d\udcaf',
  '1234':'\ud83d\udd22',
  '1st_place_medal':'\ud83e\udd47',
  '2nd_place_medal':'\ud83e\udd48',
  '3rd_place_medal':'\ud83e\udd49',
  '8ball':'\ud83c\udfb1',
  'a':'\ud83c\udd70\ufe0f',
  'ab':'\ud83c\udd8e',
  'abc':'\ud83d\udd24',
  'abcd':'\ud83d\udd21',
  'accept':'\ud83c\ude51',
  'aerial_tramway':'\ud83d\udea1',
  'airplane':'\u2708\ufe0f',
  'alarm_clock':'\u23f0',
  'alembic':'\u2697\ufe0f',
  'alien':'\ud83d\udc7d',
  'ambulance':'\ud83d\ude91',
  'amphora':'\ud83c\udffa',
  'anchor':'\u2693\ufe0f',
  'angel':'\ud83d\udc7c',
  'anger':'\ud83d\udca2',
  'angry':'\ud83d\ude20',
  'anguished':'\ud83d\ude27',
  'ant':'\ud83d\udc1c',
  'apple':'\ud83c\udf4e',
  'aquarius':'\u2652\ufe0f',
  'aries':'\u2648\ufe0f',
  'arrow_backward':'\u25c0\ufe0f',
  'arrow_double_down':'\u23ec',
  'arrow_double_up':'\u23eb',
  'arrow_down':'\u2b07\ufe0f',
  'arrow_down_small':'\ud83d\udd3d',
  'arrow_forward':'\u25b6\ufe0f',
  'arrow_heading_down':'\u2935\ufe0f',
  'arrow_heading_up':'\u2934\ufe0f',
  'arrow_left':'\u2b05\ufe0f',
  'arrow_lower_left':'\u2199\ufe0f',
  'arrow_lower_right':'\u2198\ufe0f',
  'arrow_right':'\u27a1\ufe0f',
  'arrow_right_hook':'\u21aa\ufe0f',
  'arrow_up':'\u2b06\ufe0f',
  'arrow_up_down':'\u2195\ufe0f',
  'arrow_up_small':'\ud83d\udd3c',
  'arrow_upper_left':'\u2196\ufe0f',
  'arrow_upper_right':'\u2197\ufe0f',
  'arrows_clockwise':'\ud83d\udd03',
  'arrows_counterclockwise':'\ud83d\udd04',
  'art':'\ud83c\udfa8',
  'articulated_lorry':'\ud83d\ude9b',
  'artificial_satellite':'\ud83d\udef0',
  'astonished':'\ud83d\ude32',
  'athletic_shoe':'\ud83d\udc5f',
  'atm':'\ud83c\udfe7',
  'atom_symbol':'\u269b\ufe0f',
  'avocado':'\ud83e\udd51',
  'b':'\ud83c\udd71\ufe0f',
  'baby':'\ud83d\udc76',
  'baby_bottle':'\ud83c\udf7c',
  'baby_chick':'\ud83d\udc24',
  'baby_symbol':'\ud83d\udebc',
  'back':'\ud83d\udd19',
  'bacon':'\ud83e\udd53',
  'badminton':'\ud83c\udff8',
  'baggage_claim':'\ud83d\udec4',
  'baguette_bread':'\ud83e\udd56',
  'balance_scale':'\u2696\ufe0f',
  'balloon':'\ud83c\udf88',
  'ballot_box':'\ud83d\uddf3',
  'ballot_box_with_check':'\u2611\ufe0f',
  'bamboo':'\ud83c\udf8d',
  'banana':'\ud83c\udf4c',
  'bangbang':'\u203c\ufe0f',
  'bank':'\ud83c\udfe6',
  'bar_chart':'\ud83d\udcca',
  'barber':'\ud83d\udc88',
  'baseball':'\u26be\ufe0f',
  'basketball':'\ud83c\udfc0',
  'basketball_man':'\u26f9\ufe0f',
  'basketball_woman':'\u26f9\ufe0f&zwj;\u2640\ufe0f',
  'bat':'\ud83e\udd87',
  'bath':'\ud83d\udec0',
  'bathtub':'\ud83d\udec1',
  'battery':'\ud83d\udd0b',
  'beach_umbrella':'\ud83c\udfd6',
  'bear':'\ud83d\udc3b',
  'bed':'\ud83d\udecf',
  'bee':'\ud83d\udc1d',
  'beer':'\ud83c\udf7a',
  'beers':'\ud83c\udf7b',
  'beetle':'\ud83d\udc1e',
  'beginner':'\ud83d\udd30',
  'bell':'\ud83d\udd14',
  'bellhop_bell':'\ud83d\udece',
  'bento':'\ud83c\udf71',
  'biking_man':'\ud83d\udeb4',
  'bike':'\ud83d\udeb2',
  'biking_woman':'\ud83d\udeb4&zwj;\u2640\ufe0f',
  'bikini':'\ud83d\udc59',
  'biohazard':'\u2623\ufe0f',
  'bird':'\ud83d\udc26',
  'birthday':'\ud83c\udf82',
  'black_circle':'\u26ab\ufe0f',
  'black_flag':'\ud83c\udff4',
  'black_heart':'\ud83d\udda4',
  'black_joker':'\ud83c\udccf',
  'black_large_square':'\u2b1b\ufe0f',
  'black_medium_small_square':'\u25fe\ufe0f',
  'black_medium_square':'\u25fc\ufe0f',
  'black_nib':'\u2712\ufe0f',
  'black_small_square':'\u25aa\ufe0f',
  'black_square_button':'\ud83d\udd32',
  'blonde_man':'\ud83d\udc71',
  'blonde_woman':'\ud83d\udc71&zwj;\u2640\ufe0f',
  'blossom':'\ud83c\udf3c',
  'blowfish':'\ud83d\udc21',
  'blue_book':'\ud83d\udcd8',
  'blue_car':'\ud83d\ude99',
  'blue_heart':'\ud83d\udc99',
  'blush':'\ud83d\ude0a',
  'boar':'\ud83d\udc17',
  'boat':'\u26f5\ufe0f',
  'bomb':'\ud83d\udca3',
  'book':'\ud83d\udcd6',
  'bookmark':'\ud83d\udd16',
  'bookmark_tabs':'\ud83d\udcd1',
  'books':'\ud83d\udcda',
  'boom':'\ud83d\udca5',
  'boot':'\ud83d\udc62',
  'bouquet':'\ud83d\udc90',
  'bowing_man':'\ud83d\ude47',
  'bow_and_arrow':'\ud83c\udff9',
  'bowing_woman':'\ud83d\ude47&zwj;\u2640\ufe0f',
  'bowling':'\ud83c\udfb3',
  'boxing_glove':'\ud83e\udd4a',
  'boy':'\ud83d\udc66',
  'bread':'\ud83c\udf5e',
  'bride_with_veil':'\ud83d\udc70',
  'bridge_at_night':'\ud83c\udf09',
  'briefcase':'\ud83d\udcbc',
  'broken_heart':'\ud83d\udc94',
  'bug':'\ud83d\udc1b',
  'building_construction':'\ud83c\udfd7',
  'bulb':'\ud83d\udca1',
  'bullettrain_front':'\ud83d\ude85',
  'bullettrain_side':'\ud83d\ude84',
  'burrito':'\ud83c\udf2f',
  'bus':'\ud83d\ude8c',
  'business_suit_levitating':'\ud83d\udd74',
  'busstop':'\ud83d\ude8f',
  'bust_in_silhouette':'\ud83d\udc64',
  'busts_in_silhouette':'\ud83d\udc65',
  'butterfly':'\ud83e\udd8b',
  'cactus':'\ud83c\udf35',
  'cake':'\ud83c\udf70',
  'calendar':'\ud83d\udcc6',
  'call_me_hand':'\ud83e\udd19',
  'calling':'\ud83d\udcf2',
  'camel':'\ud83d\udc2b',
  'camera':'\ud83d\udcf7',
  'camera_flash':'\ud83d\udcf8',
  'camping':'\ud83c\udfd5',
  'cancer':'\u264b\ufe0f',
  'candle':'\ud83d\udd6f',
  'candy':'\ud83c\udf6c',
  'canoe':'\ud83d\udef6',
  'capital_abcd':'\ud83d\udd20',
  'capricorn':'\u2651\ufe0f',
  'car':'\ud83d\ude97',
  'card_file_box':'\ud83d\uddc3',
  'card_index':'\ud83d\udcc7',
  'card_index_dividers':'\ud83d\uddc2',
  'carousel_horse':'\ud83c\udfa0',
  'carrot':'\ud83e\udd55',
  'cat':'\ud83d\udc31',
  'cat2':'\ud83d\udc08',
  'cd':'\ud83d\udcbf',
  'chains':'\u26d3',
  'champagne':'\ud83c\udf7e',
  'chart':'\ud83d\udcb9',
  'chart_with_downwards_trend':'\ud83d\udcc9',
  'chart_with_upwards_trend':'\ud83d\udcc8',
  'checkered_flag':'\ud83c\udfc1',
  'cheese':'\ud83e\uddc0',
  'cherries':'\ud83c\udf52',
  'cherry_blossom':'\ud83c\udf38',
  'chestnut':'\ud83c\udf30',
  'chicken':'\ud83d\udc14',
  'children_crossing':'\ud83d\udeb8',
  'chipmunk':'\ud83d\udc3f',
  'chocolate_bar':'\ud83c\udf6b',
  'christmas_tree':'\ud83c\udf84',
  'church':'\u26ea\ufe0f',
  'cinema':'\ud83c\udfa6',
  'circus_tent':'\ud83c\udfaa',
  'city_sunrise':'\ud83c\udf07',
  'city_sunset':'\ud83c\udf06',
  'cityscape':'\ud83c\udfd9',
  'cl':'\ud83c\udd91',
  'clamp':'\ud83d\udddc',
  'clap':'\ud83d\udc4f',
  'clapper':'\ud83c\udfac',
  'classical_building':'\ud83c\udfdb',
  'clinking_glasses':'\ud83e\udd42',
  'clipboard':'\ud83d\udccb',
  'clock1':'\ud83d\udd50',
  'clock10':'\ud83d\udd59',
  'clock1030':'\ud83d\udd65',
  'clock11':'\ud83d\udd5a',
  'clock1130':'\ud83d\udd66',
  'clock12':'\ud83d\udd5b',
  'clock1230':'\ud83d\udd67',
  'clock130':'\ud83d\udd5c',
  'clock2':'\ud83d\udd51',
  'clock230':'\ud83d\udd5d',
  'clock3':'\ud83d\udd52',
  'clock330':'\ud83d\udd5e',
  'clock4':'\ud83d\udd53',
  'clock430':'\ud83d\udd5f',
  'clock5':'\ud83d\udd54',
  'clock530':'\ud83d\udd60',
  'clock6':'\ud83d\udd55',
  'clock630':'\ud83d\udd61',
  'clock7':'\ud83d\udd56',
  'clock730':'\ud83d\udd62',
  'clock8':'\ud83d\udd57',
  'clock830':'\ud83d\udd63',
  'clock9':'\ud83d\udd58',
  'clock930':'\ud83d\udd64',
  'closed_book':'\ud83d\udcd5',
  'closed_lock_with_key':'\ud83d\udd10',
  'closed_umbrella':'\ud83c\udf02',
  'cloud':'\u2601\ufe0f',
  'cloud_with_lightning':'\ud83c\udf29',
  'cloud_with_lightning_and_rain':'\u26c8',
  'cloud_with_rain':'\ud83c\udf27',
  'cloud_with_snow':'\ud83c\udf28',
  'clown_face':'\ud83e\udd21',
  'clubs':'\u2663\ufe0f',
  'cocktail':'\ud83c\udf78',
  'coffee':'\u2615\ufe0f',
  'coffin':'\u26b0\ufe0f',
  'cold_sweat':'\ud83d\ude30',
  'comet':'\u2604\ufe0f',
  'computer':'\ud83d\udcbb',
  'computer_mouse':'\ud83d\uddb1',
  'confetti_ball':'\ud83c\udf8a',
  'confounded':'\ud83d\ude16',
  'confused':'\ud83d\ude15',
  'congratulations':'\u3297\ufe0f',
  'construction':'\ud83d\udea7',
  'construction_worker_man':'\ud83d\udc77',
  'construction_worker_woman':'\ud83d\udc77&zwj;\u2640\ufe0f',
  'control_knobs':'\ud83c\udf9b',
  'convenience_store':'\ud83c\udfea',
  'cookie':'\ud83c\udf6a',
  'cool':'\ud83c\udd92',
  'policeman':'\ud83d\udc6e',
  'copyright':'\u00a9\ufe0f',
  'corn':'\ud83c\udf3d',
  'couch_and_lamp':'\ud83d\udecb',
  'couple':'\ud83d\udc6b',
  'couple_with_heart_woman_man':'\ud83d\udc91',
  'couple_with_heart_man_man':'\ud83d\udc68&zwj;\u2764\ufe0f&zwj;\ud83d\udc68',
  'couple_with_heart_woman_woman':'\ud83d\udc69&zwj;\u2764\ufe0f&zwj;\ud83d\udc69',
  'couplekiss_man_man':'\ud83d\udc68&zwj;\u2764\ufe0f&zwj;\ud83d\udc8b&zwj;\ud83d\udc68',
  'couplekiss_man_woman':'\ud83d\udc8f',
  'couplekiss_woman_woman':'\ud83d\udc69&zwj;\u2764\ufe0f&zwj;\ud83d\udc8b&zwj;\ud83d\udc69',
  'cow':'\ud83d\udc2e',
  'cow2':'\ud83d\udc04',
  'cowboy_hat_face':'\ud83e\udd20',
  'crab':'\ud83e\udd80',
  'crayon':'\ud83d\udd8d',
  'credit_card':'\ud83d\udcb3',
  'crescent_moon':'\ud83c\udf19',
  'cricket':'\ud83c\udfcf',
  'crocodile':'\ud83d\udc0a',
  'croissant':'\ud83e\udd50',
  'crossed_fingers':'\ud83e\udd1e',
  'crossed_flags':'\ud83c\udf8c',
  'crossed_swords':'\u2694\ufe0f',
  'crown':'\ud83d\udc51',
  'cry':'\ud83d\ude22',
  'crying_cat_face':'\ud83d\ude3f',
  'crystal_ball':'\ud83d\udd2e',
  'cucumber':'\ud83e\udd52',
  'cupid':'\ud83d\udc98',
  'curly_loop':'\u27b0',
  'currency_exchange':'\ud83d\udcb1',
  'curry':'\ud83c\udf5b',
  'custard':'\ud83c\udf6e',
  'customs':'\ud83d\udec3',
  'cyclone':'\ud83c\udf00',
  'dagger':'\ud83d\udde1',
  'dancer':'\ud83d\udc83',
  'dancing_women':'\ud83d\udc6f',
  'dancing_men':'\ud83d\udc6f&zwj;\u2642\ufe0f',
  'dango':'\ud83c\udf61',
  'dark_sunglasses':'\ud83d\udd76',
  'dart':'\ud83c\udfaf',
  'dash':'\ud83d\udca8',
  'date':'\ud83d\udcc5',
  'deciduous_tree':'\ud83c\udf33',
  'deer':'\ud83e\udd8c',
  'department_store':'\ud83c\udfec',
  'derelict_house':'\ud83c\udfda',
  'desert':'\ud83c\udfdc',
  'desert_island':'\ud83c\udfdd',
  'desktop_computer':'\ud83d\udda5',
  'male_detective':'\ud83d\udd75\ufe0f',
  'diamond_shape_with_a_dot_inside':'\ud83d\udca0',
  'diamonds':'\u2666\ufe0f',
  'disappointed':'\ud83d\ude1e',
  'disappointed_relieved':'\ud83d\ude25',
  'dizzy':'\ud83d\udcab',
  'dizzy_face':'\ud83d\ude35',
  'do_not_litter':'\ud83d\udeaf',
  'dog':'\ud83d\udc36',
  'dog2':'\ud83d\udc15',
  'dollar':'\ud83d\udcb5',
  'dolls':'\ud83c\udf8e',
  'dolphin':'\ud83d\udc2c',
  'door':'\ud83d\udeaa',
  'doughnut':'\ud83c\udf69',
  'dove':'\ud83d\udd4a',
  'dragon':'\ud83d\udc09',
  'dragon_face':'\ud83d\udc32',
  'dress':'\ud83d\udc57',
  'dromedary_camel':'\ud83d\udc2a',
  'drooling_face':'\ud83e\udd24',
  'droplet':'\ud83d\udca7',
  'drum':'\ud83e\udd41',
  'duck':'\ud83e\udd86',
  'dvd':'\ud83d\udcc0',
  'e-mail':'\ud83d\udce7',
  'eagle':'\ud83e\udd85',
  'ear':'\ud83d\udc42',
  'ear_of_rice':'\ud83c\udf3e',
  'earth_africa':'\ud83c\udf0d',
  'earth_americas':'\ud83c\udf0e',
  'earth_asia':'\ud83c\udf0f',
  'egg':'\ud83e\udd5a',
  'eggplant':'\ud83c\udf46',
  'eight_pointed_black_star':'\u2734\ufe0f',
  'eight_spoked_asterisk':'\u2733\ufe0f',
  'electric_plug':'\ud83d\udd0c',
  'elephant':'\ud83d\udc18',
  'email':'\u2709\ufe0f',
  'end':'\ud83d\udd1a',
  'envelope_with_arrow':'\ud83d\udce9',
  'euro':'\ud83d\udcb6',
  'european_castle':'\ud83c\udff0',
  'european_post_office':'\ud83c\udfe4',
  'evergreen_tree':'\ud83c\udf32',
  'exclamation':'\u2757\ufe0f',
  'expressionless':'\ud83d\ude11',
  'eye':'\ud83d\udc41',
  'eye_speech_bubble':'\ud83d\udc41&zwj;\ud83d\udde8',
  'eyeglasses':'\ud83d\udc53',
  'eyes':'\ud83d\udc40',
  'face_with_head_bandage':'\ud83e\udd15',
  'face_with_thermometer':'\ud83e\udd12',
  'fist_oncoming':'\ud83d\udc4a',
  'factory':'\ud83c\udfed',
  'fallen_leaf':'\ud83c\udf42',
  'family_man_woman_boy':'\ud83d\udc6a',
  'family_man_boy':'\ud83d\udc68&zwj;\ud83d\udc66',
  'family_man_boy_boy':'\ud83d\udc68&zwj;\ud83d\udc66&zwj;\ud83d\udc66',
  'family_man_girl':'\ud83d\udc68&zwj;\ud83d\udc67',
  'family_man_girl_boy':'\ud83d\udc68&zwj;\ud83d\udc67&zwj;\ud83d\udc66',
  'family_man_girl_girl':'\ud83d\udc68&zwj;\ud83d\udc67&zwj;\ud83d\udc67',
  'family_man_man_boy':'\ud83d\udc68&zwj;\ud83d\udc68&zwj;\ud83d\udc66',
  'family_man_man_boy_boy':'\ud83d\udc68&zwj;\ud83d\udc68&zwj;\ud83d\udc66&zwj;\ud83d\udc66',
  'family_man_man_girl':'\ud83d\udc68&zwj;\ud83d\udc68&zwj;\ud83d\udc67',
  'family_man_man_girl_boy':'\ud83d\udc68&zwj;\ud83d\udc68&zwj;\ud83d\udc67&zwj;\ud83d\udc66',
  'family_man_man_girl_girl':'\ud83d\udc68&zwj;\ud83d\udc68&zwj;\ud83d\udc67&zwj;\ud83d\udc67',
  'family_man_woman_boy_boy':'\ud83d\udc68&zwj;\ud83d\udc69&zwj;\ud83d\udc66&zwj;\ud83d\udc66',
  'family_man_woman_girl':'\ud83d\udc68&zwj;\ud83d\udc69&zwj;\ud83d\udc67',
  'family_man_woman_girl_boy':'\ud83d\udc68&zwj;\ud83d\udc69&zwj;\ud83d\udc67&zwj;\ud83d\udc66',
  'family_man_woman_girl_girl':'\ud83d\udc68&zwj;\ud83d\udc69&zwj;\ud83d\udc67&zwj;\ud83d\udc67',
  'family_woman_boy':'\ud83d\udc69&zwj;\ud83d\udc66',
  'family_woman_boy_boy':'\ud83d\udc69&zwj;\ud83d\udc66&zwj;\ud83d\udc66',
  'family_woman_girl':'\ud83d\udc69&zwj;\ud83d\udc67',
  'family_woman_girl_boy':'\ud83d\udc69&zwj;\ud83d\udc67&zwj;\ud83d\udc66',
  'family_woman_girl_girl':'\ud83d\udc69&zwj;\ud83d\udc67&zwj;\ud83d\udc67',
  'family_woman_woman_boy':'\ud83d\udc69&zwj;\ud83d\udc69&zwj;\ud83d\udc66',
  'family_woman_woman_boy_boy':'\ud83d\udc69&zwj;\ud83d\udc69&zwj;\ud83d\udc66&zwj;\ud83d\udc66',
  'family_woman_woman_girl':'\ud83d\udc69&zwj;\ud83d\udc69&zwj;\ud83d\udc67',
  'family_woman_woman_girl_boy':'\ud83d\udc69&zwj;\ud83d\udc69&zwj;\ud83d\udc67&zwj;\ud83d\udc66',
  'family_woman_woman_girl_girl':'\ud83d\udc69&zwj;\ud83d\udc69&zwj;\ud83d\udc67&zwj;\ud83d\udc67',
  'fast_forward':'\u23e9',
  'fax':'\ud83d\udce0',
  'fearful':'\ud83d\ude28',
  'feet':'\ud83d\udc3e',
  'female_detective':'\ud83d\udd75\ufe0f&zwj;\u2640\ufe0f',
  'ferris_wheel':'\ud83c\udfa1',
  'ferry':'\u26f4',
  'field_hockey':'\ud83c\udfd1',
  'file_cabinet':'\ud83d\uddc4',
  'file_folder':'\ud83d\udcc1',
  'film_projector':'\ud83d\udcfd',
  'film_strip':'\ud83c\udf9e',
  'fire':'\ud83d\udd25',
  'fire_engine':'\ud83d\ude92',
  'fireworks':'\ud83c\udf86',
  'first_quarter_moon':'\ud83c\udf13',
  'first_quarter_moon_with_face':'\ud83c\udf1b',
  'fish':'\ud83d\udc1f',
  'fish_cake':'\ud83c\udf65',
  'fishing_pole_and_fish':'\ud83c\udfa3',
  'fist_raised':'\u270a',
  'fist_left':'\ud83e\udd1b',
  'fist_right':'\ud83e\udd1c',
  'flags':'\ud83c\udf8f',
  'flashlight':'\ud83d\udd26',
  'fleur_de_lis':'\u269c\ufe0f',
  'flight_arrival':'\ud83d\udeec',
  'flight_departure':'\ud83d\udeeb',
  'floppy_disk':'\ud83d\udcbe',
  'flower_playing_cards':'\ud83c\udfb4',
  'flushed':'\ud83d\ude33',
  'fog':'\ud83c\udf2b',
  'foggy':'\ud83c\udf01',
  'football':'\ud83c\udfc8',
  'footprints':'\ud83d\udc63',
  'fork_and_knife':'\ud83c\udf74',
  'fountain':'\u26f2\ufe0f',
  'fountain_pen':'\ud83d\udd8b',
  'four_leaf_clover':'\ud83c\udf40',
  'fox_face':'\ud83e\udd8a',
  'framed_picture':'\ud83d\uddbc',
  'free':'\ud83c\udd93',
  'fried_egg':'\ud83c\udf73',
  'fried_shrimp':'\ud83c\udf64',
  'fries':'\ud83c\udf5f',
  'frog':'\ud83d\udc38',
  'frowning':'\ud83d\ude26',
  'frowning_face':'\u2639\ufe0f',
  'frowning_man':'\ud83d\ude4d&zwj;\u2642\ufe0f',
  'frowning_woman':'\ud83d\ude4d',
  'middle_finger':'\ud83d\udd95',
  'fuelpump':'\u26fd\ufe0f',
  'full_moon':'\ud83c\udf15',
  'full_moon_with_face':'\ud83c\udf1d',
  'funeral_urn':'\u26b1\ufe0f',
  'game_die':'\ud83c\udfb2',
  'gear':'\u2699\ufe0f',
  'gem':'\ud83d\udc8e',
  'gemini':'\u264a\ufe0f',
  'ghost':'\ud83d\udc7b',
  'gift':'\ud83c\udf81',
  'gift_heart':'\ud83d\udc9d',
  'girl':'\ud83d\udc67',
  'globe_with_meridians':'\ud83c\udf10',
  'goal_net':'\ud83e\udd45',
  'goat':'\ud83d\udc10',
  'golf':'\u26f3\ufe0f',
  'golfing_man':'\ud83c\udfcc\ufe0f',
  'golfing_woman':'\ud83c\udfcc\ufe0f&zwj;\u2640\ufe0f',
  'gorilla':'\ud83e\udd8d',
  'grapes':'\ud83c\udf47',
  'green_apple':'\ud83c\udf4f',
  'green_book':'\ud83d\udcd7',
  'green_heart':'\ud83d\udc9a',
  'green_salad':'\ud83e\udd57',
  'grey_exclamation':'\u2755',
  'grey_question':'\u2754',
  'grimacing':'\ud83d\ude2c',
  'grin':'\ud83d\ude01',
  'grinning':'\ud83d\ude00',
  'guardsman':'\ud83d\udc82',
  'guardswoman':'\ud83d\udc82&zwj;\u2640\ufe0f',
  'guitar':'\ud83c\udfb8',
  'gun':'\ud83d\udd2b',
  'haircut_woman':'\ud83d\udc87',
  'haircut_man':'\ud83d\udc87&zwj;\u2642\ufe0f',
  'hamburger':'\ud83c\udf54',
  'hammer':'\ud83d\udd28',
  'hammer_and_pick':'\u2692',
  'hammer_and_wrench':'\ud83d\udee0',
  'hamster':'\ud83d\udc39',
  'hand':'\u270b',
  'handbag':'\ud83d\udc5c',
  'handshake':'\ud83e\udd1d',
  'hankey':'\ud83d\udca9',
  'hatched_chick':'\ud83d\udc25',
  'hatching_chick':'\ud83d\udc23',
  'headphones':'\ud83c\udfa7',
  'hear_no_evil':'\ud83d\ude49',
  'heart':'\u2764\ufe0f',
  'heart_decoration':'\ud83d\udc9f',
  'heart_eyes':'\ud83d\ude0d',
  'heart_eyes_cat':'\ud83d\ude3b',
  'heartbeat':'\ud83d\udc93',
  'heartpulse':'\ud83d\udc97',
  'hearts':'\u2665\ufe0f',
  'heavy_check_mark':'\u2714\ufe0f',
  'heavy_division_sign':'\u2797',
  'heavy_dollar_sign':'\ud83d\udcb2',
  'heavy_heart_exclamation':'\u2763\ufe0f',
  'heavy_minus_sign':'\u2796',
  'heavy_multiplication_x':'\u2716\ufe0f',
  'heavy_plus_sign':'\u2795',
  'helicopter':'\ud83d\ude81',
  'herb':'\ud83c\udf3f',
  'hibiscus':'\ud83c\udf3a',
  'high_brightness':'\ud83d\udd06',
  'high_heel':'\ud83d\udc60',
  'hocho':'\ud83d\udd2a',
  'hole':'\ud83d\udd73',
  'honey_pot':'\ud83c\udf6f',
  'horse':'\ud83d\udc34',
  'horse_racing':'\ud83c\udfc7',
  'hospital':'\ud83c\udfe5',
  'hot_pepper':'\ud83c\udf36',
  'hotdog':'\ud83c\udf2d',
  'hotel':'\ud83c\udfe8',
  'hotsprings':'\u2668\ufe0f',
  'hourglass':'\u231b\ufe0f',
  'hourglass_flowing_sand':'\u23f3',
  'house':'\ud83c\udfe0',
  'house_with_garden':'\ud83c\udfe1',
  'houses':'\ud83c\udfd8',
  'hugs':'\ud83e\udd17',
  'hushed':'\ud83d\ude2f',
  'ice_cream':'\ud83c\udf68',
  'ice_hockey':'\ud83c\udfd2',
  'ice_skate':'\u26f8',
  'icecream':'\ud83c\udf66',
  'id':'\ud83c\udd94',
  'ideograph_advantage':'\ud83c\ude50',
  'imp':'\ud83d\udc7f',
  'inbox_tray':'\ud83d\udce5',
  'incoming_envelope':'\ud83d\udce8',
  'tipping_hand_woman':'\ud83d\udc81',
  'information_source':'\u2139\ufe0f',
  'innocent':'\ud83d\ude07',
  'interrobang':'\u2049\ufe0f',
  'iphone':'\ud83d\udcf1',
  'izakaya_lantern':'\ud83c\udfee',
  'jack_o_lantern':'\ud83c\udf83',
  'japan':'\ud83d\uddfe',
  'japanese_castle':'\ud83c\udfef',
  'japanese_goblin':'\ud83d\udc7a',
  'japanese_ogre':'\ud83d\udc79',
  'jeans':'\ud83d\udc56',
  'joy':'\ud83d\ude02',
  'joy_cat':'\ud83d\ude39',
  'joystick':'\ud83d\udd79',
  'kaaba':'\ud83d\udd4b',
  'key':'\ud83d\udd11',
  'keyboard':'\u2328\ufe0f',
  'keycap_ten':'\ud83d\udd1f',
  'kick_scooter':'\ud83d\udef4',
  'kimono':'\ud83d\udc58',
  'kiss':'\ud83d\udc8b',
  'kissing':'\ud83d\ude17',
  'kissing_cat':'\ud83d\ude3d',
  'kissing_closed_eyes':'\ud83d\ude1a',
  'kissing_heart':'\ud83d\ude18',
  'kissing_smiling_eyes':'\ud83d\ude19',
  'kiwi_fruit':'\ud83e\udd5d',
  'koala':'\ud83d\udc28',
  'koko':'\ud83c\ude01',
  'label':'\ud83c\udff7',
  'large_blue_circle':'\ud83d\udd35',
  'large_blue_diamond':'\ud83d\udd37',
  'large_orange_diamond':'\ud83d\udd36',
  'last_quarter_moon':'\ud83c\udf17',
  'last_quarter_moon_with_face':'\ud83c\udf1c',
  'latin_cross':'\u271d\ufe0f',
  'laughing':'\ud83d\ude06',
  'leaves':'\ud83c\udf43',
  'ledger':'\ud83d\udcd2',
  'left_luggage':'\ud83d\udec5',
  'left_right_arrow':'\u2194\ufe0f',
  'leftwards_arrow_with_hook':'\u21a9\ufe0f',
  'lemon':'\ud83c\udf4b',
  'leo':'\u264c\ufe0f',
  'leopard':'\ud83d\udc06',
  'level_slider':'\ud83c\udf9a',
  'libra':'\u264e\ufe0f',
  'light_rail':'\ud83d\ude88',
  'link':'\ud83d\udd17',
  'lion':'\ud83e\udd81',
  'lips':'\ud83d\udc44',
  'lipstick':'\ud83d\udc84',
  'lizard':'\ud83e\udd8e',
  'lock':'\ud83d\udd12',
  'lock_with_ink_pen':'\ud83d\udd0f',
  'lollipop':'\ud83c\udf6d',
  'loop':'\u27bf',
  'loud_sound':'\ud83d\udd0a',
  'loudspeaker':'\ud83d\udce2',
  'love_hotel':'\ud83c\udfe9',
  'love_letter':'\ud83d\udc8c',
  'low_brightness':'\ud83d\udd05',
  'lying_face':'\ud83e\udd25',
  'm':'\u24c2\ufe0f',
  'mag':'\ud83d\udd0d',
  'mag_right':'\ud83d\udd0e',
  'mahjong':'\ud83c\udc04\ufe0f',
  'mailbox':'\ud83d\udceb',
  'mailbox_closed':'\ud83d\udcea',
  'mailbox_with_mail':'\ud83d\udcec',
  'mailbox_with_no_mail':'\ud83d\udced',
  'man':'\ud83d\udc68',
  'man_artist':'\ud83d\udc68&zwj;\ud83c\udfa8',
  'man_astronaut':'\ud83d\udc68&zwj;\ud83d\ude80',
  'man_cartwheeling':'\ud83e\udd38&zwj;\u2642\ufe0f',
  'man_cook':'\ud83d\udc68&zwj;\ud83c\udf73',
  'man_dancing':'\ud83d\udd7a',
  'man_facepalming':'\ud83e\udd26&zwj;\u2642\ufe0f',
  'man_factory_worker':'\ud83d\udc68&zwj;\ud83c\udfed',
  'man_farmer':'\ud83d\udc68&zwj;\ud83c\udf3e',
  'man_firefighter':'\ud83d\udc68&zwj;\ud83d\ude92',
  'man_health_worker':'\ud83d\udc68&zwj;\u2695\ufe0f',
  'man_in_tuxedo':'\ud83e\udd35',
  'man_judge':'\ud83d\udc68&zwj;\u2696\ufe0f',
  'man_juggling':'\ud83e\udd39&zwj;\u2642\ufe0f',
  'man_mechanic':'\ud83d\udc68&zwj;\ud83d\udd27',
  'man_office_worker':'\ud83d\udc68&zwj;\ud83d\udcbc',
  'man_pilot':'\ud83d\udc68&zwj;\u2708\ufe0f',
  'man_playing_handball':'\ud83e\udd3e&zwj;\u2642\ufe0f',
  'man_playing_water_polo':'\ud83e\udd3d&zwj;\u2642\ufe0f',
  'man_scientist':'\ud83d\udc68&zwj;\ud83d\udd2c',
  'man_shrugging':'\ud83e\udd37&zwj;\u2642\ufe0f',
  'man_singer':'\ud83d\udc68&zwj;\ud83c\udfa4',
  'man_student':'\ud83d\udc68&zwj;\ud83c\udf93',
  'man_teacher':'\ud83d\udc68&zwj;\ud83c\udfeb',
  'man_technologist':'\ud83d\udc68&zwj;\ud83d\udcbb',
  'man_with_gua_pi_mao':'\ud83d\udc72',
  'man_with_turban':'\ud83d\udc73',
  'tangerine':'\ud83c\udf4a',
  'mans_shoe':'\ud83d\udc5e',
  'mantelpiece_clock':'\ud83d\udd70',
  'maple_leaf':'\ud83c\udf41',
  'martial_arts_uniform':'\ud83e\udd4b',
  'mask':'\ud83d\ude37',
  'massage_woman':'\ud83d\udc86',
  'massage_man':'\ud83d\udc86&zwj;\u2642\ufe0f',
  'meat_on_bone':'\ud83c\udf56',
  'medal_military':'\ud83c\udf96',
  'medal_sports':'\ud83c\udfc5',
  'mega':'\ud83d\udce3',
  'melon':'\ud83c\udf48',
  'memo':'\ud83d\udcdd',
  'men_wrestling':'\ud83e\udd3c&zwj;\u2642\ufe0f',
  'menorah':'\ud83d\udd4e',
  'mens':'\ud83d\udeb9',
  'metal':'\ud83e\udd18',
  'metro':'\ud83d\ude87',
  'microphone':'\ud83c\udfa4',
  'microscope':'\ud83d\udd2c',
  'milk_glass':'\ud83e\udd5b',
  'milky_way':'\ud83c\udf0c',
  'minibus':'\ud83d\ude90',
  'minidisc':'\ud83d\udcbd',
  'mobile_phone_off':'\ud83d\udcf4',
  'money_mouth_face':'\ud83e\udd11',
  'money_with_wings':'\ud83d\udcb8',
  'moneybag':'\ud83d\udcb0',
  'monkey':'\ud83d\udc12',
  'monkey_face':'\ud83d\udc35',
  'monorail':'\ud83d\ude9d',
  'moon':'\ud83c\udf14',
  'mortar_board':'\ud83c\udf93',
  'mosque':'\ud83d\udd4c',
  'motor_boat':'\ud83d\udee5',
  'motor_scooter':'\ud83d\udef5',
  'motorcycle':'\ud83c\udfcd',
  'motorway':'\ud83d\udee3',
  'mount_fuji':'\ud83d\uddfb',
  'mountain':'\u26f0',
  'mountain_biking_man':'\ud83d\udeb5',
  'mountain_biking_woman':'\ud83d\udeb5&zwj;\u2640\ufe0f',
  'mountain_cableway':'\ud83d\udea0',
  'mountain_railway':'\ud83d\ude9e',
  'mountain_snow':'\ud83c\udfd4',
  'mouse':'\ud83d\udc2d',
  'mouse2':'\ud83d\udc01',
  'movie_camera':'\ud83c\udfa5',
  'moyai':'\ud83d\uddff',
  'mrs_claus':'\ud83e\udd36',
  'muscle':'\ud83d\udcaa',
  'mushroom':'\ud83c\udf44',
  'musical_keyboard':'\ud83c\udfb9',
  'musical_note':'\ud83c\udfb5',
  'musical_score':'\ud83c\udfbc',
  'mute':'\ud83d\udd07',
  'nail_care':'\ud83d\udc85',
  'name_badge':'\ud83d\udcdb',
  'national_park':'\ud83c\udfde',
  'nauseated_face':'\ud83e\udd22',
  'necktie':'\ud83d\udc54',
  'negative_squared_cross_mark':'\u274e',
  'nerd_face':'\ud83e\udd13',
  'neutral_face':'\ud83d\ude10',
  'new':'\ud83c\udd95',
  'new_moon':'\ud83c\udf11',
  'new_moon_with_face':'\ud83c\udf1a',
  'newspaper':'\ud83d\udcf0',
  'newspaper_roll':'\ud83d\uddde',
  'next_track_button':'\u23ed',
  'ng':'\ud83c\udd96',
  'no_good_man':'\ud83d\ude45&zwj;\u2642\ufe0f',
  'no_good_woman':'\ud83d\ude45',
  'night_with_stars':'\ud83c\udf03',
  'no_bell':'\ud83d\udd15',
  'no_bicycles':'\ud83d\udeb3',
  'no_entry':'\u26d4\ufe0f',
  'no_entry_sign':'\ud83d\udeab',
  'no_mobile_phones':'\ud83d\udcf5',
  'no_mouth':'\ud83d\ude36',
  'no_pedestrians':'\ud83d\udeb7',
  'no_smoking':'\ud83d\udead',
  'non-potable_water':'\ud83d\udeb1',
  'nose':'\ud83d\udc43',
  'notebook':'\ud83d\udcd3',
  'notebook_with_decorative_cover':'\ud83d\udcd4',
  'notes':'\ud83c\udfb6',
  'nut_and_bolt':'\ud83d\udd29',
  'o':'\u2b55\ufe0f',
  'o2':'\ud83c\udd7e\ufe0f',
  'ocean':'\ud83c\udf0a',
  'octopus':'\ud83d\udc19',
  'oden':'\ud83c\udf62',
  'office':'\ud83c\udfe2',
  'oil_drum':'\ud83d\udee2',
  'ok':'\ud83c\udd97',
  'ok_hand':'\ud83d\udc4c',
  'ok_man':'\ud83d\ude46&zwj;\u2642\ufe0f',
  'ok_woman':'\ud83d\ude46',
  'old_key':'\ud83d\udddd',
  'older_man':'\ud83d\udc74',
  'older_woman':'\ud83d\udc75',
  'om':'\ud83d\udd49',
  'on':'\ud83d\udd1b',
  'oncoming_automobile':'\ud83d\ude98',
  'oncoming_bus':'\ud83d\ude8d',
  'oncoming_police_car':'\ud83d\ude94',
  'oncoming_taxi':'\ud83d\ude96',
  'open_file_folder':'\ud83d\udcc2',
  'open_hands':'\ud83d\udc50',
  'open_mouth':'\ud83d\ude2e',
  'open_umbrella':'\u2602\ufe0f',
  'ophiuchus':'\u26ce',
  'orange_book':'\ud83d\udcd9',
  'orthodox_cross':'\u2626\ufe0f',
  'outbox_tray':'\ud83d\udce4',
  'owl':'\ud83e\udd89',
  'ox':'\ud83d\udc02',
  'package':'\ud83d\udce6',
  'page_facing_up':'\ud83d\udcc4',
  'page_with_curl':'\ud83d\udcc3',
  'pager':'\ud83d\udcdf',
  'paintbrush':'\ud83d\udd8c',
  'palm_tree':'\ud83c\udf34',
  'pancakes':'\ud83e\udd5e',
  'panda_face':'\ud83d\udc3c',
  'paperclip':'\ud83d\udcce',
  'paperclips':'\ud83d\udd87',
  'parasol_on_ground':'\u26f1',
  'parking':'\ud83c\udd7f\ufe0f',
  'part_alternation_mark':'\u303d\ufe0f',
  'partly_sunny':'\u26c5\ufe0f',
  'passenger_ship':'\ud83d\udef3',
  'passport_control':'\ud83d\udec2',
  'pause_button':'\u23f8',
  'peace_symbol':'\u262e\ufe0f',
  'peach':'\ud83c\udf51',
  'peanuts':'\ud83e\udd5c',
  'pear':'\ud83c\udf50',
  'pen':'\ud83d\udd8a',
  'pencil2':'\u270f\ufe0f',
  'penguin':'\ud83d\udc27',
  'pensive':'\ud83d\ude14',
  'performing_arts':'\ud83c\udfad',
  'persevere':'\ud83d\ude23',
  'person_fencing':'\ud83e\udd3a',
  'pouting_woman':'\ud83d\ude4e',
  'phone':'\u260e\ufe0f',
  'pick':'\u26cf',
  'pig':'\ud83d\udc37',
  'pig2':'\ud83d\udc16',
  'pig_nose':'\ud83d\udc3d',
  'pill':'\ud83d\udc8a',
  'pineapple':'\ud83c\udf4d',
  'ping_pong':'\ud83c\udfd3',
  'pisces':'\u2653\ufe0f',
  'pizza':'\ud83c\udf55',
  'place_of_worship':'\ud83d\uded0',
  'plate_with_cutlery':'\ud83c\udf7d',
  'play_or_pause_button':'\u23ef',
  'point_down':'\ud83d\udc47',
  'point_left':'\ud83d\udc48',
  'point_right':'\ud83d\udc49',
  'point_up':'\u261d\ufe0f',
  'point_up_2':'\ud83d\udc46',
  'police_car':'\ud83d\ude93',
  'policewoman':'\ud83d\udc6e&zwj;\u2640\ufe0f',
  'poodle':'\ud83d\udc29',
  'popcorn':'\ud83c\udf7f',
  'post_office':'\ud83c\udfe3',
  'postal_horn':'\ud83d\udcef',
  'postbox':'\ud83d\udcee',
  'potable_water':'\ud83d\udeb0',
  'potato':'\ud83e\udd54',
  'pouch':'\ud83d\udc5d',
  'poultry_leg':'\ud83c\udf57',
  'pound':'\ud83d\udcb7',
  'rage':'\ud83d\ude21',
  'pouting_cat':'\ud83d\ude3e',
  'pouting_man':'\ud83d\ude4e&zwj;\u2642\ufe0f',
  'pray':'\ud83d\ude4f',
  'prayer_beads':'\ud83d\udcff',
  'pregnant_woman':'\ud83e\udd30',
  'previous_track_button':'\u23ee',
  'prince':'\ud83e\udd34',
  'princess':'\ud83d\udc78',
  'printer':'\ud83d\udda8',
  'purple_heart':'\ud83d\udc9c',
  'purse':'\ud83d\udc5b',
  'pushpin':'\ud83d\udccc',
  'put_litter_in_its_place':'\ud83d\udeae',
  'question':'\u2753',
  'rabbit':'\ud83d\udc30',
  'rabbit2':'\ud83d\udc07',
  'racehorse':'\ud83d\udc0e',
  'racing_car':'\ud83c\udfce',
  'radio':'\ud83d\udcfb',
  'radio_button':'\ud83d\udd18',
  'radioactive':'\u2622\ufe0f',
  'railway_car':'\ud83d\ude83',
  'railway_track':'\ud83d\udee4',
  'rainbow':'\ud83c\udf08',
  'rainbow_flag':'\ud83c\udff3\ufe0f&zwj;\ud83c\udf08',
  'raised_back_of_hand':'\ud83e\udd1a',
  'raised_hand_with_fingers_splayed':'\ud83d\udd90',
  'raised_hands':'\ud83d\ude4c',
  'raising_hand_woman':'\ud83d\ude4b',
  'raising_hand_man':'\ud83d\ude4b&zwj;\u2642\ufe0f',
  'ram':'\ud83d\udc0f',
  'ramen':'\ud83c\udf5c',
  'rat':'\ud83d\udc00',
  'record_button':'\u23fa',
  'recycle':'\u267b\ufe0f',
  'red_circle':'\ud83d\udd34',
  'registered':'\u00ae\ufe0f',
  'relaxed':'\u263a\ufe0f',
  'relieved':'\ud83d\ude0c',
  'reminder_ribbon':'\ud83c\udf97',
  'repeat':'\ud83d\udd01',
  'repeat_one':'\ud83d\udd02',
  'rescue_worker_helmet':'\u26d1',
  'restroom':'\ud83d\udebb',
  'revolving_hearts':'\ud83d\udc9e',
  'rewind':'\u23ea',
  'rhinoceros':'\ud83e\udd8f',
  'ribbon':'\ud83c\udf80',
  'rice':'\ud83c\udf5a',
  'rice_ball':'\ud83c\udf59',
  'rice_cracker':'\ud83c\udf58',
  'rice_scene':'\ud83c\udf91',
  'right_anger_bubble':'\ud83d\uddef',
  'ring':'\ud83d\udc8d',
  'robot':'\ud83e\udd16',
  'rocket':'\ud83d\ude80',
  'rofl':'\ud83e\udd23',
  'roll_eyes':'\ud83d\ude44',
  'roller_coaster':'\ud83c\udfa2',
  'rooster':'\ud83d\udc13',
  'rose':'\ud83c\udf39',
  'rosette':'\ud83c\udff5',
  'rotating_light':'\ud83d\udea8',
  'round_pushpin':'\ud83d\udccd',
  'rowing_man':'\ud83d\udea3',
  'rowing_woman':'\ud83d\udea3&zwj;\u2640\ufe0f',
  'rugby_football':'\ud83c\udfc9',
  'running_man':'\ud83c\udfc3',
  'running_shirt_with_sash':'\ud83c\udfbd',
  'running_woman':'\ud83c\udfc3&zwj;\u2640\ufe0f',
  'sa':'\ud83c\ude02\ufe0f',
  'sagittarius':'\u2650\ufe0f',
  'sake':'\ud83c\udf76',
  'sandal':'\ud83d\udc61',
  'santa':'\ud83c\udf85',
  'satellite':'\ud83d\udce1',
  'saxophone':'\ud83c\udfb7',
  'school':'\ud83c\udfeb',
  'school_satchel':'\ud83c\udf92',
  'scissors':'\u2702\ufe0f',
  'scorpion':'\ud83e\udd82',
  'scorpius':'\u264f\ufe0f',
  'scream':'\ud83d\ude31',
  'scream_cat':'\ud83d\ude40',
  'scroll':'\ud83d\udcdc',
  'seat':'\ud83d\udcba',
  'secret':'\u3299\ufe0f',
  'see_no_evil':'\ud83d\ude48',
  'seedling':'\ud83c\udf31',
  'selfie':'\ud83e\udd33',
  'shallow_pan_of_food':'\ud83e\udd58',
  'shamrock':'\u2618\ufe0f',
  'shark':'\ud83e\udd88',
  'shaved_ice':'\ud83c\udf67',
  'sheep':'\ud83d\udc11',
  'shell':'\ud83d\udc1a',
  'shield':'\ud83d\udee1',
  'shinto_shrine':'\u26e9',
  'ship':'\ud83d\udea2',
  'shirt':'\ud83d\udc55',
  'shopping':'\ud83d\udecd',
  'shopping_cart':'\ud83d\uded2',
  'shower':'\ud83d\udebf',
  'shrimp':'\ud83e\udd90',
  'signal_strength':'\ud83d\udcf6',
  'six_pointed_star':'\ud83d\udd2f',
  'ski':'\ud83c\udfbf',
  'skier':'\u26f7',
  'skull':'\ud83d\udc80',
  'skull_and_crossbones':'\u2620\ufe0f',
  'sleeping':'\ud83d\ude34',
  'sleeping_bed':'\ud83d\udecc',
  'sleepy':'\ud83d\ude2a',
  'slightly_frowning_face':'\ud83d\ude41',
  'slightly_smiling_face':'\ud83d\ude42',
  'slot_machine':'\ud83c\udfb0',
  'small_airplane':'\ud83d\udee9',
  'small_blue_diamond':'\ud83d\udd39',
  'small_orange_diamond':'\ud83d\udd38',
  'small_red_triangle':'\ud83d\udd3a',
  'small_red_triangle_down':'\ud83d\udd3b',
  'smile':'\ud83d\ude04',
  'smile_cat':'\ud83d\ude38',
  'smiley':'\ud83d\ude03',
  'smiley_cat':'\ud83d\ude3a',
  'smiling_imp':'\ud83d\ude08',
  'smirk':'\ud83d\ude0f',
  'smirk_cat':'\ud83d\ude3c',
  'smoking':'\ud83d\udeac',
  'snail':'\ud83d\udc0c',
  'snake':'\ud83d\udc0d',
  'sneezing_face':'\ud83e\udd27',
  'snowboarder':'\ud83c\udfc2',
  'snowflake':'\u2744\ufe0f',
  'snowman':'\u26c4\ufe0f',
  'snowman_with_snow':'\u2603\ufe0f',
  'sob':'\ud83d\ude2d',
  'soccer':'\u26bd\ufe0f',
  'soon':'\ud83d\udd1c',
  'sos':'\ud83c\udd98',
  'sound':'\ud83d\udd09',
  'space_invader':'\ud83d\udc7e',
  'spades':'\u2660\ufe0f',
  'spaghetti':'\ud83c\udf5d',
  'sparkle':'\u2747\ufe0f',
  'sparkler':'\ud83c\udf87',
  'sparkles':'\u2728',
  'sparkling_heart':'\ud83d\udc96',
  'speak_no_evil':'\ud83d\ude4a',
  'speaker':'\ud83d\udd08',
  'speaking_head':'\ud83d\udde3',
  'speech_balloon':'\ud83d\udcac',
  'speedboat':'\ud83d\udea4',
  'spider':'\ud83d\udd77',
  'spider_web':'\ud83d\udd78',
  'spiral_calendar':'\ud83d\uddd3',
  'spiral_notepad':'\ud83d\uddd2',
  'spoon':'\ud83e\udd44',
  'squid':'\ud83e\udd91',
  'stadium':'\ud83c\udfdf',
  'star':'\u2b50\ufe0f',
  'star2':'\ud83c\udf1f',
  'star_and_crescent':'\u262a\ufe0f',
  'star_of_david':'\u2721\ufe0f',
  'stars':'\ud83c\udf20',
  'station':'\ud83d\ude89',
  'statue_of_liberty':'\ud83d\uddfd',
  'steam_locomotive':'\ud83d\ude82',
  'stew':'\ud83c\udf72',
  'stop_button':'\u23f9',
  'stop_sign':'\ud83d\uded1',
  'stopwatch':'\u23f1',
  'straight_ruler':'\ud83d\udccf',
  'strawberry':'\ud83c\udf53',
  'stuck_out_tongue':'\ud83d\ude1b',
  'stuck_out_tongue_closed_eyes':'\ud83d\ude1d',
  'stuck_out_tongue_winking_eye':'\ud83d\ude1c',
  'studio_microphone':'\ud83c\udf99',
  'stuffed_flatbread':'\ud83e\udd59',
  'sun_behind_large_cloud':'\ud83c\udf25',
  'sun_behind_rain_cloud':'\ud83c\udf26',
  'sun_behind_small_cloud':'\ud83c\udf24',
  'sun_with_face':'\ud83c\udf1e',
  'sunflower':'\ud83c\udf3b',
  'sunglasses':'\ud83d\ude0e',
  'sunny':'\u2600\ufe0f',
  'sunrise':'\ud83c\udf05',
  'sunrise_over_mountains':'\ud83c\udf04',
  'surfing_man':'\ud83c\udfc4',
  'surfing_woman':'\ud83c\udfc4&zwj;\u2640\ufe0f',
  'sushi':'\ud83c\udf63',
  'suspension_railway':'\ud83d\ude9f',
  'sweat':'\ud83d\ude13',
  'sweat_drops':'\ud83d\udca6',
  'sweat_smile':'\ud83d\ude05',
  'sweet_potato':'\ud83c\udf60',
  'swimming_man':'\ud83c\udfca',
  'swimming_woman':'\ud83c\udfca&zwj;\u2640\ufe0f',
  'symbols':'\ud83d\udd23',
  'synagogue':'\ud83d\udd4d',
  'syringe':'\ud83d\udc89',
  'taco':'\ud83c\udf2e',
  'tada':'\ud83c\udf89',
  'tanabata_tree':'\ud83c\udf8b',
  'taurus':'\u2649\ufe0f',
  'taxi':'\ud83d\ude95',
  'tea':'\ud83c\udf75',
  'telephone_receiver':'\ud83d\udcde',
  'telescope':'\ud83d\udd2d',
  'tennis':'\ud83c\udfbe',
  'tent':'\u26fa\ufe0f',
  'thermometer':'\ud83c\udf21',
  'thinking':'\ud83e\udd14',
  'thought_balloon':'\ud83d\udcad',
  'ticket':'\ud83c\udfab',
  'tickets':'\ud83c\udf9f',
  'tiger':'\ud83d\udc2f',
  'tiger2':'\ud83d\udc05',
  'timer_clock':'\u23f2',
  'tipping_hand_man':'\ud83d\udc81&zwj;\u2642\ufe0f',
  'tired_face':'\ud83d\ude2b',
  'tm':'\u2122\ufe0f',
  'toilet':'\ud83d\udebd',
  'tokyo_tower':'\ud83d\uddfc',
  'tomato':'\ud83c\udf45',
  'tongue':'\ud83d\udc45',
  'top':'\ud83d\udd1d',
  'tophat':'\ud83c\udfa9',
  'tornado':'\ud83c\udf2a',
  'trackball':'\ud83d\uddb2',
  'tractor':'\ud83d\ude9c',
  'traffic_light':'\ud83d\udea5',
  'train':'\ud83d\ude8b',
  'train2':'\ud83d\ude86',
  'tram':'\ud83d\ude8a',
  'triangular_flag_on_post':'\ud83d\udea9',
  'triangular_ruler':'\ud83d\udcd0',
  'trident':'\ud83d\udd31',
  'triumph':'\ud83d\ude24',
  'trolleybus':'\ud83d\ude8e',
  'trophy':'\ud83c\udfc6',
  'tropical_drink':'\ud83c\udf79',
  'tropical_fish':'\ud83d\udc20',
  'truck':'\ud83d\ude9a',
  'trumpet':'\ud83c\udfba',
  'tulip':'\ud83c\udf37',
  'tumbler_glass':'\ud83e\udd43',
  'turkey':'\ud83e\udd83',
  'turtle':'\ud83d\udc22',
  'tv':'\ud83d\udcfa',
  'twisted_rightwards_arrows':'\ud83d\udd00',
  'two_hearts':'\ud83d\udc95',
  'two_men_holding_hands':'\ud83d\udc6c',
  'two_women_holding_hands':'\ud83d\udc6d',
  'u5272':'\ud83c\ude39',
  'u5408':'\ud83c\ude34',
  'u55b6':'\ud83c\ude3a',
  'u6307':'\ud83c\ude2f\ufe0f',
  'u6708':'\ud83c\ude37\ufe0f',
  'u6709':'\ud83c\ude36',
  'u6e80':'\ud83c\ude35',
  'u7121':'\ud83c\ude1a\ufe0f',
  'u7533':'\ud83c\ude38',
  'u7981':'\ud83c\ude32',
  'u7a7a':'\ud83c\ude33',
  'umbrella':'\u2614\ufe0f',
  'unamused':'\ud83d\ude12',
  'underage':'\ud83d\udd1e',
  'unicorn':'\ud83e\udd84',
  'unlock':'\ud83d\udd13',
  'up':'\ud83c\udd99',
  'upside_down_face':'\ud83d\ude43',
  'v':'\u270c\ufe0f',
  'vertical_traffic_light':'\ud83d\udea6',
  'vhs':'\ud83d\udcfc',
  'vibration_mode':'\ud83d\udcf3',
  'video_camera':'\ud83d\udcf9',
  'video_game':'\ud83c\udfae',
  'violin':'\ud83c\udfbb',
  'virgo':'\u264d\ufe0f',
  'volcano':'\ud83c\udf0b',
  'volleyball':'\ud83c\udfd0',
  'vs':'\ud83c\udd9a',
  'vulcan_salute':'\ud83d\udd96',
  'walking_man':'\ud83d\udeb6',
  'walking_woman':'\ud83d\udeb6&zwj;\u2640\ufe0f',
  'waning_crescent_moon':'\ud83c\udf18',
  'waning_gibbous_moon':'\ud83c\udf16',
  'warning':'\u26a0\ufe0f',
  'wastebasket':'\ud83d\uddd1',
  'watch':'\u231a\ufe0f',
  'water_buffalo':'\ud83d\udc03',
  'watermelon':'\ud83c\udf49',
  'wave':'\ud83d\udc4b',
  'wavy_dash':'\u3030\ufe0f',
  'waxing_crescent_moon':'\ud83c\udf12',
  'wc':'\ud83d\udebe',
  'weary':'\ud83d\ude29',
  'wedding':'\ud83d\udc92',
  'weight_lifting_man':'\ud83c\udfcb\ufe0f',
  'weight_lifting_woman':'\ud83c\udfcb\ufe0f&zwj;\u2640\ufe0f',
  'whale':'\ud83d\udc33',
  'whale2':'\ud83d\udc0b',
  'wheel_of_dharma':'\u2638\ufe0f',
  'wheelchair':'\u267f\ufe0f',
  'white_check_mark':'\u2705',
  'white_circle':'\u26aa\ufe0f',
  'white_flag':'\ud83c\udff3\ufe0f',
  'white_flower':'\ud83d\udcae',
  'white_large_square':'\u2b1c\ufe0f',
  'white_medium_small_square':'\u25fd\ufe0f',
  'white_medium_square':'\u25fb\ufe0f',
  'white_small_square':'\u25ab\ufe0f',
  'white_square_button':'\ud83d\udd33',
  'wilted_flower':'\ud83e\udd40',
  'wind_chime':'\ud83c\udf90',
  'wind_face':'\ud83c\udf2c',
  'wine_glass':'\ud83c\udf77',
  'wink':'\ud83d\ude09',
  'wolf':'\ud83d\udc3a',
  'woman':'\ud83d\udc69',
  'woman_artist':'\ud83d\udc69&zwj;\ud83c\udfa8',
  'woman_astronaut':'\ud83d\udc69&zwj;\ud83d\ude80',
  'woman_cartwheeling':'\ud83e\udd38&zwj;\u2640\ufe0f',
  'woman_cook':'\ud83d\udc69&zwj;\ud83c\udf73',
  'woman_facepalming':'\ud83e\udd26&zwj;\u2640\ufe0f',
  'woman_factory_worker':'\ud83d\udc69&zwj;\ud83c\udfed',
  'woman_farmer':'\ud83d\udc69&zwj;\ud83c\udf3e',
  'woman_firefighter':'\ud83d\udc69&zwj;\ud83d\ude92',
  'woman_health_worker':'\ud83d\udc69&zwj;\u2695\ufe0f',
  'woman_judge':'\ud83d\udc69&zwj;\u2696\ufe0f',
  'woman_juggling':'\ud83e\udd39&zwj;\u2640\ufe0f',
  'woman_mechanic':'\ud83d\udc69&zwj;\ud83d\udd27',
  'woman_office_worker':'\ud83d\udc69&zwj;\ud83d\udcbc',
  'woman_pilot':'\ud83d\udc69&zwj;\u2708\ufe0f',
  'woman_playing_handball':'\ud83e\udd3e&zwj;\u2640\ufe0f',
  'woman_playing_water_polo':'\ud83e\udd3d&zwj;\u2640\ufe0f',
  'woman_scientist':'\ud83d\udc69&zwj;\ud83d\udd2c',
  'woman_shrugging':'\ud83e\udd37&zwj;\u2640\ufe0f',
  'woman_singer':'\ud83d\udc69&zwj;\ud83c\udfa4',
  'woman_student':'\ud83d\udc69&zwj;\ud83c\udf93',
  'woman_teacher':'\ud83d\udc69&zwj;\ud83c\udfeb',
  'woman_technologist':'\ud83d\udc69&zwj;\ud83d\udcbb',
  'woman_with_turban':'\ud83d\udc73&zwj;\u2640\ufe0f',
  'womans_clothes':'\ud83d\udc5a',
  'womans_hat':'\ud83d\udc52',
  'women_wrestling':'\ud83e\udd3c&zwj;\u2640\ufe0f',
  'womens':'\ud83d\udeba',
  'world_map':'\ud83d\uddfa',
  'worried':'\ud83d\ude1f',
  'wrench':'\ud83d\udd27',
  'writing_hand':'\u270d\ufe0f',
  'x':'\u274c',
  'yellow_heart':'\ud83d\udc9b',
  'yen':'\ud83d\udcb4',
  'yin_yang':'\u262f\ufe0f',
  'yum':'\ud83d\ude0b',
  'zap':'\u26a1\ufe0f',
  'zipper_mouth_face':'\ud83e\udd10',
  'zzz':'\ud83d\udca4',

  /* special emojis :P */
  'octocat':  '<img alt=":octocat:" height="20" width="20" align="absmiddle" src="https://assets-cdn.github.com/images/icons/emoji/octocat.png">',
  'showdown': '<span style="font-family: \'Anonymous Pro\', monospace; text-decoration: underline; text-decoration-style: dashed; text-decoration-color: #3e8b8a;text-underline-position: under;">S</span>'
};

/**
 * Created by Estevao on 31-05-2015.
 */

/**
 * Showdown Converter class
 * @class
 * @param {object} [converterOptions]
 * @returns {Converter}
 */
showdown.Converter = function (converterOptions) {
  'use strict';

  var
      /**
       * Options used by this converter
       * @private
       * @type {{}}
       */
      options = {},

      /**
       * Language extensions used by this converter
       * @private
       * @type {Array}
       */
      langExtensions = [],

      /**
       * Output modifiers extensions used by this converter
       * @private
       * @type {Array}
       */
      outputModifiers = [],

      /**
       * Event listeners
       * @private
       * @type {{}}
       */
      listeners = {},

      /**
       * The flavor set in this converter
       */
      setConvFlavor = setFlavor,

      /**
       * Metadata of the document
       * @type {{parsed: {}, raw: string, format: string}}
       */
      metadata = {
        parsed: {},
        raw: '',
        format: ''
      };

  _constructor();

  /**
   * Converter constructor
   * @private
   */
  function _constructor () {
    converterOptions = converterOptions || {};

    for (var gOpt in globalOptions) {
      if (globalOptions.hasOwnProperty(gOpt)) {
        options[gOpt] = globalOptions[gOpt];
      }
    }

    // Merge options
    if (typeof converterOptions === 'object') {
      for (var opt in converterOptions) {
        if (converterOptions.hasOwnProperty(opt)) {
          options[opt] = converterOptions[opt];
        }
      }
    } else {
      throw Error('Converter expects the passed parameter to be an object, but ' + typeof converterOptions +
      ' was passed instead.');
    }

    if (options.extensions) {
      showdown.helper.forEach(options.extensions, _parseExtension);
    }
  }

  /**
   * Parse extension
   * @param {*} ext
   * @param {string} [name='']
   * @private
   */
  function _parseExtension (ext, name) {

    name = name || null;
    // If it's a string, the extension was previously loaded
    if (showdown.helper.isString(ext)) {
      ext = showdown.helper.stdExtName(ext);
      name = ext;

      // LEGACY_SUPPORT CODE
      if (showdown.extensions[ext]) {
        console.warn('DEPRECATION WARNING: ' + ext + ' is an old extension that uses a deprecated loading method.' +
          'Please inform the developer that the extension should be updated!');
        legacyExtensionLoading(showdown.extensions[ext], ext);
        return;
        // END LEGACY SUPPORT CODE

      } else if (!showdown.helper.isUndefined(extensions[ext])) {
        ext = extensions[ext];

      } else {
        throw Error('Extension "' + ext + '" could not be loaded. It was either not found or is not a valid extension.');
      }
    }

    if (typeof ext === 'function') {
      ext = ext();
    }

    if (!showdown.helper.isArray(ext)) {
      ext = [ext];
    }

    var validExt = validate(ext, name);
    if (!validExt.valid) {
      throw Error(validExt.error);
    }

    for (var i = 0; i < ext.length; ++i) {
      switch (ext[i].type) {

        case 'lang':
          langExtensions.push(ext[i]);
          break;

        case 'output':
          outputModifiers.push(ext[i]);
          break;
      }
      if (ext[i].hasOwnProperty('listeners')) {
        for (var ln in ext[i].listeners) {
          if (ext[i].listeners.hasOwnProperty(ln)) {
            listen(ln, ext[i].listeners[ln]);
          }
        }
      }
    }

  }

  /**
   * LEGACY_SUPPORT
   * @param {*} ext
   * @param {string} name
   */
  function legacyExtensionLoading (ext, name) {
    if (typeof ext === 'function') {
      ext = ext(new showdown.Converter());
    }
    if (!showdown.helper.isArray(ext)) {
      ext = [ext];
    }
    var valid = validate(ext, name);

    if (!valid.valid) {
      throw Error(valid.error);
    }

    for (var i = 0; i < ext.length; ++i) {
      switch (ext[i].type) {
        case 'lang':
          langExtensions.push(ext[i]);
          break;
        case 'output':
          outputModifiers.push(ext[i]);
          break;
        default:// should never reach here
          throw Error('Extension loader error: Type unrecognized!!!');
      }
    }
  }

  /**
   * Listen to an event
   * @param {string} name
   * @param {function} callback
   */
  function listen (name, callback) {
    if (!showdown.helper.isString(name)) {
      throw Error('Invalid argument in converter.listen() method: name must be a string, but ' + typeof name + ' given');
    }

    if (typeof callback !== 'function') {
      throw Error('Invalid argument in converter.listen() method: callback must be a function, but ' + typeof callback + ' given');
    }

    if (!listeners.hasOwnProperty(name)) {
      listeners[name] = [];
    }
    listeners[name].push(callback);
  }

  function rTrimInputText (text) {
    var rsp = text.match(/^\s*/)[0].length,
        rgx = new RegExp('^\\s{0,' + rsp + '}', 'gm');
    return text.replace(rgx, '');
  }

  /**
   * Dispatch an event
   * @private
   * @param {string} evtName Event name
   * @param {string} text Text
   * @param {{}} options Converter Options
   * @param {{}} globals
   * @returns {string}
   */
  this._dispatch = function dispatch (evtName, text, options, globals) {
    if (listeners.hasOwnProperty(evtName)) {
      for (var ei = 0; ei < listeners[evtName].length; ++ei) {
        var nText = listeners[evtName][ei](evtName, text, this, options, globals);
        if (nText && typeof nText !== 'undefined') {
          text = nText;
        }
      }
    }
    return text;
  };

  /**
   * Listen to an event
   * @param {string} name
   * @param {function} callback
   * @returns {showdown.Converter}
   */
  this.listen = function (name, callback) {
    listen(name, callback);
    return this;
  };

  /**
   * Converts a markdown string into HTML
   * @param {string} text
   * @returns {*}
   */
  this.makeHtml = function (text) {
    //check if text is not falsy
    if (!text) {
      return text;
    }

    var globals = {
      gHtmlBlocks:     [],
      gHtmlMdBlocks:   [],
      gHtmlSpans:      [],
      gUrls:           {},
      gTitles:         {},
      gDimensions:     {},
      gListLevel:      0,
      hashLinkCounts:  {},
      langExtensions:  langExtensions,
      outputModifiers: outputModifiers,
      converter:       this,
      ghCodeBlocks:    [],
      metadata: {
        parsed: {},
        raw: '',
        format: ''
      }
    };

    // This lets us use ¨ trema as an escape char to avoid md5 hashes
    // The choice of character is arbitrary; anything that isn't
    // magic in Markdown will work.
    text = text.replace(/¨/g, '¨T');

    // Replace $ with ¨D
    // RegExp interprets $ as a special character
    // when it's in a replacement string
    text = text.replace(/\$/g, '¨D');

    // Standardize line endings
    text = text.replace(/\r\n/g, '\n'); // DOS to Unix
    text = text.replace(/\r/g, '\n'); // Mac to Unix

    // Stardardize line spaces
    text = text.replace(/\u00A0/g, '&nbsp;');

    if (options.smartIndentationFix) {
      text = rTrimInputText(text);
    }

    // Make sure text begins and ends with a couple of newlines:
    text = '\n\n' + text + '\n\n';

    // detab
    text = showdown.subParser('detab')(text, options, globals);

    /**
     * Strip any lines consisting only of spaces and tabs.
     * This makes subsequent regexs easier to write, because we can
     * match consecutive blank lines with /\n+/ instead of something
     * contorted like /[ \t]*\n+/
     */
    text = text.replace(/^[ \t]+$/mg, '');

    //run languageExtensions
    showdown.helper.forEach(langExtensions, function (ext) {
      text = showdown.subParser('runExtension')(ext, text, options, globals);
    });

    // run the sub parsers
    text = showdown.subParser('metadata')(text, options, globals);
    text = showdown.subParser('hashPreCodeTags')(text, options, globals);
    text = showdown.subParser('githubCodeBlocks')(text, options, globals);
    text = showdown.subParser('hashHTMLBlocks')(text, options, globals);
    text = showdown.subParser('hashCodeTags')(text, options, globals);
    text = showdown.subParser('stripLinkDefinitions')(text, options, globals);
    text = showdown.subParser('blockGamut')(text, options, globals);
    text = showdown.subParser('unhashHTMLSpans')(text, options, globals);
    text = showdown.subParser('unescapeSpecialChars')(text, options, globals);

    // attacklab: Restore dollar signs
    text = text.replace(/¨D/g, '$$');

    // attacklab: Restore tremas
    text = text.replace(/¨T/g, '¨');

    // render a complete html document instead of a partial if the option is enabled
    text = showdown.subParser('completeHTMLDocument')(text, options, globals);

    // Run output modifiers
    showdown.helper.forEach(outputModifiers, function (ext) {
      text = showdown.subParser('runExtension')(ext, text, options, globals);
    });

    // update metadata
    metadata = globals.metadata;
    return text;
  };

  /**
   * Converts an HTML string into a markdown string
   * @param src
   * @param [HTMLParser] A WHATWG DOM and HTML parser, such as JSDOM. If none is supplied, window.document will be used.
   * @returns {string}
   */
  this.makeMarkdown = this.makeMd = function (src, HTMLParser) {

    // replace \r\n with \n
    src = src.replace(/\r\n/g, '\n');
    src = src.replace(/\r/g, '\n'); // old macs

    // due to an edge case, we need to find this: > <
    // to prevent removing of non silent white spaces
    // ex: <em>this is</em> <strong>sparta</strong>
    src = src.replace(/>[ \t]+</, '>¨NBSP;<');

    if (!HTMLParser) {
      if (window && window.document) {
        HTMLParser = window.document;
      } else {
        throw new Error('HTMLParser is undefined. If in a webworker or nodejs environment, you need to provide a WHATWG DOM and HTML such as JSDOM');
      }
    }

    var doc = HTMLParser.createElement('div');
    doc.innerHTML = src;

    var globals = {
      preList: substitutePreCodeTags(doc)
    };

    // remove all newlines and collapse spaces
    clean(doc);

    // some stuff, like accidental reference links must now be escaped
    // TODO
    // doc.innerHTML = doc.innerHTML.replace(/\[[\S\t ]]/);

    var nodes = doc.childNodes,
        mdDoc = '';

    for (var i = 0; i < nodes.length; i++) {
      mdDoc += showdown.subParser('makeMarkdown.node')(nodes[i], globals);
    }

    function clean (node) {
      for (var n = 0; n < node.childNodes.length; ++n) {
        var child = node.childNodes[n];
        if (child.nodeType === 3) {
          if (!/\S/.test(child.nodeValue) && !/^[ ]+$/.test(child.nodeValue)) {
            node.removeChild(child);
            --n;
          } else {
            child.nodeValue = child.nodeValue.split('\n').join(' ');
            child.nodeValue = child.nodeValue.replace(/(\s)+/g, '$1');
          }
        } else if (child.nodeType === 1) {
          clean(child);
        }
      }
    }

    // find all pre tags and replace contents with placeholder
    // we need this so that we can remove all indentation from html
    // to ease up parsing
    function substitutePreCodeTags (doc) {

      var pres = doc.querySelectorAll('pre'),
          presPH = [];

      for (var i = 0; i < pres.length; ++i) {

        if (pres[i].childElementCount === 1 && pres[i].firstChild.tagName.toLowerCase() === 'code') {
          var content = pres[i].firstChild.innerHTML.trim(),
              language = pres[i].firstChild.getAttribute('data-language') || '';

          // if data-language attribute is not defined, then we look for class language-*
          if (language === '') {
            var classes = pres[i].firstChild.className.split(' ');
            for (var c = 0; c < classes.length; ++c) {
              var matches = classes[c].match(/^language-(.+)$/);
              if (matches !== null) {
                language = matches[1];
                break;
              }
            }
          }

          // unescape html entities in content
          content = showdown.helper.unescapeHTMLEntities(content);

          presPH.push(content);
          pres[i].outerHTML = '<precode language="' + language + '" precodenum="' + i.toString() + '"></precode>';
        } else {
          presPH.push(pres[i].innerHTML);
          pres[i].innerHTML = '';
          pres[i].setAttribute('prenum', i.toString());
        }
      }
      return presPH;
    }

    return mdDoc;
  };

  /**
   * Set an option of this Converter instance
   * @param {string} key
   * @param {*} value
   */
  this.setOption = function (key, value) {
    options[key] = value;
  };

  /**
   * Get the option of this Converter instance
   * @param {string} key
   * @returns {*}
   */
  this.getOption = function (key) {
    return options[key];
  };

  /**
   * Get the options of this Converter instance
   * @returns {{}}
   */
  this.getOptions = function () {
    return options;
  };

  /**
   * Add extension to THIS converter
   * @param {{}} extension
   * @param {string} [name=null]
   */
  this.addExtension = function (extension, name) {
    name = name || null;
    _parseExtension(extension, name);
  };

  /**
   * Use a global registered extension with THIS converter
   * @param {string} extensionName Name of the previously registered extension
   */
  this.useExtension = function (extensionName) {
    _parseExtension(extensionName);
  };

  /**
   * Set the flavor THIS converter should use
   * @param {string} name
   */
  this.setFlavor = function (name) {
    if (!flavor.hasOwnProperty(name)) {
      throw Error(name + ' flavor was not found');
    }
    var preset = flavor[name];
    setConvFlavor = name;
    for (var option in preset) {
      if (preset.hasOwnProperty(option)) {
        options[option] = preset[option];
      }
    }
  };

  /**
   * Get the currently set flavor of this converter
   * @returns {string}
   */
  this.getFlavor = function () {
    return setConvFlavor;
  };

  /**
   * Remove an extension from THIS converter.
   * Note: This is a costly operation. It's better to initialize a new converter
   * and specify the extensions you wish to use
   * @param {Array} extension
   */
  this.removeExtension = function (extension) {
    if (!showdown.helper.isArray(extension)) {
      extension = [extension];
    }
    for (var a = 0; a < extension.length; ++a) {
      var ext = extension[a];
      for (var i = 0; i < langExtensions.length; ++i) {
        if (langExtensions[i] === ext) {
          langExtensions.splice(i, 1);
        }
      }
      for (var ii = 0; ii < outputModifiers.length; ++ii) {
        if (outputModifiers[ii] === ext) {
          outputModifiers.splice(ii, 1);
        }
      }
    }
  };

  /**
   * Get all extension of THIS converter
   * @returns {{language: Array, output: Array}}
   */
  this.getAllExtensions = function () {
    return {
      language: langExtensions,
      output: outputModifiers
    };
  };

  /**
   * Get the metadata of the previously parsed document
   * @param raw
   * @returns {string|{}}
   */
  this.getMetadata = function (raw) {
    if (raw) {
      return metadata.raw;
    } else {
      return metadata.parsed;
    }
  };

  /**
   * Get the metadata format of the previously parsed document
   * @returns {string}
   */
  this.getMetadataFormat = function () {
    return metadata.format;
  };

  /**
   * Private: set a single key, value metadata pair
   * @param {string} key
   * @param {string} value
   */
  this._setMetadataPair = function (key, value) {
    metadata.parsed[key] = value;
  };

  /**
   * Private: set metadata format
   * @param {string} format
   */
  this._setMetadataFormat = function (format) {
    metadata.format = format;
  };

  /**
   * Private: set metadata raw text
   * @param {string} raw
   */
  this._setMetadataRaw = function (raw) {
    metadata.raw = raw;
  };
};

/**
 * Turn Markdown link shortcuts into XHTML <a> tags.
 */
showdown.subParser('anchors', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('anchors.before', text, options, globals);

  var writeAnchorTag = function (wholeMatch, linkText, linkId, url, m5, m6, title) {
    if (showdown.helper.isUndefined(title)) {
      title = '';
    }
    linkId = linkId.toLowerCase();

    // Special case for explicit empty url
    if (wholeMatch.search(/\(<?\s*>? ?(['"].*['"])?\)$/m) > -1) {
      url = '';
    } else if (!url) {
      if (!linkId) {
        // lower-case and turn embedded newlines into spaces
        linkId = linkText.toLowerCase().replace(/ ?\n/g, ' ');
      }
      url = '#' + linkId;

      if (!showdown.helper.isUndefined(globals.gUrls[linkId])) {
        url = globals.gUrls[linkId];
        if (!showdown.helper.isUndefined(globals.gTitles[linkId])) {
          title = globals.gTitles[linkId];
        }
      } else {
        return wholeMatch;
      }
    }

    //url = showdown.helper.escapeCharacters(url, '*_', false); // replaced line to improve performance
    url = url.replace(showdown.helper.regexes.asteriskDashAndColon, showdown.helper.escapeCharactersCallback);

    var result = '<a href="' + url + '"';

    if (title !== '' && title !== null) {
      title = title.replace(/"/g, '&quot;');
      //title = showdown.helper.escapeCharacters(title, '*_', false); // replaced line to improve performance
      title = title.replace(showdown.helper.regexes.asteriskDashAndColon, showdown.helper.escapeCharactersCallback);
      result += ' title="' + title + '"';
    }

    // optionLinksInNewWindow only applies
    // to external links. Hash links (#) open in same page
    if (options.openLinksInNewWindow && !/^#/.test(url)) {
      // escaped _
      result += ' rel="noopener noreferrer" target="¨E95Eblank"';
    }

    result += '>' + linkText + '</a>';

    return result;
  };

  // First, handle reference-style links: [link text] [id]
  text = text.replace(/\[((?:\[[^\]]*]|[^\[\]])*)] ?(?:\n *)?\[(.*?)]()()()()/g, writeAnchorTag);

  // Next, inline-style links: [link text](url "optional title")
  // cases with crazy urls like ./image/cat1).png
  text = text.replace(/\[((?:\[[^\]]*]|[^\[\]])*)]()[ \t]*\([ \t]?<([^>]*)>(?:[ \t]*((["'])([^"]*?)\5))?[ \t]?\)/g,
    writeAnchorTag);

  // normal cases
  text = text.replace(/\[((?:\[[^\]]*]|[^\[\]])*)]()[ \t]*\([ \t]?<?([\S]+?(?:\([\S]*?\)[\S]*?)?)>?(?:[ \t]*((["'])([^"]*?)\5))?[ \t]?\)/g,
    writeAnchorTag);

  // handle reference-style shortcuts: [link text]
  // These must come last in case you've also got [link test][1]
  // or [link test](/foo)
  text = text.replace(/\[([^\[\]]+)]()()()()()/g, writeAnchorTag);

  // Lastly handle GithubMentions if option is enabled
  if (options.ghMentions) {
    text = text.replace(/(^|\s)(\\)?(@([a-z\d]+(?:[a-z\d.-]+?[a-z\d]+)*))/gmi, function (wm, st, escape, mentions, username) {
      if (escape === '\\') {
        return st + mentions;
      }

      //check if options.ghMentionsLink is a string
      if (!showdown.helper.isString(options.ghMentionsLink)) {
        throw new Error('ghMentionsLink option must be a string');
      }
      var lnk = options.ghMentionsLink.replace(/\{u}/g, username),
          target = '';
      if (options.openLinksInNewWindow) {
        target = ' rel="noopener noreferrer" target="¨E95Eblank"';
      }
      return st + '<a href="' + lnk + '"' + target + '>' + mentions + '</a>';
    });
  }

  text = globals.converter._dispatch('anchors.after', text, options, globals);
  return text;
});

// url allowed chars [a-z\d_.~:/?#[]@!$&'()*+,;=-]

var simpleURLRegex  = /([*~_]+|\b)(((https?|ftp|dict):\/\/|www\.)[^'">\s]+?\.[^'">\s]+?)()(\1)?(?=\s|$)(?!["<>])/gi,
    simpleURLRegex2 = /([*~_]+|\b)(((https?|ftp|dict):\/\/|www\.)[^'">\s]+\.[^'">\s]+?)([.!?,()\[\]])?(\1)?(?=\s|$)(?!["<>])/gi,
    delimUrlRegex   = /()<(((https?|ftp|dict):\/\/|www\.)[^'">\s]+)()>()/gi,
    simpleMailRegex = /(^|\s)(?:mailto:)?([A-Za-z0-9!#$%&'*+-/=?^_`{|}~.]+@[-a-z0-9]+(\.[-a-z0-9]+)*\.[a-z]+)(?=$|\s)/gmi,
    delimMailRegex  = /<()(?:mailto:)?([-.\w]+@[-a-z0-9]+(\.[-a-z0-9]+)*\.[a-z]+)>/gi,

    replaceLink = function (options) {
      'use strict';
      return function (wm, leadingMagicChars, link, m2, m3, trailingPunctuation, trailingMagicChars) {
        link = link.replace(showdown.helper.regexes.asteriskDashAndColon, showdown.helper.escapeCharactersCallback);
        var lnkTxt = link,
            append = '',
            target = '',
            lmc    = leadingMagicChars || '',
            tmc    = trailingMagicChars || '';
        if (/^www\./i.test(link)) {
          link = link.replace(/^www\./i, 'http://www.');
        }
        if (options.excludeTrailingPunctuationFromURLs && trailingPunctuation) {
          append = trailingPunctuation;
        }
        if (options.openLinksInNewWindow) {
          target = ' rel="noopener noreferrer" target="¨E95Eblank"';
        }
        return lmc + '<a href="' + link + '"' + target + '>' + lnkTxt + '</a>' + append + tmc;
      };
    },

    replaceMail = function (options, globals) {
      'use strict';
      return function (wholeMatch, b, mail) {
        var href = 'mailto:';
        b = b || '';
        mail = showdown.subParser('unescapeSpecialChars')(mail, options, globals);
        if (options.encodeEmails) {
          href = showdown.helper.encodeEmailAddress(href + mail);
          mail = showdown.helper.encodeEmailAddress(mail);
        } else {
          href = href + mail;
        }
        return b + '<a href="' + href + '">' + mail + '</a>';
      };
    };

showdown.subParser('autoLinks', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('autoLinks.before', text, options, globals);

  text = text.replace(delimUrlRegex, replaceLink(options));
  text = text.replace(delimMailRegex, replaceMail(options, globals));

  text = globals.converter._dispatch('autoLinks.after', text, options, globals);

  return text;
});

showdown.subParser('simplifiedAutoLinks', function (text, options, globals) {
  'use strict';

  if (!options.simplifiedAutoLink) {
    return text;
  }

  text = globals.converter._dispatch('simplifiedAutoLinks.before', text, options, globals);

  if (options.excludeTrailingPunctuationFromURLs) {
    text = text.replace(simpleURLRegex2, replaceLink(options));
  } else {
    text = text.replace(simpleURLRegex, replaceLink(options));
  }
  text = text.replace(simpleMailRegex, replaceMail(options, globals));

  text = globals.converter._dispatch('simplifiedAutoLinks.after', text, options, globals);

  return text;
});

/**
 * These are all the transformations that form block-level
 * tags like paragraphs, headers, and list items.
 */
showdown.subParser('blockGamut', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('blockGamut.before', text, options, globals);

  // we parse blockquotes first so that we can have headings and hrs
  // inside blockquotes
  text = showdown.subParser('blockQuotes')(text, options, globals);
  text = showdown.subParser('headers')(text, options, globals);

  // Do Horizontal Rules:
  text = showdown.subParser('horizontalRule')(text, options, globals);

  text = showdown.subParser('lists')(text, options, globals);
  text = showdown.subParser('codeBlocks')(text, options, globals);
  text = showdown.subParser('tables')(text, options, globals);

  // We already ran _HashHTMLBlocks() before, in Markdown(), but that
  // was to escape raw HTML in the original Markdown source. This time,
  // we're escaping the markup we've just created, so that we don't wrap
  // <p> tags around block-level tags.
  text = showdown.subParser('hashHTMLBlocks')(text, options, globals);
  text = showdown.subParser('paragraphs')(text, options, globals);

  text = globals.converter._dispatch('blockGamut.after', text, options, globals);

  return text;
});

showdown.subParser('blockQuotes', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('blockQuotes.before', text, options, globals);

  // add a couple extra lines after the text and endtext mark
  text = text + '\n\n';

  var rgx = /(^ {0,3}>[ \t]?.+\n(.+\n)*\n*)+/gm;

  if (options.splitAdjacentBlockquotes) {
    rgx = /^ {0,3}>[\s\S]*?(?:\n\n)/gm;
  }

  text = text.replace(rgx, function (bq) {
    // attacklab: hack around Konqueror 3.5.4 bug:
    // "----------bug".replace(/^-/g,"") == "bug"
    bq = bq.replace(/^[ \t]*>[ \t]?/gm, ''); // trim one level of quoting

    // attacklab: clean up hack
    bq = bq.replace(/¨0/g, '');

    bq = bq.replace(/^[ \t]+$/gm, ''); // trim whitespace-only lines
    bq = showdown.subParser('githubCodeBlocks')(bq, options, globals);
    bq = showdown.subParser('blockGamut')(bq, options, globals); // recurse

    bq = bq.replace(/(^|\n)/g, '$1  ');
    // These leading spaces screw with <pre> content, so we need to fix that:
    bq = bq.replace(/(\s*<pre>[^\r]+?<\/pre>)/gm, function (wholeMatch, m1) {
      var pre = m1;
      // attacklab: hack around Konqueror 3.5.4 bug:
      pre = pre.replace(/^  /mg, '¨0');
      pre = pre.replace(/¨0/g, '');
      return pre;
    });

    return showdown.subParser('hashBlock')('<blockquote>\n' + bq + '\n</blockquote>', options, globals);
  });

  text = globals.converter._dispatch('blockQuotes.after', text, options, globals);
  return text;
});

/**
 * Process Markdown `<pre><code>` blocks.
 */
showdown.subParser('codeBlocks', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('codeBlocks.before', text, options, globals);

  // sentinel workarounds for lack of \A and \Z, safari\khtml bug
  text += '¨0';

  var pattern = /(?:\n\n|^)((?:(?:[ ]{4}|\t).*\n+)+)(\n*[ ]{0,3}[^ \t\n]|(?=¨0))/g;
  text = text.replace(pattern, function (wholeMatch, m1, m2) {
    var codeblock = m1,
        nextChar = m2,
        end = '\n';

    codeblock = showdown.subParser('outdent')(codeblock, options, globals);
    codeblock = showdown.subParser('encodeCode')(codeblock, options, globals);
    codeblock = showdown.subParser('detab')(codeblock, options, globals);
    codeblock = codeblock.replace(/^\n+/g, ''); // trim leading newlines
    codeblock = codeblock.replace(/\n+$/g, ''); // trim trailing newlines

    if (options.omitExtraWLInCodeBlocks) {
      end = '';
    }

    codeblock = '<pre><code>' + codeblock + end + '</code></pre>';

    return showdown.subParser('hashBlock')(codeblock, options, globals) + nextChar;
  });

  // strip sentinel
  text = text.replace(/¨0/, '');

  text = globals.converter._dispatch('codeBlocks.after', text, options, globals);
  return text;
});

/**
 *
 *   *  Backtick quotes are used for <code></code> spans.
 *
 *   *  You can use multiple backticks as the delimiters if you want to
 *     include literal backticks in the code span. So, this input:
 *
 *         Just type ``foo `bar` baz`` at the prompt.
 *
 *       Will translate to:
 *
 *         <p>Just type <code>foo `bar` baz</code> at the prompt.</p>
 *
 *    There's no arbitrary limit to the number of backticks you
 *    can use as delimters. If you need three consecutive backticks
 *    in your code, use four for delimiters, etc.
 *
 *  *  You can use spaces to get literal backticks at the edges:
 *
 *         ... type `` `bar` `` ...
 *
 *       Turns to:
 *
 *         ... type <code>`bar`</code> ...
 */
showdown.subParser('codeSpans', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('codeSpans.before', text, options, globals);

  if (typeof (text) === 'undefined') {
    text = '';
  }
  text = text.replace(/(^|[^\\])(`+)([^\r]*?[^`])\2(?!`)/gm,
    function (wholeMatch, m1, m2, m3) {
      var c = m3;
      c = c.replace(/^([ \t]*)/g, '');	// leading whitespace
      c = c.replace(/[ \t]*$/g, '');	// trailing whitespace
      c = showdown.subParser('encodeCode')(c, options, globals);
      c = m1 + '<code>' + c + '</code>';
      c = showdown.subParser('hashHTMLSpans')(c, options, globals);
      return c;
    }
  );

  text = globals.converter._dispatch('codeSpans.after', text, options, globals);
  return text;
});

/**
 * Create a full HTML document from the processed markdown
 */
showdown.subParser('completeHTMLDocument', function (text, options, globals) {
  'use strict';

  if (!options.completeHTMLDocument) {
    return text;
  }

  text = globals.converter._dispatch('completeHTMLDocument.before', text, options, globals);

  var doctype = 'html',
      doctypeParsed = '<!DOCTYPE HTML>\n',
      title = '',
      charset = '<meta charset="utf-8">\n',
      lang = '',
      metadata = '';

  if (typeof globals.metadata.parsed.doctype !== 'undefined') {
    doctypeParsed = '<!DOCTYPE ' +  globals.metadata.parsed.doctype + '>\n';
    doctype = globals.metadata.parsed.doctype.toString().toLowerCase();
    if (doctype === 'html' || doctype === 'html5') {
      charset = '<meta charset="utf-8">';
    }
  }

  for (var meta in globals.metadata.parsed) {
    if (globals.metadata.parsed.hasOwnProperty(meta)) {
      switch (meta.toLowerCase()) {
        case 'doctype':
          break;

        case 'title':
          title = '<title>' +  globals.metadata.parsed.title + '</title>\n';
          break;

        case 'charset':
          if (doctype === 'html' || doctype === 'html5') {
            charset = '<meta charset="' + globals.metadata.parsed.charset + '">\n';
          } else {
            charset = '<meta name="charset" content="' + globals.metadata.parsed.charset + '">\n';
          }
          break;

        case 'language':
        case 'lang':
          lang = ' lang="' + globals.metadata.parsed[meta] + '"';
          metadata += '<meta name="' + meta + '" content="' + globals.metadata.parsed[meta] + '">\n';
          break;

        default:
          metadata += '<meta name="' + meta + '" content="' + globals.metadata.parsed[meta] + '">\n';
      }
    }
  }

  text = doctypeParsed + '<html' + lang + '>\n<head>\n' + title + charset + metadata + '</head>\n<body>\n' + text.trim() + '\n</body>\n</html>';

  text = globals.converter._dispatch('completeHTMLDocument.after', text, options, globals);
  return text;
});

/**
 * Convert all tabs to spaces
 */
showdown.subParser('detab', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('detab.before', text, options, globals);

  // expand first n-1 tabs
  text = text.replace(/\t(?=\t)/g, '    '); // g_tab_width

  // replace the nth with two sentinels
  text = text.replace(/\t/g, '¨A¨B');

  // use the sentinel to anchor our regex so it doesn't explode
  text = text.replace(/¨B(.+?)¨A/g, function (wholeMatch, m1) {
    var leadingText = m1,
        numSpaces = 4 - leadingText.length % 4;  // g_tab_width

    // there *must* be a better way to do this:
    for (var i = 0; i < numSpaces; i++) {
      leadingText += ' ';
    }

    return leadingText;
  });

  // clean up sentinels
  text = text.replace(/¨A/g, '    ');  // g_tab_width
  text = text.replace(/¨B/g, '');

  text = globals.converter._dispatch('detab.after', text, options, globals);
  return text;
});

showdown.subParser('ellipsis', function (text, options, globals) {
  'use strict';

  if (!options.ellipsis) {
    return text;
  }

  text = globals.converter._dispatch('ellipsis.before', text, options, globals);

  text = text.replace(/\.\.\./g, '…');

  text = globals.converter._dispatch('ellipsis.after', text, options, globals);

  return text;
});

/**
 * Turn emoji codes into emojis
 *
 * List of supported emojis: https://github.com/showdownjs/showdown/wiki/Emojis
 */
showdown.subParser('emoji', function (text, options, globals) {
  'use strict';

  if (!options.emoji) {
    return text;
  }

  text = globals.converter._dispatch('emoji.before', text, options, globals);

  var emojiRgx = /:([\S]+?):/g;

  text = text.replace(emojiRgx, function (wm, emojiCode) {
    if (showdown.helper.emojis.hasOwnProperty(emojiCode)) {
      return showdown.helper.emojis[emojiCode];
    }
    return wm;
  });

  text = globals.converter._dispatch('emoji.after', text, options, globals);

  return text;
});

/**
 * Smart processing for ampersands and angle brackets that need to be encoded.
 */
showdown.subParser('encodeAmpsAndAngles', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('encodeAmpsAndAngles.before', text, options, globals);

  // Ampersand-encoding based entirely on Nat Irons's Amputator MT plugin:
  // http://bumppo.net/projects/amputator/
  text = text.replace(/&(?!#?[xX]?(?:[0-9a-fA-F]+|\w+);)/g, '&amp;');

  // Encode naked <'s
  text = text.replace(/<(?![a-z\/?$!])/gi, '&lt;');

  // Encode <
  text = text.replace(/</g, '&lt;');

  // Encode >
  text = text.replace(/>/g, '&gt;');

  text = globals.converter._dispatch('encodeAmpsAndAngles.after', text, options, globals);
  return text;
});

/**
 * Returns the string, with after processing the following backslash escape sequences.
 *
 * attacklab: The polite way to do this is with the new escapeCharacters() function:
 *
 *    text = escapeCharacters(text,"\\",true);
 *    text = escapeCharacters(text,"`*_{}[]()>#+-.!",true);
 *
 * ...but we're sidestepping its use of the (slow) RegExp constructor
 * as an optimization for Firefox.  This function gets called a LOT.
 */
showdown.subParser('encodeBackslashEscapes', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('encodeBackslashEscapes.before', text, options, globals);

  text = text.replace(/\\(\\)/g, showdown.helper.escapeCharactersCallback);
  text = text.replace(/\\([`*_{}\[\]()>#+.!~=|:-])/g, showdown.helper.escapeCharactersCallback);

  text = globals.converter._dispatch('encodeBackslashEscapes.after', text, options, globals);
  return text;
});

/**
 * Encode/escape certain characters inside Markdown code runs.
 * The point is that in code, these characters are literals,
 * and lose their special Markdown meanings.
 */
showdown.subParser('encodeCode', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('encodeCode.before', text, options, globals);

  // Encode all ampersands; HTML entities are not
  // entities within a Markdown code span.
  text = text
    .replace(/&/g, '&amp;')
  // Do the angle bracket song and dance:
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
  // Now, escape characters that are magic in Markdown:
    .replace(/([*_{}\[\]\\=~-])/g, showdown.helper.escapeCharactersCallback);

  text = globals.converter._dispatch('encodeCode.after', text, options, globals);
  return text;
});

/**
 * Within tags -- meaning between < and > -- encode [\ ` * _ ~ =] so they
 * don't conflict with their use in Markdown for code, italics and strong.
 */
showdown.subParser('escapeSpecialCharsWithinTagAttributes', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('escapeSpecialCharsWithinTagAttributes.before', text, options, globals);

  // Build a regex to find HTML tags.
  var tags     = /<\/?[a-z\d_:-]+(?:[\s]+[\s\S]+?)?>/gi,
      comments = /<!(--(?:(?:[^>-]|-[^>])(?:[^-]|-[^-])*)--)>/gi;

  text = text.replace(tags, function (wholeMatch) {
    return wholeMatch
      .replace(/(.)<\/?code>(?=.)/g, '$1`')
      .replace(/([\\`*_~=|])/g, showdown.helper.escapeCharactersCallback);
  });

  text = text.replace(comments, function (wholeMatch) {
    return wholeMatch
      .replace(/([\\`*_~=|])/g, showdown.helper.escapeCharactersCallback);
  });

  text = globals.converter._dispatch('escapeSpecialCharsWithinTagAttributes.after', text, options, globals);
  return text;
});

/**
 * Handle github codeblocks prior to running HashHTML so that
 * HTML contained within the codeblock gets escaped properly
 * Example:
 * ```ruby
 *     def hello_world(x)
 *       puts "Hello, #{x}"
 *     end
 * ```
 */
showdown.subParser('githubCodeBlocks', function (text, options, globals) {
  'use strict';

  // early exit if option is not enabled
  if (!options.ghCodeBlocks) {
    return text;
  }

  text = globals.converter._dispatch('githubCodeBlocks.before', text, options, globals);

  text += '¨0';

  text = text.replace(/(?:^|\n)(?: {0,3})(```+|~~~+)(?: *)([^\s`~]*)\n([\s\S]*?)\n(?: {0,3})\1/g, function (wholeMatch, delim, language, codeblock) {
    var end = (options.omitExtraWLInCodeBlocks) ? '' : '\n';

    // First parse the github code block
    codeblock = showdown.subParser('encodeCode')(codeblock, options, globals);
    codeblock = showdown.subParser('detab')(codeblock, options, globals);
    codeblock = codeblock.replace(/^\n+/g, ''); // trim leading newlines
    codeblock = codeblock.replace(/\n+$/g, ''); // trim trailing whitespace

    codeblock = '<pre><code' + (language ? ' class="' + language + ' language-' + language + '"' : '') + '>' + codeblock + end + '</code></pre>';

    codeblock = showdown.subParser('hashBlock')(codeblock, options, globals);

    // Since GHCodeblocks can be false positives, we need to
    // store the primitive text and the parsed text in a global var,
    // and then return a token
    return '\n\n¨G' + (globals.ghCodeBlocks.push({text: wholeMatch, codeblock: codeblock}) - 1) + 'G\n\n';
  });

  // attacklab: strip sentinel
  text = text.replace(/¨0/, '');

  return globals.converter._dispatch('githubCodeBlocks.after', text, options, globals);
});

showdown.subParser('hashBlock', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('hashBlock.before', text, options, globals);
  text = text.replace(/(^\n+|\n+$)/g, '');
  text = '\n\n¨K' + (globals.gHtmlBlocks.push(text) - 1) + 'K\n\n';
  text = globals.converter._dispatch('hashBlock.after', text, options, globals);
  return text;
});

/**
 * Hash and escape <code> elements that should not be parsed as markdown
 */
showdown.subParser('hashCodeTags', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('hashCodeTags.before', text, options, globals);

  var repFunc = function (wholeMatch, match, left, right) {
    var codeblock = left + showdown.subParser('encodeCode')(match, options, globals) + right;
    return '¨C' + (globals.gHtmlSpans.push(codeblock) - 1) + 'C';
  };

  // Hash naked <code>
  text = showdown.helper.replaceRecursiveRegExp(text, repFunc, '<code\\b[^>]*>', '</code>', 'gim');

  text = globals.converter._dispatch('hashCodeTags.after', text, options, globals);
  return text;
});

showdown.subParser('hashElement', function (text, options, globals) {
  'use strict';

  return function (wholeMatch, m1) {
    var blockText = m1;

    // Undo double lines
    blockText = blockText.replace(/\n\n/g, '\n');
    blockText = blockText.replace(/^\n/, '');

    // strip trailing blank lines
    blockText = blockText.replace(/\n+$/g, '');

    // Replace the element text with a marker ("¨KxK" where x is its key)
    blockText = '\n\n¨K' + (globals.gHtmlBlocks.push(blockText) - 1) + 'K\n\n';

    return blockText;
  };
});

showdown.subParser('hashHTMLBlocks', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('hashHTMLBlocks.before', text, options, globals);

  var blockTags = [
        'pre',
        'div',
        'h1',
        'h2',
        'h3',
        'h4',
        'h5',
        'h6',
        'blockquote',
        'table',
        'dl',
        'ol',
        'ul',
        'script',
        'noscript',
        'form',
        'fieldset',
        'iframe',
        'math',
        'style',
        'section',
        'header',
        'footer',
        'nav',
        'article',
        'aside',
        'address',
        'audio',
        'canvas',
        'figure',
        'hgroup',
        'output',
        'video',
        'p'
      ],
      repFunc = function (wholeMatch, match, left, right) {
        var txt = wholeMatch;
        // check if this html element is marked as markdown
        // if so, it's contents should be parsed as markdown
        if (left.search(/\bmarkdown\b/) !== -1) {
          txt = left + globals.converter.makeHtml(match) + right;
        }
        return '\n\n¨K' + (globals.gHtmlBlocks.push(txt) - 1) + 'K\n\n';
      };

  if (options.backslashEscapesHTMLTags) {
    // encode backslash escaped HTML tags
    text = text.replace(/\\<(\/?[^>]+?)>/g, function (wm, inside) {
      return '&lt;' + inside + '&gt;';
    });
  }

  // hash HTML Blocks
  for (var i = 0; i < blockTags.length; ++i) {

    var opTagPos,
        rgx1     = new RegExp('^ {0,3}(<' + blockTags[i] + '\\b[^>]*>)', 'im'),
        patLeft  = '<' + blockTags[i] + '\\b[^>]*>',
        patRight = '</' + blockTags[i] + '>';
    // 1. Look for the first position of the first opening HTML tag in the text
    while ((opTagPos = showdown.helper.regexIndexOf(text, rgx1)) !== -1) {

      // if the HTML tag is \ escaped, we need to escape it and break


      //2. Split the text in that position
      var subTexts = showdown.helper.splitAtIndex(text, opTagPos),
          //3. Match recursively
          newSubText1 = showdown.helper.replaceRecursiveRegExp(subTexts[1], repFunc, patLeft, patRight, 'im');

      // prevent an infinite loop
      if (newSubText1 === subTexts[1]) {
        break;
      }
      text = subTexts[0].concat(newSubText1);
    }
  }
  // HR SPECIAL CASE
  text = text.replace(/(\n {0,3}(<(hr)\b([^<>])*?\/?>)[ \t]*(?=\n{2,}))/g,
    showdown.subParser('hashElement')(text, options, globals));

  // Special case for standalone HTML comments
  text = showdown.helper.replaceRecursiveRegExp(text, function (txt) {
    return '\n\n¨K' + (globals.gHtmlBlocks.push(txt) - 1) + 'K\n\n';
  }, '^ {0,3}<!--', '-->', 'gm');

  // PHP and ASP-style processor instructions (<?...?> and <%...%>)
  text = text.replace(/(?:\n\n)( {0,3}(?:<([?%])[^\r]*?\2>)[ \t]*(?=\n{2,}))/g,
    showdown.subParser('hashElement')(text, options, globals));

  text = globals.converter._dispatch('hashHTMLBlocks.after', text, options, globals);
  return text;
});

/**
 * Hash span elements that should not be parsed as markdown
 */
showdown.subParser('hashHTMLSpans', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('hashHTMLSpans.before', text, options, globals);

  function hashHTMLSpan (html) {
    return '¨C' + (globals.gHtmlSpans.push(html) - 1) + 'C';
  }

  // Hash Self Closing tags
  text = text.replace(/<[^>]+?\/>/gi, function (wm) {
    return hashHTMLSpan(wm);
  });

  // Hash tags without properties
  text = text.replace(/<([^>]+?)>[\s\S]*?<\/\1>/g, function (wm) {
    return hashHTMLSpan(wm);
  });

  // Hash tags with properties
  text = text.replace(/<([^>]+?)\s[^>]+?>[\s\S]*?<\/\1>/g, function (wm) {
    return hashHTMLSpan(wm);
  });

  // Hash self closing tags without />
  text = text.replace(/<[^>]+?>/gi, function (wm) {
    return hashHTMLSpan(wm);
  });

  /*showdown.helper.matchRecursiveRegExp(text, '<code\\b[^>]*>', '</code>', 'gi');*/

  text = globals.converter._dispatch('hashHTMLSpans.after', text, options, globals);
  return text;
});

/**
 * Unhash HTML spans
 */
showdown.subParser('unhashHTMLSpans', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('unhashHTMLSpans.before', text, options, globals);

  for (var i = 0; i < globals.gHtmlSpans.length; ++i) {
    var repText = globals.gHtmlSpans[i],
        // limiter to prevent infinite loop (assume 10 as limit for recurse)
        limit = 0;

    while (/¨C(\d+)C/.test(repText)) {
      var num = RegExp.$1;
      repText = repText.replace('¨C' + num + 'C', globals.gHtmlSpans[num]);
      if (limit === 10) {
        console.error('maximum nesting of 10 spans reached!!!');
        break;
      }
      ++limit;
    }
    text = text.replace('¨C' + i + 'C', repText);
  }

  text = globals.converter._dispatch('unhashHTMLSpans.after', text, options, globals);
  return text;
});

/**
 * Hash and escape <pre><code> elements that should not be parsed as markdown
 */
showdown.subParser('hashPreCodeTags', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('hashPreCodeTags.before', text, options, globals);

  var repFunc = function (wholeMatch, match, left, right) {
    // encode html entities
    var codeblock = left + showdown.subParser('encodeCode')(match, options, globals) + right;
    return '\n\n¨G' + (globals.ghCodeBlocks.push({text: wholeMatch, codeblock: codeblock}) - 1) + 'G\n\n';
  };

  // Hash <pre><code>
  text = showdown.helper.replaceRecursiveRegExp(text, repFunc, '^ {0,3}<pre\\b[^>]*>\\s*<code\\b[^>]*>', '^ {0,3}</code>\\s*</pre>', 'gim');

  text = globals.converter._dispatch('hashPreCodeTags.after', text, options, globals);
  return text;
});

showdown.subParser('headers', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('headers.before', text, options, globals);

  var headerLevelStart = (isNaN(parseInt(options.headerLevelStart))) ? 1 : parseInt(options.headerLevelStart),

      // Set text-style headers:
      //	Header 1
      //	========
      //
      //	Header 2
      //	--------
      //
      setextRegexH1 = (options.smoothLivePreview) ? /^(.+)[ \t]*\n={2,}[ \t]*\n+/gm : /^(.+)[ \t]*\n=+[ \t]*\n+/gm,
      setextRegexH2 = (options.smoothLivePreview) ? /^(.+)[ \t]*\n-{2,}[ \t]*\n+/gm : /^(.+)[ \t]*\n-+[ \t]*\n+/gm;

  text = text.replace(setextRegexH1, function (wholeMatch, m1) {

    var spanGamut = showdown.subParser('spanGamut')(m1, options, globals),
        hID = (options.noHeaderId) ? '' : ' id="' + headerId(m1) + '"',
        hLevel = headerLevelStart,
        hashBlock = '<h' + hLevel + hID + '>' + spanGamut + '</h' + hLevel + '>';
    return showdown.subParser('hashBlock')(hashBlock, options, globals);
  });

  text = text.replace(setextRegexH2, function (matchFound, m1) {
    var spanGamut = showdown.subParser('spanGamut')(m1, options, globals),
        hID = (options.noHeaderId) ? '' : ' id="' + headerId(m1) + '"',
        hLevel = headerLevelStart + 1,
        hashBlock = '<h' + hLevel + hID + '>' + spanGamut + '</h' + hLevel + '>';
    return showdown.subParser('hashBlock')(hashBlock, options, globals);
  });

  // atx-style headers:
  //  # Header 1
  //  ## Header 2
  //  ## Header 2 with closing hashes ##
  //  ...
  //  ###### Header 6
  //
  var atxStyle = (options.requireSpaceBeforeHeadingText) ? /^(#{1,6})[ \t]+(.+?)[ \t]*#*\n+/gm : /^(#{1,6})[ \t]*(.+?)[ \t]*#*\n+/gm;

  text = text.replace(atxStyle, function (wholeMatch, m1, m2) {
    var hText = m2;
    if (options.customizedHeaderId) {
      hText = m2.replace(/\s?\{([^{]+?)}\s*$/, '');
    }

    var span = showdown.subParser('spanGamut')(hText, options, globals),
        hID = (options.noHeaderId) ? '' : ' id="' + headerId(m2) + '"',
        hLevel = headerLevelStart - 1 + m1.length,
        header = '<h' + hLevel + hID + '>' + span + '</h' + hLevel + '>';

    return showdown.subParser('hashBlock')(header, options, globals);
  });

  function headerId (m) {
    var title,
        prefix;

    // It is separate from other options to allow combining prefix and customized
    if (options.customizedHeaderId) {
      var match = m.match(/\{([^{]+?)}\s*$/);
      if (match && match[1]) {
        m = match[1];
      }
    }

    title = m;

    // Prefix id to prevent causing inadvertent pre-existing style matches.
    if (showdown.helper.isString(options.prefixHeaderId)) {
      prefix = options.prefixHeaderId;
    } else if (options.prefixHeaderId === true) {
      prefix = 'section-';
    } else {
      prefix = '';
    }

    if (!options.rawPrefixHeaderId) {
      title = prefix + title;
    }

    if (options.ghCompatibleHeaderId) {
      title = title
        .replace(/ /g, '-')
        // replace previously escaped chars (&, ¨ and $)
        .replace(/&amp;/g, '')
        .replace(/¨T/g, '')
        .replace(/¨D/g, '')
        // replace rest of the chars (&~$ are repeated as they might have been escaped)
        // borrowed from github's redcarpet (some they should produce similar results)
        .replace(/[&+$,\/:;=?@"#{}|^¨~\[\]`\\*)(%.!'<>]/g, '')
        .toLowerCase();
    } else if (options.rawHeaderId) {
      title = title
        .replace(/ /g, '-')
        // replace previously escaped chars (&, ¨ and $)
        .replace(/&amp;/g, '&')
        .replace(/¨T/g, '¨')
        .replace(/¨D/g, '$')
        // replace " and '
        .replace(/["']/g, '-')
        .toLowerCase();
    } else {
      title = title
        .replace(/[^\w]/g, '')
        .toLowerCase();
    }

    if (options.rawPrefixHeaderId) {
      title = prefix + title;
    }

    if (globals.hashLinkCounts[title]) {
      title = title + '-' + (globals.hashLinkCounts[title]++);
    } else {
      globals.hashLinkCounts[title] = 1;
    }
    return title;
  }

  text = globals.converter._dispatch('headers.after', text, options, globals);
  return text;
});

/**
 * Turn Markdown link shortcuts into XHTML <a> tags.
 */
showdown.subParser('horizontalRule', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('horizontalRule.before', text, options, globals);

  var key = showdown.subParser('hashBlock')('<hr />', options, globals);
  text = text.replace(/^ {0,2}( ?-){3,}[ \t]*$/gm, key);
  text = text.replace(/^ {0,2}( ?\*){3,}[ \t]*$/gm, key);
  text = text.replace(/^ {0,2}( ?_){3,}[ \t]*$/gm, key);

  text = globals.converter._dispatch('horizontalRule.after', text, options, globals);
  return text;
});

/**
 * Turn Markdown image shortcuts into <img> tags.
 */
showdown.subParser('images', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('images.before', text, options, globals);

  var inlineRegExp      = /!\[([^\]]*?)][ \t]*()\([ \t]?<?([\S]+?(?:\([\S]*?\)[\S]*?)?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(["'])([^"]*?)\6)?[ \t]?\)/g,
      crazyRegExp       = /!\[([^\]]*?)][ \t]*()\([ \t]?<([^>]*)>(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(?:(["'])([^"]*?)\6))?[ \t]?\)/g,
      base64RegExp      = /!\[([^\]]*?)][ \t]*()\([ \t]?<?(data:.+?\/.+?;base64,[A-Za-z0-9+/=\n]+?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(["'])([^"]*?)\6)?[ \t]?\)/g,
      referenceRegExp   = /!\[([^\]]*?)] ?(?:\n *)?\[([\s\S]*?)]()()()()()/g,
      refShortcutRegExp = /!\[([^\[\]]+)]()()()()()/g;

  function writeImageTagBase64 (wholeMatch, altText, linkId, url, width, height, m5, title) {
    url = url.replace(/\s/g, '');
    return writeImageTag (wholeMatch, altText, linkId, url, width, height, m5, title);
  }

  function writeImageTag (wholeMatch, altText, linkId, url, width, height, m5, title) {

    var gUrls   = globals.gUrls,
        gTitles = globals.gTitles,
        gDims   = globals.gDimensions;

    linkId = linkId.toLowerCase();

    if (!title) {
      title = '';
    }
    // Special case for explicit empty url
    if (wholeMatch.search(/\(<?\s*>? ?(['"].*['"])?\)$/m) > -1) {
      url = '';

    } else if (url === '' || url === null) {
      if (linkId === '' || linkId === null) {
        // lower-case and turn embedded newlines into spaces
        linkId = altText.toLowerCase().replace(/ ?\n/g, ' ');
      }
      url = '#' + linkId;

      if (!showdown.helper.isUndefined(gUrls[linkId])) {
        url = gUrls[linkId];
        if (!showdown.helper.isUndefined(gTitles[linkId])) {
          title = gTitles[linkId];
        }
        if (!showdown.helper.isUndefined(gDims[linkId])) {
          width = gDims[linkId].width;
          height = gDims[linkId].height;
        }
      } else {
        return wholeMatch;
      }
    }

    altText = altText
      .replace(/"/g, '&quot;')
    //altText = showdown.helper.escapeCharacters(altText, '*_', false);
      .replace(showdown.helper.regexes.asteriskDashAndColon, showdown.helper.escapeCharactersCallback);
    //url = showdown.helper.escapeCharacters(url, '*_', false);
    url = url.replace(showdown.helper.regexes.asteriskDashAndColon, showdown.helper.escapeCharactersCallback);
    var result = '<img src="' + url + '" alt="' + altText + '"';

    if (title && showdown.helper.isString(title)) {
      title = title
        .replace(/"/g, '&quot;')
      //title = showdown.helper.escapeCharacters(title, '*_', false);
        .replace(showdown.helper.regexes.asteriskDashAndColon, showdown.helper.escapeCharactersCallback);
      result += ' title="' + title + '"';
    }

    if (width && height) {
      width  = (width === '*') ? 'auto' : width;
      height = (height === '*') ? 'auto' : height;

      result += ' width="' + width + '"';
      result += ' height="' + height + '"';
    }

    result += ' />';

    return result;
  }

  // First, handle reference-style labeled images: ![alt text][id]
  text = text.replace(referenceRegExp, writeImageTag);

  // Next, handle inline images:  ![alt text](url =<width>x<height> "optional title")

  // base64 encoded images
  text = text.replace(base64RegExp, writeImageTagBase64);

  // cases with crazy urls like ./image/cat1).png
  text = text.replace(crazyRegExp, writeImageTag);

  // normal cases
  text = text.replace(inlineRegExp, writeImageTag);

  // handle reference-style shortcuts: ![img text]
  text = text.replace(refShortcutRegExp, writeImageTag);

  text = globals.converter._dispatch('images.after', text, options, globals);
  return text;
});

showdown.subParser('italicsAndBold', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('italicsAndBold.before', text, options, globals);

  // it's faster to have 3 separate regexes for each case than have just one
  // because of backtracing, in some cases, it could lead to an exponential effect
  // called "catastrophic backtrace". Ominous!

  function parseInside (txt, left, right) {
    /*
    if (options.simplifiedAutoLink) {
      txt = showdown.subParser('simplifiedAutoLinks')(txt, options, globals);
    }
    */
    return left + txt + right;
  }

  // Parse underscores
  if (options.literalMidWordUnderscores) {
    text = text.replace(/\b___(\S[\s\S]*?)___\b/g, function (wm, txt) {
      return parseInside (txt, '<strong><em>', '</em></strong>');
    });
    text = text.replace(/\b__(\S[\s\S]*?)__\b/g, function (wm, txt) {
      return parseInside (txt, '<strong>', '</strong>');
    });
    text = text.replace(/\b_(\S[\s\S]*?)_\b/g, function (wm, txt) {
      return parseInside (txt, '<em>', '</em>');
    });
  } else {
    text = text.replace(/___(\S[\s\S]*?)___/g, function (wm, m) {
      return (/\S$/.test(m)) ? parseInside (m, '<strong><em>', '</em></strong>') : wm;
    });
    text = text.replace(/__(\S[\s\S]*?)__/g, function (wm, m) {
      return (/\S$/.test(m)) ? parseInside (m, '<strong>', '</strong>') : wm;
    });
    text = text.replace(/_([^\s_][\s\S]*?)_/g, function (wm, m) {
      // !/^_[^_]/.test(m) - test if it doesn't start with __ (since it seems redundant, we removed it)
      return (/\S$/.test(m)) ? parseInside (m, '<em>', '</em>') : wm;
    });
  }

  // Now parse asterisks
  if (options.literalMidWordAsterisks) {
    text = text.replace(/([^*]|^)\B\*\*\*(\S[\s\S]*?)\*\*\*\B(?!\*)/g, function (wm, lead, txt) {
      return parseInside (txt, lead + '<strong><em>', '</em></strong>');
    });
    text = text.replace(/([^*]|^)\B\*\*(\S[\s\S]*?)\*\*\B(?!\*)/g, function (wm, lead, txt) {
      return parseInside (txt, lead + '<strong>', '</strong>');
    });
    text = text.replace(/([^*]|^)\B\*(\S[\s\S]*?)\*\B(?!\*)/g, function (wm, lead, txt) {
      return parseInside (txt, lead + '<em>', '</em>');
    });
  } else {
    text = text.replace(/\*\*\*(\S[\s\S]*?)\*\*\*/g, function (wm, m) {
      return (/\S$/.test(m)) ? parseInside (m, '<strong><em>', '</em></strong>') : wm;
    });
    text = text.replace(/\*\*(\S[\s\S]*?)\*\*/g, function (wm, m) {
      return (/\S$/.test(m)) ? parseInside (m, '<strong>', '</strong>') : wm;
    });
    text = text.replace(/\*([^\s*][\s\S]*?)\*/g, function (wm, m) {
      // !/^\*[^*]/.test(m) - test if it doesn't start with ** (since it seems redundant, we removed it)
      return (/\S$/.test(m)) ? parseInside (m, '<em>', '</em>') : wm;
    });
  }


  text = globals.converter._dispatch('italicsAndBold.after', text, options, globals);
  return text;
});

/**
 * Form HTML ordered (numbered) and unordered (bulleted) lists.
 */
showdown.subParser('lists', function (text, options, globals) {
  'use strict';

  /**
   * Process the contents of a single ordered or unordered list, splitting it
   * into individual list items.
   * @param {string} listStr
   * @param {boolean} trimTrailing
   * @returns {string}
   */
  function processListItems (listStr, trimTrailing) {
    // The $g_list_level global keeps track of when we're inside a list.
    // Each time we enter a list, we increment it; when we leave a list,
    // we decrement. If it's zero, we're not in a list anymore.
    //
    // We do this because when we're not inside a list, we want to treat
    // something like this:
    //
    //    I recommend upgrading to version
    //    8. Oops, now this line is treated
    //    as a sub-list.
    //
    // As a single paragraph, despite the fact that the second line starts
    // with a digit-period-space sequence.
    //
    // Whereas when we're inside a list (or sub-list), that line will be
    // treated as the start of a sub-list. What a kludge, huh? This is
    // an aspect of Markdown's syntax that's hard to parse perfectly
    // without resorting to mind-reading. Perhaps the solution is to
    // change the syntax rules such that sub-lists must start with a
    // starting cardinal number; e.g. "1." or "a.".
    globals.gListLevel++;

    // trim trailing blank lines:
    listStr = listStr.replace(/\n{2,}$/, '\n');

    // attacklab: add sentinel to emulate \z
    listStr += '¨0';

    var rgx = /(\n)?(^ {0,3})([*+-]|\d+[.])[ \t]+((\[(x|X| )?])?[ \t]*[^\r]+?(\n{1,2}))(?=\n*(¨0| {0,3}([*+-]|\d+[.])[ \t]+))/gm,
        isParagraphed = (/\n[ \t]*\n(?!¨0)/.test(listStr));

    // Since version 1.5, nesting sublists requires 4 spaces (or 1 tab) indentation,
    // which is a syntax breaking change
    // activating this option reverts to old behavior
    if (options.disableForced4SpacesIndentedSublists) {
      rgx = /(\n)?(^ {0,3})([*+-]|\d+[.])[ \t]+((\[(x|X| )?])?[ \t]*[^\r]+?(\n{1,2}))(?=\n*(¨0|\2([*+-]|\d+[.])[ \t]+))/gm;
    }

    listStr = listStr.replace(rgx, function (wholeMatch, m1, m2, m3, m4, taskbtn, checked) {
      checked = (checked && checked.trim() !== '');

      var item = showdown.subParser('outdent')(m4, options, globals),
          bulletStyle = '';

      // Support for github tasklists
      if (taskbtn && options.tasklists) {
        bulletStyle = ' class="task-list-item" style="list-style-type: none;"';
        item = item.replace(/^[ \t]*\[(x|X| )?]/m, function () {
          var otp = '<input type="checkbox" disabled style="margin: 0px 0.35em 0.25em -1.6em; vertical-align: middle;"';
          if (checked) {
            otp += ' checked';
          }
          otp += '>';
          return otp;
        });
      }

      // ISSUE #312
      // This input: - - - a
      // causes trouble to the parser, since it interprets it as:
      // <ul><li><li><li>a</li></li></li></ul>
      // instead of:
      // <ul><li>- - a</li></ul>
      // So, to prevent it, we will put a marker (¨A)in the beginning of the line
      // Kind of hackish/monkey patching, but seems more effective than overcomplicating the list parser
      item = item.replace(/^([-*+]|\d\.)[ \t]+[\S\n ]*/g, function (wm2) {
        return '¨A' + wm2;
      });

      // m1 - Leading line or
      // Has a double return (multi paragraph) or
      // Has sublist
      if (m1 || (item.search(/\n{2,}/) > -1)) {
        item = showdown.subParser('githubCodeBlocks')(item, options, globals);
        item = showdown.subParser('blockGamut')(item, options, globals);
      } else {
        // Recursion for sub-lists:
        item = showdown.subParser('lists')(item, options, globals);
        item = item.replace(/\n$/, ''); // chomp(item)
        item = showdown.subParser('hashHTMLBlocks')(item, options, globals);

        // Colapse double linebreaks
        item = item.replace(/\n\n+/g, '\n\n');
        if (isParagraphed) {
          item = showdown.subParser('paragraphs')(item, options, globals);
        } else {
          item = showdown.subParser('spanGamut')(item, options, globals);
        }
      }

      // now we need to remove the marker (¨A)
      item = item.replace('¨A', '');
      // we can finally wrap the line in list item tags
      item =  '<li' + bulletStyle + '>' + item + '</li>\n';

      return item;
    });

    // attacklab: strip sentinel
    listStr = listStr.replace(/¨0/g, '');

    globals.gListLevel--;

    if (trimTrailing) {
      listStr = listStr.replace(/\s+$/, '');
    }

    return listStr;
  }

  function styleStartNumber (list, listType) {
    // check if ol and starts by a number different than 1
    if (listType === 'ol') {
      var res = list.match(/^ *(\d+)\./);
      if (res && res[1] !== '1') {
        return ' start="' + res[1] + '"';
      }
    }
    return '';
  }

  /**
   * Check and parse consecutive lists (better fix for issue #142)
   * @param {string} list
   * @param {string} listType
   * @param {boolean} trimTrailing
   * @returns {string}
   */
  function parseConsecutiveLists (list, listType, trimTrailing) {
    // check if we caught 2 or more consecutive lists by mistake
    // we use the counterRgx, meaning if listType is UL we look for OL and vice versa
    var olRgx = (options.disableForced4SpacesIndentedSublists) ? /^ ?\d+\.[ \t]/gm : /^ {0,3}\d+\.[ \t]/gm,
        ulRgx = (options.disableForced4SpacesIndentedSublists) ? /^ ?[*+-][ \t]/gm : /^ {0,3}[*+-][ \t]/gm,
        counterRxg = (listType === 'ul') ? olRgx : ulRgx,
        result = '';

    if (list.search(counterRxg) !== -1) {
      (function parseCL (txt) {
        var pos = txt.search(counterRxg),
            style = styleStartNumber(list, listType);
        if (pos !== -1) {
          // slice
          result += '\n\n<' + listType + style + '>\n' + processListItems(txt.slice(0, pos), !!trimTrailing) + '</' + listType + '>\n';

          // invert counterType and listType
          listType = (listType === 'ul') ? 'ol' : 'ul';
          counterRxg = (listType === 'ul') ? olRgx : ulRgx;

          //recurse
          parseCL(txt.slice(pos));
        } else {
          result += '\n\n<' + listType + style + '>\n' + processListItems(txt, !!trimTrailing) + '</' + listType + '>\n';
        }
      })(list);
    } else {
      var style = styleStartNumber(list, listType);
      result = '\n\n<' + listType + style + '>\n' + processListItems(list, !!trimTrailing) + '</' + listType + '>\n';
    }

    return result;
  }

  /** Start of list parsing **/
  text = globals.converter._dispatch('lists.before', text, options, globals);
  // add sentinel to hack around khtml/safari bug:
  // http://bugs.webkit.org/show_bug.cgi?id=11231
  text += '¨0';

  if (globals.gListLevel) {
    text = text.replace(/^(( {0,3}([*+-]|\d+[.])[ \t]+)[^\r]+?(¨0|\n{2,}(?=\S)(?![ \t]*(?:[*+-]|\d+[.])[ \t]+)))/gm,
      function (wholeMatch, list, m2) {
        var listType = (m2.search(/[*+-]/g) > -1) ? 'ul' : 'ol';
        return parseConsecutiveLists(list, listType, true);
      }
    );
  } else {
    text = text.replace(/(\n\n|^\n?)(( {0,3}([*+-]|\d+[.])[ \t]+)[^\r]+?(¨0|\n{2,}(?=\S)(?![ \t]*(?:[*+-]|\d+[.])[ \t]+)))/gm,
      function (wholeMatch, m1, list, m3) {
        var listType = (m3.search(/[*+-]/g) > -1) ? 'ul' : 'ol';
        return parseConsecutiveLists(list, listType, false);
      }
    );
  }

  // strip sentinel
  text = text.replace(/¨0/, '');
  text = globals.converter._dispatch('lists.after', text, options, globals);
  return text;
});

/**
 * Parse metadata at the top of the document
 */
showdown.subParser('metadata', function (text, options, globals) {
  'use strict';

  if (!options.metadata) {
    return text;
  }

  text = globals.converter._dispatch('metadata.before', text, options, globals);

  function parseMetadataContents (content) {
    // raw is raw so it's not changed in any way
    globals.metadata.raw = content;

    // escape chars forbidden in html attributes
    // double quotes
    content = content
      // ampersand first
      .replace(/&/g, '&amp;')
      // double quotes
      .replace(/"/g, '&quot;');

    content = content.replace(/\n {4}/g, ' ');
    content.replace(/^([\S ]+): +([\s\S]+?)$/gm, function (wm, key, value) {
      globals.metadata.parsed[key] = value;
      return '';
    });
  }

  text = text.replace(/^\s*«««+(\S*?)\n([\s\S]+?)\n»»»+\n/, function (wholematch, format, content) {
    parseMetadataContents(content);
    return '¨M';
  });

  text = text.replace(/^\s*---+(\S*?)\n([\s\S]+?)\n---+\n/, function (wholematch, format, content) {
    if (format) {
      globals.metadata.format = format;
    }
    parseMetadataContents(content);
    return '¨M';
  });

  text = text.replace(/¨M/g, '');

  text = globals.converter._dispatch('metadata.after', text, options, globals);
  return text;
});

/**
 * Remove one level of line-leading tabs or spaces
 */
showdown.subParser('outdent', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('outdent.before', text, options, globals);

  // attacklab: hack around Konqueror 3.5.4 bug:
  // "----------bug".replace(/^-/g,"") == "bug"
  text = text.replace(/^(\t|[ ]{1,4})/gm, '¨0'); // attacklab: g_tab_width

  // attacklab: clean up hack
  text = text.replace(/¨0/g, '');

  text = globals.converter._dispatch('outdent.after', text, options, globals);
  return text;
});

/**
 *
 */
showdown.subParser('paragraphs', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('paragraphs.before', text, options, globals);
  // Strip leading and trailing lines:
  text = text.replace(/^\n+/g, '');
  text = text.replace(/\n+$/g, '');

  var grafs = text.split(/\n{2,}/g),
      grafsOut = [],
      end = grafs.length; // Wrap <p> tags

  for (var i = 0; i < end; i++) {
    var str = grafs[i];
    // if this is an HTML marker, copy it
    if (str.search(/¨(K|G)(\d+)\1/g) >= 0) {
      grafsOut.push(str);

    // test for presence of characters to prevent empty lines being parsed
    // as paragraphs (resulting in undesired extra empty paragraphs)
    } else if (str.search(/\S/) >= 0) {
      str = showdown.subParser('spanGamut')(str, options, globals);
      str = str.replace(/^([ \t]*)/g, '<p>');
      str += '</p>';
      grafsOut.push(str);
    }
  }

  /** Unhashify HTML blocks */
  end = grafsOut.length;
  for (i = 0; i < end; i++) {
    var blockText = '',
        grafsOutIt = grafsOut[i],
        codeFlag = false;
    // if this is a marker for an html block...
    // use RegExp.test instead of string.search because of QML bug
    while (/¨(K|G)(\d+)\1/.test(grafsOutIt)) {
      var delim = RegExp.$1,
          num   = RegExp.$2;

      if (delim === 'K') {
        blockText = globals.gHtmlBlocks[num];
      } else {
        // we need to check if ghBlock is a false positive
        if (codeFlag) {
          // use encoded version of all text
          blockText = showdown.subParser('encodeCode')(globals.ghCodeBlocks[num].text, options, globals);
        } else {
          blockText = globals.ghCodeBlocks[num].codeblock;
        }
      }
      blockText = blockText.replace(/\$/g, '$$$$'); // Escape any dollar signs

      grafsOutIt = grafsOutIt.replace(/(\n\n)?¨(K|G)\d+\2(\n\n)?/, blockText);
      // Check if grafsOutIt is a pre->code
      if (/^<pre\b[^>]*>\s*<code\b[^>]*>/.test(grafsOutIt)) {
        codeFlag = true;
      }
    }
    grafsOut[i] = grafsOutIt;
  }
  text = grafsOut.join('\n');
  // Strip leading and trailing lines:
  text = text.replace(/^\n+/g, '');
  text = text.replace(/\n+$/g, '');
  return globals.converter._dispatch('paragraphs.after', text, options, globals);
});

/**
 * Run extension
 */
showdown.subParser('runExtension', function (ext, text, options, globals) {
  'use strict';

  if (ext.filter) {
    text = ext.filter(text, globals.converter, options);

  } else if (ext.regex) {
    // TODO remove this when old extension loading mechanism is deprecated
    var re = ext.regex;
    if (!(re instanceof RegExp)) {
      re = new RegExp(re, 'g');
    }
    text = text.replace(re, ext.replace);
  }

  return text;
});

/**
 * These are all the transformations that occur *within* block-level
 * tags like paragraphs, headers, and list items.
 */
showdown.subParser('spanGamut', function (text, options, globals) {
  'use strict';

  text = globals.converter._dispatch('spanGamut.before', text, options, globals);
  text = showdown.subParser('codeSpans')(text, options, globals);
  text = showdown.subParser('escapeSpecialCharsWithinTagAttributes')(text, options, globals);
  text = showdown.subParser('encodeBackslashEscapes')(text, options, globals);

  // Process anchor and image tags. Images must come first,
  // because ![foo][f] looks like an anchor.
  text = showdown.subParser('images')(text, options, globals);
  text = showdown.subParser('anchors')(text, options, globals);

  // Make links out of things like `<http://example.com/>`
  // Must come after anchors, because you can use < and >
  // delimiters in inline links like [this](<url>).
  text = showdown.subParser('autoLinks')(text, options, globals);
  text = showdown.subParser('simplifiedAutoLinks')(text, options, globals);
  text = showdown.subParser('emoji')(text, options, globals);
  text = showdown.subParser('underline')(text, options, globals);
  text = showdown.subParser('italicsAndBold')(text, options, globals);
  text = showdown.subParser('strikethrough')(text, options, globals);
  text = showdown.subParser('ellipsis')(text, options, globals);

  // we need to hash HTML tags inside spans
  text = showdown.subParser('hashHTMLSpans')(text, options, globals);

  // now we encode amps and angles
  text = showdown.subParser('encodeAmpsAndAngles')(text, options, globals);

  // Do hard breaks
  if (options.simpleLineBreaks) {
    // GFM style hard breaks
    // only add line breaks if the text does not contain a block (special case for lists)
    if (!/\n\n¨K/.test(text)) {
      text = text.replace(/\n+/g, '<br />\n');
    }
  } else {
    // Vanilla hard breaks
    text = text.replace(/  +\n/g, '<br />\n');
  }

  text = globals.converter._dispatch('spanGamut.after', text, options, globals);
  return text;
});

showdown.subParser('strikethrough', function (text, options, globals) {
  'use strict';

  function parseInside (txt) {
    if (options.simplifiedAutoLink) {
      txt = showdown.subParser('simplifiedAutoLinks')(txt, options, globals);
    }
    return '<del>' + txt + '</del>';
  }

  if (options.strikethrough) {
    text = globals.converter._dispatch('strikethrough.before', text, options, globals);
    text = text.replace(/(?:~){2}([\s\S]+?)(?:~){2}/g, function (wm, txt) { return parseInside(txt); });
    text = globals.converter._dispatch('strikethrough.after', text, options, globals);
  }

  return text;
});

/**
 * Strips link definitions from text, stores the URLs and titles in
 * hash references.
 * Link defs are in the form: ^[id]: url "optional title"
 */
showdown.subParser('stripLinkDefinitions', function (text, options, globals) {
  'use strict';

  var regex       = /^ {0,3}\[([^\]]+)]:[ \t]*\n?[ \t]*<?([^>\s]+)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*\n?[ \t]*(?:(\n*)["|'(](.+?)["|')][ \t]*)?(?:\n+|(?=¨0))/gm,
      base64Regex = /^ {0,3}\[([^\]]+)]:[ \t]*\n?[ \t]*<?(data:.+?\/.+?;base64,[A-Za-z0-9+/=\n]+?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*\n?[ \t]*(?:(\n*)["|'(](.+?)["|')][ \t]*)?(?:\n\n|(?=¨0)|(?=\n\[))/gm;

  // attacklab: sentinel workarounds for lack of \A and \Z, safari\khtml bug
  text += '¨0';

  var replaceFunc = function (wholeMatch, linkId, url, width, height, blankLines, title) {

    // if there aren't two instances of linkId it must not be a reference link so back out
    linkId = linkId.toLowerCase();
    if (text.toLowerCase().split(linkId).length - 1 < 2) {
      return wholeMatch;
    }
    if (url.match(/^data:.+?\/.+?;base64,/)) {
      // remove newlines
      globals.gUrls[linkId] = url.replace(/\s/g, '');
    } else {
      globals.gUrls[linkId] = showdown.subParser('encodeAmpsAndAngles')(url, options, globals);  // Link IDs are case-insensitive
    }

    if (blankLines) {
      // Oops, found blank lines, so it's not a title.
      // Put back the parenthetical statement we stole.
      return blankLines + title;

    } else {
      if (title) {
        globals.gTitles[linkId] = title.replace(/"|'/g, '&quot;');
      }
      if (options.parseImgDimensions && width && height) {
        globals.gDimensions[linkId] = {
          width:  width,
          height: height
        };
      }
    }
    // Completely remove the definition from the text
    return '';
  };

  // first we try to find base64 link references
  text = text.replace(base64Regex, replaceFunc);

  text = text.replace(regex, replaceFunc);

  // attacklab: strip sentinel
  text = text.replace(/¨0/, '');

  return text;
});

showdown.subParser('tables', function (text, options, globals) {
  'use strict';

  if (!options.tables) {
    return text;
  }

  var tableRgx       = /^ {0,3}\|?.+\|.+\n {0,3}\|?[ \t]*:?[ \t]*(?:[-=]){2,}[ \t]*:?[ \t]*\|[ \t]*:?[ \t]*(?:[-=]){2,}[\s\S]+?(?:\n\n|¨0)/gm,
      //singeColTblRgx = /^ {0,3}\|.+\|\n {0,3}\|[ \t]*:?[ \t]*(?:[-=]){2,}[ \t]*:?[ \t]*\|[ \t]*\n(?: {0,3}\|.+\|\n)+(?:\n\n|¨0)/gm;
      singeColTblRgx = /^ {0,3}\|.+\|[ \t]*\n {0,3}\|[ \t]*:?[ \t]*(?:[-=]){2,}[ \t]*:?[ \t]*\|[ \t]*\n( {0,3}\|.+\|[ \t]*\n)*(?:\n|¨0)/gm;

  function parseStyles (sLine) {
    if (/^:[ \t]*--*$/.test(sLine)) {
      return ' style="text-align:left;"';
    } else if (/^--*[ \t]*:[ \t]*$/.test(sLine)) {
      return ' style="text-align:right;"';
    } else if (/^:[ \t]*--*[ \t]*:$/.test(sLine)) {
      return ' style="text-align:center;"';
    } else {
      return '';
    }
  }

  function parseHeaders (header, style) {
    var id = '';
    header = header.trim();
    // support both tablesHeaderId and tableHeaderId due to error in documentation so we don't break backwards compatibility
    if (options.tablesHeaderId || options.tableHeaderId) {
      id = ' id="' + header.replace(/ /g, '_').toLowerCase() + '"';
    }
    header = showdown.subParser('spanGamut')(header, options, globals);

    return '<th' + id + style + '>' + header + '</th>\n';
  }

  function parseCells (cell, style) {
    var subText = showdown.subParser('spanGamut')(cell, options, globals);
    return '<td' + style + '>' + subText + '</td>\n';
  }

  function buildTable (headers, cells) {
    var tb = '<table>\n<thead>\n<tr>\n',
        tblLgn = headers.length;

    for (var i = 0; i < tblLgn; ++i) {
      tb += headers[i];
    }
    tb += '</tr>\n</thead>\n<tbody>\n';

    for (i = 0; i < cells.length; ++i) {
      tb += '<tr>\n';
      for (var ii = 0; ii < tblLgn; ++ii) {
        tb += cells[i][ii];
      }
      tb += '</tr>\n';
    }
    tb += '</tbody>\n</table>\n';
    return tb;
  }

  function parseTable (rawTable) {
    var i, tableLines = rawTable.split('\n');

    for (i = 0; i < tableLines.length; ++i) {
      // strip wrong first and last column if wrapped tables are used
      if (/^ {0,3}\|/.test(tableLines[i])) {
        tableLines[i] = tableLines[i].replace(/^ {0,3}\|/, '');
      }
      if (/\|[ \t]*$/.test(tableLines[i])) {
        tableLines[i] = tableLines[i].replace(/\|[ \t]*$/, '');
      }
      // parse code spans first, but we only support one line code spans
      tableLines[i] = showdown.subParser('codeSpans')(tableLines[i], options, globals);
    }

    var rawHeaders = tableLines[0].split('|').map(function (s) { return s.trim();}),
        rawStyles = tableLines[1].split('|').map(function (s) { return s.trim();}),
        rawCells = [],
        headers = [],
        styles = [],
        cells = [];

    tableLines.shift();
    tableLines.shift();

    for (i = 0; i < tableLines.length; ++i) {
      if (tableLines[i].trim() === '') {
        continue;
      }
      rawCells.push(
        tableLines[i]
          .split('|')
          .map(function (s) {
            return s.trim();
          })
      );
    }

    if (rawHeaders.length < rawStyles.length) {
      return rawTable;
    }

    for (i = 0; i < rawStyles.length; ++i) {
      styles.push(parseStyles(rawStyles[i]));
    }

    for (i = 0; i < rawHeaders.length; ++i) {
      if (showdown.helper.isUndefined(styles[i])) {
        styles[i] = '';
      }
      headers.push(parseHeaders(rawHeaders[i], styles[i]));
    }

    for (i = 0; i < rawCells.length; ++i) {
      var row = [];
      for (var ii = 0; ii < headers.length; ++ii) {
        if (showdown.helper.isUndefined(rawCells[i][ii])) {

        }
        row.push(parseCells(rawCells[i][ii], styles[ii]));
      }
      cells.push(row);
    }

    return buildTable(headers, cells);
  }

  text = globals.converter._dispatch('tables.before', text, options, globals);

  // find escaped pipe characters
  text = text.replace(/\\(\|)/g, showdown.helper.escapeCharactersCallback);

  // parse multi column tables
  text = text.replace(tableRgx, parseTable);

  // parse one column tables
  text = text.replace(singeColTblRgx, parseTable);

  text = globals.converter._dispatch('tables.after', text, options, globals);

  return text;
});

showdown.subParser('underline', function (text, options, globals) {
  'use strict';

  if (!options.underline) {
    return text;
  }

  text = globals.converter._dispatch('underline.before', text, options, globals);

  if (options.literalMidWordUnderscores) {
    text = text.replace(/\b___(\S[\s\S]*?)___\b/g, function (wm, txt) {
      return '<u>' + txt + '</u>';
    });
    text = text.replace(/\b__(\S[\s\S]*?)__\b/g, function (wm, txt) {
      return '<u>' + txt + '</u>';
    });
  } else {
    text = text.replace(/___(\S[\s\S]*?)___/g, function (wm, m) {
      return (/\S$/.test(m)) ? '<u>' + m + '</u>' : wm;
    });
    text = text.replace(/__(\S[\s\S]*?)__/g, function (wm, m) {
      return (/\S$/.test(m)) ? '<u>' + m + '</u>' : wm;
    });
  }

  // escape remaining underscores to prevent them being parsed by italic and bold
  text = text.replace(/(_)/g, showdown.helper.escapeCharactersCallback);

  text = globals.converter._dispatch('underline.after', text, options, globals);

  return text;
});

/**
 * Swap back in all the special characters we've hidden.
 */
showdown.subParser('unescapeSpecialChars', function (text, options, globals) {
  'use strict';
  text = globals.converter._dispatch('unescapeSpecialChars.before', text, options, globals);

  text = text.replace(/¨E(\d+)E/g, function (wholeMatch, m1) {
    var charCodeToReplace = parseInt(m1);
    return String.fromCharCode(charCodeToReplace);
  });

  text = globals.converter._dispatch('unescapeSpecialChars.after', text, options, globals);
  return text;
});

showdown.subParser('makeMarkdown.blockquote', function (node, globals) {
  'use strict';

  var txt = '';
  if (node.hasChildNodes()) {
    var children = node.childNodes,
        childrenLength = children.length;

    for (var i = 0; i < childrenLength; ++i) {
      var innerTxt = showdown.subParser('makeMarkdown.node')(children[i], globals);

      if (innerTxt === '') {
        continue;
      }
      txt += innerTxt;
    }
  }
  // cleanup
  txt = txt.trim();
  txt = '> ' + txt.split('\n').join('\n> ');
  return txt;
});

showdown.subParser('makeMarkdown.codeBlock', function (node, globals) {
  'use strict';

  var lang = node.getAttribute('language'),
      num  = node.getAttribute('precodenum');
  return '```' + lang + '\n' + globals.preList[num] + '\n```';
});

showdown.subParser('makeMarkdown.codeSpan', function (node) {
  'use strict';

  return '`' + node.innerHTML + '`';
});

showdown.subParser('makeMarkdown.emphasis', function (node, globals) {
  'use strict';

  var txt = '';
  if (node.hasChildNodes()) {
    txt += '*';
    var children = node.childNodes,
        childrenLength = children.length;
    for (var i = 0; i < childrenLength; ++i) {
      txt += showdown.subParser('makeMarkdown.node')(children[i], globals);
    }
    txt += '*';
  }
  return txt;
});

showdown.subParser('makeMarkdown.header', function (node, globals, headerLevel) {
  'use strict';

  var headerMark = new Array(headerLevel + 1).join('#'),
      txt = '';

  if (node.hasChildNodes()) {
    txt = headerMark + ' ';
    var children = node.childNodes,
        childrenLength = children.length;

    for (var i = 0; i < childrenLength; ++i) {
      txt += showdown.subParser('makeMarkdown.node')(children[i], globals);
    }
  }
  return txt;
});

showdown.subParser('makeMarkdown.hr', function () {
  'use strict';

  return '---';
});

showdown.subParser('makeMarkdown.image', function (node) {
  'use strict';

  var txt = '';
  if (node.hasAttribute('src')) {
    txt += '![' + node.getAttribute('alt') + '](';
    txt += '<' + node.getAttribute('src') + '>';
    if (node.hasAttribute('width') && node.hasAttribute('height')) {
      txt += ' =' + node.getAttribute('width') + 'x' + node.getAttribute('height');
    }

    if (node.hasAttribute('title')) {
      txt += ' "' + node.getAttribute('title') + '"';
    }
    txt += ')';
  }
  return txt;
});

showdown.subParser('makeMarkdown.links', function (node, globals) {
  'use strict';

  var txt = '';
  if (node.hasChildNodes() && node.hasAttribute('href')) {
    var children = node.childNodes,
        childrenLength = children.length;
    txt = '[';
    for (var i = 0; i < childrenLength; ++i) {
      txt += showdown.subParser('makeMarkdown.node')(children[i], globals);
    }
    txt += '](';
    txt += '<' + node.getAttribute('href') + '>';
    if (node.hasAttribute('title')) {
      txt += ' "' + node.getAttribute('title') + '"';
    }
    txt += ')';
  }
  return txt;
});

showdown.subParser('makeMarkdown.list', function (node, globals, type) {
  'use strict';

  var txt = '';
  if (!node.hasChildNodes()) {
    return '';
  }
  var listItems       = node.childNodes,
      listItemsLenght = listItems.length,
      listNum = node.getAttribute('start') || 1;

  for (var i = 0; i < listItemsLenght; ++i) {
    if (typeof listItems[i].tagName === 'undefined' || listItems[i].tagName.toLowerCase() !== 'li') {
      continue;
    }

    // define the bullet to use in list
    var bullet = '';
    if (type === 'ol') {
      bullet = listNum.toString() + '. ';
    } else {
      bullet = '- ';
    }

    // parse list item
    txt += bullet + showdown.subParser('makeMarkdown.listItem')(listItems[i], globals);
    ++listNum;
  }

  // add comment at the end to prevent consecutive lists to be parsed as one
  txt += '\n<!-- -->\n';
  return txt.trim();
});

showdown.subParser('makeMarkdown.listItem', function (node, globals) {
  'use strict';

  var listItemTxt = '';

  var children = node.childNodes,
      childrenLenght = children.length;

  for (var i = 0; i < childrenLenght; ++i) {
    listItemTxt += showdown.subParser('makeMarkdown.node')(children[i], globals);
  }
  // if it's only one liner, we need to add a newline at the end
  if (!/\n$/.test(listItemTxt)) {
    listItemTxt += '\n';
  } else {
    // it's multiparagraph, so we need to indent
    listItemTxt = listItemTxt
      .split('\n')
      .join('\n    ')
      .replace(/^ {4}$/gm, '')
      .replace(/\n\n+/g, '\n\n');
  }

  return listItemTxt;
});



showdown.subParser('makeMarkdown.node', function (node, globals, spansOnly) {
  'use strict';

  spansOnly = spansOnly || false;

  var txt = '';

  // edge case of text without wrapper paragraph
  if (node.nodeType === 3) {
    return showdown.subParser('makeMarkdown.txt')(node, globals);
  }

  // HTML comment
  if (node.nodeType === 8) {
    return '<!--' + node.data + '-->\n\n';
  }

  // process only node elements
  if (node.nodeType !== 1) {
    return '';
  }

  var tagName = node.tagName.toLowerCase();

  switch (tagName) {

    //
    // BLOCKS
    //
    case 'h1':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.header')(node, globals, 1) + '\n\n'; }
      break;
    case 'h2':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.header')(node, globals, 2) + '\n\n'; }
      break;
    case 'h3':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.header')(node, globals, 3) + '\n\n'; }
      break;
    case 'h4':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.header')(node, globals, 4) + '\n\n'; }
      break;
    case 'h5':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.header')(node, globals, 5) + '\n\n'; }
      break;
    case 'h6':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.header')(node, globals, 6) + '\n\n'; }
      break;

    case 'p':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.paragraph')(node, globals) + '\n\n'; }
      break;

    case 'blockquote':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.blockquote')(node, globals) + '\n\n'; }
      break;

    case 'hr':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.hr')(node, globals) + '\n\n'; }
      break;

    case 'ol':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.list')(node, globals, 'ol') + '\n\n'; }
      break;

    case 'ul':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.list')(node, globals, 'ul') + '\n\n'; }
      break;

    case 'precode':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.codeBlock')(node, globals) + '\n\n'; }
      break;

    case 'pre':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.pre')(node, globals) + '\n\n'; }
      break;

    case 'table':
      if (!spansOnly) { txt = showdown.subParser('makeMarkdown.table')(node, globals) + '\n\n'; }
      break;

    //
    // SPANS
    //
    case 'code':
      txt = showdown.subParser('makeMarkdown.codeSpan')(node, globals);
      break;

    case 'em':
    case 'i':
      txt = showdown.subParser('makeMarkdown.emphasis')(node, globals);
      break;

    case 'strong':
    case 'b':
      txt = showdown.subParser('makeMarkdown.strong')(node, globals);
      break;

    case 'del':
      txt = showdown.subParser('makeMarkdown.strikethrough')(node, globals);
      break;

    case 'a':
      txt = showdown.subParser('makeMarkdown.links')(node, globals);
      break;

    case 'img':
      txt = showdown.subParser('makeMarkdown.image')(node, globals);
      break;

    default:
      txt = node.outerHTML + '\n\n';
  }

  // common normalization
  // TODO eventually

  return txt;
});

showdown.subParser('makeMarkdown.paragraph', function (node, globals) {
  'use strict';

  var txt = '';
  if (node.hasChildNodes()) {
    var children = node.childNodes,
        childrenLength = children.length;
    for (var i = 0; i < childrenLength; ++i) {
      txt += showdown.subParser('makeMarkdown.node')(children[i], globals);
    }
  }

  // some text normalization
  txt = txt.trim();

  return txt;
});

showdown.subParser('makeMarkdown.pre', function (node, globals) {
  'use strict';

  var num  = node.getAttribute('prenum');
  return '<pre>' + globals.preList[num] + '</pre>';
});

showdown.subParser('makeMarkdown.strikethrough', function (node, globals) {
  'use strict';

  var txt = '';
  if (node.hasChildNodes()) {
    txt += '~~';
    var children = node.childNodes,
        childrenLength = children.length;
    for (var i = 0; i < childrenLength; ++i) {
      txt += showdown.subParser('makeMarkdown.node')(children[i], globals);
    }
    txt += '~~';
  }
  return txt;
});

showdown.subParser('makeMarkdown.strong', function (node, globals) {
  'use strict';

  var txt = '';
  if (node.hasChildNodes()) {
    txt += '**';
    var children = node.childNodes,
        childrenLength = children.length;
    for (var i = 0; i < childrenLength; ++i) {
      txt += showdown.subParser('makeMarkdown.node')(children[i], globals);
    }
    txt += '**';
  }
  return txt;
});

showdown.subParser('makeMarkdown.table', function (node, globals) {
  'use strict';

  var txt = '',
      tableArray = [[], []],
      headings   = node.querySelectorAll('thead>tr>th'),
      rows       = node.querySelectorAll('tbody>tr'),
      i, ii;
  for (i = 0; i < headings.length; ++i) {
    var headContent = showdown.subParser('makeMarkdown.tableCell')(headings[i], globals),
        allign = '---';

    if (headings[i].hasAttribute('style')) {
      var style = headings[i].getAttribute('style').toLowerCase().replace(/\s/g, '');
      switch (style) {
        case 'text-align:left;':
          allign = ':---';
          break;
        case 'text-align:right;':
          allign = '---:';
          break;
        case 'text-align:center;':
          allign = ':---:';
          break;
      }
    }
    tableArray[0][i] = headContent.trim();
    tableArray[1][i] = allign;
  }

  for (i = 0; i < rows.length; ++i) {
    var r = tableArray.push([]) - 1,
        cols = rows[i].getElementsByTagName('td');

    for (ii = 0; ii < headings.length; ++ii) {
      var cellContent = ' ';
      if (typeof cols[ii] !== 'undefined') {
        cellContent = showdown.subParser('makeMarkdown.tableCell')(cols[ii], globals);
      }
      tableArray[r].push(cellContent);
    }
  }

  var cellSpacesCount = 3;
  for (i = 0; i < tableArray.length; ++i) {
    for (ii = 0; ii < tableArray[i].length; ++ii) {
      var strLen = tableArray[i][ii].length;
      if (strLen > cellSpacesCount) {
        cellSpacesCount = strLen;
      }
    }
  }

  for (i = 0; i < tableArray.length; ++i) {
    for (ii = 0; ii < tableArray[i].length; ++ii) {
      if (i === 1) {
        if (tableArray[i][ii].slice(-1) === ':') {
          tableArray[i][ii] = showdown.helper.padEnd(tableArray[i][ii].slice(-1), cellSpacesCount - 1, '-') + ':';
        } else {
          tableArray[i][ii] = showdown.helper.padEnd(tableArray[i][ii], cellSpacesCount, '-');
        }
      } else {
        tableArray[i][ii] = showdown.helper.padEnd(tableArray[i][ii], cellSpacesCount);
      }
    }
    txt += '| ' + tableArray[i].join(' | ') + ' |\n';
  }

  return txt.trim();
});

showdown.subParser('makeMarkdown.tableCell', function (node, globals) {
  'use strict';

  var txt = '';
  if (!node.hasChildNodes()) {
    return '';
  }
  var children = node.childNodes,
      childrenLength = children.length;

  for (var i = 0; i < childrenLength; ++i) {
    txt += showdown.subParser('makeMarkdown.node')(children[i], globals, true);
  }
  return txt.trim();
});

showdown.subParser('makeMarkdown.txt', function (node) {
  'use strict';

  var txt = node.nodeValue;

  // multiple spaces are collapsed
  txt = txt.replace(/ +/g, ' ');

  // replace the custom ¨NBSP; with a space
  txt = txt.replace(/¨NBSP;/g, ' ');

  // ", <, > and & should replace escaped html entities
  txt = showdown.helper.unescapeHTMLEntities(txt);

  // escape markdown magic characters
  // emphasis, strong and strikethrough - can appear everywhere
  // we also escape pipe (|) because of tables
  // and escape ` because of code blocks and spans
  txt = txt.replace(/([*_~|`])/g, '\\$1');

  // escape > because of blockquotes
  txt = txt.replace(/^(\s*)>/g, '\\$1>');

  // hash character, only troublesome at the beginning of a line because of headers
  txt = txt.replace(/^#/gm, '\\#');

  // horizontal rules
  txt = txt.replace(/^(\s*)([-=]{3,})(\s*)$/, '$1\\$2$3');

  // dot, because of ordered lists, only troublesome at the beginning of a line when preceded by an integer
  txt = txt.replace(/^( {0,3}\d+)\./gm, '$1\\.');

  // +, * and -, at the beginning of a line becomes a list, so we need to escape them also (asterisk was already escaped)
  txt = txt.replace(/^( {0,3})([+-])/gm, '$1\\$2');

  // images and links, ] followed by ( is problematic, so we escape it
  txt = txt.replace(/]([\s]*)\(/g, '\\]$1\\(');

  // reference URIs must also be escaped
  txt = txt.replace(/^ {0,3}\[([\S \t]*?)]:/gm, '\\[$1]:');

  return txt;
});

var root = this;

// AMD Loader
if (true) {
  !(__WEBPACK_AMD_DEFINE_RESULT__ = (function () {
    'use strict';
    return showdown;
  }).call(exports, __webpack_require__, exports, module),
		__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));

// CommonJS/nodeJS Loader
} else {}
}).call(this);

//# sourceMappingURL=showdown.js.map


/***/ }),

/***/ 88428:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(5117);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 63040:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(83097);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 52262:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(80764);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 22065:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(47961);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 10453:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(6636);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 25050:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(25745);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 3593:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(32575);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 67860:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(58363);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 31662:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(53095);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 27455:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(57778);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 31014:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(6380);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 81941:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(17960);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 96230:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(38802);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 73320:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(99665);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 12823:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(96592);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 82069:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(35618);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 36876:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(447);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 4262:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(99275);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 51417:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(4350);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 4849:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(17155);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 62460:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(83098);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 89490:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(16734);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 39513:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(50368);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 20220:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(12986);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 67524:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(69688);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 69734:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(8881);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 93417:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(75958);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 79623:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(83570);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 44229:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(98223);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 18308:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(67269);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 42922:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(77925);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 18673:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(68987);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 11024:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(21445);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 50894:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(43379);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 53915:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(75187);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 40376:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(9427);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 85077:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(6009);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 94422:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(18856);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 49732:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(10910);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 48234:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(79269);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 29192:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(93871);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 6399:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(85263);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 53224:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(17831);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 69153:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(9723);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 48336:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(77544);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 76997:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(31485);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 5096:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(54909);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 37166:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(21092);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 36710:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(30108);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 23759:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(66103);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 98696:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(37541);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 42536:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(6785);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 65860:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(6361);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 37519:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(43812);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 85765:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(93914);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 18812:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(10707);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 48408:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(93379);
            var content = __webpack_require__(30822);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 93379:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isOldIE = function isOldIE() {
  var memo;
  return function memorize() {
    if (typeof memo === 'undefined') {
      // Test for IE <= 9 as proposed by Browserhacks
      // @see http://browserhacks.com/#hack-e71d8692f65334173fee715c222cb805
      // Tests for existence of standard globals is to allow style-loader
      // to operate correctly into non-standard environments
      // @see https://github.com/webpack-contrib/style-loader/issues/177
      memo = Boolean(window && document && document.all && !window.atob);
    }

    return memo;
  };
}();

var getTarget = function getTarget() {
  var memo = {};
  return function memorize(target) {
    if (typeof memo[target] === 'undefined') {
      var styleTarget = document.querySelector(target); // Special case to return head of iframe instead of iframe itself

      if (window.HTMLIFrameElement && styleTarget instanceof window.HTMLIFrameElement) {
        try {
          // This will throw an exception if access to iframe is blocked
          // due to cross-origin restrictions
          styleTarget = styleTarget.contentDocument.head;
        } catch (e) {
          // istanbul ignore next
          styleTarget = null;
        }
      }

      memo[target] = styleTarget;
    }

    return memo[target];
  };
}();

var stylesInDom = [];

function getIndexByIdentifier(identifier) {
  var result = -1;

  for (var i = 0; i < stylesInDom.length; i++) {
    if (stylesInDom[i].identifier === identifier) {
      result = i;
      break;
    }
  }

  return result;
}

function modulesToDom(list, options) {
  var idCountMap = {};
  var identifiers = [];

  for (var i = 0; i < list.length; i++) {
    var item = list[i];
    var id = options.base ? item[0] + options.base : item[0];
    var count = idCountMap[id] || 0;
    var identifier = "".concat(id, " ").concat(count);
    idCountMap[id] = count + 1;
    var index = getIndexByIdentifier(identifier);
    var obj = {
      css: item[1],
      media: item[2],
      sourceMap: item[3]
    };

    if (index !== -1) {
      stylesInDom[index].references++;
      stylesInDom[index].updater(obj);
    } else {
      stylesInDom.push({
        identifier: identifier,
        updater: addStyle(obj, options),
        references: 1
      });
    }

    identifiers.push(identifier);
  }

  return identifiers;
}

function insertStyleElement(options) {
  var style = document.createElement('style');
  var attributes = options.attributes || {};

  if (typeof attributes.nonce === 'undefined') {
    var nonce =  true ? __webpack_require__.nc : 0;

    if (nonce) {
      attributes.nonce = nonce;
    }
  }

  Object.keys(attributes).forEach(function (key) {
    style.setAttribute(key, attributes[key]);
  });

  if (typeof options.insert === 'function') {
    options.insert(style);
  } else {
    var target = getTarget(options.insert || 'head');

    if (!target) {
      throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
    }

    target.appendChild(style);
  }

  return style;
}

function removeStyleElement(style) {
  // istanbul ignore if
  if (style.parentNode === null) {
    return false;
  }

  style.parentNode.removeChild(style);
}
/* istanbul ignore next  */


var replaceText = function replaceText() {
  var textStore = [];
  return function replace(index, replacement) {
    textStore[index] = replacement;
    return textStore.filter(Boolean).join('\n');
  };
}();

function applyToSingletonTag(style, index, remove, obj) {
  var css = remove ? '' : obj.media ? "@media ".concat(obj.media, " {").concat(obj.css, "}") : obj.css; // For old IE

  /* istanbul ignore if  */

  if (style.styleSheet) {
    style.styleSheet.cssText = replaceText(index, css);
  } else {
    var cssNode = document.createTextNode(css);
    var childNodes = style.childNodes;

    if (childNodes[index]) {
      style.removeChild(childNodes[index]);
    }

    if (childNodes.length) {
      style.insertBefore(cssNode, childNodes[index]);
    } else {
      style.appendChild(cssNode);
    }
  }
}

function applyToTag(style, options, obj) {
  var css = obj.css;
  var media = obj.media;
  var sourceMap = obj.sourceMap;

  if (media) {
    style.setAttribute('media', media);
  } else {
    style.removeAttribute('media');
  }

  if (sourceMap && typeof btoa !== 'undefined') {
    css += "\n/*# sourceMappingURL=data:application/json;base64,".concat(btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))), " */");
  } // For old IE

  /* istanbul ignore if  */


  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    while (style.firstChild) {
      style.removeChild(style.firstChild);
    }

    style.appendChild(document.createTextNode(css));
  }
}

var singleton = null;
var singletonCounter = 0;

function addStyle(obj, options) {
  var style;
  var update;
  var remove;

  if (options.singleton) {
    var styleIndex = singletonCounter++;
    style = singleton || (singleton = insertStyleElement(options));
    update = applyToSingletonTag.bind(null, style, styleIndex, false);
    remove = applyToSingletonTag.bind(null, style, styleIndex, true);
  } else {
    style = insertStyleElement(options);
    update = applyToTag.bind(null, style, options);

    remove = function remove() {
      removeStyleElement(style);
    };
  }

  update(obj);
  return function updateStyle(newObj) {
    if (newObj) {
      if (newObj.css === obj.css && newObj.media === obj.media && newObj.sourceMap === obj.sourceMap) {
        return;
      }

      update(obj = newObj);
    } else {
      remove();
    }
  };
}

module.exports = function (list, options) {
  options = options || {}; // Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
  // tags it will allow on a page

  if (!options.singleton && typeof options.singleton !== 'boolean') {
    options.singleton = isOldIE();
  }

  list = list || [];
  var lastIdentifiers = modulesToDom(list, options);
  return function update(newList) {
    newList = newList || [];

    if (Object.prototype.toString.call(newList) !== '[object Array]') {
      return;
    }

    for (var i = 0; i < lastIdentifiers.length; i++) {
      var identifier = lastIdentifiers[i];
      var index = getIndexByIdentifier(identifier);
      stylesInDom[index].references--;
    }

    var newLastIdentifiers = modulesToDom(newList, options);

    for (var _i = 0; _i < lastIdentifiers.length; _i++) {
      var _identifier = lastIdentifiers[_i];

      var _index = getIndexByIdentifier(_identifier);

      if (stylesInDom[_index].references === 0) {
        stylesInDom[_index].updater();

        stylesInDom.splice(_index, 1);
      }
    }

    lastIdentifiers = newLastIdentifiers;
  };
};

/***/ }),

/***/ 18739:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const template = __webpack_require__(20793)
const Root = __webpack_require__(48715)
__webpack_require__(10410)
__webpack_require__(9548)
__webpack_require__(69921)
__webpack_require__(36901)
__webpack_require__(29569)
__webpack_require__(65626)
__webpack_require__(63040)
__webpack_require__(88428)

module.exports = class MainApp extends Root {

}
  .define({
    name: 'main-app',
    template,
  })


/***/ }),

/***/ 65626:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(84138)
const config = __webpack_require__(36758)
const { buildCollections } = __webpack_require__(49045)

const collections = buildCollections(config.server.url)

module.exports = {
  collections
}


/***/ }),

/***/ 23752:
/***/ (function(module) {


module.exports = {
  server: {
    url: 'http://localhost:8081',
  }
}

/***/ }),

/***/ 36758:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const config = __webpack_require__(86273)

module.exports = config(
  __webpack_require__(23752),
  __webpack_require__(46957),
)

/***/ }),

/***/ 46957:
/***/ (function(module) {


module.exports = {
}

/***/ }),

/***/ 14498:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

console.log('required project setup')
const config = __webpack_require__(36758)
const AuthService = __webpack_require__(86230)
const NotificationsService = __webpack_require__(2884)
const global = __webpack_require__(98864)
const Navigator = __webpack_require__(98118)
const Loader = __webpack_require__(98198)
const MenuService = __webpack_require__(55070)

Object.assign(global, {
  auth: new AuthService(config.server.url),
  notifications: new NotificationsService(),
  navigator: new Navigator(),
  menu: new MenuService()
})

global.menu.links.push({
  label: 'Home',
  class: 'fa-solid fa-house',
  url: '/',
})

Object.assign(global.components, {
  Loader
})
module.exports = global

/***/ }),

/***/ 40386:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(8240)
__webpack_require__(52262)

module.exports = class EditableText extends Component {
  edit() {
    this.editing = true
    setTimeout(() => {
      this.input.focus()
    })
  }
}
  .define({
    name: 'app-editable-text',
    template,
  })
  .properties({
    value: 'any',
    editing: 'bool',
  })


/***/ }),

/***/ 98198:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const template = __webpack_require__(97946)
const BaseLoader = __webpack_require__(28255)
__webpack_require__(22065)

module.exports = class Loader extends BaseLoader {

}
  .define({
    name: 'app-loader',
    template,
  })

/***/ }),

/***/ 97612:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(11661)
__webpack_require__(10453)

module.exports = class LoadingContent extends Component {

}
  .define({
    name: 'loading-content',
    template,
  })
  .properties({
    color: 'any',
  })



/***/ }),

/***/ 71252:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {




module.exports = {
  ...__webpack_require__(79786),
  ...__webpack_require__(55235),
}

/***/ }),

/***/ 55235:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Model } = __webpack_require__(75314)
const componentsService = __webpack_require__(69066)
const markdownService = __webpack_require__(49173)
const { parse } = __webpack_require__(66221)
const childs = Model
  .getAllChilds()
  .filter((c) => !c.definition.abstract)

const childNames = childs.map((c) => c.definition.name)
const regex = new RegExp(`{% (${childNames.join('|')}) (.*?) %}`, 'g')

module.exports = {
  model: {
    type: 'lang',
    filter(s) {
      return s.replace(regex, (all, typeName, index) => {
        index = index.split(';').reduce((acc, s) => {
          const [k, v] = s.split(':')
          acc[k] = v
          return acc
        }, {})

        const type = childs.find((c) => c.definition.name === typeName)
        const rowComponent = componentsService.get(type, 'row')
        const model = type.parse(index)
        const id = markdownService.register(model)
        return `<a :v-link="this.${id}.@url" class="model">
          <${rowComponent.definition.name} :model="this.${id}">
          </${rowComponent.definition.name}>
        </a>
        `
      })
    }
  }
}


/***/ }),

/***/ 79786:
/***/ (function(module) {

module.exports = {
  youtube: {
    type: 'lang',
    filter: (s) => {
      return s.replace(/\{% youtube(?:\(([)]+)\))? (.+) %\}/g, (all, attrs, url) => {
        let width
        let height

        if (attrs) {
          const d = attrs.split(',').map((el) => parseInt(el))
          if (d.every((el) => el) && d.length === 2) {
            width = d[0]
            height = d[1]
          }

          if (!width || !height) {
            const dict = eqdict(attrs)
            width = parseInt(dict.width)
            height = parseInt(dict.height)
          }
        }

        try {
          url = (() => {
            const u = new URL(url)
            const v = u.searchParams.get('v')

            if (v) {
              return v
            }

            return u.pathname.replace(/$.*\//, '')
          })()
        } catch (e) { }

        const div = document.createElement('div')
        div.innerHTML = `<iframe width="${width || 560}" height="${height || 315}"
                src="https://www.youtube.com/embed/${url}"
                frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
                allowfullscreen></iframe>`
        return div.outerHTML
      })
    }
  }
}

/***/ }),

/***/ 76165:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const showdown = __webpack_require__(53787)
const extensions = __webpack_require__(71252)
const service = __webpack_require__(49173)
const Scope = __webpack_require__(29777)
__webpack_require__(25050)

const converter = new showdown.Converter({
  parseImgDimensions: true,
  simplifiedAutoLink: true,
  strikethrough: true,
  tables: true,
  disableForced4SpacesIndentedSublists: true,
  backslashEscapesHTMLTags: true,
  emoji: true,
  metadata: false
})

Object.entries(extensions).map(([k, v]) => converter.addExtension(v, k))


module.exports = class Markdown extends Component {
  constructor() {
    super()
    this.on('propertyChanged:value', this.b(this.update))
  }

  onInit() {
    this.update()
  }

  update() {
    if (this.contentScope) {
      this.contentScope.destroy()
      this.contentScope = null
    }
    if (!this.value) { return }

    const html = converter.makeHtml(this.value)
    this.innerHTML = html
    const scope = this.scope.child()
    scope.variables.this = service.variables
    scope.renderContent(this)
    this.contentScope = scope
  }
}
  .define({
    name: 'mark-down',
  })
  .properties({
    value: 'any',
  })


/***/ }),

/***/ 49173:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { makeId } = __webpack_require__(71513)

const variables = {}

const register = (value) => {
  const id = makeId()
  variables[id] = value
  return id
}

module.exports = {
  register,
  variables,
}

/***/ }),

/***/ 4937:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(25232)
__webpack_require__(3593)

const getCover = (bound1, bound2) => {
  const max = Math.min(bound1[1], bound2[1])
  const min = Math.max(bound1[0], bound2[0])
  return Math.max(max - min, 0)
}

const focusClass = 'summary-section-focus'

module.exports = class Summary extends Component {

  constructor() {
    super()
    this.on('propertyChanged:root', this.b(this.update))
  }

  onInit() {
    this.update()
  }

  update() {
    if (!this.root) { return }
    const root = this.root
    this.sections = [...root.querySelectorAll('[summary-section]')]
    this.current = this.sections[0]

    root.addEventListener('scroll', () => {
      const rootBound = [root.scrollTop, root.clientHeight + root.scrollTop]
      if (this.current) {
        this.current.classList.remove(focusClass)
      }
      this.current = this.sections
        .map(({ element, title }) => {
          const height = element.clientHeight
          const start = element.offsetTop
          const elementBound = [start, start + height]

          const cover = getCover(rootBound, elementBound)
          const percentage = (cover * 100 / height)
          return {
            element,
            percentage,
            cover,
          }
        })
        .sort((a, b) => b.percentage - a.percentage)
        .filter(({ percentage }, i, c) => percentage == c[0].percentage)
        .sort((a, b) => b.cover - a.cover)
      [0]
        .element
      this.current.classList.add(focusClass)
    })
  }

  focus(element) {
    element.scrollIntoView({
      behavior: 'smooth'
    })
  }
}
  .define({
    name: 'content-summary',
    template,
  })
  .properties({
    root: 'any',
    sections: 'any',
    current: 'any',
  })



/***/ }),

/***/ 50157:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(76298)
__webpack_require__(67860)

const OFFSET = 50

module.exports = class Tooltip extends Component {
  constructor() {
    super()
    this.show = false

  }

  onInit() {
    this.listen(this.parentElement, 'mouseenter', this.onMouseEnter)
    this.listen(this.parentElement, 'mouseleave', this.onMouseLeave)
  }

  onMouseEnter() {
    this.show = true
  }

  onMouseLeave() {
    this.show = false
  }
}
  .define({
    name: 'tool-tip',
    template,
  })
  .properties({
    show: 'any',
  })


/***/ }),

/***/ 85050:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(11633)
const { bound } = __webpack_require__(27274)

__webpack_require__(31662)

module.exports = class WheelSelect extends Component {


  constructor() {
    super()
    this.on('propertyChanged:option', this.b(this.update))
  }

  onInit() {
    this.index = 0
    if (!this.option) {
      this.option = this.options[0]
    }
    this.update()
  }

  update() {
    this.index = this.options.indexOf(this.option)
  }

  setOption(option) {
    this.option = option
    this.event('changed', { option })
  }

  onWheel(event) {
    event.preventDefault()
    let index = this.index
    index += event.deltaY > 0 ? 1 : -1
    index = bound(index, [0, this.options.length - 1])
    this.setOption(this.options[index])
  }
}
  .define({
    name: 'wheel-select',
    template,
  })
  .properties({
    options: 'any',
    option: 'any',
    index: 'any',
  })


/***/ }),

/***/ 86020:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(40386)
__webpack_require__(98198)
__webpack_require__(50157)
__webpack_require__(97612)
__webpack_require__(4937)
__webpack_require__(76165)
__webpack_require__(85050)

/***/ }),

/***/ 56909:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const LayoutRouter = __webpack_require__(50742)

const router = new LayoutRouter({
  layout: __webpack_require__(42994)
})

router.route('/empty', async (req, res) => {
  await res.page(__webpack_require__.e(/* import() */ 302).then(__webpack_require__.t.bind(__webpack_require__, 39302, 23)))
})

module.exports = router

/***/ }),

/***/ 29569:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(86020)

/***/ }),

/***/ 42994:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Layout = __webpack_require__(87968)
const template = __webpack_require__(5911)
__webpack_require__(27455)

module.exports = class Empty extends Layout {

}.define({
  name: 'empty-layout',
  template,
})

/***/ }),

/***/ 74808:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Layout = __webpack_require__(87968)
const template = __webpack_require__(48631)
__webpack_require__(31014)
__webpack_require__(57945)
const { auth, menu } = __webpack_require__(14498)

module.exports = class Main extends Layout {

}
  .define({
    name: 'app-layout-main',
    template,
  })
  .variables({
    auth,
    menu,
  })
  .properties({
    open: 'any'
  })
  .localStorage({
    name: 'main',
    properties: ['open'],
  })

/***/ }),

/***/ 83974:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const LayoutRouter = __webpack_require__(50742)
const router = new LayoutRouter({
  layout: __webpack_require__(74808)
})

router.route(/^\/$/, (req, res) => {
  res.page(__webpack_require__.e(/* import() */ 643).then(__webpack_require__.t.bind(__webpack_require__, 78643, 23)))
})

router.route('/code-30000', (req, res) => {
  res.page(__webpack_require__.e(/* import() */ 60).then(__webpack_require__.t.bind(__webpack_require__, 14060, 23)))
})

router.route('/not-found', (req, res) => {
  res.navigate('/code-30000', true)
})

/*
router.use((req, res) => {
  res.navigate('/not-found')
})
*/

module.exports = router

/***/ }),

/***/ 36466:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497);
const template = __webpack_require__(52609);
const ModalContainer = __webpack_require__(65013)
__webpack_require__(81941);


module.exports = class Modal extends Component {
  constructor() {
    super()
    this.isOpened = false
  }

  show() {
    console.log('show', this)
    ModalContainer.instance.show(this)
    this.isOpened = true
  }

  close() {
    console.log('closing', this)
    this.isOpened = false
    ModalContainer.instance.close()
  }
}
  .define({
    name: "mo-dal",
    template,
  })
  .properties({
    title: 'any',
    isOpened: 'any',
  })


/***/ }),

/***/ 65013:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(77215)

__webpack_require__(96230)


module.exports = class ModalContainer extends Component {
  constructor() {
    super()
    if (this.constructor.instance) { throw new Error('ModalContainer already instanciated') }
    this.constructor.instance = this
  }

  removeListener() {
    if (this.modal) {
      this.modal.off('destroyed', this.b(this.onModalDestroyed))
    }
  }

  show(modal) {
    this.removeListener()
    this.modal = modal
    if (this.modal) {
      this.modal.on('destroyed', this.b(this.onModalDestroyed))
    }
  }

  onModalDestroyed() {
    this.removeListener()
    this.modal = null
  }

  close() {
    this.modal = null
  }
}
  .define({
    name: 'modal-container',
    template,
  })
  .properties({
    modal: 'any',
  })


/***/ }),

/***/ 9548:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(65013)
__webpack_require__(36466)

/***/ }),

/***/ 69521:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Card = __webpack_require__(72294)
const template = __webpack_require__(49283)
const defaultImage = __webpack_require__(87396)
const { Entity } = __webpack_require__(84138)
__webpack_require__(73320)

module.exports = class SystemCard extends Card {
  async update() {
    await super.update()
    if (!this.model) { return }
    await this.model.image?.load()
  }
}
  .define({
    name: 'entity-card',
    template,
  })
  .variables({
    defaultImage,
  })
  .register(Entity, 'card')



/***/ }),

/***/ 24916:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const ViewModel = __webpack_require__(81590)

module.exports = class Camera extends ViewModel {

}
  .define()
  .properties({
    x: 'any',
    y: 'any',
    zoom: 'any',
  })



/***/ }),

/***/ 88542:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const ViewModel = __webpack_require__(81590)

module.exports = class Layer extends ViewModel {

}
  .define()
  .properties({
    x: 'any',
    y: 'any',
  })



/***/ }),

/***/ 52768:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Holder = __webpack_require__(84658)
const mixer = __webpack_require__(96172)
const ViewModel = __webpack_require__(81590)

module.exports = class Object extends mixer.extends(ViewModel, [Holder]) {

}
  .define()
  .properties({
    x: 'any',
    y: 'any',
    object: 'any',
  })



/***/ }),

/***/ 46620:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {


const template = __webpack_require__(79332)
const Camera = __webpack_require__(24916)
const Layer = __webpack_require__(88542)
const MapObject = __webpack_require__(52768)
const { calculateDistance, getMax } = __webpack_require__(66428)
const Component = __webpack_require__(58497)
const { collections } = __webpack_require__(65626)
const { bound: boundInt } = __webpack_require__(27274)
__webpack_require__(12823)

const toRadians = (angle) => {
  return angle * (Math.PI / 180);
}


const MAX_LAYERS = 6
const LAYER_DISTANCE = 400
const MIN_ZOOM = 0.2
const MAX_ZOOM = 2
const RADIANS = toRadians(360)

const bound = (position, square) => {
  position.x = boundInt(position.x, square.x)
  position.y = boundInt(position.y, square.y)
}


module.exports = class Map extends Component {
  constructor() {
    super()
    this.on('propertyChanged:object', this.b(this.update))
    this.camera = new Camera({
      x: 0,
      y: 0,
      zoom: 1.0,
    })
  }

  async start() {
    const systems = await collections.entities.find([{
      $is: ['$this', 'system']
    }], {
      load: {
        organization: true,
      }
    })

    this.mapObjects = systems
      .filter((system) => system.starmap?.position)
      .map((object) => {
        if (!object.organization)
          console.log(object)
        return new MapObject({
          object,
          name: object.name,
          position: object.starmap.position,
          color: object.organization?.color,
          x: 0,
          y: 0,
        })
      })
    this.layers = [...Array(MAX_LAYERS)].map((nop, i) => {
      return new Layer({
        x: 0,
        y: 0,
        size: (i + 1) * LAYER_DISTANCE
      })
    })

    if (!this.object) {
      this.object = systems[0]
    }

    this.update()
    Object.assign(this.camera, {
      x: this.clientWidth / 2,
      y: this.clientHeight / 2,
    })
  }

  onInit() {
    this.start()
  }

  update() {
    const mapObject = this.mapObjects.find((mo) => mo.object === this.object)
    if (!mapObject) { return }
    this.mapObject = mapObject
    Object.assign(this.mapObject, {
      x: 0,
      y: 0,
    })

    const maxDistance = getMax(this.mapObject, this.mapObjects)
    let min = 0
    this.layers.forEach((layer, i) => {
      const max = 100 / MAX_LAYERS * (i + 1)
      let matching = []
      this.mapObjects.forEach((o) => {
        if (o === this.mapObject) { return }
        const distance = calculateDistance(o.position, this.mapObject.position)
        const percentage = distance * 100 / maxDistance

        if (percentage >= min && percentage <= max + 1) {
          matching.push(o)
        }
      })
      const length = matching.length
      matching.forEach((o, i) => {
        const position = i * (RADIANS / length)
        o.x = Math.cos(position) * layer.size / 2
        o.y = Math.sin(position) * layer.size / 2
      })
      min = max
    })
  }

  onMouseDown() {
    this.isDown = true
  }

  onMouseUp() {
    this.isDown = false
  }

  onMove(event) {
    if (!this.isDown) { return }
    this.camera.x += event.movementX
    this.camera.y += event.movementY
    bound(this.camera, {
      x: [0, this.clientWidth],
      y: [0, this.clientHeight]
    })
  }

  onWheel(event) {
    if (!event.ctrlKey) { return }

    event.preventDefault()
    const zoom = this.camera.zoom - (event.deltaY / 1000)
    this.camera.zoom = boundInt(zoom, [MIN_ZOOM, MAX_ZOOM])
  }

  destroy() {
    this.mapObjects.forEach((o) => o.destroy())
    return super.destroy()
  }
}
  .define({
    name: 'app-map',
    template,
  })
  .properties({
    object: 'any',
    currentObject: 'any',
    mapObjects: 'any',
    layers: 'any',
  })


/***/ }),

/***/ 66428:
/***/ (function(module) {

const calculateDistance = (p1, p2) => {
  var a = p2.x - p1.x;
  var b = p2.y - p1.y;
  var c = p2.z - p1.z;

  return Math.sqrt(a * a + b * b + c * c);
}

const getMax = (object, objects) => {
  let max = 0
  for (const o of objects) {
    if (object === o) { continue }
    const distance = calculateDistance(object.position, o.position)
    if (distance > max) {
      max = distance
    }
  }
  return max
}

module.exports = {
  getMax,
  calculateDistance
}

/***/ }),

/***/ 560:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Row = __webpack_require__(31009)
const template = __webpack_require__(54990)
const { Entity } = __webpack_require__(84138)
__webpack_require__(82069)

module.exports = class EntityRow extends Row {
  constructor(...args) {
    super(...args)
  }
}
  .define({
    name: 'app-entity-row',
    template,
  })
  .register(Entity, 'row', {
    affiliation: true,
  })

/***/ }),

/***/ 55316:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(69521)
__webpack_require__(560)
__webpack_require__(46620)

/***/ }),

/***/ 69921:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(39350)
__webpack_require__(49851)
__webpack_require__(55316)

/***/ }),

/***/ 58254:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Card = __webpack_require__(72294)
const template = __webpack_require__(24572)
const { Planet } = __webpack_require__(84138)
__webpack_require__(36876)

module.exports = class PlanetRow extends Card { }
  .define({
    name: 'planet-row',
    template,
  })
  .register(Planet, 'row')



/***/ }),

/***/ 65809:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(58254)

/***/ }),

/***/ 49851:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(65809)

/***/ }),

/***/ 57295:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Row = __webpack_require__(31009)
const template = __webpack_require__(33566)
const { System } = __webpack_require__(84138)
__webpack_require__(4262)

module.exports = class SystemRow extends Row {

}
  .define({
    name: 'system-row',
    template,
  })
  .register(System, 'row')



/***/ }),

/***/ 32718:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(57295)

/***/ }),

/***/ 39350:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(32718)

/***/ }),

/***/ 24802:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Little = __webpack_require__(8478)
const template = __webpack_require__(39174)
const { Organization } = __webpack_require__(84138)
__webpack_require__(51417)

module.exports = class OrganizationLittle extends Little { }
  .define({
    name: 'organization-little',
    template,
  })
  .register(Organization, 'little')



/***/ }),

/***/ 48602:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Row = __webpack_require__(31009)
const template = __webpack_require__(1333)
const { Organization } = __webpack_require__(84138)
__webpack_require__(4849)

module.exports = class AffiliationRow extends Row { }
  .define({
    name: 'organization-row',
    template,
  })
  .register(Organization, 'row')


/***/ }),

/***/ 36901:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(24802)
__webpack_require__(48602)

/***/ }),

/***/ 10410:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(68989)
const { notifications } = __webpack_require__(98864)
__webpack_require__(62460);


module.exports = class NotificationsList extends Component {

}

  .define({
    name: "app-notifications-list",
    template,
  })
  .variables({
    service: notifications,
  })


/***/ }),

/***/ 78031:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Propertiable = __webpack_require__(85020)

module.exports = class Notification extends mixer.extends([Propertiable]) {
  constructor(values) {
    super()
    Object.assign(this, values)
  }
  close() {
    if (this.isClosed) {
      return
    }
    this.isClosed = true
    setTimeout(() => {
      const index = service.notifications.indexOf(this)
      if (index === -1) {
        return
      }
      service.notifications.splice(index, 1)
    }, 500);
  }
}
  .define()
  .properties({
    isClosed: 'any',
    message: 'any'
  })


/***/ }),

/***/ 2884:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Service = __webpack_require__(25033)
const Array = __webpack_require__(83083)
const Notification = __webpack_require__(78031)

const types = {
  info: {
    icon: '<i class="fa-solid fa-circle-info"></i>',
  },
  success: {
    icon: '<i class="fa-solid fa-check"></i>',
  },
  error: {
    icon: '<i class="fa-solid fa-exclamation"></i>',
  },
};

Object.entries(types).forEach(([name, type]) => {
  type.name = name;
})

module.exports = class NotificationService extends Service {
  constructor() {
    super()
    this.notifications = new Array()
  }
  notify(notification) {
    notification.type = types[notification.type] || types.info
    notification = new Notification(notification)
    this.notifications.push(notification)
  }
}
  .define()
  .properties({
    notifications: "any",
  })


/***/ }),

/***/ 58790:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const authRouter = __webpack_require__(35564)
const modelingRouter = __webpack_require__(70650)
const storageRouter = __webpack_require__(36119)
const mainLayoutRouter = __webpack_require__(83974)
const emptyLayoutRouter = __webpack_require__(56909)

emptyLayoutRouter.use(authRouter)
mainLayoutRouter.use(modelingRouter)
mainLayoutRouter.use(storageRouter)
module.exports = {
  emptyLayoutRouter,
  mainLayoutRouter
}

/***/ }),

/***/ 39227:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(36405)
__webpack_require__(58950)
__webpack_require__(47062)
__webpack_require__(78002)
__webpack_require__(91922)
__webpack_require__(63982)
__webpack_require__(7300)
__webpack_require__(84138)
__webpack_require__(34543)
__webpack_require__(95529)
__webpack_require__(14498)

__webpack_require__(72488)

__webpack_require__(54280)
__webpack_require__(71014)
__webpack_require__(4150)
__webpack_require__(51224)

/***/ }),

/***/ 35293:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Propertiable = __webpack_require__(85020)

class Context extends mixer.extends([Propertiable]) {
  constructor(values = {}) {
    super()
    Object.assign(this, values)
  }
}
const context = new Context()

module.exports = context

/***/ }),

/***/ 55070:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Service = __webpack_require__(25033)
const Array = __webpack_require__(83083)

module.exports = class MenuService extends Service {
  constructor() {
    super()
    this.links = new Array()
  }
}
  .define()

/***/ }),

/***/ 95529:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(89490)

/***/ }),

/***/ 38988:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Definitions = __webpack_require__(58439)

module.exports = mixer.mixin((base) => {
  return class Base extends base {

    static hasMixin(mixin) {
      return this.allDependencies.some((d) => d === (mixin.mixin || mixin))
    }

    static define(definition = {}) {
      if (this.definition?.owner === this) {
        throw new Error(`Class ${this.name} already defined`)
      }

      if (!this.dependenciesOwner) {
        this.dependenciesOwner = this
      } else {
        this.dependencies = []
      }
      const parent = this.definition?.owner
      const parents = [parent, ...(this.dependencies || [])]
        .filter((p) => p?.definition)
      parents.forEach((parent) => {
        parent.definition.childs.push(this)
      })
      this.definition = {
        ...definition,
        childs: [],
        parent,
        parents,
        owner: this,
      }

      this.definitions = new Definitions(this)

      return this
    }

    static set(values) {
      Object.assign(this, values)
      return this
    }

    static findChild(check) {
      if (check(this)) {
        return this
      }
      for (const child of this.definition.childs) {
        const subChild = child.findChild(check)
        if (subChild) {
          return subChild
        }
      }
      return null
    }

    static getAllChilds() {
      const childs = [this]
      for (const child of this.definition.childs) {
        childs.push(...child.getAllChilds())
      }
      return childs
    }

  }
})


/***/ }),

/***/ 58439:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Tree = __webpack_require__(9948)

module.exports = class Definitions extends Tree {
  constructor(owner) {
    super()
    this.push(owner.definition)
    this.owner = owner
    owner.definition.parents
      .filter((o) => o.definitions)
      .forEach((parent) => {
        this.push(parent.definitions)
      })
  }
}

/***/ }),

/***/ 46073:
/***/ (function(module) {

module.exports = class Mixin {
  constructor() {
    throw new Error('Cannot instanciante mixin')
  }
}

/***/ }),

/***/ 86273:
/***/ (function(module) {




module.exports = (...subConfigs) => {

  const config = {}

  const processObject = (configObject, object) => {
    Object.entries(object)
      .forEach(([key, value]) => {
        const configValue = configObject[key]
        if (!configValue) {
          configObject[key] = value
          return
        }
        const valueType = typeof value
        const configType = typeof configValue
        if (valueType !== configType) {
          throw new Error()
        }

        if (valueType === 'object') {
          if (Array.isArray(value)) {
            configObject[key] = value
            return
          }
          processObject(configValue, value)
          return
        }

        configObject[key] = value
      })
  }

  subConfigs.forEach((subConfig) => processObject(config, subConfig))
  return config
}

/***/ }),

/***/ 96172:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Mixin = __webpack_require__(46073)
const getMixin = (dependency) => {
  return dependency.mixin || dependency
}

const processDependency = (allDependencies, dependency) => {
  const index = allDependencies.indexOf(dependency)
  if (index !== -1) {
    return null
  }
  allDependencies.push(dependency)
  const dependencies = processDependencies(allDependencies, dependency.mixinDependencies)
  return [
    ...dependencies,
    dependency,
  ]
}

const processDependencies = (allDependencies, dependenciesTree) => {
  return dependenciesTree
    .flatMap((dependency) => processDependency(allDependencies, dependency))
    .filter((o) => o)
}

const buildBase = (base, dependenciesTree) => {
  const allDependencies = [...(base.allDependencies || [])]
  const dependencies = processDependencies(allDependencies, dependenciesTree)
  let currentClass = base
  for (const dependency of dependencies) {

    currentClass = dependency.fn(currentClass)
  }
  Object.assign(currentClass, {
    allDependencies,
    dependencies,
    dependenciesOwner: null,
  })

  return currentClass
}

const getDependencies = (dependencies = []) => {
  const baseType = mixer.Base
  if (!baseType) {
    return dependencies
  }

  return [baseType, ...dependencies]
}

const mixer = {
  base: null,
  proxy(...args) {
    const mixin = this.mixin(...args)
    const type = this.extends([mixin]);
    type.mixin = mixin;

    return new Proxy(type, {
      apply: (target, thisArg, args) => {
        //fallback for babel ?
        if (thisArg) {
          return target.apply(thisArg, args);
        }
        return fn.apply(null, args);
      }
    })
  },
  mixin(...args) {
    const fn = args.find((arg) => typeof arg === 'function')
    const mixinDependencies = getDependencies(args.find((arg) => Array.isArray(arg)))
    const base = mixer.extends(Mixin, mixinDependencies)

    const mixin = fn(base)

    Object.assign(mixin, {
      mixinDependencies,
      fn,
      base,
    })
    return mixin
  },
  extends(arg1, arg2) {
    let base
    let dependencies
    if (typeof arg1 === 'function') {
      base = arg1
      dependencies = getDependencies(arg2)
    } else {
      base = class { }
      dependencies = getDependencies(arg1)
    }
    return buildBase(base, dependencies)
  },
  is(object, mixinOrClass) {
    if (object.constructor === mixinOrClass) { return true }
    const dependencies = object.constructor?.allDependencies

    if (dependencies) {
      const hasMixin = dependencies.find((d) => d === getMixin(mixinOrClass))
      if (hasMixin) {
        return true
      }
    }
    if (typeof mixinOrClass !== 'function') {
      return false
    }

    if (object instanceof mixinOrClass) {
      return true
    }

    return false
  },
}

module.exports = mixer

/***/ }),

/***/ 3764:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)

module.exports = mixer.mixin([], (baseClass) => {

  return class Abstractable extends baseClass {
    constructor(...args) {
      super(...args)
      if (this.constructor.definition.abstract) {
        throw new Error(`Cannot instanciate abstract type ${this.constructor.definition.name}`)
      }
    }
  }
})

/***/ }),

/***/ 59222:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const symbol = Symbol("BindedFunctions")
const Destroyable = __webpack_require__(79330)

module.exports = mixer.mixin([Destroyable], (baseClass) => {

  return class Bindeable extends baseClass {
    constructor(...args) {
      super(...args)
      this[symbol] = []
    }

    b(fn) {
      const existing = this[symbol].find((bf) => {
        return bf.initialFunction == fn;
      })
      if (existing)
        return existing.fn
      const bindedFunction = fn.bind(this)
      this[symbol].push({
        initialFunction: fn,
        fn: bindedFunction
      });
      return bindedFunction
    }

  }
})

/***/ }),

/***/ 79330:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)

const Destroyable = mixer.mixin((base) => {
  return class Destroyable extends base {

    constructor(...args) {
      super(...args)
      Object.defineProperty(this, '@destroyed', {
        enumerable: false,
        writable: true,
        value: false
      })
    }

    onAlreadyDestroyed() {
      console.error(this)
      throw new Error('Already destroyed')
    }

    destroy() {
      if (this['@destroyed']) {
        this.onAlreadyDestroyed()
      }
      this['@destroyed'] = true
    }
  }
})


module.exports = Destroyable

/***/ }),

/***/ 48774:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)

module.exports = mixer.mixin((baseClass) => {

  return class Equalable extends baseClass {
    static equals(value1, value2) {
      throw new Error('Compare not implemented')
    }
  }
})

/***/ }),

/***/ 16761:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Destroyable = __webpack_require__(79330)
const events = Symbol("events")
const otherEvents = Symbol("otherEvents")

class Listener {
  constructor(options) {
    Object.assign(this, options)
  }

  remove() {
    this.eventable.off(this)
  }
}


const Eventable = mixer.mixin([Destroyable], (baseClass) => {
  return class Eventable extends baseClass {
    constructor(...args) {
      super(...args)
      Object.defineProperties(this, {
        [events]: {
          enumerable: false,
          writable: true,
          value: []
        },
        [otherEvents]: {
          enumerable: false,
          writable: true,
          value: []
        }
      })
    }

    on(...args) {
      if (args.length === 3) {
        const [source, eventName, callback] = args
        const listener = source.on(eventName, callback)
        this[otherEvents].push(listener)
        return listener
      } else {
        const [eventName, callback] = args
        const listener = new Listener({
          eventable: this,
          eventName,
          callback,
        })
        let event = this[events][eventName]
        if (!event) {
          event = {
            listeners: []
          }
          this[events][eventName] = event
        }

        this[events][eventName].listeners.push(listener)

        return listener
      }
    }

    async emit(eventName, args = [], options = {}) {
      let event = this[events][eventName]
      if (!event) {
        event = {
          listeners: [],
        }
        this[events][eventName] = event
      }
      const listeners = [...event.listeners]
      await Promise.all(listeners.map((l) => l.callback(...args)))
    }

    off(eventName, callback) {
      if (eventName instanceof Listener) {
        callback = eventName.callback
        eventName = eventName.eventName
      }

      const event = this[events][eventName]
      if (!event) { return }
      const index = event.listeners.findIndex((listener) => listener.eventName === eventName && listener.callback === callback)

      if (index === -1) { return }
      event.listeners.splice(index, 1)
    }

    destroy() {
      super.destroy()
      Object.values(events)
        .forEach((event) => {
          event.listeners.forEach((listener) => {
            listener.remove()
          })
        })

      this[otherEvents].forEach((listener) => listener.remove())
      this[otherEvents] = null
      this.emit('destroyed')
    }
  }
})

Object.assign(Eventable, {
  events,
  otherEvents
})

module.exports = Eventable

/***/ }),

/***/ 61760:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Tree = __webpack_require__(9948)
const Mixin = __webpack_require__(46073)
module.exports = class Properties extends Tree {
  constructor(owner) {
    super()
    this.owner = owner
    owner.definition.parents
      .filter((p) => p.properties)
      .forEach((parent) => {
        this.push(parent.properties)
      })
  }

  call(...args) {
    super.call(...args)
    return this.owner
  }

  shouldIterateTree(it, tree) {
    const shouldIterateTree = !(this.owner.prototype instanceof Mixin) || !it.from || !(tree instanceof Properties)
    return shouldIterateTree
  }

  push(...args) {
    args.forEach((arg) => {
      if (arg instanceof Properties) {
        super.push(arg)
        return
      }
      if (typeof arg === 'object') {
        Object.entries(arg)
          .forEach(([name, property]) => {
            if (typeof property !== 'object') {
              property = {
                type: property,
              }
            }
            property.name = name
            super.push(property)
            this.owner.sanitizeProperty(property)
          })
        return
      }
      throw new Error(`Property type not recognized`)
    })
  }
}

/***/ }),

/***/ 85020:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172);
const Destroyable = __webpack_require__(79330)
const Eventable = __webpack_require__(16761)
const Properties = __webpack_require__(61760)

const mixin = mixer.mixin([Eventable, Destroyable], (base) => {
  return class Propertiable extends base {
    static sanitizeProperty(property) {

    }

    defineProperty(property) {
      Object.defineProperty(this, property.name, {
        get: function () {
          return this.values[property.name]
        },
        set: async function (newValue) {
          if (this['@destroyed']) { return }
          if (newValue === this[property.name]) { return }
          await this.setPropertyValue(property, newValue)
        },
        enumerable: true,
      })
    }

    static define(definition) {
      super.define(definition)
      this.properties = new Properties(this)
      return this
    }

    constructor(...args) {
      super(...args)
      const properties = this.constructor.properties
      if (properties) {
        try {
          properties.forEach((p) => this.defineProperty(p))
        } catch (err) {
          console.error(this, [...properties])
          throw err
        }
      }

      Object.defineProperty(this, 'values', { enumerable: false, writable: true, value: {} })
    }

    async set(values, options) {
      await Promise.all(Object.entries(values).map(async ([k, v]) => {
        const property = this.constructor.properties.find((p) => p.name === k)
        if (!property) { return }
        await this.setPropertyValue(property, v, options)
      }))
    }

    async propertyChanged(property, value, oldValue) {
      await Promise.all([
        this.emit('propertyChanged', [property, value, oldValue]),
        this.emit(`propertyChanged:${property.name}`, [value, oldValue])
      ])
    }

    async setPropertyValue(property, value, options) {
      if (!this.values) {
        this.values = {}
      }
      const oldValue = this.values[property.name]
      this.values[property.name] = value
      await this.propertyChanged(property, value, oldValue, options)
    }

    destroy() {
      // we set null before super.destroy as we want it to trigger the events
      this.constructor.properties.forEach((p) => {
        this[p.name] = null
      })
      super.destroy()
    }
  }
})

module.exports = mixin

/***/ }),

/***/ 36405:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

Error.stackTraceLimit = 50
const Base = __webpack_require__(38988)
const mixer = __webpack_require__(96172)

mixer.Base = Base




/***/ }),

/***/ 83083:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Bindeable = __webpack_require__(59222)
const Destroyable = __webpack_require__(79330)
const Eventable = __webpack_require__(16761)

module.exports = class IntermediateArray extends mixer.extends(Array, [Eventable, Destroyable, Bindeable]) {

  push(...args) {
    const result = super.push(...args)
    this.changed()
    return result
  }

  pop(...args) {
    const result = super.pop(...args)
    this.changed()
    return result
  }

  unshift(...args) {
    const result = super.unshift(...args)
    this.changed()
    return result
  }

  shift(...args) {
    const result = super.shift(...args)
    this.changed()
    return result
  }

  splice(...args) {
    const result = super.splice(...args)
    this.changed()
    return result
  }

  async changed() {
    await Promise.all([
      this.emit('changed'),
      this.emit('propertyChanged:length')
    ])
  }

  remove(object) {
    var result = this.tryRemove(object)
    if (!result)
      throw new RangeError()
  }

  tryRemove(object) {
    var index = this.indexOf(object);
    if (index != -1) {
      this.splice(index, 1)
      return true
    } else
      return false
  }

  has(object) {
    return this.indexOf(object) !== -1
  }

  onLinkUpdated() {
    const { source, fn, args } = this.linkOptions
    this.splice(0, this.length)
    const result = source[fn](...args)
    this.push(...result)
  }

  link(source, fn, ...args) {
    this.linkOptions = {
      source,
      fn,
      args,
    }
    this.on(source, 'changed', this.b(this.onLinkUpdated))
  }

  filterLink(fn) {
    const array = new IntermediateArray()
    array.link(this, 'filter', fn)
    const result = this.filter(fn)
    array.push(...result)
    return array
  }

  sortLink(fn) {
    const array = new IntermediateArray()
    array.link(this, 'sort', fn)
    const result = this.sort(fn)
    array.push(...result)
    return array
  }
}

/***/ }),

/***/ 66715:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const ExtensibleFunction = __webpack_require__(68324)
module.exports = class Event extends ExtensibleFunction {
  constructor(options = {}) {
    super((...args) => this.listen(...args))
    this.options = options
    this.listeners = []
  }

  listen(listener) {
    this.listeners.push(listener)
  }

  async trigger(...args) {
    let i = 0
    for (const listener of this.listeners) {
      await listener(...args)
    }
  }
}


/***/ }),

/***/ 68324:
/***/ (function(module) {

module.exports = class ExtensibleFunction extends Function {
  constructor(f) {
    return Object.setPrototypeOf(f, new.target.prototype);
  }
}

/***/ }),

/***/ 9948:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Mixin = __webpack_require__(46073)
const ExtensibleFunction = __webpack_require__(68324)

let id = 0
class Tree extends ExtensibleFunction {



  constructor(...values) {
    super((...args) => this.call(...args))
    this.content = values
  }
  call(...args) {
    this.push(...args)
    return this
  }
  push(...objs) {
    this.content.push(...objs)
  }

  get count() {
    let count = 0;
    for (let obj of this.content) {

      if (obj instanceof this.constructor) {
        count += obj.count
      } else {
        count++
      }
    }
    return count
  }

  forEach(fn) {
    for (var object of this) {
      fn(object);
    }
  }

  find(fn) {
    for (var obj of this) {
      if (fn(obj))
        return obj;
    }
    return null;
  }

  filter(fn) {
    var result = [];
    for (var obj of this) {
      if (fn(obj)) {
        result.push(obj);
      }
    }
    return result;
  }

  map(fn) {
    var result = [];
    for (var obj of this) {
      result.push(fn(obj));
    }
    return result;
  }

  reduce(fn, acc) {
    this.forEach((value) => {
      acc = fn(acc, value)
    })
    return acc
  }

  beautify() {
    return this.content.map((object) => {
      if (object instanceof this.constructor) {
        return object.beautify()
      }
      return object
    })
  }

  iterate(it) {
    if (it.index > this.content.length) {
      return {
        done: true
      }
    }
    let value = this.content[it.index]

    if (value instanceof this.constructor) {
      if (!this.shouldIterateTree(it, value)) {
        it.index++
        return this.iterate(it)
      }
      if (!it.it)
        it.it = value[Symbol.iterator](this)
      let result = it.it.next()
      if (result.done) {
        it.index++
        it.it = null
        return this.iterate(it)
      }
      value = result.value
    } else {
      it.index++
    }

    return {
      value,
      done: it.index > this.content.length
    }
  }

  shouldIterateTree(it, tree) {
    return true
  }

  [Symbol.iterator](from) {
    const it = {
      id: id++,
      index: 0,
      it: null,
      from
    }
    return {
      next: () => {
        return this.iterate(it)
      }
    }
  }

}

module.exports = Tree

/***/ }),

/***/ 49185:
/***/ (function(module) {

const chain = (array, fn, final) => {
  let index = 0
  const inner = () => {
    if (index === array.length) {
      return final()
    }

    const object = array[index++]
    return fn(object, inner)
  }
  return inner()
}

module.exports = {
  chain,
}

/***/ }),

/***/ 97204:
/***/ (function(module) {


const validateType = (accept, type) => {
  const [category, extension] = type.split('/')
  return accept.some((t) => {
    if (t === type) { return true }
    const [c, e] = t.split('/')
    if (e === '*' && c === category) { return true }

    return false
  })
}

module.exports = {
  validateType,
}

/***/ }),

/***/ 66221:
/***/ (function(module) {

const parse = (string) => {
  string = string.replace(/\s*(['"])?([a-z0-9A-Z_\.]+)(['"])?\s*:([^,\}]+)(,)?/g, '"$2": $4$5');
  return JSON.parse(string)
}


module.exports = {
  parse,
}

/***/ }),

/***/ 27274:
/***/ (function(module) {

const bound = (int, [min, max]) => {
  if (int < min) {
    return min
  }
  if (int > max) {
    return max
  }
  return int
}

module.exports = {
  bound,
}

/***/ }),

/***/ 82832:
/***/ (function(module) {

const set = (object, path, value) => {
  const split = path.split('.')
  const name = split.pop()
  for (const segment of split) {
    object = object[segment]
  }
  object[name] = value
}

/**
 * it should throw an error if the path is invalid
 * @param {*} object 
 * @param {*} path 
 * @returns 
 */
const get = (object, path) => {
  const split = path.split('.')
  for (const segment of split) {

    object = object[segment]
  }

  return object
}

const pathsCopy = (paths) => {
  const copy = Object.entries(paths)
    .reduce((acc, [k, v]) => {
      if (typeof v === 'object') {
        v = pathsCopy(v)
      }
      acc[k] = v
      return acc
    }, {})
  return copy
}

/**
 * 
 * @param {*} paths1 
 * @param {*} paths2 
 * @returns a - b 
 */
const pathsDiff = (paths1, paths2) => {
  const diff = Object.entries(paths1)
    .reduce((acc, [propertyName, value1]) => {
      let value2 = paths2[propertyName]
      if (!value2) {
        if (value1) {
          acc[propertyName] = value1
        }
      } else {
        if (value2 === true) {
          value2 = {}
        }
        if (value1 === true) {
          value1 = {}
        }
        const subDiff = pathsDiff(value1, value2)
        if (Object.keys(subDiff).length) {
          acc[propertyName] = subDiff
        }
      }
      return acc
    }, {})

  return diff
}


module.exports = {
  set,
  get,
  pathsDiff,
  pathsCopy,
}

/***/ }),

/***/ 52208:
/***/ (function(module) {


const forEach = (type, fn) => {
  while (type.prototype.__proto__) {
    const stop = fn(type)
    if (stop) {
      return
    }
    type = type.prototype.__proto__.constructor
  }
}

const get = (type) => {
  const result = []
  forEach(type, (t) => {
    result.push(t)
  })
  return result
}

const getParent = (type) => {
  return type.prototype.__proto__.constructor
}

const filter = (type, fn) => {
  const result = []
  forEach(type, (parent) => {
    const match = fn(parent)
    if (match) {
      result.push(parent)
    }
  })
  return result
}

const find = (type, fn) => {
  let result = null
  forEach(type, (parent) => {
    const match = fn(parent)
    if (match) {
      result = parent
      return true
    }
  })
  return result
}

const getCommonAncestor = (...types) => {
  let target = types[0]
  while (target) {
    let good = true
    for (const type of types) {
      if (type !== target && !(type.prototype instanceof target) && !(target.prototype instanceof type)) {
        good = false
      }
    }
    if (good) {
      return target
    }
    target = target.prototype.__proto__.constructor
  }
  return null
}

module.exports = {
  get,
  forEach,
  filter,
  find,
  getParent,
  getCommonAncestor,
}

/***/ }),

/***/ 71513:
/***/ (function(module) {

const makeId = (length = 12) => {
  let result = '';
  const characters = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
  const charactersLength = characters.length;
  let counter = 0;
  while (counter < length) {
    result += characters.charAt(Math.floor(Math.random() * charactersLength));
    counter += 1;
  }
  return result
}


module.exports = {
  makeId,
}

/***/ }),

/***/ 81084:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Bindeable = __webpack_require__(59222)
const Propertiable = __webpack_require__(85020)
const Listening = __webpack_require__(60864)
const mixer = __webpack_require__(96172)
const Scope = __webpack_require__(29777)
const { base } = __webpack_require__(34543)
const Destroyable = __webpack_require__(79330)
const BindingFunction = __webpack_require__(11374)

const Base = mixer.mixin([Destroyable, Bindeable, Propertiable, Listening], (base) => {
  return class Base extends base {

    static variables(variables) {
      this._variables = {
        ...(this._variables || {}),
        ...variables
      }
      return this
    }

    constructor() {
      super()
      this.isInitialized = false
      this.bindings = []
    }

    getVariables() {
      return this.variables
    }

    async bind(name, value) {
      const variables = this.getVariables()
      const binding = new BindingFunction(value, variables, (value) => {
        this[name] = value
      })
      await binding.update()
      this.bindings.push(binding)
    }

    async initialize() {
      if (this.isInitialized) {
        throw new Error('Already initialized')
      }
      await this.onInit()
      this.isInitialized = true
    }

    async onInit() { }

    propertyChanged(...args) {
      if (!this.isInitialized) {
        return
      }

      return super.propertyChanged(...args)
    }

    destroy() {
      this.bindings.forEach((b) => b.destroy())
      super.destroy()
    }

  }

})
  .define()


module.exports = mixer.mixin([Base, ...base], (base) => class extends base { })
  .define()
  .properties({
    isInitialized: { type: 'any' }
  })


/***/ }),

/***/ 58497:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Base = __webpack_require__(81084)
const { workers } = __webpack_require__(98864)
const Scope = __webpack_require__(29777)
class temp extends HTMLElement {

}

module.exports = class Component extends mixer.extends(temp, [Base]) {
  static define(definition) {
    if (definition?.name) {
      customElements.define(definition.name, this)
    }
    return super.define(definition)
  }

  constructor() {
    super()
    this.scope = null
  }

  attach(scope) {
    //this.upScope = scope
    this.scope = scope.child({
      source: this,
      variables: this.constructor._variables
    })
    return this
  }

  async initialize() {
    this.scope.type = this.constructor
    await super.initialize()
    this.initialContent = [...this.childNodes]
    await this.initializeTemplate()
    Promise.resolve(this.onReady())
      .catch((err) => {
        console.error(err)
      })
  }

  onReady() { }

  async initializeTemplate() {
    const definition = this.constructor.definitions.find((d) => d.template)
    if (definition) {
      this.innerHTML = definition.template
      await this.scope.renderContent(this)
    }

    await this.scope.renderSlots(this.initialContent, this.scope.parent)
  }

  event(name, arg) {
    const event = new CustomEvent(name, {
      bubbles: false,
    })
    Object.assign(event, arg)
    return this.dispatchEvent(event)
  }
}

/***/ }),

/***/ 96148:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(70955)
const BindingFunction = __webpack_require__(11374)

module.exports = class If extends Virtual {
  async onInit() {
    await this.bind('condition', this.initialValue)
    this.on('propertyChanged:condition', this.b(this.onConditionChanged))
    this.parent = this.el.parentElement
    this.position = [...this.parent.childNodes].indexOf(this.el)
    await this.onConditionChanged()
  }

  async onConditionChanged() {
    const condition = await this.condition
    if (condition) {
      // already in the dom
      await this.scope.initialize(this.el)
      if (this.el.parentElement) { return }
      this.parent.insertBefore(this.el, this.parent.childNodes[this.position])
    } else {
      this.el.remove()
    }
  }

  async preventInitialize() {
    const condition = await this.condition
    return !condition
  }
}
  .define({
    name: 'if'
  })
  .properties({
    condition: 'any',
  })


/***/ }),

/***/ 29777:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const { workers } = __webpack_require__(98864)
const Destroyable = __webpack_require__(79330)
const Eventable = __webpack_require__(16761)
const Vars = __webpack_require__(59979)
const { moveAttributes } = __webpack_require__(54829)
const BindingFunction = __webpack_require__(11374)
const { getElementFromTemplate } = __webpack_require__(94533)

workers.push({
  process(scope, { node }) {
    if (node.nodeType !== Node.ELEMENT_NODE) { return }
    if (!node.hasAttribute('slot')) { return }
    const slotName = node.getAttribute('slot') || 'main'
    node.removeAttribute('slot')
    if (scope.slots[slotName]) {
      throw new Error(`Slot ${slotName} already exists`)
    }
    scope.slots[slotName] = {
      node,
      children: [...node.childNodes]
    }
  }
})


const processors = [
  async (scope, node) => {
    if (node.nodeName !== 'SELF') {
      return false
    }

    // move self content
    const parent = node.parentElement
    node.remove()
    while (node.childNodes.length) {
      parent.appendChild(node.childNodes[0])
    }

    // move attributes
    const component = scope.variables.this
    moveAttributes(node, component)
    await scope.process(component)

    // process
    await scope.renderContent(parent)
    return true
  },
  async (scope, node) => {
    if (node.nodeName !== 'SUPER') {
      return false
    }

    // process super attributes
    const component = scope.variables.this
    moveAttributes(node, component)
    await scope.process(component)

    // process super template
    const nextDefinition = scope.type.definitions.filter((d) => d.template)[1]
    if (!nextDefinition) {
      throw new Error(`Cannot invoke 'super' as there is no parent with a template`)
    }
    const initialContent = [...node.childNodes]

    // create a temp div and set inner-html, and use child nodes to replace super node
    const container = document.createElement('div')
    container.innerHTML = nextDefinition.template
    const superNodes = [...container.childNodes]
    node.replaceWith(...superNodes)

    // render new child nodes
    const childScope = scope.child()
    childScope.type = nextDefinition.owner
    childScope.slots = {}
    for (const node of superNodes) {
      await childScope.render(node)
    }

    // process super content
    await childScope.renderSlots(initialContent)
    return true
  },
  async (scope, node) => {
    if (node.nodeName !== 'SUPER-SLOT') {
      return false
    }

    node.replaceWith(...scope.currentSlot.children)
    return true
  },
  async (scope, node) => {
    if (node.nodeName !== 'VARS') {
      return false
    }
    const vars = scope.variables.$
    for (const attribute of node.attributes) {
      const propertyName = attribute.name.replace(':', '')
      if (!vars.hasOwnProperty(propertyName)) {
        vars.defineProperty({
          name: propertyName
        })
      }


      const bindingFunction = new BindingFunction(attribute.value, scope.variables, (value) => {
        vars[propertyName] = value
      })

      await bindingFunction.update()

      vars.bindingFunctions.push(bindingFunction)
    }
    node.remove()
    return true
  },
]

const findFirstNode = (nodes) => {
  for (node of nodes) {
    if (node.nodeType === Node.TEXT_NODE && node.textContent.trim() === '') {
      continue
    }
    return node
  }
  return null
}

module.exports = class Scope extends mixer.extends([Destroyable, Eventable]) {
  constructor({ source, parent, variables }) {
    super()
    this.parent = parent
    this.states = []
    this.variables = {
      ...(this.parent?.variables || {}),
      ...(variables || {}),
      scope: this,
    }
    if (source) {
      this.variables.this = source
    }
    this.variables.$ = new Vars()
    this.slots = {}
    this.childs = []
  }

  child(options = {}) {
    const child = new Scope({
      parent: this,
      ...options
    })
    this.childs.push(child)
    return child
  }

  async renderSlots(nodes, renderScope) {
    if (!nodes.length) { return }
    if (!renderScope) {
      renderScope = this
    }
    const renderSlot = async (slotName, nodes) => {
      const slot = this.slots[slotName]
      if (!slot) {
        throw new Error(`Slot ${slotName} not found`)
      }
      this.currentSlot = slot

      const { node } = slot
      node.replaceChildren(...nodes)
      for (const node of nodes) {
        await renderScope.render(node)
      }

      // it means it wasn't called in the initialize of the component
      if (renderScope === this) {
        this.parent.slots[slotName] = {
          node,
          children: [...node.childNodes],
        }
      }

    }
    const firstNode = findFirstNode(nodes)
    if (!firstNode) {
      return
    }
    if (firstNode.nodeName !== 'SLOT') {
      await renderSlot('main', nodes)
    } else {
      const slots = [...nodes].filter((n) => n.nodeType === Node.ELEMENT_NODE)
      for (const slot of slots) {
        const slotName = slot.getAttribute('name') || 'main'
        slot.removeAttribute('name')
        await renderSlot(slotName, [...slot.childNodes])
      }
    }

  }

  async process(node) {

    const state = { node, scope: this }
    if (!node.hederaStates) {
      node.hederaStates = []
    }
    node.hederaStates.push(state)
    this.states.push(state)
    this.variables.node = node
    for (const worker of workers) {
      await worker.process(this, state)
    }
    await this.initializeVirtuals(state)
    this.variables.node = null
    return state
  }

  async renderTemplate(template, variables) {
    const node = getElementFromTemplate(template)
    return this.render(node, variables)
  }

  getState(node) {
    const state = this.states.find((state) => state.node === node)
    if (state) { return state }

    return this.parent.getState(node)
  }

  async renderContent(node) {
    for (const n of node.childNodes) {
      await this.render(n)
    }
  }

  async render(node, variables = {}) {
    Object.assign(this.variables, variables)
    for (const processor of processors) {
      if (await processor(this, node)) {
        return null
      }
    }
    if (node.rendered) { return node }
    node.rendered = true
    if (node.attach) {
      node = await node.attach(this)
    }
    if (!node) { return null }

    const state = await this.process(node)
    await this.initialize(state)
    return node
  }

  async initialize(state) {
    if (!state.node) {
      state = this.states.find(({ node }) => node === state)
    }
    const { node } = state
    if (node.isInitialized) {
      //console.warn('Already initialized', node)
      return
    }
    if (state.virtuals) {
      for (const virtual of state.virtuals) {
        if (await virtual.preventInitialize()) {
          return
        }
      }
    }
    if (node.initialize) {
      await node.initialize()
    } else {
      await this.renderContent(node)
      node.isInitialized = true
    }
  }

  async initializeVirtuals(state) {
    if (!state.virtuals) { return }

    for (const virtual of state.virtuals) {
      if (!virtual.isInitialized) {
        await virtual.initialize()
      }
    }
  }

  release(node) {
    const index = this.states.findIndex((state) => state.node === node)
    if (index === -1) {
      throw new Error()
    }
    const state = this.states[index]
    workers.forEach((w) => w.destroy && w.destroy(state))
    this.states.splice(index, 1)
  }

  destroy() {
    super.destroy()
    while (this.childs.length) {
      this.childs[0].destroy()
    }
    while (this.states.length) {
      this.release(this.states[0].node)
    }
    if (this.parent) {
      this.parent.childs.splice(this.parent.childs.indexOf(this), 1)
    }

    this.parent = null
    this.childs = null
    this.nodes = null
  }
}


/***/ }),

/***/ 25033:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Propertiable = __webpack_require__(85020)
const mixer = __webpack_require__(96172)
const Bindeable = __webpack_require__(59222)

module.exports = class Service extends mixer.extends([Propertiable, Bindeable]) {
  constructor(...args) {
    super(...args)
    if (this.instance) { throw new Error('Already instanciated') }
    this.constructor.instance = this
  }
}
  .define()

/***/ }),

/***/ 59979:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const ViewModel = __webpack_require__(81590)

module.exports = class Vars extends ViewModel {
  constructor() {
    super()
    this.bindingFunctions = []
  }
  destroy() {
    this.bindingFunctions.forEach((b) => b.destroy())
    super.destroy()
  }
}
  .define()

/***/ }),

/***/ 81590:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Propertiable = __webpack_require__(85020)
const mixer = __webpack_require__(96172)

module.exports = class ViewModel extends mixer.extends([Propertiable]) {
  constructor(values = {}) {
    super()
    Object.assign(this, values)
  }
}

/***/ }),

/***/ 70955:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { virtuals } = __webpack_require__(29583)
const mixer = __webpack_require__(96172)
const Base = __webpack_require__(81084)

module.exports = class Virtual extends mixer.extends([Base]) {

  static define(definition) {
    if (definition.name) {
      virtuals.push(this)
    }
    return super.define(definition)
  }

  constructor(scope, variables, el, initialValue) {
    super()
    this.scope = scope
    this.variables = variables
    this.el = el
    this.initialValue = initialValue
  }

  getVariables() {
    return this.variables
  }

  preventInitialize() {
    return false
  }
}


/***/ }),

/***/ 29583:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { workers } = __webpack_require__(98864)
const { dashToCamel } = __webpack_require__(54829)

const virtuals = []

workers.push({
  process(scope, state) {
    const { node } = state
    if (!node.attributes) { return }

    state.virtuals = [];
    ([...node.attributes])
      .filter((attr) => attr.name.startsWith(':v-'))
      .forEach((attr) => {
        const [virtualName, ...args] = dashToCamel(attr.name.replace(':v-', '')).split('.')
        const virtualClass = virtuals.find((v) => v.definition.name === virtualName)
        if (!virtualClass) { return }
        const params = args.reduce((acc, arg) => {
          acc[arg] = true
          return acc
        }, {})
        const virtual = new virtualClass(scope, {...scope.variables}, node, attr.value, params)
        state.virtuals.push(virtual)
        node.removeAttribute(attr.name)
      })
  },
  destroy(state) {
    if (!state.virtuals) { return }
    state.virtuals.forEach((virtual) => {
      virtual.destroy()
    })
    state.virtuals = null
  }
})

module.exports = {
  virtuals,
}

/***/ }),

/***/ 96802:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {


const worker = __webpack_require__(72265)
const { workers } = __webpack_require__(98864)

workers.push(worker)

/***/ }),

/***/ 72265:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { set } = __webpack_require__(82832)

const as = (scope, node) => {
  const as = node.getAttribute('as')
  if (!as) { return }
  //console.log('as',thisArg, as, node)
  node.removeAttribute('as')
  set(scope.variables.this, as, node)
}

const attach = (scope, node) => {
  if (!node.getAttribute('attach') || !node.id) { return }

  set(scope.variables.this, node.id, node)
}

const _in = (scope, node) => {
  const attr = node.getAttribute('in')
  if (!attr) { return }
  const thisArg = scope.variables.this
  if (!thisArg[attr]) {
    thisArg[attr] = []
  }
  thisArg[attr].push(node)
}

module.exports = {
  process(scope, { node }) {
    if (node.nodeType !== Node.ELEMENT_NODE) { return }

    as(scope, node)
    attach(scope, node)
    _in(scope, node)
  }
}

/***/ }),

/***/ 64751:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const template = __webpack_require__(7394)
const Component = __webpack_require__(58497)
__webpack_require__(39513)

module.exports = class ContextMenu extends Component {
  onInit() {
    this.listen(this.parentElement, 'contextmenu', this.b(this.onContextMenu))
  }

  onContextMenu(e) {
    if (this.contains(e.target)) { return }
    this.left = e.offsetX
    this.top = e.offsetY
    e.preventDefault()
    this.open = true
    this.focus()
  }
}
  .define({
    name: 'context-menu',
    template,
  })
  .properties({
    open: 'any',
    left: 'any',
    top: 'any',
  })

/***/ }),

/***/ 24781:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const template = __webpack_require__(22474)
const Component = __webpack_require__(58497)
__webpack_require__(20220)

module.exports = class ContextMenu extends Component {
  onInit() {
    this.contextMenu = this.closest('context-menu')
  }

  onSelected(){
    console.log('on selected')
    this.event('selected')
    this.contextMenu.open = false
  }
}
  .define({
    name: 'context-menu-item',
    template,
  })

/***/ }),

/***/ 55336:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(64751)
__webpack_require__(24781)

/***/ }),

/***/ 38087:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

const { workers } = __webpack_require__(98864)
const { createFunction, dashToCamel } = __webpack_require__(54829)

const prefix = ':on-'

const suffixes = {
  stop: {
    onEvent(event) {
      event.stopPropagation()
    }
  },
  prevent: {
    onEvent(event) {
      console.log('prevent', event)
      event.preventDefault()
    }
  },
  'no-capture': {
    onListen(options) {
      options.capture = false
    }
  },
  capture: {
    onListen(options) {
      options.capture = true
    }
  }
}

workers.push({
  process(scope, state) {
    const { node } = state
    if (!node.attributes) { return }
    state.listeners = [];
    ([...node.attributes])
      .filter((attr) => attr.name.startsWith(prefix))
      .forEach((attr) => {
        const options = {}
        let [eventName, ...suffixeNames] = attr.name.replace(prefix, '').split('.')
        suffixeNames.forEach((s) => {
          const suffix = suffixes[s]
          suffix.onListen && suffix.onListen(options)
        })
        eventName = dashToCamel(eventName)
        const vars = { ...scope.variables }
        delete vars.this
        const callback = (event) => {
          suffixeNames.forEach((s) => {
            const suffix = suffixes[s]
            suffix.onEvent && suffix.onEvent(event)
          })
          suffixeNames.forEach((s) => {
            const suffix = suffixes[s]
            suffix.onEvent && suffix.onEvent(event)
          })
          const eventVars = { ...vars, event }

          const fn = createFunction(attr.nodeValue, eventVars)
          fn.call(scope.variables.this)
        }
        node.addEventListener(eventName, callback, options)
        node.removeAttribute(attr.name)
        state.listeners.push({ eventName, callback })
      })
  },
  destroy(state) {
    if (!state.listeners) { return }
    state.listeners.forEach(({ eventName, callback }) => {
      state.node.removeEventListener(eventName, callback)
    })
  }
})


/***/ }),

/***/ 73078:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Propertiable = __webpack_require__(85020)

module.exports = class It extends mixer.extends([Propertiable]) {
  constructor(values) {
    super()
    Object.assign(this, values)
  }

  destroy() {
    this.scope.destroy()
    this.element.remove()
  }
}
  .define()
  .properties({
    index: 'number',
  })


/***/ }),

/***/ 4958:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Bindeable = __webpack_require__(59222)

const Array = __webpack_require__(83083)
const Eventable = __webpack_require__(16761)
const Destroyable = __webpack_require__(79330)

let id = 0
module.exports = class ObservableArrayHandler extends mixer.extends([Bindeable, Eventable]) {
  static handle(source) {
    return source instanceof Array
  }

  constructor(repeater) {
    super()
    this.id = id++
    this.repeater = repeater
    this.source = this.repeater.source
    this.on(this.source, 'changed', this.b(this.onSourceChanged))
    this.timeout = null
    this.modifs = []
  }


  insertIt(itToInsert) {
    const { index, element } = itToInsert
    const parent = this.repeater.el
    if (element === parent.children[index]) { return }
    if (index >= parent.children.length) {
      parent.appendChild(element)
    } else {
      parent.insertBefore(element, parent.children[index])
    }
  }

  onSourceChanged() {
    if (this.timeout) {
      clearTimeout(this.timeout)
    }
    this.timeout = setTimeout(() => this.updateIterations())
  }

  async updateIterations() {
    if (this['@destroyed']) { return }
    const processedIts = []
    for (const object of this.source) {
      const index = this.source.indexOf(object)
      let it = this.repeater.iterations.find((it) => it.object === object)
      if (it) {
        it.index = index
        this.insertIt(it)
      } else {
        it = this.repeater.iteration(object, index)
        this.insertIt(it)
        it.element = await it.scope.render(it.element)
      }
      processedIts.push(it)
    }

    const its = [...this.repeater.iterations]
    for (const it of its) {
      const index = processedIts.indexOf(it)
      if (index === -1) {
        it.destroy()
        const i = this.repeater.iterations.indexOf(it)
        this.repeater.iterations.splice(i, 1)
      }
    }
  }

}



/***/ }),

/***/ 87606:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

module.exports = [
  __webpack_require__(4958)
]

/***/ }),

/***/ 61044:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(70955)
const It = __webpack_require__(73078)
const { getElementFromTemplate, getElementFromObject } = __webpack_require__(94533)
const handlers = __webpack_require__(87606)

module.exports = class For extends Virtual {
  async onInit() {
    this.iterations = null
    this.on('propertyChanged:source', this.b(this.onSourceChanged))
    const value = this.initialValue
    const [name, remaining] = value.split(/ of /)
    const [source, template] = remaining.split(/ with /)
    await this.bind('name', `'${name}'`)
    await this.bind('source', source)
    if (template) {
      await this.bind('template', template)
    } else {
      this.template = [...this.el.childNodes].find((child) => child.nodeType === Node.ELEMENT_NODE)
      this.el.innerHTML = ''
    }
    await this.onSourceChanged()
  }

  iteration(object, index) {
    const scope = this.scope.child()
    scope.variables[this.name] = object
    scope.variables.index = index
    let element
    if (typeof this.template === 'function') {
      const result = this.template(object, scope)
      if (result instanceof HTMLTemplateElement) {
        element = getElementFromTemplate(result)
      } else {
        element = result
      }
    } else {
      element = getElementFromObject(this.template)
    }

    const it = new It({ index, object, element, scope })


    this.iterations.push(it)
    return it
  }

  reset() {
    if (this.handler) {
      this.handler.destroy()
      this.handler = null
    }
    if (this.iterations) {
      this.iterations.forEach((it) => {
        it.destroy()
      })
    }
    this.iterations = []
  }

  async onSourceChanged() {
    this.reset()
    if (!this.source) { return }

    const handler = handlers.find((handler) => handler.handle(this.source))
    if (handler) {
      this.handler = new handler(this)
    } else {
      this.handler = null
    }

    let i = 0
    while (i < this.source.length) {
      await new Promise(async(resolve) => {
        setTimeout(async () => {
          const it = this.iteration(this.source[i], i)
          this.el.appendChild(it.element)
          it.element = await it.scope.render(it.element)
          i++
          resolve()
        }, 0)
      })
    }
  }

  preventInitialize() {
    return true
  }

  /**
   * no need to destroy iterations as their scopes will be destroyed as the current local scope will be destroyed
   */
  destroy() {
    if (this.handler) {
      this.handler.destroy()
    }
    super.destroy()
  }
}
  .define({
    name: 'for'
  })
  .properties({
    source: 'any',
    name: 'any',
  })


/***/ }),

/***/ 98864:
/***/ (function(module) {

module.exports = {
  components: {},
  workers: []
}

/***/ }),

/***/ 23498:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const template = __webpack_require__(92395)
const Component = __webpack_require__(58497)
const Array = __webpack_require__(83083)
const Panel = __webpack_require__(41407)
const { getElementFromTemplate } = __webpack_require__(94533)
const TabPanel = __webpack_require__(64960)
const { getHoverState } = __webpack_require__(81638)

__webpack_require__(67524)

module.exports = class EditableGrid extends Component {
  constructor() {
    super()
    this.maxDeepth = 2
    this.size = [3, 2]
  }

  onInit() {
    this.column = new Array()
    this.addRow(this.column, new Array())
  }

  onReady() {
    for (const element of this.initSlot.children) {
      this.add(element)
    }
  }

  findFirstPanel(array) {
    if (!array) {
      array = this.column
    }
    for (const object of array) {
      if (object instanceof Panel) {
        return object
      }
      const panel = this.findFirstPanel(object)
      if (panel) {
        return panel
      }
    }
    return null
  }

  addRowItem(row, item, position) {
    if (position === undefined) {
      position = row.length
    }
    item.row = row
    item.grid = this
    row.splice(position, 0, item)
    return item
  }

  addRow(column, row, position) {
    if (position === undefined) {
      position = column.length
    }
    column.splice(position, 0, row)
    row.column = column
    return row
  }

  async add(object) {
    if (object instanceof Panel) {
      object.grid = this
      this.column[0].push(object)
      return
    }
    const objectInfos = await this.templateObject(object)
    if (!this.focusPanel) {
      const panel = await this.createTabPanel(objectInfos)
      const row = this.column[0]
      this.addRowItem(row, panel, row.length)
      this.focusPanel = panel
    } else {
      this.focusPanel.objects.push(objectInfos)
      this.focusPanel.currentObject = objectInfos
    }
  }

  repositionColumn(column) {
    if (column.length !== 1 || !column.row) { return }

    const columnIndex = column.row.indexOf(column)

    column.row.splice(columnIndex, 1)
    for (const object of column[0].reverse()) {
      this.addRowItem(column.row, object, columnIndex)
    }
  }

  async createTabPanel(...args) {
    const panel = new TabPanel(...args)
    panel.grid = this
    return this.scope.render(panel)
  }

  async cell(object, scope) {
    if (object instanceof Panel) {
      return object
    }
    const child = scope.child()
    let element = getElementFromTemplate(this.columnTemplate)
    child.variables.column = object
    element = await child.render(element)
    return element
  }


  removeRowIfEmpty(row) {
    if (row.length || (!row.column.row && row.column.length === 1)) {
      //this.repositionRow(row)
      return
    }
    row.column.remove(row)
    this.removeColumnIfEmpty(row.column)
  }

  removeColumnIfEmpty(column) {
    if (column.length || !column.row) {
      this.repositionColumn(column)
      return false
    }
    column.row.remove(column)
    this.removeRowIfEmpty(column.row)
    return true
  }

  async templateObject(data) {
    const template = this.templates.find((t) => data instanceof t.for)
    let element = getElementFromTemplate(template)
    const child = this.scope.parent.child()
    child.variables[template.as || 'data'] = data
    element = await child.render(element)
    return {
      data,
      element,
      scope: child,
    }
  }

  onRowDragOver(row, node, e) {
    if (row.column.length === 1) { return }
    e.preventDefault()
    this.hoverRow = node
    this.hoverMode = getHoverState(node, e)
  }

  onRowDrop(row) {

  }

  dragEnd(){
    this.dragInfos = null
    this.hoverMode = null
    this.hoverMode = null
  }
}
  .define({
    name: 'editable-grid',
    template,
  })
  .variables({
    Panel,
  })
  .properties({
    dragInfos: 'any',
    focusPanel: 'any',
    hoverRow: 'any',
    hoverMode: 'any',
  })

/***/ }),

/***/ 41407:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const template = __webpack_require__(13970)
const Component = __webpack_require__(58497)
__webpack_require__(69734)



module.exports = class Panel extends Component {

}
  .define({
    name: 'grid-panel',
    template,
  })

/***/ }),

/***/ 64960:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const template = __webpack_require__(80982)
const Array = __webpack_require__(83083)
const Panel = __webpack_require__(41407)
const { getHoverState } = __webpack_require__(81638)
__webpack_require__(93417)




module.exports = class TabPanel extends Panel {

  constructor(...values) {
    super()
    this.objects = new Array(...values)
    this.currentObject = values[0]
  }

  drag(object) {
    this.grid.dragInfos = {
      panel: this,
      object,
    }
  }

  dragEnd(){
    this.grid.dragEnd()
  }

  removeObject(object) {
    this.objects.remove(object)
    object.scope.destroy(object.element)
    if (!this.removeIfEmpty()) {
      this.currentObject = this.objects[0]
    }
  }

  removeIfEmpty() {
    if (this.objects.length) {
      return false
    }
    this.row.remove(this)
    this.grid.removeRowIfEmpty(this.row)

    if (this.grid.focusPanel === this) {
      this.grid.focusPanel = null
    }
    this.destroy()
    return true
  }

  async onDrop() {
    if (!this.grid.dragInfos) { return }
    if (!this.hover) { return }

    const { panel, object } = this.grid.dragInfos
    panel.objects.remove(object)

    const panelPosition = this.row.indexOf(this)

    if (['left', 'right'].indexOf(this.hover) !== -1) {
      const newPanel = await this.grid.createTabPanel(object)
      const position = panelPosition + (this.hover === 'right' ? 1 : 0)
      console.log(this.hover, position)
      this.grid.addRowItem(this.row, newPanel, position)
    } else if (['top', 'bottom'].indexOf(this.hover) !== -1) {
      const newPanel = await this.grid.createTabPanel(object)
      if (this.row.length === 1) {
        const rowPosition = this.row.column.indexOf(this.row)
        const position = rowPosition + (this.hover === 'bottom' ? 1 : 0)
        const column = this.row.column
        const row = this.grid.addRow(column, new Array(), position)
        this.grid.addRowItem(row, newPanel)
      } else {
        const rootRow = this.row
        rootRow.remove(this)
        const column = new Array()
        let panels = [newPanel, this]
        if (this.hover === 'bottom') {
          panels = panels.reverse()
        }
        for (const panel of panels) {
          panel.grid = this.grid
          const row = this.grid.addRow(column, new Array())
          this.grid.addRowItem(row, panel)
        }

        this.grid.addRowItem(rootRow, column, panelPosition)
      }
    } else if (this.hover === 'full') {
      this.objects.push(object)
    }

    if (this !== panel) {
      this.removeIfEmpty()
    }

    if(!panel.removeIfEmpty()){
      panel.currentObject = panel.objects[0]
    }
    
    this.hover = null
  }

  onDragOver(node, e) {
    if (!this.grid.dragInfos) { return }
    const { panel, object } = this.grid.dragInfos
    if (this === panel && panel.objects.length === 1) {
      panel.hover = null
      return
    }
    e.preventDefault()
    this.hover = getHoverState(node, e)
  }

  destroy() {
    super.destroy()
    this.objects.forEach(({ scope }) => scope.destroy())
  }
}
  .define({
    name: 'grid-tab-panel',
    template,
  })
  .properties({
    currentObject: 'any',
    hover: 'any',
  })

/***/ }),

/***/ 10335:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(23498)
__webpack_require__(41407)
__webpack_require__(64960)

/***/ }),

/***/ 81638:
/***/ (function(module) {

const getHoverState = (node, e) => {
  const percent = 5
  if (e.offsetX < node.offsetWidth / percent) {
    return 'left'
  } else if (e.offsetX > node.offsetWidth - node.offsetWidth / percent) {
    return 'right'
  } else if (e.offsetY < node.offsetHeight / percent) {
    return 'top'
  } else if (e.offsetY > node.offsetHeight - node.offsetHeight / percent) {
    return 'bottom'
  } else {
    return 'full'
  }
}

module.exports = {
  getHoverState
}

/***/ }),

/***/ 54280:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(70955)
__webpack_require__(48715)
__webpack_require__(96802)
__webpack_require__(38087)
__webpack_require__(41832)
__webpack_require__(61044)
__webpack_require__(62821)
__webpack_require__(36436)
__webpack_require__(59510)
__webpack_require__(65058)
__webpack_require__(38098)
__webpack_require__(83853)
__webpack_require__(96148)
__webpack_require__(10335)
__webpack_require__(79623)
__webpack_require__(55336)

/***/ }),

/***/ 27866:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(70955)
const SelectableRoot = __webpack_require__(1233)

class Selectable extends Virtual {
  async onInit() {
    await this.bind('callback', `()=>${this.initialValue}`)
    const rootEl = this.el.closest('[selectable-root]')
    if (!rootEl) {
      throw new Error('Root not found')
    }

    const state = rootEl.hederaStates.find((s) => s.virtuals?.find((v) => v instanceof SelectableRoot))
    const root = state.virtuals.find((v) => v instanceof SelectableRoot)
    this.root = root
    this.el.setAttribute('selectable', '')

    this.listen(this.el, 'click', (e) => {
      root.select(this.el)
    })

    this.listen(this.el, 'dblclick', (e) => {
      this.callback()
    })

    root.focus()
  }
}
Selectable
  .define({
    name: 'selectable'
  })
  .properties({
    callback: 'any',
    mode: 'any',
    within: 'any',
  })

SelectableRoot.Selectable = Selectable
module.exports = Selectable


/***/ }),

/***/ 1233:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(70955)
const Selectable = __webpack_require__(27866)

const keys = [
  [(e) => e.key === 'ArrowUp', -1],
  [(e) => e.key === 'ArrowDown', 1],
  [(e) => e.key === 'ArrowRight', 1],
  [(e) => e.key === 'ArrowLeft', -1],
  [(e) => e.key === 'Tab' && !e.shiftKey, 1],
  [(e) => e.key === 'Tab' && e.shiftKey, -1],
]

const modes = {
  horizontal: ['ArrowRight', 'ArrowLeft', 'Tab'],
  vertical: ['ArrowUp', 'ArrowDown', 'Tab']
}

module.exports = class SelectableRoot extends Virtual {
  async onInit() {
    this.mode = 'vertical'
    if (this.initialValue) {
      await this.bind('mode', this.initialValue)
    }
    this.el.setAttribute('selectable-root', '')
    this.el.setAttribute('tabindex', '0')
    this.listen(this.el, 'keydown', this.b(this.onKeyDown), true)
  }

  focus() {
    const current = this.getSelected()
    if (!current) {
      this.selectFirst()
    }
  }

  getSelectable(node) {
    return node.hederaStates.find((s) => s.virtuals?.find((v) => v instanceof SelectableRoot.Selectable)?.root === this)
  }


  getSelectables(selector = '') {
    return [...this.el.querySelectorAll(`[selectable]${selector}`)]
      .filter((n) => this.getSelectable(n))
  }

  getSelected() {
    return this.getSelectables('.selected')[0]
  }

  selectFirst() {
    const first = this.getSelectables()[0]
    first.classList.add('selected')
  }

  select(selectable) {
    const current = this.getSelected()
    if (current) {
      current.classList.remove('selected')
    }
    selectable.classList.add('selected')
  }

  async onKeyDown(e) {
    if (e.key === 'Enter') {
      e.preventDefault()
      const selected = this.getSelected()
      await this.getSelectable(selected).callback()
      return
    }
    const mode = this.mode
    if (modes[mode].indexOf(e.key) === -1) { return }

    const key = keys.find(([check]) => check(e))
    if (!key) { return }

    e.preventDefault()
    const current = this.getSelected()
    if (!current) {
      this.selectFirst()
    } else {
      const selectables = this.getSelectables()
      const currentIndex = selectables.indexOf(current)
      let nextIndex = currentIndex + key[1]

      if (nextIndex >= selectables.length) {
        nextIndex = 0
      } else if (nextIndex < 0) {
        nextIndex = selectables.length - 1
      }
      const next = selectables[nextIndex]
      if (next) {
        current.classList.remove('selected')
        next.classList.add('selected')
      }
    }
  }
}
  .define({
    name: 'selectableRoot'
  })
  .properties({
    mode: 'any',
  })


/***/ }),

/***/ 38098:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(27866)
__webpack_require__(1233)

/***/ }),

/***/ 36436:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(70955)
const { navigator } = __webpack_require__(98864)

module.exports = class Link extends Virtual {
  async onInit() {
    await this.bind('href', this.initialValue)

    this.el.addEventListener('click', (event) => {
      event.preventDefault()
      if (this.href) {
        navigator.navigate(this.href)
      }
    })

    this.on('propertyChanged:href', () => {
      this.updateHref()
    })
    navigator.on('change', () => {
      this.updateActive()
    })

    this.updateActive()
    this.updateHref()
  }

  updateHref() {
    this.el.href = this.href || ''
  }

  updateActive() {
    const isActive = this.href === navigator.currentUrl
    this.el.classList[isActive ? 'add' : 'remove']('active')
  }
}
  .define({
    name: 'link'
  })
  .properties({
    href: 'any',
  })


/***/ }),

/***/ 28255:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)

module.exports = class BaseLoader extends Component {

}
  .define()
  .properties({
    size: 'any',
  })


/***/ }),

/***/ 70071:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { components: { Loader } } = __webpack_require__(98864)
const Virtual = __webpack_require__(70955)

module.exports = class LoaderVirtual extends Virtual {
  async onInit() {
    this.on('propertyChanged', this.b(this.update))
    this.loader = new Loader()
    const [isLoading, size] = this.initialValue.split(/ size /)
    await this.bind('isLoading', isLoading)
    await this.bind('size', size)
    await this.scope.render(this.loader)
    this.update()
  }

  update() {
    this.loader.size = this.size
    if (this.isLoading) {
      this.el.appendChild(this.loader)
    } else if (this.loader.parentElement === this.el) {
      this.el.removeChild(this.loader)
    }
  }
}
  .define({
    name: 'loader'
  })
  .properties({
    isLoading: 'any',
    size: 'any',
  })


/***/ }),

/***/ 83853:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(70071)

/***/ }),

/***/ 60864:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Bindeable = __webpack_require__(59222)
const Destroyable = __webpack_require__(79330)

module.exports = mixer.mixin([Bindeable, Destroyable], (base) => {
  return class Interactable extends base {
    constructor(...args) {
      super(...args)
      this.listeners = []
    }

    listen(el, event, callback, options) {
      el.addEventListener(event, this.b(callback), options)
      this.listeners.push({ el, event, callback })
    }

    stopListen(el, event, callback) {
      const index = this.listeners.findIndex((l) => {
        return l.el === el && l.event === event && l.callback === callback
      })
      if (index === -1) { return }

      el.removeEventListener(event, this.b(callback))
      this.listeners.splice(index, 1)
    }

    destroy() {
      this.listeners.forEach(({ el, event, callback }) => {
        el.removeEventListener(event, this.b(callback))
      })
      super.destroy()
    }
  }
})

/***/ }),

/***/ 46466:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Propertiable = __webpack_require__(85020)
const mixer = __webpack_require__(96172)

module.exports = mixer.mixin([Propertiable], (base) => {
  return class LocalStorageable extends base {
    static localStorage(options) {
      this.localStorageOptions = options
      return this
    }

    constructor(...args) {
      super(...args)
      const options = this.constructor.localStorageOptions
      if (!options) { return }

      let initialValues = localStorage.getItem(options.name)
      if (initialValues) {
        initialValues = JSON.parse(initialValues)
        Object.assign(this, initialValues)
      }

      this.on('propertyChanged', () => {
        const values = this.values
        const valuesToSave = Object.entries(values)
          .filter(([name]) => options.properties.indexOf(name) !== -1)
          .reduce((acc, [name, value]) => {
            acc[name] = value
            return acc
          }, {})

        localStorage.setItem(options.name, JSON.stringify(valuesToSave))
      })
    }
  }
})

/***/ }),

/***/ 87968:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const LocalStorageable = __webpack_require__(46466)
const mixer = __webpack_require__(96172)

module.exports = class Layout extends mixer.extends(Component, [LocalStorageable]) {



  onReady() {
    if (!this.container)
      throw new Error("Layout '" + this.constructor.name + "' must implement a container");
  }

  async setContent(content) {
    const oldContent = this.content

    this.content = content
    this.contentReady = false
    await this.scope.render(this.content)
    if (oldContent) {
      this.scope.release(oldContent)
      this.container.removeChild(oldContent)
    }
    this.container.appendChild(this.content)
    this.contentReady = true

  }
}
  .define()
  .properties({
    contentReady: 'any',
  })


/***/ }),

/***/ 98118:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Router = __webpack_require__(18709)

module.exports = class Navigator extends Router {
  constructor() {
    super()
    this.use((req, res, next) => {
      res.navigate = (...args) => this.navigate(...args)
      return next()
    })
  }

  async start() {
    window.addEventListener('popstate', async () => {
      await this.processCurrent()
    })

    await this.processCurrent()
  }

  async processCurrent() {
    this.currentUrl = window.location.pathname
    const req = {
      url: this.currentUrl,
    }

    const res = {}
    await this.process(req, res)
    await this.emit('change')
  }

  async navigate(url, replaceState) {
    history[replaceState ? "replaceState" : "pushState"]({}, '', url)
    await this.processCurrent()
  }
}


/***/ }),

/***/ 48715:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const Scope = __webpack_require__(29777)
const Layout = __webpack_require__(87968)
const RootRouter = __webpack_require__(41572)

module.exports = class Root extends Layout {
  constructor() {
    super()
    this.router = new RootRouter(this)
  }

  async start(variables) {
    const scope = new Scope({ source: this, variables })
    await scope.render(this)
  }

  async loadLayouts(req, layouts) {
    let current = this
    for (let layoutType of layouts) {
      let args = {}
      if (!(layoutType.prototype instanceof Layout)) {
        layoutType = layoutType(req)
      }
      if (layoutType instanceof Array) {
        args = layoutType[1]
        layoutType = layoutType[0]
      }
      layoutType = await layoutType
      if (layoutType.default) {
        layoutType = layoutType.default
      }
      if (!(current.content instanceof layoutType)) {
        const layout = new layoutType()
        Object.assign(layout, args)
        await current.setContent(layout)
      } else {
        Object.assign(current.content, args)
      }
      current = current.content
    }
    return current
  }

  async setPage(req, layouts, pageImport, args) {
    const bottomLayout = await this.loadLayouts(req, layouts)
    const pageModule = await pageImport
    const pageType = pageModule.default
    const page = new pageType(...args)
    await bottomLayout.setContent(page)
  }
}.define()

/***/ }),

/***/ 50742:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Router = __webpack_require__(18709)

module.exports = class LayoutRouter extends Router {
  async onMatch(req, res, next) {
    res.layouts = [...res.layouts, this.layout]
    await super.onMatch(req, res, next)
  }
}
  .define()

/***/ }),

/***/ 41572:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Router = __webpack_require__(18709)

module.exports = class RootRoute extends Router {
  constructor(root) {
    super()
    this.root = root
  }

  async onMatch(req, res, next) {
    res.layouts = []
    const self = this
    res.page = async function (pageImport, ...args) {
      await self.root.setPage(this.req, this.layouts, pageImport, args)
    }

    await super.onMatch(req, res, next)
  }
}
  .define()

/***/ }),

/***/ 18709:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Eventable = __webpack_require__(16761)
const mixer = __webpack_require__(96172)
const { chain } = __webpack_require__(49185)

const functionToProcessable = (fn) => {
  return {
    async process(req, res, next) { await fn(req, res, next) }
  }
}

class Router extends mixer.extends([Eventable]) {

  constructor(values = {}) {
    super()
    Object.assign(this, {
      processables: [],
      ...values,
    })
  }

  route(url, ...middlewares) {
    const route = new Router({
      url,
      processables: middlewares.map(functionToProcessable),
    })
    this.processables.push(route)
  }

  use(...args) {
    let router
    if (args.length === 1 && args[0] instanceof Router) {
      router = args[0]
      this.processables.push(router)
    } else {
      this.processables.push(...args.map(functionToProcessable))
    }
  }

  async onMatch(req, res, next) {
    await chain(this.processables, (p, subNext) => {
      p.process(req, res, subNext)
    }, next)
  }

  async process(req, res, next = () => { }) {
    let match
    if (this.url != undefined) {
      let urlToMatch = this.url
      if (urlToMatch === '') {
        urlToMatch = /^$/
      }
      match = req.url.match(urlToMatch)
      if (!match) {
        return next()
      }
    }

    const routerReq = {
      __proto__: req,
    }

    if (this.url) {
      const url = req.url.replace(match[0], '')
      Object.assign(routerReq, {
        match,
        url
      })
    }

    const routerRes = {
      __proto__: res,
      req,
    }

    await this.onMatch(routerReq, routerRes, next)
  }
}


module.exports = Router
  .define()

/***/ }),

/***/ 48495:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const BindingFunction = __webpack_require__(11374)

const expressionRegex = /{{.*?}}/g

module.exports = class BindingExpression {
  constructor(expression, variables, callback) {
    this.expression = expression
    this.variables = variables
    this.callback = callback
    this.functions = []
  }

  async update() {
    let value = this.expression
    this.destroy()
    this.functions = []
    for (const path of this.expression.match(expressionRegex)) {
      const sanitizePath = path.replace('{{', '').replace('}}', '')
      const bindingFunction = new BindingFunction(sanitizePath, this.variables, async () => {
        await this.update()
      })

      bindingFunction.update(false)
      const initialValue = await bindingFunction.getValue()
      value = value.replace(path, initialValue)
      this.functions.push(bindingFunction)
    }

    this.expression.match(expressionRegex)
    await this.callback(value)
  }

  destroy() {
    this.functions.forEach((p) => p.destroy())
  }
}


/***/ }),

/***/ 11374:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Event = __webpack_require__(66715)

const { createFunction } = __webpack_require__(54829)

const processFunctionString = (string) => {
  let sanitized = string
  const paths = string.match(/((?:[\w|$|?]+\.@?[\w|$|.|@|?]+))/g) || []
  //.filter((p) => p.indexOf('@') !== -1)
  paths
    .forEach((p) => {
      sanitized = sanitized.replace(p, p.replace(/@/g, ''))
    })

  return {
    sanitized,
    paths,
  }
}


const onBindingGetProperty = new Event()
const onBindingDestroyed = new Event()


module.exports = class BindingFunction {

  static onBindingGetProperty = onBindingGetProperty
  static onBindingDestroyed = onBindingDestroyed
  constructor(functionString, variables, callback) {
    this.functionString = functionString
    this.variables = variables
    this.callback = callback
    this.listeners = []
    this.holdables = []
    const { sanitized, paths } = processFunctionString(functionString)
    this.paths = paths
    const vars = {
      ...variables
    }
    delete vars.this
    this.sanitizedFunctionString = sanitized
    this.function = createFunction('return ' + sanitized, vars)
  }

  async getValue() {
    const thisArg = this.variables.this
    const value = await this.function.call(thisArg)
    return value
  }

  async update(trigger = true) {
    this.destroy()
    this.listeners = []
    this.paths.forEach((path) => {
      const segments = path.split('.')
      let value = this.variables
      segments.forEach((segment) => {
        if (!value) { return }

        const propertyName = segment.replace(/[@?]+/g, '')
        if (segment.startsWith('@')) {
          if (!value.on) {
            console.log(path, { value })
            throw new Error()
          }
          const listener = value.on(`propertyChanged:${propertyName}`, async () => {
            await this.update()
          })
          this.listeners.push(listener)
        }
        value = value[propertyName]
        if (value) {
          onBindingGetProperty.trigger(this, value)
        }
      })
    })
    if (trigger) {
      const value = await this.getValue()
      await this.callback(value)
      return value
    }
  }

  destroy() {
    this.listeners.forEach((l) => l.remove())
    onBindingDestroyed.trigger(this)
  }
}

/***/ }),

/***/ 41832:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

const { workers } = __webpack_require__(98864)
const { set } = __webpack_require__(82832)
const { dashToCamel } = __webpack_require__(54829)
const BindingFunction = __webpack_require__(11374)
const BindingExpression = __webpack_require__(48495)

const attributes = {
  class: (() => {
    const caches = []
    return (node, value, key) => {
      if (!value) {
        value = ''
      }
      const classes = value.split(' ').filter((o) => o && ['undefined', 'false', 'null'].indexOf(o) === -1)
      let cache = caches.find((c) => c.node === node && c.key === key)
      if (cache) {
        for (const cssClass of cache.classes) {
          node.classList.remove(cssClass)
        }
      } else {
        cache = {
          node,
          classes,
          key
        }
        caches.push(cache)
      }
      classes.forEach((c) => node.classList.add(c))
      cache.classes = classes
    }
  })(),
  innerHtml(node, value, key) {
    node.innerHTML = value
  }
}

const setAttr = (node, path, value, key) => {
  const attr = attributes[path]
  if (attr) {
    attr(node, value, key)
    return
  }
  set(node, path, value)
}

const PREFIX = ':'

const initBindings = (state) => {
  if (!state.bindings) {
    state.bindings = []
  }
}

const destroy = (state) => {
  if (!state.bindings) { return }
  state.bindings.forEach((b) => b.destroy())
  state.bindings = null
}

workers.push({
  async process(scope, state) {
    const { node } = state
    if (node.nodeType !== Node.TEXT_NODE) { return }
    if (node.textContent.indexOf('{{=') === -1) { return }
    initBindings(state)
    const functionContent = node.textContent.replace('{{=', '').replace('}}', '').trim()
    let child
    const parent = node.parentElement
    const binding = new BindingFunction(functionContent, { ...scope.variables }, async (value) => {
      if (child) {
        child.destroy()
        parent.innerHTML = ''
        child = null
      }
      if (!value) { return }
      child = scope.child()
      parent.appendChild(value)
      await child.render(value)
    })
    await binding.update()
    node.remove()
    state.bindings.push(binding)
  },
  destroy
}, {
  async process(scope, state) {
    const { node } = state
    if (node.nodeType !== Node.TEXT_NODE) { return }
    if (node.textContent.search('{{[^=]') === -1) { return }
    initBindings(state)
    const bindingExpression = new BindingExpression(node.textContent, { ...scope.variables }, (value) => {
      node.textContent = value
    })
    await bindingExpression.update()
    state.bindings.push(bindingExpression)
  },
  destroy
}, {
  async process(scope, state) {
    const { node } = state
    if (!node.attributes) { return }
    initBindings(state)
    const attributes = [...node.attributes]
      .filter((attr) => attr.name.startsWith(PREFIX))
    for (const attr of attributes) {
      const path = dashToCamel(attr.name.replace(PREFIX, ''))
      const bindingFunction = new BindingFunction(attr.nodeValue, { ...scope.variables }, (value) => {
        setAttr(node, path, value, attr.nodeValue)
      })
      await bindingFunction.update()
      state.bindings.push(bindingFunction)
      node.removeAttribute(attr.name)
    }
  },
  destroy
})



/***/ }),

/***/ 34543:
/***/ (function(module) {


module.exports = {
  base: []
}

/***/ }),

/***/ 40745:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(80725)
__webpack_require__(44229)

const MAX = 1000

const staticKeys = {
  ctrl: (e) => e.ctrlKey,
  alt: (e) => e.altKey
}

const getKeys = (keysString) => {
  const keysArray = keysString.split('+')
  const keys = {}
  for (let key of keysArray) {
    key = key.toUpperCase()
    const existing = keys[key]
    keys[key] = existing ? existing + 1 : 1
  }
  return keys
}

const matchKeys = (keys1, keys2) => {
  const entries = Object.entries(keys1)
  for (const [k, v] of entries) {
    if (v !== keys2[k]) {
      return false
    }
  }
  return true
}

const keysHtml = {
  ARROWDOWN: '<i class="fa-solid fa-arrow-down"></i>',
  ARROWUP: '<i class="fa-solid fa-arrow-up"></i>'
}

module.exports = class ShortCut extends Component {
  constructor() {
    super()
    this.target = this.parentElement
    this.on('propertyChanged:key', this.b(this.updateKeys))

  }

  onInit() {
    this.updateKeys()
    if (!this.callback) { return }
    this.target.addEventListener('keydown', this.b(this.onKeyDown))
  }

  updateKeys() {
    this.keys = getKeys(this.key)
    this.keyHtml = Object.keys(this.keys).map((key) => {
      let keyHtml = keysHtml[key]
      if (keyHtml) { return keyHtml }
      return key
    }).join('+')
  }

  start() {
    this.reset()
    this.startDate = new Date()
  }

  stackKey(key) {
    key = key.toUpperCase()
    if (!this.keys[key]) { return }
    const existing = this.keysPressed[key]
    this.keysPressed[key] = Math.min(this.keys[key], existing ? existing + 1 : 1)
  }

  reset() {
    this.keysPressed = {}
    this.startDate = null
  }

  trigger() {
    this.reset()
    if (this.callback) {
      setTimeout(() => this.callback())
    }
  }

  stack(e) {
    const pressedStaticKeys = Object.entries(staticKeys).filter(([k, c]) => c(e))
    pressedStaticKeys.forEach(([k]) => this.stackKey(k))
    this.stackKey(e.key)
    if (matchKeys(this.keys, this.keysPressed)) {
      this.trigger()
    }
  }


  onKeyDown(e) {
    if (this.startDate) {
      if (new Date() - this.startDate > MAX) {
        this.start()
      }
    } else {
      this.start()
    }

    this.stack(e)
  }
}
  .define({
    name: 'short-cut',
    template,
  })
  .properties({
    callback: 'any',
    keyHtml: 'any',
    key: 'any',
  })


/***/ }),

/***/ 15609:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(6937)
__webpack_require__(18308)

module.exports = class ShortCuts extends Component { }
  .define({
    name: 'short-cuts',
    template
  })


/***/ }),

/***/ 59510:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(41181)
__webpack_require__(15609)
__webpack_require__(40745)

/***/ }),

/***/ 41181:
/***/ (function() {


let displayed = false
const className = 'shortcut'
const check = (e) => e.altKey



window.addEventListener('keydown', (e) => {
  if (e.altKey) { e.preventDefault() }
  if (!check(e)) { return }

  document.body.classList.add(className)
})

window.addEventListener('keyup', (e) => {
  if (check(e)) { return }

  document.body.classList.remove(className)
})


/***/ }),

/***/ 59937:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(70955)
const utils = __webpack_require__(9365)

module.exports = class TabLoop extends Virtual {
  constructor(el) {
    super(el)
    let current = null
    window.addEventListener('focusin', (e) => {
      const target = document.activeElement
      if (this.el.contains(target)) {
        current = target
      } else if (current) {
        utils.focusFirst(this.el)
      }
    })
  }
}
  .define({
    name: 'tabLoop'
  })



/***/ }),

/***/ 65058:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(59937)

/***/ }),

/***/ 9365:
/***/ (function(module) {

const focusable = 'button, input,href, select, textarea, [tabindex]:not([tabindex="-1"])'


const getFocusables = () => {
  return [...document.querySelectorAll(focusable)]
    .filter((el) => {
      return el.offsetParent
    })
}

const focusFromCurrent = (increment) => {
  const focusables = getFocusables()
  let index = focusables.indexOf(document.activeElement)
  if (index === -1) {
    index = 0
  } else {
    index = index + increment
    if (index > focusables.length) {
      index = 0
    } else if (index < 0) {
      index = focusables.length - 1
    }
  }
  const next = focusables[index]
  next.focus()
}

const next = () => {
  focusFromCurrent(+1)
}
const previous = () => {
  focusFromCurrent(-1)
}

const focusFirst = (el) => {
  const first = el.querySelector(focusable)
  if (first) {
    first.focus()
  }
}

const tab = {
  next,
  previous,
  focusFirst
}


module.exports = tab

/***/ }),

/***/ 54829:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {




const isCustomElement = (node) => {
  return node.tagName && typeof customElements.get(node.tagName.toLowerCase()) != "undefined";
}

const createFunction = (stringFunction, variables = {}) => {
  const functionArgs = []
  const variablesArgs = []

  for (var [name, value] of Object.entries(variables)) {
    functionArgs.push(name)
    variablesArgs.push(value)
  }
  functionArgs.push(stringFunction)
  return function () {

    const fn = Function.apply(null, functionArgs)
    return fn.apply(this, variablesArgs)
  }
}


const dashToCamel = (string) => string.replace(/-([a-z])/g, (g) => g[1].toUpperCase())

const attributes = {
  class(node, value) {
    value.split(' ').forEach((cssClass) => {
      node.classList.add(cssClass)
    })
  },
}

const moveAttributes = (from, to) => {
  [...from.attributes]
    .forEach((attr) => {
      from.removeAttribute(attr.name)
      const attrType = attributes[attr.name]
      if (attrType) {
        attrType(from, attr.value)
      } else {
        to.setAttribute(attr.name, attr.nodeValue)
      }

    })
}

module.exports = {
  dashToCamel,
  createFunction,
  isCustomElement,
  template: __webpack_require__(94533),
  moveAttributes
}

/***/ }),

/***/ 94533:
/***/ (function(module) {

const wrapMap = {
  option: [1, "<select multiple='multiple'>", "</select>"],
  legend: [1, "<fieldset>", "</fieldset>"],
  area: [1, "<map>", "</map>"],
  param: [1, "<object>", "</object>"],
  thead: [1, "<table>", "</table>"],
  tr: [2, "<table><tbody>", "</tbody></table>"],
  col: [2, "<table><tbody></tbody><colgroup>", "</colgroup></table>"],
  td: [3, "<table><tbody><tr>", "</tr></tbody></table>"],
  body: [0, "", ""],
  _default: [1, "<div>", "</div>"]
};
wrapMap.optgroup = wrapMap.option;
wrapMap.tbody = wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;
wrapMap.th = wrapMap.td;
const str2DOMElement = (html) => {

  var match = /<\s*\w.*?>/g.exec(html);
  var element = document.createElement('div');
  if (match != null) {
    var tag = match[0].replace(/</g, '').replace(/>/g, '').split(' ')[0];
    if (tag.toLowerCase() === 'body') {
      var dom = document.implementation.createDocument('http://www.w3.org/1999/xhtml', 'html', null);
      var body = document.createElement("body");
      // keeping the attributes
      element.innerHTML = html.replace(/<body/g, '<div').replace(/<\/body>/g, '</div>');
      var attrs = element.firstChild.attributes;
      body.innerHTML = html;
      for (var i = 0; i < attrs.length; i++) {
        body.setAttribute(attrs[i].name, attrs[i].value);
      }
      return body;
    } else {
      var map = wrapMap[tag] || wrapMap._default,
        element;
      html = map[1] + html + map[2];
      element.innerHTML = html;
      // Descend through wrappers to the right content
      var j = map[0] + 1;
      while (j--) {
        element = element.lastChild;
      }
    }
  } else {
    element.innerHTML = html;
    element = element.lastChild;
  }
  return element;
}

const isTemplate = (template) => {
  return (template instanceof HTMLScriptElement || template instanceof HTMLTemplateElement || typeof template === "string");
}

const getElementFromTemplate = (template) => {
  const clone = document.importNode(template.content, true)
  return [...clone.childNodes].find((child) => child.nodeType === Node.ELEMENT_NODE)
}

const getElementFromObject = (template) => {
  if (template instanceof HTMLTemplateElement) {
    return getElementFromTemplate(template)
  } else if (typeof template === "string") {
    return str2DOMElement(template)
  } else if (template instanceof HTMLElement || template instanceof SVGElement) {
    return template.cloneNode(true)
  } else {
    throw new Error()
  }
}

module.exports = {
  isTemplate,
  getElementFromTemplate,
  getElementFromObject,
}

/***/ }),

/***/ 52778:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Destroyable = __webpack_require__(79330)
const Virtual = __webpack_require__(70955)

module.exports = class Exit extends Virtual {
  async onInit() {
    const [callback, when] = this.initialValue.split(/ when /)
    await this.bind('callback', `()=>${callback}`)
    if(when){
      await this.bind('when', when)
    }
    
    this.listen(window, 'click', this.onWindowClicked, true)
    this.listen(window, 'keydown', this.onWindowKeyDown, true)
    if (this.when === undefined) {
      this.when = true
    }
  }

  onWindowKeyDown(e) {
    if (e.key === "Escape") {
      this.trigger()
    }
  }

  onWindowClicked(e) {
    if (!this.el.contains(e.target)) {
      this.trigger()
    }
  }

  trigger() {
    if (this['@destroyed']) { return }
    if (!this.when) { return }
    this.callback()
  }
}
  .define({
    name: 'exit'
  })
  .properties({
    callback: 'any',
  })


/***/ }),

/***/ 17474:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(70955)

module.exports = class InputDelay extends Virtual {
  async onInit(){
    this.timeout = null
    await this.bind('callback', `()=>${this.initialValue}`)
    this.listen(this.el, 'input', this.onChange)
  }

  onChange() {
    if (this.timeout) {
      clearTimeout(this.timeout)
    }

    this.timeout = setTimeout(() => {
      this.callback()
    }, 500)
  }
}
  .define({
    name: 'inputDelay'
  })
  .properties({
    callback: 'any',
  })


/***/ }),

/***/ 62821:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(17474)
__webpack_require__(52778)

/***/ }),

/***/ 7300:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

const setup = __webpack_require__(47062)
const context = __webpack_require__(35293)

setup.routing.actions.push({
  url: '/jwt',
  content: '<i class="fa-solid fa-key"></i>',
  check(user) {
    return context.user === user
  },
  async execute(req, res, next) {
    await res.page(__webpack_require__.e(/* import() */ 838).then(__webpack_require__.t.bind(__webpack_require__, 24838, 23)))
  }
})

/***/ }),

/***/ 88872:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const { Model, String } = __webpack_require__(75314)
const User = __webpack_require__(62541)
const securityStrategy = __webpack_require__(5085)
const ControllerError = __webpack_require__(5145)

module.exports = class Jwt extends mixer.extends(Model) {

}
  .define({
    name: 'jwt',
    pluralName: 'jwts',
    root: true,
  })
  .properties({
    user: {
      type: User,
      state: {
        required: true,
      }
    },
    name: {
      type: String,
      state: {
        required: true
      }
    },
    id: {
      type: String,
      state: {
        disabled: true,
      }
    },
    key: {
      type: String,
      state: {
        disabled: true
      }
    }
  })
  .controllers({
    create: {
      requires: [securityStrategy],
      check(context) {
        if (!context.user) {
          throw new ControllerError(`You must be connected to create a jwt'`)
        }
      },
    },
    update: {
      requires: [securityStrategy],
      check(context, jwt) {
        if (!jwt.user.equals(context.user)) {
          throw new ControllerError(`User don't have sufficient rights`)
        }
      },
    }
  })

/***/ }),

/***/ 72488:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(88872)

/***/ }),

/***/ 86230:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const axios = __webpack_require__(11605)
const Service = __webpack_require__(25033)
const { User } = __webpack_require__(6619)
const context = __webpack_require__(35293)
const { defaultLoad } = __webpack_require__(4637)
module.exports = class AuthService extends Service {
  constructor(url) {
    super()
    this.url = url
    this.me = null
    this.on('propertyChanged:me', this.b(this.onMeChanged))
  }

  onMeChanged() {
    context.user = this.me
  }

  parseUser(json) {
    const user = User.parse(json, { singleInstance: true })
    user.setLoadState(defaultLoad)
    return user
  }

  async request(action, payload) {
    const url = `${this.url}/api/auth${action}`
    const response = await axios({
      url,
      method: 'POST',
      data: payload,
      withCredentials: true,
    })

    return response.data
  }

  async getMe() {
    const { me } = await this.request('/me')
    if (!me) { return null }
    this.me = this.parseUser(me)
  }

  async login(user) {
    const { me } = await this.request('/login', user)
    this.me = this.parseUser(me)
  }

  async changePassword(user) {
    const response = await this.request('/change-password', user)
    return response
  }

  async signup(user) {
    const { me } = await this.request('/signup', user)
    this.me = this.parseUser(me)
  }

  async logout() {
    await this.request('/logout')
    this.me = null
  }
}
  .define()
  .properties({
    me: 'any',
  })



/***/ }),

/***/ 21247:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const template = __webpack_require__(28142)
const Component = __webpack_require__(58497)
const context = __webpack_require__(35293)
__webpack_require__(42922)

module.exports = class UserMenu extends Component {
  constructor() {
    super()
    this.isOpen = false
  }
}
  .define({
    name: 'user-menu',
    template,
  })
  .properties({
    isOpen: 'any',
  })
  .variables({
    context,
  })

/***/ }),

/***/ 47319:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Row = __webpack_require__(31009)
const template = __webpack_require__(13219)
const { User } = __webpack_require__(6619)
__webpack_require__(18673)

module.exports = class UserRow extends Row {

}
  .define({
    name: 'user-row',
    template,
  })
  .register(User, 'row')

/***/ }),

/***/ 49318:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(21247)
__webpack_require__(47319)

/***/ }),

/***/ 71014:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

const context = __webpack_require__(35293)
context.defineProperty({
  name: 'user',
})
__webpack_require__(49318)


/***/ }),

/***/ 35564:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Router = __webpack_require__(18709)
const context = __webpack_require__(35293)
const { auth } = __webpack_require__(98864)

const router = new Router()

const notConnected = (req, res, next) => {
  if (context.me) {
    return res.navigate('/')
  }
  return next()
}

router.route('/login', notConnected, (req, res) => {
  res.page(__webpack_require__.e(/* import() */ 174).then(__webpack_require__.t.bind(__webpack_require__, 99174, 23)))
})

router.route('/signup', notConnected, (req, res) => {
  res.page(__webpack_require__.e(/* import() */ 724).then(__webpack_require__.t.bind(__webpack_require__, 58724, 23)))
})

router.route('/logout', (req, res) => {
  auth.logout()
  return res.navigate('/')
})

module.exports = router

/***/ }),

/***/ 97098:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const { Object, String, Password } = __webpack_require__(75314)

module.exports = class Credentials extends mixer.extends(Object) {

}
  .define()
  .properties({
    username: {
      type: String,
      state: {
        required: true
      }
    },
    password: {
      type: Password,
      state: {
        required: true,
      }
    }
  })

/***/ }),

/***/ 54716:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const ControllerError = __webpack_require__(5145)
const { Model, String } = __webpack_require__(75314)
const setup = __webpack_require__(99213)
const mixer = __webpack_require__(96172)

const isAdminOrSelf = async (context, user) => {
  if (!context.user.equals(user) && !await context.user.is(context, 'admin')) {
    throw new ControllerError('You cannot manage user')
  }
}

module.exports = class Group extends mixer.extends(Model, [...setup.group]) {

}
  .define({
    name: 'group',
    pluralName: 'groups',
    root: true,
  })
  .indexes({
    name: {
      properties: ['name'],
      unique: true,
    }
  })
  .properties({
    name: {
      type: String,
      state: {
        required: true,
      }
    },
  })
  .controllers({
    create: {
      check(context) {
        if (!context.setup && !context.user) {
          throw new ControllerError(`You cannot create groups`)
        }
      },
      logic(context, user) {

      }
    },
    update: {
      check: isAdminOrSelf,
      logic(context, newUser, oldUser) {
        if (!context.user?.roles.admin) {
          newUser.roles.disabled = true
        }
      }
    },
    delete: {
      check: isAdminOrSelf
    }
  })


/***/ }),

/***/ 62541:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Model, String } = __webpack_require__(75314)
const mixer = __webpack_require__(96172)
const Pageable = __webpack_require__(67328)
const Wikiable = __webpack_require__(67846)
const ControllerError = __webpack_require__(5145)
const setup = __webpack_require__(99213)

const isSelfOrAdmin = async (context, user) => {
  if (!context.user) {
    throw new ControllerError('User is not logged in')
  }
  if (!context.user.equals(user) && !await context.user.is(context, 'admin')) {
    throw new ControllerError(`User doesn't have sufficient rights`)
  }
}

module.exports = class User extends mixer.extends(Model, [Pageable, Wikiable, ...setup.user]) {
  async is(context, name) {
    await this.groups.load(context)
    return this.groups.some((group) => group.name === name)
  }
}
  .define({
    name: 'user',
    pluralName: 'users',
    root: true,
    codeField: 'username',
    searchField: 'username',
    titleField: 'username',
  })
  .indexes({
    username: {
      properties: ['username'],
      unique: true,
    }
  })
  .properties({
    username: {
      type: String,
      state: {
        required: true,
      }
    },
    password: {
      type: String,
      state: {
        required: true,
      }
    },
  })
  .controllers({
    create: {
      async check(context) {
        if (!context.signup && !context.setup && (!context.user || !await context.user.is(context, 'admin'))) {
          throw new ControllerError(`You cannot create an user`)
        }
      },
    },
    update: {
      check: isSelfOrAdmin,
    },
    delete: {
      check: isSelfOrAdmin
    }
  })


/***/ }),

/***/ 6619:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { HasMany, OwnMany } = __webpack_require__(75314)
const Group = __webpack_require__(54716)
const User = __webpack_require__(62541)

Group.properties({
  users: {
    type: HasMany.of(User),
    on: 'groups'
  }
})

User.properties({
  groups: {
    type: OwnMany.of(Group),
  }
})

module.exports = {
  Group,
  User,
  Credentials: __webpack_require__(97098),
}

/***/ }),

/***/ 5085:
/***/ (function() {



/***/ }),

/***/ 99213:
/***/ (function(module) {


module.exports = {
  user: [],
  group: [],
}

/***/ }),

/***/ 4637:
/***/ (function(module) {


const defaultLoad = {
  groups: true
}

module.exports = {
  defaultLoad
}

/***/ }),

/***/ 68847:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const axios = __webpack_require__(79574)
const context = __webpack_require__(35293)
const QueryResult = __webpack_require__(38188)
const Models = __webpack_require__(76953)
const mixer = __webpack_require__(96172)
const Bindeable = __webpack_require__(59222)
const { objectToFilter } = __webpack_require__(75095)


const getArgs = (args) => {
  return args.filter((arg) => arg !== context)
}


module.exports = class Collection extends mixer.extends([Bindeable]) {
  constructor(values) {
    super()
    Object.assign(this, values)
  }

  async request(options) {
    try {
      let headers = options.headers
      if (this.headersBuilder) {
        const additionalHeaders = await this.headersBuilder()
        Object.assign(headers, additionalHeaders)
      }

      const response = await axios({
        ...options,
        method: 'POST',
        withCredentials: true,
        headers,
      })
      const result = response.data
      //console.log(action, ...args, result)
      return result
    } catch (err) {
      if (err.cause) {
        console.error(err.cause)
        throw err
      }
      if (err.response) {
        throw new Error(`API error on url ${options.url}: ${JSON.stringify(err.response.data, null, ' ')} `,)
      }
      throw err
    }
  }

  async apiRequest(action, body, options = {}) {
    const url = `${this.url}/api/collections/${this.type.definition.pluralName}${action}`
    //console.log(action, JSON.stringify(body, null, ' '), JSON.stringify(options, null, ' '))

    const result = await this.request({
      url,
      data: body,
      headers: {
        'Content-Type': 'application/json'
      },
    })

    return result
  }

  async findByUniqueIndex(index, options) {
    const model = this.instances.find((m) => m.anyUniqueIndexMatch(index))
    if (model) { return model }

    return this.findOne(objectToFilter(index), options)
  }

  async findOne(...args) {
    const [filter, options] = getArgs(args)
    const [result] = await this.find(filter, {
      ...options,
      limit: 1,
    })

    return result
  }

  async find(...args) {
    const [filter, options] = getArgs(args)
    const result = await this.query([{
      filter
    }], options)

    return result
  }

  async query(...args) {
    const [stages, options = {}] = getArgs(args)
    const modelsJson = await this.apiRequest('/query', [stages, options])
    const modelsArray = modelsJson.map((modelJson) => {
      const model = this.type.parse(modelJson, { singleInstance: true })
      model.setLoadState(options.load || {})
      return model
    })
    const models = new (QueryResult.of(this.type))(modelsArray, { stages, options })
    return models
  }

  async create(modelJson) {
    if (modelJson.toJSON) {
      modelJson = modelJson.toJSON()
    }
    const json = await this.apiRequest('/create', [modelJson])
    const instance = this.type.parse(json, { singleInstance: true })
    instance.setLoadState(true)
    await this.onModelCreated(instance)
    return instance
  }

  async onModelCreated(model) {
    for (const ref of Models.references) {
      if (ref.onModelCreated) {
        await ref.onModelCreated(model)
      }
    }
  }

  async onModelUpdated(model) {
    for (const ref of Models.references) {
      await ref.onModelUpdated(model)
    }
  }

  async update(query, patches) {
    const json = await this.apiRequest('/update', [query, patches])
    const instance = this.type.parse(json, { singleInstance: true })
    await this.onModelUpdated(instance)
    return instance
  }

  async createOrUpdate(...args) {
    let modelJson
    let query
    if (args.length === 2) {
      [query, modelJson] = args
    } else {
      const [model] = args
      const index = model.getFirstUniqueIndex()
      if (!index) {
        console.log(JSON.stringify(model.toJSON(), null, ' '))
        throw new Error()
      }

      modelJson = model
      query = [index]
    }
    if (modelJson.toJSON) {
      modelJson = modelJson.toJSON()
    }
    const json = await this.apiRequest('/create-or-update', [query, modelJson])
    console.log('create-or-update', json)
    const resultModel = this.type.parse(json)
    resultModel.setLoadState(true)
    this.hold(resultModel)
    return resultModel
  }
}


/***/ }),

/***/ 5934:
/***/ (function(module) {

const singleInstanceOptions = { singleInstance: true }


const arrayInstances = []

const loop = () => {
  for (const instances of arrayInstances) {
    instances.checkDuplicates()
  }
  //setTimeout(loop, 1000)
}

loop()


module.exports = class Instances extends Array {
  constructor(type) {
    super()
    this.type = type
  }

  checkDuplicates() {
    for (const i of this) {
      for (const j of this) {
        if (i === j) { continue }
        if (i.equals(j)) {
          console.log('equals', i.toJSON(), j.toJSON())
          Object.assign(i, j)
          j.tranform(i)
          console.info('Transformation completed', i, j)
        }
      }
    }
  }

  getInstance(json) {
    let model = this.find((m) => m._id === json._id)
    if (!model) {
      model = this.type.parse({
        '@type': json['@type']
      })
      model.set(json, singleInstanceOptions)
      model.on('destroyed', () => this.onInstanceDestroyed(model))
      this.push(model)
      //this.checkDuplicates()
    } else {
      model.set(json, singleInstanceOptions)
    }

    return model
  }

  onInstanceDestroyed(instance) {
    const index = this.indexOf(instance)
    if (index !== -1) {
      this.splice(index, 1)
    }
  }
}

/***/ }),

/***/ 49045:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Model } = __webpack_require__(75314)
const Collection = __webpack_require__(68847)
const Instances = __webpack_require__(5934)

const collectionsTypesMap = [
  [Model, Collection]
]

const buildCollections = (url, options = {}) => {
  const rootModelTypes = Model
    .getAllChilds()
    .filter((t) => t.definition.root)
  const collections = rootModelTypes.reduce((acc, type) => {
    const collectionTypePair = collectionsTypesMap.find(([t]) => t === type || type.prototype instanceof t)
    if (!collectionTypePair) {
      throw new Error(`Collection type not find for type '${type.definition.name}'`)
    }
    const collectionType = collectionTypePair[1]
    const instances = new Instances(type)
    const collection = new collectionType({ type, url, instances, ...options })
    acc[type.definition.pluralName] = collection
    Object.assign(type,{
      collection,
      instances
    })
    return acc
  }, {})
  return collections
}

module.exports = {
  collectionsTypesMap,
  buildCollections,
}

/***/ }),

/***/ 36958:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172);


module.exports = mixer.mixin((base) => {
  return class extends base {
    async onModelCreated(model) {
      const { template } = this.constructor.definitions.find((d) => d.template)
      const isInsanceOfTemplate = model instanceof template
      if (!isInsanceOfTemplate) { return }
      if(model[this.property.on] === this.owner){
        this.push(model)
      }
    }
  }
})

/***/ }),

/***/ 32522:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)

module.exports = mixer.mixin((base) => {
  return class ClientModelMixin extends base {
    
  }
})

/***/ }),

/***/ 8144:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)


module.exports = mixer.mixin((base) => {
  return class extends base {
    async onModelCreated(model) {
      await this.checkModel(model)
    }

    async onModelUpdated(model) {
      await this.checkModel(model)
    }
  }
})

/***/ }),

/***/ 64812:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Destroyable = __webpack_require__(79330)

module.exports = mixer.mixin([Destroyable], (base) => {
  const references = []
  return class Referenceable extends base {
    static references = references
    constructor(...args) {
      super(...args)
      references.push(this)
    }

    destroy() {
      const index = references.indexOf(this)
      references.splice(index, 1)
      return super.destroy()
    }
  }

})
  .define()



/***/ }),

/***/ 36157:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Destroyable = __webpack_require__(79330)
const mixer = __webpack_require__(96172)
const Equalable = __webpack_require__(48774);
//const Transformable = require('./Transformable')

const SingleInstance = mixer.mixin([Destroyable, Equalable], (base) => {
  return class SingleInstance extends base {
    static parse(object, options = {}, context) {
      if(object == null){
        return object
      }
      if (!options.singleInstance) {
        return super.parse(object, options, context)
      }

      const instance = this.instances.getInstance(object)
      instance.set(object, options)
      return instance
    }
  }
})

module.exports = SingleInstance

/***/ }),

/***/ 30766:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const BaseHoldable = __webpack_require__(30690)

module.exports = mixer.mixin((base) => {
  return class ArrayHolder extends base {
    changed() {
      if (this.copy) {
        this.copy.forEach((o) => {
          if (mixer.is(o, BaseHoldable)) {
            o.release(this)
          }
        })
      }
      this.copy = [...this]
      this.copy.forEach((o) => {
        if (mixer.is(o, BaseHoldable)) {
          o.hold(this)
        }
      })
      return super.changed()
    }
  }
})


/***/ }),

/***/ 30690:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)

module.exports = mixer.mixin((base) => {
  return class Holdable extends base {
    hold(reference) {
      throw new Error('Not implemented')
    }
    
    release(reference) {
      throw new Error('Not implemented')
    }
  }
})


/***/ }),

/***/ 65571:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Destroyable = __webpack_require__(79330)
const BaseHoldable = __webpack_require__(30690)

const state = '@holdState'
const instances = []

const HOLD_DURATION = 5

let circulars

const countCirculars = (references, limits) => {
  return references
    .filter((instance) => {
      if (limits.indexOf(instance) !== -1) {
        return true
      }
      return isCircular(instance, limits)
    })
    .length
}

const isCircular = (instance, limits = []) => {
  if (circulars.indexOf(instance) !== -1) { return true }
  limits = [...limits, instance]
  const references = instance[state]
    .references
    .filter((reference) => reference[state])
  if (references.length !== instance[state].references.length) {
    return false
  }
  const count = countCirculars(references, limits)
  const circular = count === references.length
  if (circular) {
    //console.log('---CIRCULAR', instance)
    circulars.push(circular)
  }
  return circular
}

const shouldDestroy = (instance) => {
  const currentDate = new Date()
  const instanceState = instance[state]
  if (currentDate - instanceState.lastDate < HOLD_DURATION * 1000) { return false }
  if (!instanceState.references.length) { return true }

  const circular = isCircular(instance)
  return circular
}

const log = () => {
  const objectToLog = instances.reduce((acc, h) => {
    acc[h.toString()] = h
    return acc
  }, {})
  //console.log(instances.length, objectToLog)
}

//loop()

const loop = () => {
  log()
  circulars = []
  instances
    .filter(shouldDestroy)
    .forEach((i) => {
      i.destroy()
    })
  setTimeout(loop, HOLD_DURATION / 3 * 1000)
}

const mixin = mixer.mixin([Destroyable, BaseHoldable], (base) => {
  return class Holdable extends base {
    constructor(...args) {
      super(...args)
      Object.defineProperty(this, state, {
        enumerable: true,
        writable: true,
        value: {
          references: [],
          lastDate: new Date()
        }
      })
      instances.push(this)
    }

    hold(reference) {
      if (reference === this) {
        return
        console.error({ ...reference })
        throw new Error('A object cannot hold itself')
      }
      const existing = this[state].references.indexOf(reference)
      if (existing !== -1) {
        //throw new Error('Could not hold')
        return
      }
      this[state].references.push(reference)
      this[state].lastDate = new Date()
    }

    async holdWhile(fn) {
      const id = {}
      this.hold(id)
      await fn()
      this.release(id)
    }

    release(reference) {
      const referenceIndex = this[state].references.indexOf(reference)
      if (referenceIndex === -1) {
        //throw new Error('Could not release')
        return
      }

      this[state].references.splice(referenceIndex, 1)
      this[state].lastDate = new Date()
    }

    destroy() {
      console.log('destroying', this)
      const index = instances.indexOf(this)
      if (index !== -1) {
        instances.splice(index, 1)
      }
      return super.destroy()
    }
  }
})
mixin.state = state
module.exports = mixin


/***/ }),

/***/ 98947:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const BaseHoldable = __webpack_require__(30690)

module.exports = mixer.mixin([BaseHoldable], (base) => {
  return class HoldableFragment extends base {
    hold(reference) {
      if(!this.owner){
        console.log(this)
      }
      this.owner.hold(reference)
    }

    release(reference) {
      this.owner.release(reference)
    }
  }
})
  .define()


/***/ }),

/***/ 84658:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Propertiable = __webpack_require__(85020)
const BaseHoldable  = __webpack_require__(30690)

module.exports = mixer.mixin([Propertiable], (base) => {
  return class Holder extends base {
    propertyChanged(property, value, oldValue) {
      if (oldValue && mixer.is(oldValue, BaseHoldable )) {
        oldValue.release(this)
      }
      if (value && mixer.is(value, BaseHoldable)) {
        value.hold(this)
      }
      return super.propertyChanged(property, value, oldValue)
    }
  }
})


/***/ }),

/***/ 40073:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Eventable = __webpack_require__(16761)
const Destroyable = __webpack_require__(79330)

module.exports = mixer.mixin([Destroyable, Eventable], (base) => {
  return class Transformable extends base {
    transform(to) {
      this.emit('transformed', [to])
      this.destroy()
    }
  }
})



/***/ }),

/***/ 8522:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {


const Holdable = __webpack_require__(65571)
const HoldableFragment = __webpack_require__(98947)
const Holder = __webpack_require__(84658)
const ArrayHolder = __webpack_require__(30766)
const Transformable = __webpack_require__(40073)
const Referenceable = __webpack_require__(64812)
const ClientHasMany = __webpack_require__(36958)
const SingleInstance = __webpack_require__(36157)
const ClientQueryResult = __webpack_require__(8144)
const context = __webpack_require__(35293)
const ClientModel = __webpack_require__(32522)
const setup = __webpack_require__(58950)

const { object, model, arrayAssociation, baseModels, models, hasMany, queryResult } = setup


model.before.push(Holdable, Holder, Transformable, SingleInstance)

baseModels.before.push(ArrayHolder, Referenceable)
models.before.push(Holdable, ClientModel)
arrayAssociation.before.push(HoldableFragment)
hasMany.before.push(ClientHasMany)
queryResult.before.push(ClientQueryResult)

setup.getArgs = (args) => {
  if (args[0] !== context) {
    args.unshift(context)
  }
  return args
}

/***/ }),

/***/ 59928:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Propertiable = __webpack_require__(85020)

module.exports = mixer.mixin([Propertiable], (base) => {
  return class extends base {

    reset(){
      super.reset()
      this.readOnly = false
      this.hidden = false
    }
  }
})
  .define()
  .properties({
    hidden: 'any',
    readOnly: 'any',
  })

/***/ }),

/***/ 8353:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(83152)
const componentsService = __webpack_require__(69066)
__webpack_require__(11024)

module.exports = class Branch extends Component {
  constructor() {
    super()
    this.on('propertyChanged:branch', this.b(this.update))
  }

  async onReady() {
    await this.update()

  }

  async update() {
    this.loading = true
    await this.branch.load()
    this.loading = false
  }
}
  .define({
    name: 'model-branch',
    template,
  })
  .properties({
    branch: 'any',
    loading: 'any',
  })


/***/ }),

/***/ 72294:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Model } = __webpack_require__(75314)
const ModelComponent = __webpack_require__(27653)
const template = __webpack_require__(42296)
const Pageable = __webpack_require__(67328)
const { navigator } = __webpack_require__(98864)
const mixer = __webpack_require__(96172)
__webpack_require__(50894)

module.exports = class Card extends ModelComponent {

}
  .define({
    name: 'model-card',
    template,
    type: 'card',
  })
  .variables({
    Pageable,
    navigator,
    mixer,
  })

/***/ }),

/***/ 8478:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const ModelComponent = __webpack_require__(27653)

module.exports = class Little extends ModelComponent {}

/***/ }),

/***/ 27653:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const componentsService = __webpack_require__(69066)
const { moveAttributes } = __webpack_require__(54829)
const { getParent } = __webpack_require__(52208)

module.exports = class ModelComponent extends Component {
  constructor(model) {
    super()
    this.model = model
    if (model && !this.parentElement) {
      const parent = getParent(this.constructor)
      if (parent === ModelComponent) {
        throw new Error('Cannot instanciate')
      }
    }

  }

  async replace(scope) {
    const parent = getParent(this.constructor)
    if (parent !== ModelComponent) { return null }
    let model = this.model
    if (!model) {
      const div = document.createElement('div')
      const modelAttribute = this.getAttribute(':model')
      if (!modelAttribute) {
        throw new Error('Cannot replace')
      }
      div.setAttribute(':model', modelAttribute)
      await scope.process(div)
      model = div.model
    }
    const typeName = this.constructor.definition.type
    if (!typeName) {
      throw new Error('Cannot replace')
    }
    const type = componentsService.get(model.constructor, typeName)
    const replace = new type(model)
    moveAttributes(this, replace)
    this.destroy()
    return replace
  }

  static register(type, name) {
    componentsService.register(type, name, this)
    return this
  }

  async attach(scope) {
    let replace = await this.replace(scope)
    if (replace) {
      if (!this.parentElement) {
        throw new Error('Cannot replace')
      }
      this.replaceWith(replace)
      return replace.attach(scope)
    }

    return super.attach(scope)
  }

  async onReady() {
    await this.update()
  }

  async update() {
    if (!this.model) { return }
    await this.model.load()
  }


}
  .define()
  .properties({
    model: 'any',
  })


/***/ }),

/***/ 44396:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(28944)
const componentsService = __webpack_require__(69066)
__webpack_require__(53915)

module.exports = class ModelSearch extends Component {
  constructor() {
    super()
  }

  onChange() {
    try {
      const query = eval(this.input.value)

    } catch (err) {
       
    }
  }

  template(result) {
    const componentType = componentsService.get(result.constructor, 'card')
    return new componentType(result)
  }
}
  .define({
    name: 'model-search',
    template,
  })
  .properties({
    results: 'any',
    open: 'any',
    length: 'any',
    type: 'any',
  })


/***/ }),

/***/ 31009:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const ModelComponent = __webpack_require__(27653)
const template = __webpack_require__(44871)

__webpack_require__(40376)


module.exports = class Row extends ModelComponent {

}
  .define({
    name: 'model-row',
    type: 'row',
    template
  })


/***/ }),

/***/ 57945:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(21458)
const Propertiable = __webpack_require__(85020)
const mixer = __webpack_require__(96172)
const componentsService = __webpack_require__(69066)
const interaction = __webpack_require__(38098)
const Pageable = __webpack_require__(67328)
const Mixin = __webpack_require__(46073)

__webpack_require__(85077)

class SearchableResults extends mixer.extends([Propertiable]) {
  constructor(type) {
    super()
    this.type = type
  }

  async search(key) {
    this.error = false
    this.loading = true
    const { searchField } = this.type.definitions.find((d) => d.searchField)
    try {
      this.results = await this.type.collection.find([{
        $match: [`$${searchField}`, key]
      }], {
        type: this.type.definition.name,
        limit: 3
      })
    } catch (e) {
      console.error(e)
      this.error = e.message
    } finally {
      this.loading = false
    }
  }
}

SearchableResults
  .define()
  .properties({
    loading: 'bool',
    results: 'any',
    error: 'bool',
  })

const childs = Pageable
  .getAllChilds()
  .filter((c) => !c.definition.abstract && !(c.prototype instanceof Mixin))

module.exports = class Search extends Component {
  constructor() {
    super()
    this.searchables = childs.map((type) => {
      return new SearchableResults(type)
    })
    this.first = true
  }

  start() {
    this.open = true
    if (this.first) {
      this.search()
      this.first = false
    }

  }

  stop() {
    this.open = false
    this.input.blur()
  }

  async search() {
    const promises = this.searchables.map(async (searchable) => {
      await searchable.search(this.input.value)
    })
    await Promise.all(promises)
  }

  selectSuggestion(suggestion) {
    suggestion.click()
    this.stop()
  }

  empty() {
    this.input.value = ''
    this.search()
  }
}
  .define({
    name: 'app-search',
    template,
  })
  .properties({
    results: 'any',
    open: 'any',
    length: 'any',
  })


/***/ }),

/***/ 49492:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

//require('./TypeFilter')
__webpack_require__(8353)
__webpack_require__(44396)
__webpack_require__(72294)


/***/ }),

/***/ 69066:
/***/ (function(module) {


const registries = {

}

const register = (type, name, component, load) => {
  if (!registries[name]) {
    registries[name] = []
  }
  registries[name].push({
    type,
    component,
    load
  })
}

const get = (type, name) => {
  const registry = registries[name]
  const find = registry.find((r) => r.type === type)
  if (find) {
    return find.component
  }

  const parents = type.definition?.parents
  if (!parents) { return null }
  for (const parent of parents) {
    const parentFind = get(parent, name)
    if (parentFind) {
      return parentFind
    }
  }
  return null
}

module.exports = {
  register,
  get,
}

/***/ }),

/***/ 87758:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Field = __webpack_require__(98656)
const template = __webpack_require__(89418)
__webpack_require__(94422)

module.exports = class BoolField extends Field {

}
  .define({
    name: 'bool-field',
    template,
  })

/***/ }),

/***/ 47892:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Field = __webpack_require__(98656)
const template = __webpack_require__(60175)
__webpack_require__(49732)

module.exports = class DateField extends Field {
  constructor() {
    super()
    this.value = ''
  }



}
  .define({
    name: 'date-field',
    template,
  })


/***/ }),

/***/ 98656:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const Array = __webpack_require__(83083)
const template = __webpack_require__(61510)
__webpack_require__(48234)

module.exports = class Field extends Component {
  constructor(values = {}) {
    super()
    Object.assign(this, values)
  }

  onInit() {
    this.on(this.state, 'propertyChanged:value', this.b(this.onValueChanged))
    this.onValueChanged()
  }

  onValueChanged() {

  }

  message(text) {
    const msg = {
      text,
      start: new Date()
    }
    this.state.messages.push(msg)
    setTimeout(() => {
      this.state.messages.tryRemove(msg)
    }, 5000)
  }

  getValue() {
    return this.value
  }

  setValue(value) {
    this.state.value = value
    this.touched = -1
    this.event('changed', { field: this })
  }
}
  .define({
    template,
  })
  .properties({
    header: 'any',
    state: 'any',
    touched: 'any',
  })

/***/ }),

/***/ 45902:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Field = __webpack_require__(98656)
const template = __webpack_require__(66278)
__webpack_require__(29192)

module.exports = class MarkdownField extends Field {

}
  .define({
    name: 'markdown-field',
    template,
  })

/***/ }),

/***/ 63444:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Field = __webpack_require__(98656)
const template = __webpack_require__(71617)
__webpack_require__(6399)

module.exports = class NumberField extends Field {

}
  .define({
    name: 'number-field',
    template,
  })
  .properties({
    step: 'any',
  })


/***/ }),

/***/ 19303:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Password, String } = __webpack_require__(75314)
const Field = __webpack_require__(98656)
const template = __webpack_require__(73406)
__webpack_require__(53224)

const map = [
  [Password, 'password'],
  [String, 'text']
]

module.exports = class TextField extends Field {
  onInit() {
    
      console.log(this)
    this.type = map.find(([t]) => {
      const type = this.state.property.type
      return type === t || type.prototype instanceof t
    })[1]
  }
}
  .define({
    name: 'text-field',
    template,
  })

/***/ }),

/***/ 33612:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

module.exports = {
  TextField: __webpack_require__(19303),
  BoolField: __webpack_require__(87758),
  DateField: __webpack_require__(47892),
  NumberField: __webpack_require__(63444),
  MarkdownField:__webpack_require__(45902)
}


/***/ }),

/***/ 6138:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(24119)
const context = __webpack_require__(35293)
const RootModelState = __webpack_require__(57880)
const ObjectForm = __webpack_require__(17644)
__webpack_require__(69153)

const applyStates = (targetStates, statesPatch) => {
  Object.entries(statesPatch)
    .forEach(([k, patch]) => {
      const target = targetStates[k]
      Object.assign(target, patch)
      if (patch.states) {
        applyStates(target.states, patch.states)
      }
    })
}

const propagate = (state, patch) => {
  Object.values(state.states)
    .forEach((childState) => {
      Object.assign(childState, patch)
      if (childState.states) {
        propagate(childState, patch)
      }
    })
}

module.exports = class ChildModelForm extends ObjectForm {

  buildState(value) {
    return new RootModelState({
      property: {
        type: value.constructor,
      },
      value,
      isEdit: !!this.model,
      required: true,
      context,
    })
  }

  async onSubmitSuccess(value) {
    try {
      let model
      if (this.model) {
        model = await this.type.collection.update({
          _id: this.model._id,
        }, {
          $set: value
        })
      } else {
        model = await this.type.collection.create(value)
      }

      this.event('saved', { model })
    } catch (error) {
      console.log('error', error)
      this.event('error', { error })
      throw error
    }
  }

  isCreate() { return !this.model }

  async updateStates() {
    await this.state.applyLogics()

    if (this.states) {
      applyStates(this.state.states, this.states)
    }
    await this.state.validate()
  }

  async onFieldsetChanged() {
    await this.updateStates()
  }
}
  .define({
    name: 'child-model-form',
    template,
  })



/***/ }),

/***/ 49625:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const template = __webpack_require__(85251)
const componentsService = __webpack_require__(69066)
const Field = __webpack_require__(98656)
const ChildModelForm = __webpack_require__(6138)
__webpack_require__(48336)

module.exports = class ModelField extends Field {
  constructor(values) {
    super(values)
    this.default()
    //this.on('propertyChanged:value', this.b(this.update))
  }

  select(model) {
    this.value = model
    this.panel.close()
  }

  default() {
    this.mode = 'default'
    this.suggestionsOpen = false
  }

  edit() {
    this.mode = 'edit'
    this.suggestionsOpen = true
    this.input.focus()
  }

  selectSuggestion(suggestion) {
    this.default()
    this.setValue(suggestion)
  }

  async onFocus() {
    await this.search()
  }

  templateSuggestion(suggestion) {
    if (!suggestion) { return null }
    const componentType = componentsService.get(suggestion.constructor, 'row')
    return new componentType(suggestion)
  }

  async search() {
    const value = this.input.value
    if (value == this.lastValue) { return }
    this.lastValue = value
    const type = this.state.property.type
    const searchField = type.definition.searchField || 'name'
    this.suggestions = await type.collection.find([
      ...(this.state.filters || []),
      {
        $match: [`$${searchField}`, value]
      }
    ], {
      limit: 3,
    })
    this.suggestionsOpen = true
  }

  async create() {
    this.mode = 'create'
    const form = new ChildModelForm({
      type: this.state.type,
      object: null,
      label: this.label,
    })

    form.addEventListener('saved', ({ model }) => {
      this.value = model
    })
    this.form.show(form)
  }

  onSaved({ model }) {
    this.setValue(model)
  }
}
  .define({
    name: 'model-field',
    template,
  })
  .properties({
    panelModel: 'any',
    mode: 'any',
    suggestions: 'any',
    suggestionsOpen: 'any',
  })


/***/ }),

/***/ 37797:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(49998)
const Array = __webpack_require__(83083)

__webpack_require__(76997)

module.exports = class ModelForm extends Component {
  constructor() {
    super()
    this.forms = new Array()
  }

  target(form) {
    const index = this.forms.indexOf(form)
    if (index === -1) { throw new Error() }

    this.forms.splice(index + 1, this.forms.length - index + 1)
  }

  show(form) {
    this.forms.push(form)
    form.addEventListener('saved', () => {
      this.forms.remove(form)
    })
  }

  onSaved({ model }) {
    console.log('model saved', model)
    this.event('saved', { model })
  }
}
  .define({
    name: 'model-form',
    template,
  })
  .properties({
    model: 'any',
    type: 'any',
    states: 'any',
  })




/***/ }),

/***/ 95858:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const template = __webpack_require__(13698)
const { String, Bool, Model, Object: ObjectType, Number } = __webpack_require__(75314)
const { TextField, BoolField, DateField, NumberField, MarkdownField } = __webpack_require__(33612)
const ModelField = __webpack_require__(49625)
const Field = __webpack_require__(98656)
const Markdown = __webpack_require__(40999)

__webpack_require__(5096)

const typesFieldmapping = [
  [Markdown, MarkdownField],
  [String, TextField],
  [Bool, BoolField],
  [Number, NumberField],
  [Model, ModelField],
]

const getMapping = (type) => {
  return typesFieldmapping.find(([t]) => type === t || type.prototype instanceof t)
}

class ObjectFieldset extends Field {
  static typesFieldmapping = typesFieldmapping

  onInit() {
    this.updateTypes()
    if (!this.state.value && this.state.required) {
      this.create()
    }
    super.onInit()
  }

  create() {
    this.setValue(new (this.types[0])())
  }

  onSelectedTypeChanged(type) {
    if (this.value?.constructor === type) { return }
    let value
    if (type) {
      const oldValue = this.value
      value = new type()
      Object.assign(value, oldValue)
    } else {
      value = null
    }

    this.setValue(value)
  }

  async updateTypes() {
    this.types = this.state.property.type
      .getAllChilds()
      .filter((c) => !c.definition.abstract)
  }

  onValueChanged() {
    if (!this.state.value) {
      this.fields = []
      return
    }
    const type = this.state.property.type
    const properties = type.properties
    this.fields = Object.entries(this.state.states)
      .reduce((acc, [propertyName, state]) => {
        const property = properties.find((p) => p.name === propertyName)
        const mapping = getMapping(property.type)
        const fieldType = mapping[1]
        const field = new fieldType({
          state,
          childForm: this.childForm,
          fieldset: this,
          form: this.form,
        })
        acc[property.name] = field
        field.addEventListener('changed', this.b(this.onFieldChanged))
        return acc
      }, {})
  }

  onFieldChanged() {
    this.touched = true
    this.event('changed', { field: this })
  }

  showErrors() {
    this.touched = true
    Object.values(this.fields)
      .forEach((field) => {
        if (field.showErrors) {
          field.showErrors()
        } else {
          field.touched = true
        }
      })
  }

}

typesFieldmapping.push([ObjectType, ObjectFieldset])

ObjectFieldset
  .define({
    name: 'object-fieldset',
    template,
  })
  .properties({
    types: 'any',
    fields: 'any',
  })

module.exports = ObjectFieldset

/***/ }),

/***/ 17644:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(37551)
const context = __webpack_require__(35293)
const RootObjectState = __webpack_require__(50010)
__webpack_require__(37166)

const applyStates = (targetStates, statesPatch) => {
  console.log({ targetStates, statesPatch })
  Object.entries(statesPatch)
    .forEach(([k, patch]) => {
      const target = targetStates[k]
      console.log({ target, patch })
      Object.assign(target, patch)
      if (patch.states) {
        applyStates(target.states, patch.states)
      }
    })
}

const propagate = (state, patch) => {
  Object.values(state.states)
    .forEach((childState) => {
      Object.assign(childState, patch)
      if (childState.states) {
        propagate(childState, patch)
      }
    })
}

module.exports = class ObjectForm extends Component {
  constructor(values = {}) {
    super()
    Object.assign(this, values)
    this.mode = 'edit'
    this.on('propertyChanged:mode', this.b(this.onModeChanged))
    this.on('propertyChanged:states', this.b(this.updateStates))
  }

  buildState(value) {
    return new RootObjectState({
      property: {
        type: value.constructor,
      },
      value,
      isEdit: !!this.object,
      required: true,
      context,
    })
  }

  async onInit() {
    let value
    if (!this.object) {
      value = new this.type()
    } else {
      value = this.object.shadowClone()
    }
    this.state = this.buildState(value)
  }

  async onReady() {
    if (this.mode === 'read') {
      await this.updateStates()
    }

    await this.onModeChanged()
  }

  async onModeChanged() {
    if (this.mode === 'read') {
      propagate(this.state, { readOnly: this.mode === 'read', required: false })
    } else {
      await this.updateStates()
    }
  }

  handleFieldError() {
    const state = this.state.findFirstStateWithError()
    if (!state) { return false }
    const field = this.fieldset.fields[state.property.name]
    field.focus()
    field.scrollTo()
    return true
  }

  onSubmitSuccess() {

  }

  async onSubmit(e) {
    console.log('submit', e)
    e.stopPropagation()
    e.preventDefault()
    await this.updateStates()
    this.fieldset.showErrors()
    if (this.handleFieldError()) { return }

    const object = this.state.value
    this.event('submit', { object })
    await this.onSubmitSuccess(object)
  }

  async updateStates() {
    await this.state.applyLogics()

    if (this.states) {
      applyStates(this.state.states, this.states)
    }
    await this.state.validate()
  }

  async onFieldsetChanged() {
    await this.updateStates()
  }
}
  .define({
    name: 'object-form',
    template,
  })
  .properties({
    object: 'any',
    type: 'any',
    clone: 'any',
    mode: 'any',
    states: 'any',
  })




/***/ }),

/***/ 4590:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(37797)
__webpack_require__(6138)
__webpack_require__(95858)
__webpack_require__(17644)

/***/ }),

/***/ 51224:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(49492)
__webpack_require__(4590)
const mixer = __webpack_require__(96172)
const { onBindingDestroyed, onBindingGetProperty } = __webpack_require__(11374)
const BaseHoldable = __webpack_require__(30690)

onBindingGetProperty((binding, value) => {
  if (!mixer.is(value, BaseHoldable)) { return }
  if (!binding.holdables) {
    binding.holdables = []
  }
  binding.holdables.push(value)
  value.hold(binding)
})

onBindingDestroyed((binding) => {
  binding.holdables?.forEach((h) => h.release(binding))
})

/***/ }),

/***/ 70650:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Pageable = __webpack_require__(67328)
const { Model } = __webpack_require__(75314)
const LayoutRouter = __webpack_require__(50742)
const { actions } = __webpack_require__(73531)

const types = Model.getAllChilds()
  .filter((t) => mixer.is(t.prototype, Pageable))
  .filter((t) => !t.definition.abstract)

const url = new RegExp(`/(${types.map((t) => t.definition.name).join('|')})/([\\d|\\w]*)`)

const router = new LayoutRouter({
  url,
  layout: (req) => [__webpack_require__.e(/* import() */ 542).then(__webpack_require__.t.bind(__webpack_require__, 37542, 23)), { model: req.model }]
})

router.use(async (req, res, next) => {
  const typeName = req.match[1]
  const code = req.match[2]
  const type = types.find((t) => t.definition.name === typeName)
  const { codeField } = type.definitions.find((d) => d.codeField)
  const model = await type.collection.findByUniqueIndex({
    [codeField]: code
  }, {
    type: type.definition.name,
  })
  if (!model) {
    return res.notFound()
  }
  Object.assign(req, {
    type,
    model
  })
  return next()
})


actions
  .filter((action) => action.url !== undefined)
  .forEach((action) => {
    router.route(action.url, async (req, res, next) => {
      if (action.check && !await action.check(req.model)) {
        return next()
      }

      await action.execute(req, res, next)
    })
  })

module.exports = router

/***/ }),

/***/ 73531:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {


const setup = {
  layout: {
    header: {
      before: []
    }
  },
  types: [],
  actions: [
    {
      name: 'explorer',
      url: '/explorer',
      content: '<i class="fa-solid fa-list"></i>',
      async execute(req, res, next) {
        await res.page(__webpack_require__.e(/* import() */ 467).then(__webpack_require__.t.bind(__webpack_require__, 69467, 23)), req.model)
      }
    },
    {
      name: 'edit',
      url: '/edit',
      content: '<i class="fa-solid fa-pen"></i>',
      async check(model) {
        await model.canUpdate()
        return true
      },
      async execute(req, res, next) {
        await res.page(__webpack_require__.e(/* import() */ 40).then(__webpack_require__.t.bind(__webpack_require__, 53040, 23)), req.model)
      }
    },
    {
      name: 'delete',
      content: '<i class="red fa-solid fa-trash"></i>',
      class: 'warning',
      async check(model) {
        await model.canDelete()
        return true
      },
      async execute(model) {
        await model.delete()
      }
    }
  ]
}

module.exports = setup

/***/ }),

/***/ 47062:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(8522)
const hederaSetup = __webpack_require__(34543)
const Holder = __webpack_require__(84658)
const setup = __webpack_require__(58950)
const StateMixin = __webpack_require__(59928)
setup.state.push(StateMixin)

hederaSetup.base.push(Holder)

module.exports = {
  routing: __webpack_require__(73531)
}

/***/ }),

/***/ 73007:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172);
const Controllers = __webpack_require__(83795)
const ControllerError = __webpack_require__(5145)
const setup = __webpack_require__(58950)

module.exports = mixer.mixin((base) => {
  return class Controlleable extends base {
    static define(definition) {
      super.define(definition)
      this.controllers = new Controllers(this)
      return this
    }

    static async canCreate(context) {
      for (const controller of this.controllers) {
        const check = controller.create?.check
        if (check) {
          await check.call(this, context)
        }
      }
      return true
    }

    async canUpdate(...args) {
      const [context] = setup.getArgs(args)
      for (const controller of this.constructor.controllers) {
        if (controller.update?.check) {
          const result = await controller.update.check.call(this, context, this)
          if (!result) {
            return false
          }
        }
      }
      return true
    }

    async canDelete(...args) {
      const [context] = setup.getArgs(args)
      for (const controller of this.constructor.controllers) {
        if (controller.delete?.check) {
          try {
            await controller.delete.check.call(this, context, this)
          } catch (err) {
            if (err instanceof ControllerError) {
              return false
            }
            throw err
          }
        }
      }
      return true
    }
  }
})



/***/ }),

/***/ 5145:
/***/ (function(module) {


module.exports = class ControllerError extends Error {
  
}

/***/ }),

/***/ 83795:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Tree = __webpack_require__(9948)

module.exports = class Controllers extends Tree {
  constructor(owner) {
    super()
    this.owner = owner
    owner.definition.parents
      .filter((p) => p.controllers)
      .forEach((parent) => {
        this.push(parent.controllers)
      })
  }

  call(...args) {
    super.call(...args)
    return this.owner
  }
}

/***/ }),

/***/ 57106:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Destroyable = __webpack_require__(79330)
const mixer = __webpack_require__(96172)
const Propertiable = __webpack_require__(85020)
const Equalable = __webpack_require__(48774)
const typeKey = '@type'

const ignore = {}

const Buildable = mixer.mixin([Destroyable, Propertiable, Equalable], (base) => {
  return class extends base {

    constructor(values = {}, ...args) {
      super(...args)
      // disable the possibility to assign @type on buildables
      // we don't want to throw an error
      Object.defineProperty(this, '@type', { get() { }, set() { } })
      const undefinedValues = this.constructor.properties.reduce((acc, property) => {
        acc[property.name] = undefined
        return acc
      }, {})
      this.set({
        ...undefinedValues,
        ...values
      })
    }


    static parse(object, options, context) {
      if (object == null || object instanceof this) { return object }
      if (object.constructor?.hasMixin && object.constructor.hasMixin(this)) {
        return object
      }
      const typeName = object[typeKey]
      let type = this
      if (typeName && this.definition.name !== typeName) {
        type = this.findChild((c) => c.definition.name === typeName)
        if (!type) {
          throw new Error(`Type ${typeName} not find from ${this.definition.name}`)
        }
      }

      const instance = new type()
      instance.set(object, options)

      return instance
    }

    equals(object) {
      return this.constructor.equals(this, object)
    }

    static toJSON(value, paths, context) {
      return value && value.toJSON(paths, context)
    }

    setPropertyValue(property, value, options) {
      const parsedValue = property.type.parse(value, options, { owner: this, property })
      if (parsedValue === ignore) { return }

      super.setPropertyValue(property, parsedValue)
    }



    toJSON(paths = {}, context = null) {
      const values = Object.entries(this)
        .reduce((acc, [k, v]) => {
          const property = this.constructor.properties.find((p) => p.name === k)
          if (!property || (property.context !== undefined && property.context !== context)) {
            return acc
          }

          const result = property.type.toJSON(v, paths && paths[property.name] || null, context)
          if (result !== undefined) {
            acc[property.name] = result
          }
          return acc
        }, {})

      return {
        '@type': this.constructor.definition.name,
        ...values,
      }
    }
  }
})

Buildable.ignore = ignore

module.exports = Buildable

/***/ }),

/***/ 4898:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Tree = __webpack_require__(9948)

module.exports = class Indexes extends Tree {
  constructor(owner) {
    super()
    this.owner = owner
    owner.definition.parents
      .filter((p) => p.indexes)
      .forEach((parent) => {
        this.push(parent.indexes)
      })
  }

  call(...args) {
    super.call(...args)
    return this.owner
  }

  push(...args) {
    args.forEach((arg) => {
      if (arg instanceof Indexes) {
        super.push(arg)
        return
      }
      if (typeof arg === 'object') {
        Object.entries(arg)
          .forEach(([name, index]) => {
            if (Array.isArray(index)) {
              index = {
                properties: index,
              }
            }
            index.name = name
            index.owner = this.owner
            super.push(index)
          })
        return
      }
      console.error(arg)
      throw new Error('Index type not recognized')
    })
  }
}

/***/ }),

/***/ 80649:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Equalable = __webpack_require__(48774)
const Indexes = __webpack_require__(4898)

module.exports = mixer.mixin([Equalable], (base) => {
  return class Indexable extends base {
    static define(definition) {
      super.define(definition)
      this.indexes = new Indexes(this)
      return this
    }

    static equals(value1, value2) {
      if (value1.constructor !== value2.constructor) { return false }
      const doesEquals = value1.anyUniqueIndexMatch(value2)
      return doesEquals
    }

    anyUniqueIndexMatch(object) {
      return this.constructor.indexes
        .filter((index) => index.unique)
        .some((index) => {
          const undefinedProperties = index.properties.filter((p) => this[p] === object[p] && this[p] == undefined)
          if (undefinedProperties.length) { return false }
          const matchingProperties = index.properties.filter((p) => this[p] === object[p])
          return matchingProperties.length === index.properties.length
        })
    }

    getFirstUniqueIndex() {
      const indexes = this.constructor.indexes.filter((i) => i.unique)
      for (const index of indexes) {
        const values = this.getIndex(index)
        if (values) {
          return values
        }
      }
      return null
    }

    getIndex(index) {
      if (typeof index === 'string') {
        index = this.constructor.indexes.find((i) => i.name === index)
      }

      if (!index) {
        throw new Error('Index not found')
      }

      const values = index.properties.reduce((acc, p) => {
        const value = this[p]
        if (value != null) {
          acc[p] = this[p]
        }
        return acc
      }, {})

      if (Object.keys(values).length !== index.properties.length) {
        return null
      }
      return values
    }
  }
})



/***/ }),

/***/ 36423:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const loadingQueue = '@loadingQueue'
const stateKey = '@loadState'
const Propertiable = __webpack_require__(85020)
const Bool = __webpack_require__(56482)
const setup = __webpack_require__(58950)

const Loadable = mixer.mixin([Propertiable], (base) => {
  return class extends base {
    constructor(...args) {
      super(...args)

      Object.defineProperties(this, {
        [stateKey]: {
          enumerable: false,
          writable: true,
          value: 'empty'
        },
        [loadingQueue]: {
          enumerable: false,
          writable: true,
          value: []
        }
      })
    }

    setPathsState(state, paths, err) {
      throw new Error('Not implemented')
    }

    setState(state, paths, err, fromSelf, result) {
      this.setPathsState(state, paths, err, fromSelf)


      if (state === this[stateKey] || this[stateKey] === 'loaded') {
        return false
      }
      this.loaded = state === 'loaded'
      this[stateKey] = state
      if (state === 'error') {
        //console.error(err)
        this[loadingQueue].forEach(({ reject }) => reject(err))
        this[loadingQueue] = null
      } else if (state === 'loaded') {
        this[loadingQueue].forEach(({ resolve }) => resolve(result))
        this[loadingQueue] = null
      }

      return true
    }

    setLoadState(...args) {
      this.setState('loaded', ...args)
    }

    innerLoad() {
      throw new Error(`innerLoad not implemented on ${this.constructor.definition.name}`)
    }

    async loadAssociation(context, propertyName, paths) {
      throw new Error('Load association not implemented')
    }

    async load(...args) {
      const [context, paths = {}] = setup.getArgs(args)
      const state = this[stateKey]
      if (state === 'error') {
        this.state = 'empty'
      }
      //console.warn('load', this, state, paths )
      if (state === 'loaded') {
        if (paths === true) { return }
        for (const [propertyName, subPaths] of Object.entries(paths)) {
          await this.loadAssociation(context, propertyName, subPaths)
        }
        return
      }
      else if (state === 'loading') {
        const promise = new Promise((resolve, reject) => {
          this[loadingQueue].push({ resolve, reject })
        })
        await promise
        // make sure the paths are loaded
        await this.load(context, paths)
      } else if (state === 'empty') {
        this.setState('loading', paths, null, true)
        try {
          const result = await this.innerLoad(context, paths)
          //console.log(this, 'loaded')
          this.setState('loaded', paths, null, true, result)
          return result
        } catch (err) {
          this.setState('error', paths, err, null, true)
          throw err
        }
      } else {
        throw new Error(`Unknown state ${state} on ${this.toString()}`)
      }
    }

    unload() {
      this.setState('empty')
    }
  }
})
  .define()
  .properties({
    loaded: {
      type: Bool,
      context: false,
    }
  })

Loadable.stateSymbol = stateKey
Object.assign(Loadable, {
  loadingQueue,
  stateKey,
})
module.exports = Loadable

/***/ }),

/***/ 41798:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Tree = __webpack_require__(9948)
module.exports = class Methods extends Tree {
  constructor(owner) {
    super()
    this.owner = owner
    owner.definition.parents
      .filter((p) => p.methods)
      .forEach((parent) => {
        this.push(parent.methods)
      })
  }

  call(...args) {
    super.call(...args)
    return this.owner
  }

  push(...args) {
    args.forEach((arg) => {
      if (arg instanceof Methods) {
        super.push(arg)
        return
      }
      if (typeof arg === 'object') {
        Object.entries(arg)
          .forEach(([name, method]) => {
            if (Array.isArray(method)) {
              method = {
                args: method[0],
                returnType: method[1],
              }
            }
            method.args = method.args.map((arg) => {
              if (arg.type) {
                return arg
              }
              return {
                type: arg,
              }
            })
            method.name = name
            super.push(method)
          })
        return
      }
      throw new Error('Method type not recognized')
    })
  }
}

/***/ }),

/***/ 71633:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Methods = __webpack_require__(41798)

module.exports = mixer.mixin((base) => {
  return class Methodable extends base {
    static define(definition) {
      super.define(definition)
      this.methods = new Methods(this)
      return this
    }
  }
})



/***/ }),

/***/ 67328:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Propertiable = __webpack_require__(85020)
const mixer = __webpack_require__(96172)
const { String } = __webpack_require__(75314)
const proto = __webpack_require__(52208)


module.exports = mixer.mixin([Propertiable], (baseClass) => {
  return class Pageable extends baseClass {

    updateUrl(codeField) {
      const { name } = this.constructor.definition
      const code = this[codeField]
      this.url = code ? `/${name}/${code}` : null
    }

    updateTitle(titleField){
      this.title = this[titleField]
    }

    propertyChanged(property, ...args) {
      const { codeField } = this.constructor.definitions.find((d) => d.codeField)
      const { titleField } = this.constructor.definitions.find((d) => d.titleField)
      if (property.name === codeField) {
        this.updateUrl(codeField)
      }
      if (property.name === titleField) {
        this.updateTitle(titleField)
      }
      return super.propertyChanged(property, ...args)
    }

  }
})
  .define({
    codeField: 'code',
    searchField: 'name',
    titleField: 'name',
  })
  .properties({
    url: {
      type: String,
      context: false,
      state: {
        disabled: true,
      }
    },
    title: {
      type: String,
      context: false,
      state: {
        disabled: true,
      }
    }
  })

/***/ }),

/***/ 79338:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const caches = []

const mixin = mixer.mixin((base) => {
  return class Templateable extends base {
    static getLastTemplate() {
      let type = this
      while (type) {
        const template = type.definition.template
        if (template) {
          return template
        }
        type = type.definition.parent
      }
      return null
    }

    static define(definition) {
      super.define(definition)
      if (definition?.template && !(this.prototype instanceof mixin.Template)) {
        const lastTemplate = this.definition.parent?.getLastTemplate()
        const template = definition.template
        if (mixer.is(template.prototype, mixin.Template)) {
          if (lastTemplate) {
            throw new Error('Invalid template')
          }
        }
        else {
          if (
            !lastTemplate
            || !mixer.is(lastTemplate.prototype, mixin.Template)
            || !mixer.is(template.prototype, lastTemplate.definition.template)
          ) {
            //console.log(lastTemplate, template, template instanceof lastTemplate, template.prototype instanceof lastTemplate.definition.template)
            console.error(lastTemplate.name, template.name)
            throw new Error('Invalid template')
          }
        }
      }
      return this
    }

    static of(template) {
      let cache = caches.find((c) => c.template === template && c.base === this)
      if (!cache) {
        const name = `${this.name}<${template.definition.name}>`
        const c = class extends this { }
        Object.defineProperty(c, 'name', { value: name })
        c.define({
          name: c.name,
          template,
        })
        cache = {
          template,
          base: this,
          class: c,
        }
        caches.push(cache)
      }

      return cache.class
    }

  }
})

module.exports = mixin

/***/ }),

/***/ 56608:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {


const proto = __webpack_require__(52208)
const mixer = __webpack_require__(96172)
const Global = __webpack_require__(30185)
const Any = __webpack_require__(18557)


module.exports = class Scope {
  constructor(values) {
    Object.assign(this, values)
    this.variables = {
      __proto__: (this.parent?.variables || {})
    }
  }

  get root() {
    return this.parent?.root || this
  }

  getHandlers(type) {
    const typeHandlers = proto.get(type)
      .flatMap((t) => {
        const tHandlers = this.root.handlers.filter((h) => {
          return h.for === t || t.dependencies && t.dependencies?.indexOf(h.for) !== -1
        })
        return tHandlers
      })
    return typeHandlers
  }

  child() {
    const child = new this.constructor({
      parent: this,
    })
    return child
  }

  async process(body) {
    const source = await this.processFunctionCall({
      $and: body
    })
    return source.value
  }

  async processFunctionCall(functionCall) {

    let methodName = Object.keys(functionCall)[0]
    if (!methodName.startsWith('$')) {
      throw new Error()
    }

    const callObject = functionCall[methodName]
    methodName = methodName.substring(1)

    let source
    let argsObjects
    const globalMethod = Global.methods.find((m) => m.name === methodName)
    if (globalMethod) {
      source = {
        type: Global,
      }
      argsObjects = callObject
    } else {
      const [sourceObject, ...temp] = callObject
      argsObjects = temp
      source = await this.processObject(sourceObject)
    }

    const method = source.type.methods.find((m) => m.name === methodName)
    if (!method) {
      console.error(source.type)
      throw new Error(`Method '${methodName}' on type ${source.type.definition.name} not found`)
    }

    const args = await this.getArgs(source, method, argsObjects)
    if ((method.args.length < args.length - (globalMethod ? 0 : 1))) {
      throw new Error('Too many args')
    }

    const value = await this.callFunction(source, method, args.map((a) => a.value))
    if (value === undefined) {
      throw new Error('Value not found')
    }

    if (!method.returnType.getType) {
      console.error(method)
      throw new Error('returnType.getType missing')
    }

    const returnType = method.returnType.getType(source.type, args)
    if (!returnType) {
      console.log(method.name, args, returnType)
      throw new Error('Return type missing')
    }

    return {
      sourceType: 'functionCall',
      owner: source,
      method,
      value,
      scope: this,
      args,
      type: returnType
    }
  }

  async getArgs(source, method, argsObjects) {
    const args = []
    for (let i = 0; i < argsObjects.length; i++) {
      const definition = method.args[i]
      if (!definition) {
        throw new Error(`Cannot parse arg from method ${method.name} at index ${i}`)
      }
      let object = argsObjects[i]
      if (definition.spread) {

        object = argsObjects.slice(i)
        i = argsObjects.length
      }
      const arg = await this.processObject(object, {
        source,
        definition,
      })
      args.push(arg)
    }
    return args
  }

  async parse(object, context) {
    const type = context.definition.type.getType(context.source.type)
    const handler = this.getParseHandler(object, type)
    if (!handler) {
      console.log(object)
      throw new Error(`Could not find handler for parse on type ${type.definition.name} ${object}`)
    }
    const result = await handler.parse(this, object, context)
    return result
  }

  async callFunction(source, method, args = []) {
    const handlers = this.getHandlers(source.type)
    const handler = handlers.find((h) => h.methods[method.name])
    if (!handler) {
      throw new Error(`No handler found for method '${method.name}' on type ${source.type.definition.name}`)
    }
    //console.log('calling', source.value, method.name, handlers.map((h) => h.methods))
    args = [source.type === Global ? this : source, ...args]
    const result = await handler.methods[method.name](...args)
    return result
  }

  getParseHandler(object, type) {
    const typeHandlers = this.getHandlers(type)
    const typeHandler = typeHandlers.find((h) => h.parse)
    if (typeHandler) {
      return typeHandler
    }

    for (const handler of this.root.handlers) {
      if (mixer.is(handler.for.prototype, type) && handler.check && handler.check(object)) {
        const typeHandlers = this.getHandlers(handler.for)
        const typeHandler = typeHandlers.find((h) => h.parse)
        return typeHandler
      }
    }

    return null
  }

  async processObject(object, context) {
    if (object === null) {
      return {
        type: Any,
        value: null,
      }
    }
    //console.log("processObject", JSON.stringify(object, null, ' '))
    if (typeof object === 'string' && object.startsWith('$')) {
      const path = object.substring(1)
      const [sourceName, ...propertiesNames] = path.split('.')
      //console.log('getting variable', sourceName, propertiesNames, scope)
      let source = this.variables[sourceName]

      if (!source) {
        source = await this.getProperty(this.variables.this, sourceName)
      }

      if (!source) {
        throw new Error(`Source ${sourceName} not found`)
      }

      for (const propertyName of propertiesNames) {
        source = await this.getProperty(source, propertyName)
      }

      return source
    }

    if (typeof object === 'object' && !Array.isArray(object)) {
      const keys = Object.keys(object)
      if (keys.length === 1 && keys[0].startsWith('$')) {
        return await this.processFunctionCall(object)
      }
    }

    if (context) {
      const source = await this.parse(object, context)
      if (source) {
        return source
      }
    }

    console.error(object, context)
    throw new Error('Could not process object')
  }


  async getProperty(source, propertyName) {
    const property = source.type.properties.find((p) => p.name === propertyName)

    if (!property) {
      console.log(source.type)
      throw new Error(`Property ${propertyName} on type ${source.type.definition.name} not found`)
    }
    if (property.context && property.context !== 'mongo') {
      throw new Error(`Property ${property.name} can only be used in context ${property.context}`)
    }

    const value = await this.innerGetProperty(property, source)
    const any = {
      sourceType: 'property',
      owner: source,
      scope: this,
      name: propertyName,
      value,
      type: property.type,
    }

    await this.onGetProperty(property, any)
    return any
  }

  async innerGetProperty(property, source) {
    throw new Error('Not implemented')
  }

  onGetProperty() { }

  clone() {
    return new this.constructor({
      ...this,
      parent: null,
    })
  }
}

/***/ }),

/***/ 22469:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const MemoryScope = __webpack_require__(93701)

const map = {
  filter: async (scope, filter) => {
    const match = await scope.process(filter)
    return match
  }
}

const match = async (context, object, stages) => {
  if (object.load) {
    await object.load()
  }
  const scope = new MemoryScope({
    context,
  })

  scope.variables = {
    this: {
      sourceType: 'var',
      name: 'this',
      value: object,
      type: object.constructor,
    }
  }

  for (const stage of stages) {
    const stageType = Object.keys(stage)[0]
    const stageMatch = await map[stageType](scope, stage[stageType])
    if (!stageMatch) {
      return false
    }
  }
  return true
}

module.exports = {
  match
}

/***/ }),

/***/ 93701:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Scope = __webpack_require__(56608)
const handlers = __webpack_require__(52952)
const mixer = __webpack_require__(96172)
const Loadable = __webpack_require__(36423)

class MemoryScope extends Scope {
  constructor(...args) {
    super(...args)
    this.handlers = handlers
  }
  async load(path) {
    await this.variables.this.value.load(path)
  }

  async innerGetProperty(property, source) {
    const value = source.value[property.name]
    if (mixer.is(property.type.prototype, Loadable)) {
      await source.value.load()
    }
    return value
  }
}

module.exports = MemoryScope

/***/ }),

/***/ 23793:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Array = __webpack_require__(24633)
const mixer = __webpack_require__(96172)

module.exports = {
  for: Array,
  async parse(scope, objects, context) {
    const template = context.definition.type.getLastTemplate()
    const values = await Promise.all(
      objects.map(async (object) => {
        const result = await scope.processObject(object)
        if (!mixer.is(result.type.prototype, template)) {
          throw new Error()
        }
        return result.value
      })
    )
    return {
      value: values,
    }
  },
  methods: {
    async find(source, fn) {
      for (const object of objects) {
        if (await fn(object)) {
          return object
        }
      }
      return null
    },
    async some(source, fn) {
      const object = await this.find(source, fn)
      return !!object
    }
  }
}

/***/ }),

/***/ 96601:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Bool = __webpack_require__(56482)

module.exports = {
  for: Bool,
  check(value) {
    return typeof value === 'boolean'
  },
}

/***/ }),

/***/ 71300:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Branch = __webpack_require__(37963)

module.exports = {
  for: Branch,
  methods: {},
  getType: (type) => type.template,
}


/***/ }),

/***/ 88356:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Function = __webpack_require__(94971)

module.exports = {
  for: Function,
  async parse(scope, object, context) {
    if (!Array.isArray(object)) {
      throw new Error('not ok')
    }
    let [args, body] = object
    const fn = {
      scope,
      source: context.source,
      value: null,
    }

    fn.value = async (...argsValues) => {
      const child = scope.child()
      args.forEach((arg, i) => {
        const type = context.definition.args[i].getType(context.source.type)
        const argObject = {
          value: argsValues[i],
          name: arg,
          sourceType: 'arg',
          function: fn,
          type,
        }
        child.variables[arg] = argObject
        return argObject
      })

      const value = await child.process(body)
      return value
    }
   
    return fn
  }
}

/***/ }),

/***/ 64397:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Global = __webpack_require__(30185)

module.exports = {
  for: Global,
  methods: {
    and(scope, args = []) {
      for (const arg of args) {
        if (!arg) { return false }
      }
      return true
    },
    or(scope, args = []) {
      for (const arg of args) {
        if (arg) { return true }
      }
      return false
    },
    if(scope, $if, $then, $else) {
      if ($if) {
        return $then
      } else {
        return $else
      }
    }
  }
}

/***/ }),

/***/ 73757:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const HasMany = __webpack_require__(27315)

module.exports = {
  for: HasMany,
  methods: {},
  getType: (type) => type.template,
}


/***/ }),

/***/ 52952:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {


module.exports = [
  __webpack_require__(64397),
  __webpack_require__(71839),
  __webpack_require__(77048),
  __webpack_require__(96601),
  __webpack_require__(15278),
  __webpack_require__(51012),
  __webpack_require__(88356),
  __webpack_require__(23793),
  __webpack_require__(71300),
  __webpack_require__(87830),
  __webpack_require__(21783),
  __webpack_require__(73757),
]


/***/ }),

/***/ 87830:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Model = __webpack_require__(57979)

module.exports = {
  for: Model,
  methods: {
    eq({ value }, other) {
      return value.equals(other)
    },
  },
  parse(scope, value, context) {
    let model
    if (typeof value === 'string') {
      model = {
        _id: value,
      }
    } else {
      model = value
    }

    const type = context.definition.type.getType(context.source.type)
    if (!(model instanceof type)) {
      model = new type(model)
    }

    return {
      scope,
      value: model,
    }
  },
  getType: (type) => type,
}


/***/ }),

/***/ 51012:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Object = __webpack_require__(55405)

module.exports = {
  for: Object,
  methods: {
    is(source, type) {
      return source.value instanceof type
    },
  }
}

/***/ }),

/***/ 77048:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Primitive = __webpack_require__(79360)

module.exports = {
  for: Primitive,
  methods: {
    eq({ value }, other) {
      return value === other
    },
    neq({ value }, other) {
      return value !== other
    },
  },
  parse(scope, value) {
    return {
      scope,
      value,
    }
  }
}

/***/ }),

/***/ 71839:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Real } = __webpack_require__(75314)

module.exports = {
  for: Real,
  methods: {
    not({ value }) {
      return !value
    }
  }
}

/***/ }),

/***/ 15278:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { String } = __webpack_require__(75314)

module.exports = {
  for: String,
  methods: {
    match({ value }, regex) {
      const regExp = new RegExp(regex.toUpperCase())
      return value.toUpperCase().match(regExp)
    },
    toUpperCase({ value }) {
      return value.toUpperCase()
    },
  },
  parse(scope, value) {
    if (typeof value !== 'string') {
      throw new Error()
    }
    return {
      scope,
      value,
    }
  }
}

/***/ }),

/***/ 21783:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Object, Type } = __webpack_require__(75314)

const authorizedTypes = [Object]

const findType = (typeName) => {
  for (const type of authorizedTypes) {
    const types = type.getAllChilds()
    const childType = types.find((t) => t.definition.name === typeName)
    if (childType) {
      return childType
    }
  }
  return null
}

module.exports = {
  for: Type,
  parse(scope, value) {
    const type = findType(value)

    if (!type) {
      throw new Error()
    }


    return {
      scope,
      value: type,
    }
  }
}

/***/ }),

/***/ 75095:
/***/ (function(module) {

const objectToFilter = (object) => {
  return Object.entries(object)
    .map(([k, v]) => {
      return {
        $eq: [`$${k}`, v]
      }
    })
}

module.exports = {
  objectToFilter,
}

/***/ }),

/***/ 58950:
/***/ (function(module) {

const setup = {
  object: {
    before: [],
  },
  baseModels: {
    before: []
  },
  model: {
    before: [],
    after: []
  },
  arrayAssociation: {
    before: [],
    after: [],
  },
  models: {
    before: []
  },
  hasMany: {
    before: [],
  },
  ownMany: {
    before: []
  },
  queryResult: {
    before: [],
  },
  state: [],
  getArgs: (args) => args
}

module.exports = setup


/***/ }),

/***/ 56914:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const State = __webpack_require__(48522)
const { match } = __webpack_require__(22469)

module.exports = class ModelState extends State {

  reset(){
    super.reset()
    this.filters = []
  }

  async validate() {
    super.validate()
    if (!this.value || !this.filters.length) { return }
    await this.value.load()
    const doesMatch = await match(this.root.context, this.value, this.filters)
    if (!doesMatch) {
      this.errors.push('Value is not matching filters')
    }
  }
}
  .define()

/***/ }),

/***/ 94322:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const State = __webpack_require__(48522)
const { ArrayAssociation, Model, Primitive } = __webpack_require__(75314)
const ModelState = __webpack_require__(56914)
const ignore = ['_id']

const getState = (context, property) => {
  return (typeof property.state === 'function') ? property.state(context, property) : property.state || {}
}

const mapping = [
  [Primitive, State],
  [Model, ModelState],
]

const getStateType = (type) => {
  const pair = mapping.find(([t]) => t === type || type.prototype instanceof t)
  if (!pair) { return null }
  return pair[1]
}

class ObjectState extends State {
  constructor(values) {
    super(values)
    this.on('propertyChanged:value', this.b(this.onValueChanged))
    this.onValueChanged()
    this.updateStates()
  }

  onValueChanged() {
    if (!this.value) { return }
    this.on(this.value, 'propertyChanged', this.b(this.onChildValueChanged))
  }

  reset() {
    super.reset()

    Object.values(this.states).forEach((s) => {
      s.reset()
      const state = getState({ property: this.property }, s.property)
      Object.assign(s, state)
    })
  }

  async onChildValueChanged(property, value) {
    const state = this.states[property.name]
    if (!state) { return }
    await state.valueChanged()
  }

  updateStates() {
    this.states = this.property.type.properties
      .filter((p) => {
        const shouldIgnore = ignore.indexOf(p.name) !== -1
        if (shouldIgnore) { return false }
        if (p.context === false) { return false }
        if (p.type.prototype instanceof ArrayAssociation) { return false }
        return true
      })
      .reduce((acc, property) => {
        const stateType = getStateType(property.type)
        acc[property.name] = new stateType({
          objectState: this,
          property,
          root: this.root || this,
        })
        return acc
      }, {})
  }

  async validate() {
    await super.validate()
    for (const state of Object.values(this.states)) {
      await state.validate()
    }
  }

  findFirstStateWithError() {
    for (const state of Object.values(this.states)) {
      if (state.errors.length) { return state }
      if (state.states) {
        const subState = state.findFirstStateWithError()
        if (subState) { return subState }
      }
    }
    return null
  }


}
ObjectState
  .define()

mapping.push([Object, ObjectState])

module.exports = ObjectState

/***/ }),

/***/ 57880:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const RootObjectState = __webpack_require__(50010)

module.exports = class RootModelState extends RootObjectState {

  async validate() {
    await super.validate()
    const type = this.property.type
    for (const index of type.indexes) {
      if (index.build === false || !index.unique) { continue }

      const values = index.properties.reduce((acc, propertyName) => {
        acc[propertyName] = this.states[propertyName].value
        return acc
      }, {})

      const filledValues = Object.values(values).filter((v) => v != null)
      if (filledValues.length !== index.properties.length) {
        continue
      }
      const filters = Object.entries(values)
        .map(([k, v]) => ({
          $eq: [`$${k}`, v]
        }))

      if (this.value._id) {
        filters.push({
          $neq: ['$_id', this.value._id]
        })
      }

      const existingModel = await type.collection.findOne(this.context, filters, {
        type: index.owner.definition.name,
      })

      if (!existingModel) { continue }

      if (index.properties.length > 1) {
        this.errors.push(`Values combinaison on fields '${index.properties.join(', ')}' is not available`)
      } else {
        const state = this.states[index.properties[0]]
        state.errors.push('This value is already taken')
      }
    }
  }
}
  .define()

/***/ }),

/***/ 50010:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const ObjectState = __webpack_require__(94322)

module.exports = class RootObjectState extends ObjectState {
  reset() {
    super.reset()
    this.required = true
  }

  async applyLogics() {
    this.reset()
    for (const controller of this.property.type.controllers) {
      const logic = !this.isEdit ? controller.create?.logic : controller.update?.logic
      if (logic) {
        await logic(this.context, this.states, this.oldValue)
      }
    }
  }
}
  .define()

/***/ }),

/***/ 48522:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Array = __webpack_require__(83083)
const Propertiable = __webpack_require__(85020)
const mixer = __webpack_require__(96172)
const Bindeable = __webpack_require__(59222)
const setup = __webpack_require__(58950)

class BaseState extends mixer.extends([Propertiable, Bindeable]) {
  constructor(values) {
    super()
    Object.assign(this, values)
  }

  reset() {
    this.errors = new Array()
    this.message = new Array()
    this.disabled = false
    this.required = false
  }

  async valueChanged() {
    await this.propertyChanged({
      name: 'value',
    }, this.value)
  }

  set value(value) {
    if (this.objectState) {
      this.objectState.value[this.property.name] = value
    } else {
      this._value = value
      this.valueChanged()
    }
  }

  get value() {
    if (this.objectState) {
      const parent = this.objectState.value
      return parent && parent[this.property.name]
    } else {
      return this._value
    }
  }

  validate() {
    if (this.disabled) {
      this.value = undefined
      return
    }
    if (this.required && !this.value) {
      this.errors.push('This field is required')
    }
  }
}

BaseState
  .define()
  .properties({
    disabled: 'any',
    required: 'any',
    messages: 'any',
    errors: 'any',
  })

module.exports = class State extends mixer.extends(BaseState, setup.state) { }
  .define()


/***/ }),

/***/ 18557:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Propertiable = __webpack_require__(85020)

const Buildable = __webpack_require__(57106)
const Templateable = __webpack_require__(79338)
const Methodable = __webpack_require__(71633)
const utils = __webpack_require__(21129)

module.exports = mixer.mixin([Propertiable, Methodable, Buildable, Templateable], (base) => {
  return class Any extends base {
    static getType(type) {
      return this
    }


    static sanitizeProperty(property) {
      utils.propertySanitizers.forEach((sanitizer) => sanitizer(property))
    }
  }
})
  .define({
    name: 'any',
  })


/***/ }),

/***/ 24633:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const BaseArray = __webpack_require__(83083)
const mixer = __webpack_require__(96172)
const utils = __webpack_require__(21129)
const Any = __webpack_require__(18557)
const Function = __webpack_require__(94971)
const Template = __webpack_require__(18202)

const template = Template.of(Any)

const fnArg = { type: Function, args: [template] }

class Array extends mixer.extends(BaseArray, [Any]) {

  static template = template
  static fnArg = fnArg

  constructor(...values) {
    super({}, ...values)
  }

  static parse(object, owner, property) {
    if (!object) {
      return object
    }
    let array
    if (object instanceof this) {
      array = object
    } else {
      array = new this()
      array.push(...object)
    }

    return array
  }

  static equals(a1, a2) {
    if (a1.constructor !== a2.constructor) { return false }
    if (a1.length !== a2.length) { return false }

    const type = this.definition.template
    for (let i = 0; i < a1.length; i++) {
      if (!type.equals(a1[i], a2[i])) {
        return false
      }
    }
    return true
  }

  toJSON(paths, context) {
    const result = [...this].map((object) => {
      return this.constructor.definition.template.toJSON(object, paths, context)
    })
    return result
  }

  destroy() {
    this.splice(0, this.length)
    super.destroy()
  }
}

Array
  .define({
    name: 'array',
    template,
  })
  .methods({
    find: [[fnArg], template],
  })


utils.propertySanitizers.push((property) => {
  if (!Array.isArray(property.type)) { return }
  property.type = Array.of(property.type[0])
})

module.exports = Array



/***/ }),

/***/ 78702:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Loadable = __webpack_require__(36423)
const mixer = __webpack_require__(96172)
const Bindeable = __webpack_require__(59222)
const Abstractable = __webpack_require__(3764)
const setup = __webpack_require__(58950)
const BaseModels = __webpack_require__(91018)
const config = setup.arrayAssociation

class BaseArrayAssociation extends mixer.extends(BaseModels, [Abstractable, Loadable, Bindeable, ...config.before]) {
  constructor(owner, property) {
    /*
  * when using native array functions like map, filter etc, it will return an instance of the current array class, which branch in this case
  * So we add this check to return a native array if the required parameters are not passed
*/
    if (!owner || !property) {
      return []
    }
    super()
    this.owner = owner
    this.property = property
    this.owner.on('destroyed', this.b(this.destroy))
  }

  toString() {
    return `${this.property.name} of ${this.owner.toString()}`
  }

  static parse(array, options, { owner, property }) {
    let instance = owner[property.name]
    if (!instance) {
      instance = new this(owner, property)
    }

    if (array) {
      instance.splice(0, instance.length)
      instance.push(...array.map((o) =>
        this.definition.template.parse(o, options, { property: this.property, owner: this }))
      )
    }

    return instance
  }

  setPathsState(state, paths, err) {
    for (const object of this) {
      object.setState(state, paths, err)
    }
  }

  async loadAssociation(context, property, paths) {
    for (const model of this) {
      await model[property].load(context, paths)
    }
  }

  unload() {
    instance.splice(0, instance.length)
    super.unload()
  }
}

BaseArrayAssociation.define()

module.exports = class ArrayAssociation extends mixer.extends(BaseArrayAssociation, config.after) {

}
  .define({
    name: 'hasMany',
    abstract: true,
  })

/***/ }),

/***/ 91018:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Array = __webpack_require__(24633)
const setup = __webpack_require__(58950)
const mixer = __webpack_require__(96172)
const Destroyable = __webpack_require__(79330)

module.exports = class BaseModels extends mixer.extends(Array, [Destroyable, ...setup.baseModels.before]) {

}
  .define()


/***/ }),

/***/ 56482:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Primitive = __webpack_require__(79360)
const Real = __webpack_require__(24782)
const This = __webpack_require__(56907)
const Array = __webpack_require__(24633)

class Bool extends Primitive {
  static parse(value, owner, property) {
    if (value == null) { return value }
    if (typeof value !== 'boolean') {
      throw new Error(`Property ${property.name} has to be a bool, received ${value}`)
    }
    return super.parse(value, owner, property)
  }
}

Bool.define({
  name: 'bool',
})

Real.methods({
  not: [[], Bool]
})

Primitive.methods({
  eq: [[This], Bool],
  neq: [[This], Bool],
  in: [[Array.of(This)], Bool]
})

Array.methods({
  has: [[Array.template], Bool],
  some: [[Array.fnArg], Bool],
})


module.exports = Bool

/***/ }),

/***/ 37963:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const VirtualArrayAssociation = __webpack_require__(21292)

module.exports = class Branch extends VirtualArrayAssociation {
  async innerLoad(context, paths) {
    await this.owner.load(context)
    await this.update(context, paths)
  }

  resetListeners() {
    if (this.listeners) {
      this.listeners.forEach((l) => l.remove())
    }
    this.listeners = []
  }

  setPathsState(state, paths, err, fromSelf = false) {
    if (fromSelf) { return }
    //console.warn('branch set state', this, state, paths, fromSelf)
    const { name, on } = this.property
    const parent = this.owner[on]
    if (parent) {
      parent.setState(state, {
        ...paths,
        [name]: paths
      }, err)
    }
  }


  async update(context, paths = true) {
    this.resetListeners()
    const {
      on,
      name
    } = this.property
    let current = this.owner[on]
    if (!current) {
      return
    }

    await current.load({
      ...paths,
      [name]: paths
    })


    this.splice(0, this.length)
    while (current) {
      this.push(current)
      current = current[on]
    }

    this.listeners = this.map((object) => object.on(`propertyChanged:${on}`, async () => {
      await this.update(context, paths)
    }))
  }

  onModelUpdated() {

  }

  destroy() {
    super.destroy()
    this.resetListeners()
  }
}
  .define({
    name: 'branch',
  })

/***/ }),

/***/ 30748:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(54719)

module.exports = class Dynamic extends Virtual {
  static getType(sourceType, args) {
    return this.fn(args)
  }

  static with(fn) {
    const type = class extends this { }
    type.fn = fn
    return type
  }
}
  .define({
    name: 'Dynamic',
  })




/***/ }),

/***/ 94971:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(54719)

module.exports = class Function extends Virtual {

}
  .define({
    name: 'function',
  }) 

/***/ }),

/***/ 30185:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(54719)
const Bool = __webpack_require__(56482)
const Any = __webpack_require__(18557)
const Array = __webpack_require__(24633)
const Dynamic = __webpack_require__(30748)
const Map = __webpack_require__(35040)
const Scope = __webpack_require__(93178)
const { getCommonAncestor } = __webpack_require__(52208)

class Global extends Virtual {
  getType() {
    return this
  }
}

Global
  .define({
    name: 'Global',
  })
  .methods({
    and: [[{ type: Array.of(Any), spread: true }], Bool],
    or: [[{ type: Array.of(Any), spread: true }], Bool],
    if: [[Any, Any, Any], Dynamic.with((args) => getCommonAncestor(args[1].type, args[2].type))],
    let: [[Map.of(Any), Scope], Dynamic.with((args) => args[1].type)]
  })

module.exports = Global

/***/ }),

/***/ 27315:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const { hasMany } = __webpack_require__(58950)
const VirtualArrayAssociation = __webpack_require__(21292)
module.exports = class HasMany extends mixer.extends(VirtualArrayAssociation, hasMany.before) {

  async innerLoad(context, paths = {}) {
    await this.owner.load(context)
    const type = this.constructor.definition.template
    const { on } = this.property
    const targetProperty = type.properties.find((p) => p.name === on)
    let filter
    if (targetProperty.type.prototype instanceof Array) {
      filter = [
        {
          $some: [`$${on}`, [['model'], [{
            $eq: ['$model', this.owner._id]
          }]]]
        }
      ]
    } else {
      const path = `$${on}._id`
      filter = [{
        $eq: [path, this.owner._id]
      }]
    }
    const models = await type.collection.find(context, filter, {
      load: paths,
    })
    this.push(...models)
  }
}
  .define({
    name: 'hasMany',
  })

/***/ }),

/***/ 35040:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Real = __webpack_require__(24782)
const Template = __webpack_require__(18202)
const Any = __webpack_require__(18557)
const template = Template.of(Any)

module.exports = class Map extends Real {

}
  .define({
    name: 'Map',
    template,
  })


/***/ }),

/***/ 40999:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const String = __webpack_require__(14037)

module.exports = class Markdown extends String {

}
  .define({
    name: 'markdown',
  })

/***/ }),

/***/ 57979:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const ObjectType = __webpack_require__(55405)
const Loadable = __webpack_require__(36423)
const setup = __webpack_require__(58950)
const ModelMixin = __webpack_require__(48212)
const { objectToFilter } = __webpack_require__(75095)
const config = setup.model

class BaseModel extends mixer.extends(ObjectType, [ModelMixin, Loadable, ...config.before]) {

  setPathsState(state, paths, err) {
    for (const [propertyName, value] of Object.entries(paths)) {
      if (value && this[propertyName]) {
        this[propertyName].setState(state, value, err)
      }
    }
  }

  handleLoadResult(model) {
    if(model){
      Object.assign(this, model)
    } else {
      console.warn(`Model ${this.constructor.definition.name} not found`, this._id)
    }
  }

  async loadAssociation(context, propertyName, paths) {
    if (!this[propertyName]) {
      return
    }

    await this[propertyName].load(context, paths)
  }

  async innerLoad(context, paths) {
    const index = this.getFirstUniqueIndex()
    if (!index) {
      console.error(this)
      throw new Error('Could not load')
    }

    const result = await this.constructor.collection.findOne(
      context,
      objectToFilter(index)
      , {
        type: this.constructor.definition.name,
        load: paths,
      })

    return this.handleLoadResult(result)
  }

  // we do nothing ..
  unload() {

  }

  toJSON(paths = {}, context) {
    if (paths) {
      return super.toJSON(paths, context)
    }
    const index = this.getIndex('id')
    if (!index) {
      console.error(this.toJSON())
      throw new Error('Could not toJSON')
    }
    return {
      '@type': this.constructor.definition.name,
      ...index,
    }
  }

  toString() {
    return this._id
  }
}

BaseModel.define()

class Model extends mixer.extends(BaseModel, [ModelMixin, ...config.after]) {

}

module.exports = Model
  .define({
    name: 'model',
    abstract: true,
  })

/***/ }),

/***/ 48212:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Bool = __webpack_require__(56482)
const String = __webpack_require__(14037)
const This = __webpack_require__(56907)
const Any = __webpack_require__(18557)
const Indexable = __webpack_require__(80649)

module.exports = mixer.mixin([Any, Indexable], (base) => {
  return class ModelMixin extends base { }
})
  .define({
    name: 'modelMixin',
  })
  .indexes({
    id: {
      properties: ['_id'],
      unique: true,
      build: false,
    }
  })
  .properties({
    _id: {
      type: String,
      state: {
        disabled: true,
      }
    },
  })
  .methods({
    eq: [[This], Bool]
  })

/***/ }),

/***/ 76953:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const setup = __webpack_require__(58950)
const mixer = __webpack_require__(96172)
const Destroyable = __webpack_require__(79330)
const BaseModels = __webpack_require__(91018)

module.exports = class Models extends mixer.extends(BaseModels, [Destroyable, ...setup.models.before]) {

}
  .define()


/***/ }),

/***/ 85779:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Primitive = __webpack_require__(79360)
const utils = __webpack_require__(21129)

class Number extends Primitive {
  static parse(value, owner, property) {
    if (value == null) { return value }
    if (typeof value !== 'number') {
      throw new Error(`Property ${property.name} has to be a number, received ${value}`)
    }
    return super.parse(value, owner, property)
  }
}

module.exports = Number

/***/ }),

/***/ 55405:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Type = __webpack_require__(50042)
const Bool = __webpack_require__(56482)
const Real = __webpack_require__(24782)
const NativeObject = Object

const setup = __webpack_require__(58950)
const Controlleable = __webpack_require__(73007)
const config = setup.object


module.exports = class Object extends mixer.extends(Real, [Controlleable, ...config.before]) {
  static equals(o1, o2) {
    if (o1.constructor !== o2.constructor) {
      return false
    }
    /*
    if (o1.constructor !== this) {
      return o1.constructor.equals(o1, o2)
    }
    */
    for (const p of o1.constructor.properties) {
      if (!p.type.equals(o1[p.name], o2[p.name])) {
        return false
      }
    }
    return true
  }

  shadowClone() {
    const json = this.toJSON()
    const shadowClone = new this.constructor()
    NativeObject.assign(shadowClone, json)
    return shadowClone
  }
}
  .define({
    name: 'object',
    abstract: true,
  })
  .methods({
    is: [[Type], Bool]
  })



/***/ }),

/***/ 40350:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const ArrayAssociation = __webpack_require__(78702)
const { ownMany } = __webpack_require__(58950)

module.exports = class OwnMany extends mixer.extends(ArrayAssociation, ownMany.before) {

  async innerLoad(context, paths = {}) {
    for (const model of this) {
      await model.load(context, paths)
    }
  }

  setPathsState(state, paths, err) {
   
  }
}
  .define({
    name: 'ownMany',
  })

/***/ }),

/***/ 53089:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const String = __webpack_require__(14037)

module.exports = class Password extends String {

}

  .define({
    name: 'password',
  })


/***/ }),

/***/ 79360:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Real = __webpack_require__(24782)

module.exports = class Primitive extends mixer.extends(Real) {
  constructor() {
    throw new Error('Cannot instenciate')
  }

  static equals(value1, value2) {
    return value1 === value2
  }

  static parse(value) {
    return value
  }

  static toJSON(value) {
    return value
  }
}
  .define({
    name: 'primitive',
  })

/***/ }),

/***/ 38188:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { match } = __webpack_require__(22469)
const Models = __webpack_require__(76953)
const mixer = __webpack_require__(96172)
const setup = __webpack_require__(58950)

module.exports = class QueryResult extends mixer.extends(Models,[...setup.queryResult.before]) {
  constructor(values, queryParams) {
    if (!queryParams) {
      return []
    }
    super(...values)
    this.queryParams = queryParams
  }

  async checkModel(...args) {
    const [context, model] = setup.getArgs(args)
    const isInsanceOfTemplate = model instanceof this.constructor.definition.template
    if (!isInsanceOfTemplate) { return }
    let index = this.indexOf(model)
    if (await match(context, model, this.queryParams.stages)) {
      if (index === -1) {
        this.push(model)
      }
    } else {
      if (index !== -1) {
        this.splice(index, 1)
      }
    }
  }

  toJSON(paths, context) {
    if(paths === undefined){
      paths = this.queryParams.options.load
    }
    return super.toJSON(paths, context)
  }
}
  .define()


/***/ }),

/***/ 24782:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Abstractable = __webpack_require__(3764)
const Equalable = __webpack_require__(48774)
const Any = __webpack_require__(18557)

module.exports = class Real extends mixer.extends([Any, Abstractable, Equalable]) {

}
  .define({
    name: 'real',
    abstract: true,
  })



/***/ }),

/***/ 93178:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(54719)

module.exports = class Scope extends Virtual {

}
  .define({
    name: 'Scope',
  }) 

/***/ }),

/***/ 14037:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Primitive = __webpack_require__(79360)
const utils = __webpack_require__(21129)
const Bool = __webpack_require__(56482)

class String extends Primitive {
  static parse(value, owner, property) {
    if (value == null) { return value }

    if (typeof value !== 'string') {
      throw new Error(`Property ${property.name} has to be a string, received ${value}`)
    }
    if (property.values && property.values.indexOf(value) === -1) {
      throw new Error(`Property ${property.name} with value ${value} does not match values: ${property.values.join(',')}`)
    }
    return super.parse(value, owner, property)
  }
}

String
  .define({
    name: 'string',
  })
  .methods({
    match: [[String], Bool],
    toUpperCase: [[], String]
  })


module.exports = String

/***/ }),

/***/ 18202:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(54719)
const Templateable = __webpack_require__(79338)

class Template extends Virtual {
  static getType(ownerType) {
    if (!ownerType.prototype instanceof this.definition.template) {
      throw new Error('Types not matching')
    }

    const lastTemplate = ownerType.getLastTemplate()
    if (lastTemplate) {
      return lastTemplate
    }
    else {
      //return this.definition.template
    }

  }

}

Template.define({
  name: 'template',
})

Templateable.Template = Template

module.exports = Template


/***/ }),

/***/ 56907:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {


const Virtual = __webpack_require__(54719)

module.exports = class This extends Virtual {
  static getType(ownerType) {
    return ownerType
  }
}
  .define({
    name: 'This',
  })




/***/ }),

/***/ 50042:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Virtual = __webpack_require__(54719)

module.exports = class Type extends Virtual {
  static build(value) {
    return value
  }

  static toJSON(value) {
    return value
  }
}
  .define({
    name: 'type',
  })



/***/ }),

/***/ 54719:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Any = __webpack_require__(18557)

module.exports = class Virtual extends Any {

}

/***/ }),

/***/ 21292:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const ArrayAssociation = __webpack_require__(78702)
const { hasMany } = __webpack_require__(58950)


module.exports = class VirtualArrayAssociation extends ArrayAssociation {
  toJSON(paths, context) {
    if (!paths) { return undefined }
    return super.toJSON(paths, context)
  }
}
  .define()

/***/ }),

/***/ 75314:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

module.exports = {
  // This always on top
  Any: __webpack_require__(18557),
  Virtual: __webpack_require__(54719),
  This: __webpack_require__(56907),
  Type: __webpack_require__(50042),
  Global: __webpack_require__(30185),
  Real: __webpack_require__(24782),
  Primitive: __webpack_require__(79360),
  Bool: __webpack_require__(56482),
  Number: __webpack_require__(85779),
  String: __webpack_require__(14037),
  Object: __webpack_require__(55405),
  Model: __webpack_require__(57979),
  Template: __webpack_require__(18202),
  Array: __webpack_require__(24633),
  Branch: __webpack_require__(37963),
  HasMany: __webpack_require__(27315),
  ArrayAssociation: __webpack_require__(78702),
  Markdown: __webpack_require__(40999),
  Password: __webpack_require__(53089),
  OwnMany: __webpack_require__(40350),
}



/***/ }),

/***/ 21129:
/***/ (function(module) {

const propertySanitizers = []


module.exports = {
  propertySanitizers,
}

/***/ }),

/***/ 40740:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const RessourceOwner = __webpack_require__(47295)

module.exports = mixer.mixin([RessourceOwner], (base) => {
  return class RessourceGroupMixin extends base {
   
  }
})

/***/ }),

/***/ 47295:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const ModelMixin = __webpack_require__(48212)

module.exports = mixer.mixin([ModelMixin], (base) => {
  return class RessourceOwner extends base { }
})
  .define({
    name: 'ressourceOwner'
  })

/***/ }),

/***/ 56816:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Object, String, OwnMany } = __webpack_require__(75314)
const RessourceOwner = __webpack_require__(47295)

const choices = ['private', 'public', 'inherited']

module.exports = class Right extends Object {
  static choices = choices
}
  .define({
    name: 'right',
  })
  .properties({
    owners: {
      type: OwnMany.of(RessourceOwner),
    },
    type: {
      type: String,
      state: {
        choices
      }
    }
  })
  .controllers({
    create: {
      logic(context, states) {
        
      }
    }
  })

/***/ }),

/***/ 23034:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const RessourceOwner = __webpack_require__(47295)
const Model = __webpack_require__(57979)
module.exports = mixer.mixin([RessourceOwner], (base) => {
  return class RessourceUserMixin extends base {
    async getRessourcesOwners(context) {
      const properties = this.constructor.properties.filter((p) => {
        if (p.type.hasMixin(RessourceOwner)) {
          return true
        }

        if (p.type.definition.template?.hasMixin(RessourceOwner)) {
          return true
        }

        return false
      })


      for (const property of properties) {
        await this[property.name].load(context)
      }

      const result = properties.flatMap((property) => {
        if (property.type.prototype instanceof Array) {
          return this[property.name]
        } else if (property.type instanceof Model) {
          return [this[property.name]]
        } else {
          throw new Error('Type not recognized')
        }
      })

      return [this, ...result]
    }
  }
})

/***/ }),

/***/ 78002:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

const managementSetup = __webpack_require__(99213)
const UserMixin = __webpack_require__(23034)
const GroupMixin = __webpack_require__(40740)

managementSetup.user.push(UserMixin)
managementSetup.group.push(GroupMixin)

/***/ }),

/***/ 57777:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Collection = __webpack_require__(68847)
const File = __webpack_require__(85799)


module.exports = class StorageCollection extends Collection {
  async upload(file, fileObject, options = {}) {
    if (fileObject) {
      if (!(fileObject instanceof File)) {
        fileObject = new File(fileObject)
      }
    }
    const formData = new FormData()
    formData.append('file', file)
    if (fileObject) {
      formData.append('values', JSON.stringify(fileObject.toJSON()))
    }

    const url = `${this.url}/api/upload/file`

    const result = await this.request({
      url,
      method: 'POST',
      data: formData,
      onUploadProgress: options.onUploadProgress,
      headers: {
        'Content-Type': 'multipart/form-data'
      },
    })

    const object = this.type.parse(result, { singleInstance: true })
    object.setLoadState(true)
    console.log(object.toJSON())
    await this.onModelCreated(object)
    return object
  }

  async uploadUrl(url, file) {
    const result = await this.request({
      url: `${this.url}/api/upload/url`,
      method: 'POST',
      data: {
        url,
        file,
      },
      headers: {
        'Content-Type': 'application/json'
      },
    })

    const object = this.type.parse(result, { singleInstance: true })
    object.setLoadState(true)
    return object
  }
}

/***/ }),

/***/ 58754:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(97870)
const { collectionsTypesMap } = __webpack_require__(49045)
const StorageCollection = __webpack_require__(57777)
const StorageObject = __webpack_require__(67968)

collectionsTypesMap.unshift([StorageObject, StorageCollection])

/***/ }),

/***/ 78353:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Card = __webpack_require__(72294)
const template = __webpack_require__(12020)
const File = __webpack_require__(85799)
__webpack_require__(36710)

module.exports = class FileCard extends Card {

}
  .define({
    name: 'file-card',
    template,
  })
  .register(File, 'card')



/***/ }),

/***/ 3969:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const template = __webpack_require__(60901)
const Field = __webpack_require__(98656)
const { validateType } = __webpack_require__(97204)

__webpack_require__(23759)

module.exports = class FileField extends Field {

  constructor(...args) {
    super(...args)
    this.on('propertyChanged:value', this.b(this.onValueChanged))
  }

  async onReady() {
    this.onValueChanged()
  }

  async onValueChanged() {
    if (!this.value) { return }
    await this.value.load()
  }

  async onInputChanged() {
    const file = this.input.files[0]
    if (!file) { return }
    await this.upload(file)
    this.input.value = null
  }

  async fileSelected() {
    this.modal.close()
    this.setValue(this.fileSelector.file)
  }

  async onDrop(event) {
    this.hover = false
    event.preventDefault()
    const file = event.dataTransfer.files[0]
    if (!file) {
      return
    }
    console.log(this.type.accept, file.type)
    if (!validateType(this.type.accept, file.type)) {
      this.message(`File type ${file.type} not accepted`)
      return
    }

    await this.upload(file)
  }

  async onClick() {
    console.log(this.modal)
    this.modal.show()
    this.fileSelector.focus()
  }

  onDragOver(event) {
    event.preventDefault()
  }

  onDragEnter(event) {
    event.preventDefault()
    this.hover = true
  }

  onDragLeave() {
    this.hover = false
  }

}
  .define({
    name: 'file-field',
    template,
  })
  .properties({
    hover: 'any',
    progress: 'any',
  })


/***/ }),

/***/ 67719:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Card = __webpack_require__(72294)
const template = __webpack_require__(68496)
const StorageObject = __webpack_require__(67968)
__webpack_require__(98696)

module.exports = class FileRow extends Card {
  async update() {
    await super.update()
    if (!this.model) { return }
    this.imgSrc = this.model.path
  }
}
  .define({
    name: 'file-row',
    template,
  })
  .properties({
    imgSrc: 'any',
  })
  .register(StorageObject, 'row')



/***/ }),

/***/ 31971:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(78353)
__webpack_require__(67719)
__webpack_require__(3969)

/***/ }),

/***/ 93820:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Card = __webpack_require__(72294)
const template = __webpack_require__(11377)
const Folder = __webpack_require__(11718)
__webpack_require__(42536)

module.exports = class FolderCard extends Card {
  destroy(){
    console.warn('destroying folder', this)
    super.destroy()
  }
}
  .define({
    name: 'folder-card',
    template,
  })
  .register(Folder, 'card')



/***/ }),

/***/ 22740:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Row = __webpack_require__(31009)
const template = __webpack_require__(77790)
const Folder = __webpack_require__(11718)
__webpack_require__(65860)

module.exports = class FolderRow extends Row {

}
  .define({
    name: 'folder-row',
    template,
  })  
  .register(Folder, 'row')



/***/ }),

/***/ 77555:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(93820)
__webpack_require__(22740)

/***/ }),

/***/ 13395:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Component = __webpack_require__(58497)
const template = __webpack_require__(92051)
const Folder = __webpack_require__(11718)
__webpack_require__(37519)

module.exports = class FolderTree extends Component {
  constructor() {
    super()
    this.on('propertyChanged:folder', this.b(this.onFolderChanged))
  }

  async onInit() {
    await this.onFolderChanged()
  }

  select(folder) {
    this.event('selected', { folder })
  }

  async onFolderChanged() {
    this.loading = true
    await this.folder.branch.load({
      children: true
    })
    await this.folder.children.load()
    if (!this.root) {
      this.calculatedRoot = this.folder.branch[this.folder.branch.length - 1]
      if (!this.calculatedRoot) {
        this.calculatedRoot = this.folder
      }
    }
    this.loading = false
  }

}
  .define({
    name: 'folder-tree',
    template,
  })
  .properties({
    folder: 'any',
    root: 'any',
    calculatedRoot: 'any',
    loading: 'any',
  })
  .variables({
    Folder
  })


/***/ }),

/***/ 83962:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Card = __webpack_require__(72294)
const template = __webpack_require__(93733)
const Image = __webpack_require__(29100)
__webpack_require__(85765)

module.exports = class ImageCard extends Card {
  async update() {
    await super.update()
    if (!this.model) { return }
    this.imgSrc = this.model.path
  }
}
  .define({
    name: 'image-card',
    template,
  })
  .properties({
    imgSrc: 'any',
  })
  .register(Image, 'card')



/***/ }),

/***/ 19909:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Card = __webpack_require__(72294)
const template = __webpack_require__(70716)
const Image = __webpack_require__(29100)
__webpack_require__(18812)

module.exports = class ImageRow extends Card {

}
  .define({
    name: 'image-row',
    template,
  })  
  .register(Image, 'row')



/***/ }),

/***/ 43244:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(83962)
__webpack_require__(19909)

/***/ }),

/***/ 17647:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(31971)
__webpack_require__(77555)
__webpack_require__(43244)
__webpack_require__(13395)

/***/ }),

/***/ 4150:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

__webpack_require__(17647)
__webpack_require__(48408)
__webpack_require__(58754)
const { menu } = __webpack_require__(98864)

const StorageObject = __webpack_require__(67968)

const ObjectFieldset = __webpack_require__(95858)
const FileField = __webpack_require__(3969)

ObjectFieldset.typesFieldmapping.unshift([StorageObject, FileField])

menu.links.push({
  class: 'fa-solid fa-folder',
  label: 'storage',
  url: '/explorer/storage',
})

/***/ }),

/***/ 36119:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Router = __webpack_require__(18709)
const Folder = __webpack_require__(11718)

const router = new Router()

router.route(/\/explorer(.+)/, async (req, res) => {
  const [, _id] = req.match
  const storageFolder = await Folder.collection.findByUniqueIndex({
    _id,
  })
  res.page(__webpack_require__.e(/* import() */ 815).then(__webpack_require__.t.bind(__webpack_require__, 86815, 23)), storageFolder)
})

module.exports = router

/***/ }),

/***/ 91922:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const setup = __webpack_require__(47062)
__webpack_require__(97870)
const Folderable = __webpack_require__(25785)
const Imageable = __webpack_require__(4873)

setup.routing.layout.header.before.push(async (model) => {
  if (!mixer.is(model, Imageable)) { return null }
  console.log(model, model.image)
  return {
    fragment: '<img class="imageable-image" :src="`${this.@model?.image?.path}?width=200&height=90`">'
  }
})

setup.routing.actions.push({
  name: 'storage',
  content: '<i class="fa-solid fa-folder"></i>',
  url: '/storage',
  check(model) {
    return mixer.is(model, Folderable)
  },
  async execute(req, res, next) {
    await req.model.folder.load()
    await res.page(__webpack_require__.e(/* import() */ 815).then(__webpack_require__.t.bind(__webpack_require__, 86815, 23)), req.model.folder)
  }
})

/***/ }),

/***/ 85799:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {


const StorageObject = __webpack_require__(67968)
const { String, Number } = __webpack_require__(75314)

module.exports = class File extends StorageObject {
  static accept = []

  get path() {
    return `/api/storage/${this._id}`
  }
}
  .define({
    name: 'file',
  })
  .properties({
    mimetype: {
      type: String,
      state: {
        required: true,
      }
    },
    size: {
      type: Number,
      required: true,
    },
    source: {
      type: String,
      state: {
        required: true
      }
    },
  })

/***/ }),

/***/ 11718:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {


const Right = __webpack_require__(56816)
const StorageObject = __webpack_require__(67968)

const newName = 'New folder'
module.exports = class Folder extends StorageObject {
  async getNewFolderName(context) {
    await this.children.load(context)
    let newFolderName = newName
    let index = 1
    while (this.children.some((c) => c.name === newFolderName)) {
      newFolderName = `${newName} (${index++})`
    }
    return newFolderName
  }
}
  .define({
    name: 'folder',
  })
  .properties({
    add: {
      type: Right,
      state: {
        required: true
      }
    }
  })
  .controllers({
    update: {
      check(context, folder) {
        return context.user?.equals(folder.user)
      }
    }
  })


/***/ }),

/***/ 29100:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {


const File = __webpack_require__(85799)

module.exports = class Image extends File {
  static accept = ['image/*']
}
  .define({
    name: 'image',
  })
  .properties({

  })

/***/ }),

/***/ 67968:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Model, String } = __webpack_require__(75314)
const Right = __webpack_require__(56816)
const RessourceOwner = __webpack_require__(47295)
const ControllerError = __webpack_require__(5145)

module.exports = class StorageObject extends Model {

}
  .define({
    name: 'storageObject',
    pluralName: 'storage',
    abstract: true,
    root: true,
  })
  .properties({
    name: {
      type: String,
      state: {
        required: true
      },
    },
    read: {
      type: Right,
      state: {
        required: true
      },
    },
    edit: {
      type: Right,
      state: {
        required: true
      },
    }
  })
  .controllers({
    create: {
      async check(context) {
        if (!context.user) {
          throw new ControllerError(`You must be logged in to create a storage object`)
        }
      },
      async logic(context, states) {
        if (context.setup) {
          states.folder.required = false
        }
      }
    },
    delete: {
      check: async (context, storageObject) => {

      }
    }
  })

/***/ }),

/***/ 97870:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Folder = __webpack_require__(11718)
const StorageObject = __webpack_require__(67968)
const { Branch } = __webpack_require__(75314)
const HasMany = __webpack_require__(27315)

StorageObject.properties({
  folder: {
    type: Folder,
    state: {
      required: true,
    }
  },
  branch: {
    type: Branch.of(Folder),
    on: 'folder',
  }
})

Folder.properties({
  children: {
    type: HasMany.of(StorageObject),
    on: 'folder',
  }
})

module.exports = {
  File: __webpack_require__(85799),
  Folder,
  StorageObject,
  Image:__webpack_require__(29100)
}

/***/ }),

/***/ 25785:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Folder = __webpack_require__(11718)
const Propertiable = __webpack_require__(85020)

module.exports = mixer.mixin([Propertiable], (base) => {
  return class Folderable extends base {

  }
})
  .define()
  .properties({
    folder: {
      type: Folder,
      state: {
        disabled: true,
      }
    },
  })

/***/ }),

/***/ 4873:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Propertiable = __webpack_require__(85020)
const Image = __webpack_require__(29100)
module.exports = mixer.mixin([Propertiable], (base) => {
  return class Imageable extends base {

  }
})
  .define()
  .properties({
    image: {
      type: Image,
    },
  })



/***/ }),

/***/ 63982:
/***/ (function(__unused_webpack_module, __unused_webpack_exports, __webpack_require__) {

const setup = __webpack_require__(47062)
const mixer = __webpack_require__(96172)
const Wikiable = __webpack_require__(67846)

setup.routing.actions.unshift({
  name: 'wiki',
  content: '<i class="fa-solid fa-file-lines"></i>',
  url: '',
  check(model) {
    return mixer.is(model, Wikiable)
  },
  async execute(req, res, next) {
    await res.page(__webpack_require__.e(/* import() */ 696).then(__webpack_require__.t.bind(__webpack_require__, 57696, 23)), req.model)
  }
})

/***/ }),

/***/ 67846:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const Propertiable = __webpack_require__(85020)
const Markdown = __webpack_require__(40999)

module.exports = mixer.mixin([Propertiable], (base) => {
  return class Wikiable extends base {

  }
})
  .define()
  .properties({
    wiki: {
      type: Markdown,
    }
  })

/***/ }),

/***/ 84138:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

module.exports = {
  ...__webpack_require__(83531),
  ...__webpack_require__(95862),
}

/***/ }),

/***/ 41217:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Model } = __webpack_require__(75314)

module.exports = class GameModel extends Model {

}
  .define({
    name: 'gameModel',
  })
  .properties({

  })

/***/ }),

/***/ 57631:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Model, String } = __webpack_require__(75314)
const mixer = __webpack_require__(96172)
const StarMap = __webpack_require__(93252)
const Pageable = __webpack_require__(67328)
const Wikiable = __webpack_require__(67846)
const Imageable = __webpack_require__(4873)
const Folderable = __webpack_require__(25785)

module.exports = class Organization extends mixer.extends(Model, [Pageable, Imageable, Folderable, Wikiable]) {
  toString() {
    return this.name || this.code || this._id
  }
}
  .define({
    name: 'organization',
    pluralName: 'organizations',
    root: true,
    searchField: 'name',
  })
  .indexes({
    code: {
      properties: ['code'],
      unique: true,
    }
  })
  .properties({
    code: String,
    color: String,
    name: String,
    starmap: StarMap,
  })



/***/ }),

/***/ 46014:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Planet = __webpack_require__(43739)
const GameEntity = __webpack_require__(25085)

module.exports = class City extends GameEntity {

}
  .define({
    name: 'city',
    pluralName: 'cities'
  })


/***/ }),

/***/ 6059:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const GameModel = __webpack_require__(41217)
const Organization = __webpack_require__(57631)
const { String, Branch, HasMany } = __webpack_require__(75314)

class Entity extends mixer.extends(GameModel) {

}

Entity
  .define({
    name: 'entity',
    pluralName: 'entities',
    abstract: true,
    root: true,
  })
  .properties({
    organization: Organization,
    parent: Entity,
    name: {
      type: String,
      state: {
        required: true,
      }
    },
    parents: {
      type: Branch.of(Entity),
      on: 'parent',
    },
    children: {
      type: HasMany.of(Entity),
      on: 'parent',
    }
  })
  .controllers({
    create:{
      check(context){
        return !!context.user
      }
    },
    update: {
      logic(context, states, oldValue) {
        console.log(states.parent)
        states.parent.filters.push({
          $neq: ['$_id', oldValue._id]
        })
      }
    }
  })


module.exports = Entity


/***/ }),

/***/ 18004:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Entity = __webpack_require__(6059)

module.exports = class EuclideanSpace extends Entity {

}
  .define({
    name: 'euclideanSpace',
    pluralName: 'euclideanSpaces'
  })
  .properties({
  })


/***/ }),

/***/ 25085:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Entity = __webpack_require__(6059)
const Starmap = __webpack_require__(93252)
const mixer = __webpack_require__(96172)
const Pageable = __webpack_require__(67328)
const { String, Markdown } = __webpack_require__(75314)
const Imageable = __webpack_require__(4873)
const Folderable = __webpack_require__(25785)
const Wikiable = __webpack_require__(67846)
const ControllerError = __webpack_require__(5145)

const isAdmin = async (context) => {
  console.log(context.user._id)
  if (!await context.user?.is(context, 'admin')) {
    throw new ControllerError('You are not admin')
  }
}

module.exports = class GameEntity extends mixer.extends(Entity, [Pageable, Folderable, Imageable, Wikiable]) {

}
  .define({
    name: 'gameEntity',
    abstract: true,
    searchField: 'name',
    codeField: 'code',
  })
  .indexes({
    code: {
      properties: ['code'],
      unique: true,
    },
    name: {
      properties: ['name'],
      unique: true,
    }
  })
  .properties({
    code: {
      type: String,
      state: {
        required: true,
      }
    },
    starmap: Starmap,
  })
  .controllers({
    create: {
      check(context) {
        return isAdmin(context)
      },
      async logic(context, states) {
        states.code.value = states.code.value?.toUpperCase()
        states.folder.disabled = true
      }
    },
    update: {
      check(context, gameEntity) {
        return isAdmin(context)
      },
      logic(context, states) {
        states.folder.disabled = true
        states.code.value = states.code.value.toUpperCase()
      }
    }
  })

/***/ }),

/***/ 863:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Location = __webpack_require__(6059)
const EuclideanSpace = __webpack_require__(18004)
class GroundPosition {

}

module.exports = class GroundLocation extends EuclideanSpace {

}
  .define({
    name: 'groundLocation',
    pluralName: 'groundLocations'
  })



/***/ }),

/***/ 65756:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const mixer = __webpack_require__(96172)
const GameEntity = __webpack_require__(25085)

module.exports = class LandingZone extends GameEntity {

}
  .define({
    name: 'landingZone',
    pluralName: 'landingZones'
  })
  .properties()

/***/ }),

/***/ 30374:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const GameEntity = __webpack_require__(25085)

const parentLogic = (states) => {
  states.parent.filters.push({
    $is: ['$this', 'planet']
  })
}

module.exports = class Moon extends GameEntity {

}
  .define({
    name: 'moon',
    pluralName: 'moons'
  })
  .controllers({
    create: {
      logic(context, states) {
        parentLogic(states)
      }
    },
    update: {
      logic(context, states) {
        parentLogic(states)
      }
    }
  })


/***/ }),

/***/ 6288:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const Entity = __webpack_require__(6059)
class OrbitPosition {

}

module.exports = class OrbitLocation extends Entity {

}
  .define({
    name: 'orbitLocation',
    pluralName: 'orbitLocations'
  })
  .properties({
    //position: OrbitPosition,
  })


/***/ }),

/***/ 43739:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const GameEntity = __webpack_require__(25085)

const parentLogic = (states) => {
  states.parent.filters.push({
    $is: ['$this', 'system']
  })
}

module.exports = class Planet extends GameEntity {

}
  .define({
    name: 'planet',
    pluralName: 'planets'
  })
  .properties({
    //parent: System,
  })
  .controllers({
    create: {
      logic(context, states) {
        parentLogic(states)
      }
    },
    update: {
      logic(context, states) {
        parentLogic(states)
      }
    }
  })


/***/ }),

/***/ 13888:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const GameEntity = __webpack_require__(25085)

module.exports = class Star extends GameEntity {

}
  .define({
    name: 'star',
    pluralName: 'stars'
  })
  .controllers({
    update: {
      logic(context, states) {
        states.parent.filters.push({
          $is: ['$this', 'system']
        })
      }
    }
  })

/***/ }),

/***/ 53353:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const GameEntity = __webpack_require__(25085)

module.exports = class System extends GameEntity {
  destroy(){
    console.warn('destroying', this)
    super.destroy()
  }
}
  .define({
    name: 'system',
    pluralName: 'systems'
  })
  .controllers({
    create: {
      logic(context, states) {
        states.parent.disabled = true
      }
    },
    update: {
      logic(context, states) {
        states.parent.disabled = true
      }
    }
  })


/***/ }),

/***/ 42039:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

module.exports = {
  Entity: __webpack_require__(6059),
  GameEntity: __webpack_require__(25085),
  System: __webpack_require__(53353),
  OrbitLocation: __webpack_require__(6288),
  Planet: __webpack_require__(43739),
  Star: __webpack_require__(13888),
  Moon: __webpack_require__(30374),
  GroundLocation: __webpack_require__(863),
  City: __webpack_require__(46014),
  LandingZone: __webpack_require__(65756),
}

/***/ }),

/***/ 83531:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

module.exports = {
  ...__webpack_require__(42039),
  Organization: __webpack_require__(57631),
}

/***/ }),

/***/ 53865:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Object, Number } = __webpack_require__(75314)

const getDimensionState = ({ property }, ownProperty) => {
  return {
    ...(property?.dimensionState || {})
  }
}

class Position2D extends Object {

}

Position2D.getDimensionState = getDimensionState

module.exports = Position2D
  .define({
    name: 'position2D',
  })
  .properties({
    x: {
      type: Number,
      state: getDimensionState
    },
    y: {
      type: Number,
      state: getDimensionState
    },
  })



/***/ }),

/***/ 70062:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Number } = __webpack_require__(75314)
const Position2D = __webpack_require__(53865)

module.exports = class Position3D extends Position2D {

}
  .define({
    name: 'position3D',
  })
  .properties({
    z: {
      type: Number,
      state:Position2D.getDimensionState
    },
  })

/***/ }),

/***/ 93252:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

const { Object, Number, String } = __webpack_require__(75314)
const Position3D = __webpack_require__(70062)

module.exports = class Starmap extends Object {

}
  .define({
    name: 'Starmap',
  })
  .properties({
    id: Number,
    type: String,
    code: String,
    position: {
      type: Position3D,
      dimensionState: {
        step: 0.0000001,
      }
    },
    orbitPeriod: {
      type: Number,
      state: {
        step: 0.01,
      }
    },
  })

/***/ }),

/***/ 95862:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

module.exports = {
  Position2D: __webpack_require__(53865),
  Position3D: __webpack_require__(70062),
  Starmap: __webpack_require__(93252),
}

/***/ }),

/***/ 87396:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";
module.exports = __webpack_require__.p + "e49e80ba30466659a444.jpg";

/***/ }),

/***/ 95405:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";
module.exports = __webpack_require__.p + "c6208cb3d190db81b952.png";

/***/ }),

/***/ 11605:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";
// Axios v1.5.1 Copyright (c) 2023 Matt Zabriskie and contributors


function bind(fn, thisArg) {
  return function wrap() {
    return fn.apply(thisArg, arguments);
  };
}

// utils is a library of generic helper functions non-specific to axios

const {toString} = Object.prototype;
const {getPrototypeOf} = Object;

const kindOf = (cache => thing => {
    const str = toString.call(thing);
    return cache[str] || (cache[str] = str.slice(8, -1).toLowerCase());
})(Object.create(null));

const kindOfTest = (type) => {
  type = type.toLowerCase();
  return (thing) => kindOf(thing) === type
};

const typeOfTest = type => thing => typeof thing === type;

/**
 * Determine if a value is an Array
 *
 * @param {Object} val The value to test
 *
 * @returns {boolean} True if value is an Array, otherwise false
 */
const {isArray} = Array;

/**
 * Determine if a value is undefined
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if the value is undefined, otherwise false
 */
const isUndefined = typeOfTest('undefined');

/**
 * Determine if a value is a Buffer
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Buffer, otherwise false
 */
function isBuffer(val) {
  return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor)
    && isFunction(val.constructor.isBuffer) && val.constructor.isBuffer(val);
}

/**
 * Determine if a value is an ArrayBuffer
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is an ArrayBuffer, otherwise false
 */
const isArrayBuffer = kindOfTest('ArrayBuffer');


/**
 * Determine if a value is a view on an ArrayBuffer
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a view on an ArrayBuffer, otherwise false
 */
function isArrayBufferView(val) {
  let result;
  if ((typeof ArrayBuffer !== 'undefined') && (ArrayBuffer.isView)) {
    result = ArrayBuffer.isView(val);
  } else {
    result = (val) && (val.buffer) && (isArrayBuffer(val.buffer));
  }
  return result;
}

/**
 * Determine if a value is a String
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a String, otherwise false
 */
const isString = typeOfTest('string');

/**
 * Determine if a value is a Function
 *
 * @param {*} val The value to test
 * @returns {boolean} True if value is a Function, otherwise false
 */
const isFunction = typeOfTest('function');

/**
 * Determine if a value is a Number
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Number, otherwise false
 */
const isNumber = typeOfTest('number');

/**
 * Determine if a value is an Object
 *
 * @param {*} thing The value to test
 *
 * @returns {boolean} True if value is an Object, otherwise false
 */
const isObject = (thing) => thing !== null && typeof thing === 'object';

/**
 * Determine if a value is a Boolean
 *
 * @param {*} thing The value to test
 * @returns {boolean} True if value is a Boolean, otherwise false
 */
const isBoolean = thing => thing === true || thing === false;

/**
 * Determine if a value is a plain Object
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a plain Object, otherwise false
 */
const isPlainObject = (val) => {
  if (kindOf(val) !== 'object') {
    return false;
  }

  const prototype = getPrototypeOf(val);
  return (prototype === null || prototype === Object.prototype || Object.getPrototypeOf(prototype) === null) && !(Symbol.toStringTag in val) && !(Symbol.iterator in val);
};

/**
 * Determine if a value is a Date
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Date, otherwise false
 */
const isDate = kindOfTest('Date');

/**
 * Determine if a value is a File
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a File, otherwise false
 */
const isFile = kindOfTest('File');

/**
 * Determine if a value is a Blob
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Blob, otherwise false
 */
const isBlob = kindOfTest('Blob');

/**
 * Determine if a value is a FileList
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a File, otherwise false
 */
const isFileList = kindOfTest('FileList');

/**
 * Determine if a value is a Stream
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Stream, otherwise false
 */
const isStream = (val) => isObject(val) && isFunction(val.pipe);

/**
 * Determine if a value is a FormData
 *
 * @param {*} thing The value to test
 *
 * @returns {boolean} True if value is an FormData, otherwise false
 */
const isFormData = (thing) => {
  let kind;
  return thing && (
    (typeof FormData === 'function' && thing instanceof FormData) || (
      isFunction(thing.append) && (
        (kind = kindOf(thing)) === 'formdata' ||
        // detect form-data instance
        (kind === 'object' && isFunction(thing.toString) && thing.toString() === '[object FormData]')
      )
    )
  )
};

/**
 * Determine if a value is a URLSearchParams object
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a URLSearchParams object, otherwise false
 */
const isURLSearchParams = kindOfTest('URLSearchParams');

/**
 * Trim excess whitespace off the beginning and end of a string
 *
 * @param {String} str The String to trim
 *
 * @returns {String} The String freed of excess whitespace
 */
const trim = (str) => str.trim ?
  str.trim() : str.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, '');

/**
 * Iterate over an Array or an Object invoking a function for each item.
 *
 * If `obj` is an Array callback will be called passing
 * the value, index, and complete array for each item.
 *
 * If 'obj' is an Object callback will be called passing
 * the value, key, and complete object for each property.
 *
 * @param {Object|Array} obj The object to iterate
 * @param {Function} fn The callback to invoke for each item
 *
 * @param {Boolean} [allOwnKeys = false]
 * @returns {any}
 */
function forEach(obj, fn, {allOwnKeys = false} = {}) {
  // Don't bother if no value provided
  if (obj === null || typeof obj === 'undefined') {
    return;
  }

  let i;
  let l;

  // Force an array if not already something iterable
  if (typeof obj !== 'object') {
    /*eslint no-param-reassign:0*/
    obj = [obj];
  }

  if (isArray(obj)) {
    // Iterate over array values
    for (i = 0, l = obj.length; i < l; i++) {
      fn.call(null, obj[i], i, obj);
    }
  } else {
    // Iterate over object keys
    const keys = allOwnKeys ? Object.getOwnPropertyNames(obj) : Object.keys(obj);
    const len = keys.length;
    let key;

    for (i = 0; i < len; i++) {
      key = keys[i];
      fn.call(null, obj[key], key, obj);
    }
  }
}

function findKey(obj, key) {
  key = key.toLowerCase();
  const keys = Object.keys(obj);
  let i = keys.length;
  let _key;
  while (i-- > 0) {
    _key = keys[i];
    if (key === _key.toLowerCase()) {
      return _key;
    }
  }
  return null;
}

const _global = (() => {
  /*eslint no-undef:0*/
  if (typeof globalThis !== "undefined") return globalThis;
  return typeof self !== "undefined" ? self : (typeof window !== 'undefined' ? window : __webpack_require__.g)
})();

const isContextDefined = (context) => !isUndefined(context) && context !== _global;

/**
 * Accepts varargs expecting each argument to be an object, then
 * immutably merges the properties of each object and returns result.
 *
 * When multiple objects contain the same key the later object in
 * the arguments list will take precedence.
 *
 * Example:
 *
 * ```js
 * var result = merge({foo: 123}, {foo: 456});
 * console.log(result.foo); // outputs 456
 * ```
 *
 * @param {Object} obj1 Object to merge
 *
 * @returns {Object} Result of all merge properties
 */
function merge(/* obj1, obj2, obj3, ... */) {
  const {caseless} = isContextDefined(this) && this || {};
  const result = {};
  const assignValue = (val, key) => {
    const targetKey = caseless && findKey(result, key) || key;
    if (isPlainObject(result[targetKey]) && isPlainObject(val)) {
      result[targetKey] = merge(result[targetKey], val);
    } else if (isPlainObject(val)) {
      result[targetKey] = merge({}, val);
    } else if (isArray(val)) {
      result[targetKey] = val.slice();
    } else {
      result[targetKey] = val;
    }
  };

  for (let i = 0, l = arguments.length; i < l; i++) {
    arguments[i] && forEach(arguments[i], assignValue);
  }
  return result;
}

/**
 * Extends object a by mutably adding to it the properties of object b.
 *
 * @param {Object} a The object to be extended
 * @param {Object} b The object to copy properties from
 * @param {Object} thisArg The object to bind function to
 *
 * @param {Boolean} [allOwnKeys]
 * @returns {Object} The resulting value of object a
 */
const extend = (a, b, thisArg, {allOwnKeys}= {}) => {
  forEach(b, (val, key) => {
    if (thisArg && isFunction(val)) {
      a[key] = bind(val, thisArg);
    } else {
      a[key] = val;
    }
  }, {allOwnKeys});
  return a;
};

/**
 * Remove byte order marker. This catches EF BB BF (the UTF-8 BOM)
 *
 * @param {string} content with BOM
 *
 * @returns {string} content value without BOM
 */
const stripBOM = (content) => {
  if (content.charCodeAt(0) === 0xFEFF) {
    content = content.slice(1);
  }
  return content;
};

/**
 * Inherit the prototype methods from one constructor into another
 * @param {function} constructor
 * @param {function} superConstructor
 * @param {object} [props]
 * @param {object} [descriptors]
 *
 * @returns {void}
 */
const inherits = (constructor, superConstructor, props, descriptors) => {
  constructor.prototype = Object.create(superConstructor.prototype, descriptors);
  constructor.prototype.constructor = constructor;
  Object.defineProperty(constructor, 'super', {
    value: superConstructor.prototype
  });
  props && Object.assign(constructor.prototype, props);
};

/**
 * Resolve object with deep prototype chain to a flat object
 * @param {Object} sourceObj source object
 * @param {Object} [destObj]
 * @param {Function|Boolean} [filter]
 * @param {Function} [propFilter]
 *
 * @returns {Object}
 */
const toFlatObject = (sourceObj, destObj, filter, propFilter) => {
  let props;
  let i;
  let prop;
  const merged = {};

  destObj = destObj || {};
  // eslint-disable-next-line no-eq-null,eqeqeq
  if (sourceObj == null) return destObj;

  do {
    props = Object.getOwnPropertyNames(sourceObj);
    i = props.length;
    while (i-- > 0) {
      prop = props[i];
      if ((!propFilter || propFilter(prop, sourceObj, destObj)) && !merged[prop]) {
        destObj[prop] = sourceObj[prop];
        merged[prop] = true;
      }
    }
    sourceObj = filter !== false && getPrototypeOf(sourceObj);
  } while (sourceObj && (!filter || filter(sourceObj, destObj)) && sourceObj !== Object.prototype);

  return destObj;
};

/**
 * Determines whether a string ends with the characters of a specified string
 *
 * @param {String} str
 * @param {String} searchString
 * @param {Number} [position= 0]
 *
 * @returns {boolean}
 */
const endsWith = (str, searchString, position) => {
  str = String(str);
  if (position === undefined || position > str.length) {
    position = str.length;
  }
  position -= searchString.length;
  const lastIndex = str.indexOf(searchString, position);
  return lastIndex !== -1 && lastIndex === position;
};


/**
 * Returns new array from array like object or null if failed
 *
 * @param {*} [thing]
 *
 * @returns {?Array}
 */
const toArray = (thing) => {
  if (!thing) return null;
  if (isArray(thing)) return thing;
  let i = thing.length;
  if (!isNumber(i)) return null;
  const arr = new Array(i);
  while (i-- > 0) {
    arr[i] = thing[i];
  }
  return arr;
};

/**
 * Checking if the Uint8Array exists and if it does, it returns a function that checks if the
 * thing passed in is an instance of Uint8Array
 *
 * @param {TypedArray}
 *
 * @returns {Array}
 */
// eslint-disable-next-line func-names
const isTypedArray = (TypedArray => {
  // eslint-disable-next-line func-names
  return thing => {
    return TypedArray && thing instanceof TypedArray;
  };
})(typeof Uint8Array !== 'undefined' && getPrototypeOf(Uint8Array));

/**
 * For each entry in the object, call the function with the key and value.
 *
 * @param {Object<any, any>} obj - The object to iterate over.
 * @param {Function} fn - The function to call for each entry.
 *
 * @returns {void}
 */
const forEachEntry = (obj, fn) => {
  const generator = obj && obj[Symbol.iterator];

  const iterator = generator.call(obj);

  let result;

  while ((result = iterator.next()) && !result.done) {
    const pair = result.value;
    fn.call(obj, pair[0], pair[1]);
  }
};

/**
 * It takes a regular expression and a string, and returns an array of all the matches
 *
 * @param {string} regExp - The regular expression to match against.
 * @param {string} str - The string to search.
 *
 * @returns {Array<boolean>}
 */
const matchAll = (regExp, str) => {
  let matches;
  const arr = [];

  while ((matches = regExp.exec(str)) !== null) {
    arr.push(matches);
  }

  return arr;
};

/* Checking if the kindOfTest function returns true when passed an HTMLFormElement. */
const isHTMLForm = kindOfTest('HTMLFormElement');

const toCamelCase = str => {
  return str.toLowerCase().replace(/[-_\s]([a-z\d])(\w*)/g,
    function replacer(m, p1, p2) {
      return p1.toUpperCase() + p2;
    }
  );
};

/* Creating a function that will check if an object has a property. */
const hasOwnProperty = (({hasOwnProperty}) => (obj, prop) => hasOwnProperty.call(obj, prop))(Object.prototype);

/**
 * Determine if a value is a RegExp object
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a RegExp object, otherwise false
 */
const isRegExp = kindOfTest('RegExp');

const reduceDescriptors = (obj, reducer) => {
  const descriptors = Object.getOwnPropertyDescriptors(obj);
  const reducedDescriptors = {};

  forEach(descriptors, (descriptor, name) => {
    let ret;
    if ((ret = reducer(descriptor, name, obj)) !== false) {
      reducedDescriptors[name] = ret || descriptor;
    }
  });

  Object.defineProperties(obj, reducedDescriptors);
};

/**
 * Makes all methods read-only
 * @param {Object} obj
 */

const freezeMethods = (obj) => {
  reduceDescriptors(obj, (descriptor, name) => {
    // skip restricted props in strict mode
    if (isFunction(obj) && ['arguments', 'caller', 'callee'].indexOf(name) !== -1) {
      return false;
    }

    const value = obj[name];

    if (!isFunction(value)) return;

    descriptor.enumerable = false;

    if ('writable' in descriptor) {
      descriptor.writable = false;
      return;
    }

    if (!descriptor.set) {
      descriptor.set = () => {
        throw Error('Can not rewrite read-only method \'' + name + '\'');
      };
    }
  });
};

const toObjectSet = (arrayOrString, delimiter) => {
  const obj = {};

  const define = (arr) => {
    arr.forEach(value => {
      obj[value] = true;
    });
  };

  isArray(arrayOrString) ? define(arrayOrString) : define(String(arrayOrString).split(delimiter));

  return obj;
};

const noop = () => {};

const toFiniteNumber = (value, defaultValue) => {
  value = +value;
  return Number.isFinite(value) ? value : defaultValue;
};

const ALPHA = 'abcdefghijklmnopqrstuvwxyz';

const DIGIT = '0123456789';

const ALPHABET = {
  DIGIT,
  ALPHA,
  ALPHA_DIGIT: ALPHA + ALPHA.toUpperCase() + DIGIT
};

const generateString = (size = 16, alphabet = ALPHABET.ALPHA_DIGIT) => {
  let str = '';
  const {length} = alphabet;
  while (size--) {
    str += alphabet[Math.random() * length|0];
  }

  return str;
};

/**
 * If the thing is a FormData object, return true, otherwise return false.
 *
 * @param {unknown} thing - The thing to check.
 *
 * @returns {boolean}
 */
function isSpecCompliantForm(thing) {
  return !!(thing && isFunction(thing.append) && thing[Symbol.toStringTag] === 'FormData' && thing[Symbol.iterator]);
}

const toJSONObject = (obj) => {
  const stack = new Array(10);

  const visit = (source, i) => {

    if (isObject(source)) {
      if (stack.indexOf(source) >= 0) {
        return;
      }

      if(!('toJSON' in source)) {
        stack[i] = source;
        const target = isArray(source) ? [] : {};

        forEach(source, (value, key) => {
          const reducedValue = visit(value, i + 1);
          !isUndefined(reducedValue) && (target[key] = reducedValue);
        });

        stack[i] = undefined;

        return target;
      }
    }

    return source;
  };

  return visit(obj, 0);
};

const isAsyncFn = kindOfTest('AsyncFunction');

const isThenable = (thing) =>
  thing && (isObject(thing) || isFunction(thing)) && isFunction(thing.then) && isFunction(thing.catch);

var utils = {
  isArray,
  isArrayBuffer,
  isBuffer,
  isFormData,
  isArrayBufferView,
  isString,
  isNumber,
  isBoolean,
  isObject,
  isPlainObject,
  isUndefined,
  isDate,
  isFile,
  isBlob,
  isRegExp,
  isFunction,
  isStream,
  isURLSearchParams,
  isTypedArray,
  isFileList,
  forEach,
  merge,
  extend,
  trim,
  stripBOM,
  inherits,
  toFlatObject,
  kindOf,
  kindOfTest,
  endsWith,
  toArray,
  forEachEntry,
  matchAll,
  isHTMLForm,
  hasOwnProperty,
  hasOwnProp: hasOwnProperty, // an alias to avoid ESLint no-prototype-builtins detection
  reduceDescriptors,
  freezeMethods,
  toObjectSet,
  toCamelCase,
  noop,
  toFiniteNumber,
  findKey,
  global: _global,
  isContextDefined,
  ALPHABET,
  generateString,
  isSpecCompliantForm,
  toJSONObject,
  isAsyncFn,
  isThenable
};

/**
 * Create an Error with the specified message, config, error code, request and response.
 *
 * @param {string} message The error message.
 * @param {string} [code] The error code (for example, 'ECONNABORTED').
 * @param {Object} [config] The config.
 * @param {Object} [request] The request.
 * @param {Object} [response] The response.
 *
 * @returns {Error} The created error.
 */
function AxiosError(message, code, config, request, response) {
  Error.call(this);

  if (Error.captureStackTrace) {
    Error.captureStackTrace(this, this.constructor);
  } else {
    this.stack = (new Error()).stack;
  }

  this.message = message;
  this.name = 'AxiosError';
  code && (this.code = code);
  config && (this.config = config);
  request && (this.request = request);
  response && (this.response = response);
}

utils.inherits(AxiosError, Error, {
  toJSON: function toJSON() {
    return {
      // Standard
      message: this.message,
      name: this.name,
      // Microsoft
      description: this.description,
      number: this.number,
      // Mozilla
      fileName: this.fileName,
      lineNumber: this.lineNumber,
      columnNumber: this.columnNumber,
      stack: this.stack,
      // Axios
      config: utils.toJSONObject(this.config),
      code: this.code,
      status: this.response && this.response.status ? this.response.status : null
    };
  }
});

const prototype$1 = AxiosError.prototype;
const descriptors = {};

[
  'ERR_BAD_OPTION_VALUE',
  'ERR_BAD_OPTION',
  'ECONNABORTED',
  'ETIMEDOUT',
  'ERR_NETWORK',
  'ERR_FR_TOO_MANY_REDIRECTS',
  'ERR_DEPRECATED',
  'ERR_BAD_RESPONSE',
  'ERR_BAD_REQUEST',
  'ERR_CANCELED',
  'ERR_NOT_SUPPORT',
  'ERR_INVALID_URL'
// eslint-disable-next-line func-names
].forEach(code => {
  descriptors[code] = {value: code};
});

Object.defineProperties(AxiosError, descriptors);
Object.defineProperty(prototype$1, 'isAxiosError', {value: true});

// eslint-disable-next-line func-names
AxiosError.from = (error, code, config, request, response, customProps) => {
  const axiosError = Object.create(prototype$1);

  utils.toFlatObject(error, axiosError, function filter(obj) {
    return obj !== Error.prototype;
  }, prop => {
    return prop !== 'isAxiosError';
  });

  AxiosError.call(axiosError, error.message, code, config, request, response);

  axiosError.cause = error;

  axiosError.name = error.name;

  customProps && Object.assign(axiosError, customProps);

  return axiosError;
};

// eslint-disable-next-line strict
var httpAdapter = null;

/**
 * Determines if the given thing is a array or js object.
 *
 * @param {string} thing - The object or array to be visited.
 *
 * @returns {boolean}
 */
function isVisitable(thing) {
  return utils.isPlainObject(thing) || utils.isArray(thing);
}

/**
 * It removes the brackets from the end of a string
 *
 * @param {string} key - The key of the parameter.
 *
 * @returns {string} the key without the brackets.
 */
function removeBrackets(key) {
  return utils.endsWith(key, '[]') ? key.slice(0, -2) : key;
}

/**
 * It takes a path, a key, and a boolean, and returns a string
 *
 * @param {string} path - The path to the current key.
 * @param {string} key - The key of the current object being iterated over.
 * @param {string} dots - If true, the key will be rendered with dots instead of brackets.
 *
 * @returns {string} The path to the current key.
 */
function renderKey(path, key, dots) {
  if (!path) return key;
  return path.concat(key).map(function each(token, i) {
    // eslint-disable-next-line no-param-reassign
    token = removeBrackets(token);
    return !dots && i ? '[' + token + ']' : token;
  }).join(dots ? '.' : '');
}

/**
 * If the array is an array and none of its elements are visitable, then it's a flat array.
 *
 * @param {Array<any>} arr - The array to check
 *
 * @returns {boolean}
 */
function isFlatArray(arr) {
  return utils.isArray(arr) && !arr.some(isVisitable);
}

const predicates = utils.toFlatObject(utils, {}, null, function filter(prop) {
  return /^is[A-Z]/.test(prop);
});

/**
 * Convert a data object to FormData
 *
 * @param {Object} obj
 * @param {?Object} [formData]
 * @param {?Object} [options]
 * @param {Function} [options.visitor]
 * @param {Boolean} [options.metaTokens = true]
 * @param {Boolean} [options.dots = false]
 * @param {?Boolean} [options.indexes = false]
 *
 * @returns {Object}
 **/

/**
 * It converts an object into a FormData object
 *
 * @param {Object<any, any>} obj - The object to convert to form data.
 * @param {string} formData - The FormData object to append to.
 * @param {Object<string, any>} options
 *
 * @returns
 */
function toFormData(obj, formData, options) {
  if (!utils.isObject(obj)) {
    throw new TypeError('target must be an object');
  }

  // eslint-disable-next-line no-param-reassign
  formData = formData || new (FormData)();

  // eslint-disable-next-line no-param-reassign
  options = utils.toFlatObject(options, {
    metaTokens: true,
    dots: false,
    indexes: false
  }, false, function defined(option, source) {
    // eslint-disable-next-line no-eq-null,eqeqeq
    return !utils.isUndefined(source[option]);
  });

  const metaTokens = options.metaTokens;
  // eslint-disable-next-line no-use-before-define
  const visitor = options.visitor || defaultVisitor;
  const dots = options.dots;
  const indexes = options.indexes;
  const _Blob = options.Blob || typeof Blob !== 'undefined' && Blob;
  const useBlob = _Blob && utils.isSpecCompliantForm(formData);

  if (!utils.isFunction(visitor)) {
    throw new TypeError('visitor must be a function');
  }

  function convertValue(value) {
    if (value === null) return '';

    if (utils.isDate(value)) {
      return value.toISOString();
    }

    if (!useBlob && utils.isBlob(value)) {
      throw new AxiosError('Blob is not supported. Use a Buffer instead.');
    }

    if (utils.isArrayBuffer(value) || utils.isTypedArray(value)) {
      return useBlob && typeof Blob === 'function' ? new Blob([value]) : Buffer.from(value);
    }

    return value;
  }

  /**
   * Default visitor.
   *
   * @param {*} value
   * @param {String|Number} key
   * @param {Array<String|Number>} path
   * @this {FormData}
   *
   * @returns {boolean} return true to visit the each prop of the value recursively
   */
  function defaultVisitor(value, key, path) {
    let arr = value;

    if (value && !path && typeof value === 'object') {
      if (utils.endsWith(key, '{}')) {
        // eslint-disable-next-line no-param-reassign
        key = metaTokens ? key : key.slice(0, -2);
        // eslint-disable-next-line no-param-reassign
        value = JSON.stringify(value);
      } else if (
        (utils.isArray(value) && isFlatArray(value)) ||
        ((utils.isFileList(value) || utils.endsWith(key, '[]')) && (arr = utils.toArray(value))
        )) {
        // eslint-disable-next-line no-param-reassign
        key = removeBrackets(key);

        arr.forEach(function each(el, index) {
          !(utils.isUndefined(el) || el === null) && formData.append(
            // eslint-disable-next-line no-nested-ternary
            indexes === true ? renderKey([key], index, dots) : (indexes === null ? key : key + '[]'),
            convertValue(el)
          );
        });
        return false;
      }
    }

    if (isVisitable(value)) {
      return true;
    }

    formData.append(renderKey(path, key, dots), convertValue(value));

    return false;
  }

  const stack = [];

  const exposedHelpers = Object.assign(predicates, {
    defaultVisitor,
    convertValue,
    isVisitable
  });

  function build(value, path) {
    if (utils.isUndefined(value)) return;

    if (stack.indexOf(value) !== -1) {
      throw Error('Circular reference detected in ' + path.join('.'));
    }

    stack.push(value);

    utils.forEach(value, function each(el, key) {
      const result = !(utils.isUndefined(el) || el === null) && visitor.call(
        formData, el, utils.isString(key) ? key.trim() : key, path, exposedHelpers
      );

      if (result === true) {
        build(el, path ? path.concat(key) : [key]);
      }
    });

    stack.pop();
  }

  if (!utils.isObject(obj)) {
    throw new TypeError('data must be an object');
  }

  build(obj);

  return formData;
}

/**
 * It encodes a string by replacing all characters that are not in the unreserved set with
 * their percent-encoded equivalents
 *
 * @param {string} str - The string to encode.
 *
 * @returns {string} The encoded string.
 */
function encode$1(str) {
  const charMap = {
    '!': '%21',
    "'": '%27',
    '(': '%28',
    ')': '%29',
    '~': '%7E',
    '%20': '+',
    '%00': '\x00'
  };
  return encodeURIComponent(str).replace(/[!'()~]|%20|%00/g, function replacer(match) {
    return charMap[match];
  });
}

/**
 * It takes a params object and converts it to a FormData object
 *
 * @param {Object<string, any>} params - The parameters to be converted to a FormData object.
 * @param {Object<string, any>} options - The options object passed to the Axios constructor.
 *
 * @returns {void}
 */
function AxiosURLSearchParams(params, options) {
  this._pairs = [];

  params && toFormData(params, this, options);
}

const prototype = AxiosURLSearchParams.prototype;

prototype.append = function append(name, value) {
  this._pairs.push([name, value]);
};

prototype.toString = function toString(encoder) {
  const _encode = encoder ? function(value) {
    return encoder.call(this, value, encode$1);
  } : encode$1;

  return this._pairs.map(function each(pair) {
    return _encode(pair[0]) + '=' + _encode(pair[1]);
  }, '').join('&');
};

/**
 * It replaces all instances of the characters `:`, `$`, `,`, `+`, `[`, and `]` with their
 * URI encoded counterparts
 *
 * @param {string} val The value to be encoded.
 *
 * @returns {string} The encoded value.
 */
function encode(val) {
  return encodeURIComponent(val).
    replace(/%3A/gi, ':').
    replace(/%24/g, '$').
    replace(/%2C/gi, ',').
    replace(/%20/g, '+').
    replace(/%5B/gi, '[').
    replace(/%5D/gi, ']');
}

/**
 * Build a URL by appending params to the end
 *
 * @param {string} url The base of the url (e.g., http://www.google.com)
 * @param {object} [params] The params to be appended
 * @param {?object} options
 *
 * @returns {string} The formatted url
 */
function buildURL(url, params, options) {
  /*eslint no-param-reassign:0*/
  if (!params) {
    return url;
  }
  
  const _encode = options && options.encode || encode;

  const serializeFn = options && options.serialize;

  let serializedParams;

  if (serializeFn) {
    serializedParams = serializeFn(params, options);
  } else {
    serializedParams = utils.isURLSearchParams(params) ?
      params.toString() :
      new AxiosURLSearchParams(params, options).toString(_encode);
  }

  if (serializedParams) {
    const hashmarkIndex = url.indexOf("#");

    if (hashmarkIndex !== -1) {
      url = url.slice(0, hashmarkIndex);
    }
    url += (url.indexOf('?') === -1 ? '?' : '&') + serializedParams;
  }

  return url;
}

class InterceptorManager {
  constructor() {
    this.handlers = [];
  }

  /**
   * Add a new interceptor to the stack
   *
   * @param {Function} fulfilled The function to handle `then` for a `Promise`
   * @param {Function} rejected The function to handle `reject` for a `Promise`
   *
   * @return {Number} An ID used to remove interceptor later
   */
  use(fulfilled, rejected, options) {
    this.handlers.push({
      fulfilled,
      rejected,
      synchronous: options ? options.synchronous : false,
      runWhen: options ? options.runWhen : null
    });
    return this.handlers.length - 1;
  }

  /**
   * Remove an interceptor from the stack
   *
   * @param {Number} id The ID that was returned by `use`
   *
   * @returns {Boolean} `true` if the interceptor was removed, `false` otherwise
   */
  eject(id) {
    if (this.handlers[id]) {
      this.handlers[id] = null;
    }
  }

  /**
   * Clear all interceptors from the stack
   *
   * @returns {void}
   */
  clear() {
    if (this.handlers) {
      this.handlers = [];
    }
  }

  /**
   * Iterate over all the registered interceptors
   *
   * This method is particularly useful for skipping over any
   * interceptors that may have become `null` calling `eject`.
   *
   * @param {Function} fn The function to call for each interceptor
   *
   * @returns {void}
   */
  forEach(fn) {
    utils.forEach(this.handlers, function forEachHandler(h) {
      if (h !== null) {
        fn(h);
      }
    });
  }
}

var InterceptorManager$1 = InterceptorManager;

var transitionalDefaults = {
  silentJSONParsing: true,
  forcedJSONParsing: true,
  clarifyTimeoutError: false
};

var URLSearchParams$1 = typeof URLSearchParams !== 'undefined' ? URLSearchParams : AxiosURLSearchParams;

var FormData$1 = typeof FormData !== 'undefined' ? FormData : null;

var Blob$1 = typeof Blob !== 'undefined' ? Blob : null;

/**
 * Determine if we're running in a standard browser environment
 *
 * This allows axios to run in a web worker, and react-native.
 * Both environments support XMLHttpRequest, but not fully standard globals.
 *
 * web workers:
 *  typeof window -> undefined
 *  typeof document -> undefined
 *
 * react-native:
 *  navigator.product -> 'ReactNative'
 * nativescript
 *  navigator.product -> 'NativeScript' or 'NS'
 *
 * @returns {boolean}
 */
const isStandardBrowserEnv = (() => {
  let product;
  if (typeof navigator !== 'undefined' && (
    (product = navigator.product) === 'ReactNative' ||
    product === 'NativeScript' ||
    product === 'NS')
  ) {
    return false;
  }

  return typeof window !== 'undefined' && typeof document !== 'undefined';
})();

/**
 * Determine if we're running in a standard browser webWorker environment
 *
 * Although the `isStandardBrowserEnv` method indicates that
 * `allows axios to run in a web worker`, the WebWorker will still be
 * filtered out due to its judgment standard
 * `typeof window !== 'undefined' && typeof document !== 'undefined'`.
 * This leads to a problem when axios post `FormData` in webWorker
 */
 const isStandardBrowserWebWorkerEnv = (() => {
  return (
    typeof WorkerGlobalScope !== 'undefined' &&
    // eslint-disable-next-line no-undef
    self instanceof WorkerGlobalScope &&
    typeof self.importScripts === 'function'
  );
})();


var platform = {
  isBrowser: true,
  classes: {
    URLSearchParams: URLSearchParams$1,
    FormData: FormData$1,
    Blob: Blob$1
  },
  isStandardBrowserEnv,
  isStandardBrowserWebWorkerEnv,
  protocols: ['http', 'https', 'file', 'blob', 'url', 'data']
};

function toURLEncodedForm(data, options) {
  return toFormData(data, new platform.classes.URLSearchParams(), Object.assign({
    visitor: function(value, key, path, helpers) {
      if (platform.isNode && utils.isBuffer(value)) {
        this.append(key, value.toString('base64'));
        return false;
      }

      return helpers.defaultVisitor.apply(this, arguments);
    }
  }, options));
}

/**
 * It takes a string like `foo[x][y][z]` and returns an array like `['foo', 'x', 'y', 'z']
 *
 * @param {string} name - The name of the property to get.
 *
 * @returns An array of strings.
 */
function parsePropPath(name) {
  // foo[x][y][z]
  // foo.x.y.z
  // foo-x-y-z
  // foo x y z
  return utils.matchAll(/\w+|\[(\w*)]/g, name).map(match => {
    return match[0] === '[]' ? '' : match[1] || match[0];
  });
}

/**
 * Convert an array to an object.
 *
 * @param {Array<any>} arr - The array to convert to an object.
 *
 * @returns An object with the same keys and values as the array.
 */
function arrayToObject(arr) {
  const obj = {};
  const keys = Object.keys(arr);
  let i;
  const len = keys.length;
  let key;
  for (i = 0; i < len; i++) {
    key = keys[i];
    obj[key] = arr[key];
  }
  return obj;
}

/**
 * It takes a FormData object and returns a JavaScript object
 *
 * @param {string} formData The FormData object to convert to JSON.
 *
 * @returns {Object<string, any> | null} The converted object.
 */
function formDataToJSON(formData) {
  function buildPath(path, value, target, index) {
    let name = path[index++];
    const isNumericKey = Number.isFinite(+name);
    const isLast = index >= path.length;
    name = !name && utils.isArray(target) ? target.length : name;

    if (isLast) {
      if (utils.hasOwnProp(target, name)) {
        target[name] = [target[name], value];
      } else {
        target[name] = value;
      }

      return !isNumericKey;
    }

    if (!target[name] || !utils.isObject(target[name])) {
      target[name] = [];
    }

    const result = buildPath(path, value, target[name], index);

    if (result && utils.isArray(target[name])) {
      target[name] = arrayToObject(target[name]);
    }

    return !isNumericKey;
  }

  if (utils.isFormData(formData) && utils.isFunction(formData.entries)) {
    const obj = {};

    utils.forEachEntry(formData, (name, value) => {
      buildPath(parsePropPath(name), value, obj, 0);
    });

    return obj;
  }

  return null;
}

/**
 * It takes a string, tries to parse it, and if it fails, it returns the stringified version
 * of the input
 *
 * @param {any} rawValue - The value to be stringified.
 * @param {Function} parser - A function that parses a string into a JavaScript object.
 * @param {Function} encoder - A function that takes a value and returns a string.
 *
 * @returns {string} A stringified version of the rawValue.
 */
function stringifySafely(rawValue, parser, encoder) {
  if (utils.isString(rawValue)) {
    try {
      (parser || JSON.parse)(rawValue);
      return utils.trim(rawValue);
    } catch (e) {
      if (e.name !== 'SyntaxError') {
        throw e;
      }
    }
  }

  return (encoder || JSON.stringify)(rawValue);
}

const defaults = {

  transitional: transitionalDefaults,

  adapter: ['xhr', 'http'],

  transformRequest: [function transformRequest(data, headers) {
    const contentType = headers.getContentType() || '';
    const hasJSONContentType = contentType.indexOf('application/json') > -1;
    const isObjectPayload = utils.isObject(data);

    if (isObjectPayload && utils.isHTMLForm(data)) {
      data = new FormData(data);
    }

    const isFormData = utils.isFormData(data);

    if (isFormData) {
      if (!hasJSONContentType) {
        return data;
      }
      return hasJSONContentType ? JSON.stringify(formDataToJSON(data)) : data;
    }

    if (utils.isArrayBuffer(data) ||
      utils.isBuffer(data) ||
      utils.isStream(data) ||
      utils.isFile(data) ||
      utils.isBlob(data)
    ) {
      return data;
    }
    if (utils.isArrayBufferView(data)) {
      return data.buffer;
    }
    if (utils.isURLSearchParams(data)) {
      headers.setContentType('application/x-www-form-urlencoded;charset=utf-8', false);
      return data.toString();
    }

    let isFileList;

    if (isObjectPayload) {
      if (contentType.indexOf('application/x-www-form-urlencoded') > -1) {
        return toURLEncodedForm(data, this.formSerializer).toString();
      }

      if ((isFileList = utils.isFileList(data)) || contentType.indexOf('multipart/form-data') > -1) {
        const _FormData = this.env && this.env.FormData;

        return toFormData(
          isFileList ? {'files[]': data} : data,
          _FormData && new _FormData(),
          this.formSerializer
        );
      }
    }

    if (isObjectPayload || hasJSONContentType ) {
      headers.setContentType('application/json', false);
      return stringifySafely(data);
    }

    return data;
  }],

  transformResponse: [function transformResponse(data) {
    const transitional = this.transitional || defaults.transitional;
    const forcedJSONParsing = transitional && transitional.forcedJSONParsing;
    const JSONRequested = this.responseType === 'json';

    if (data && utils.isString(data) && ((forcedJSONParsing && !this.responseType) || JSONRequested)) {
      const silentJSONParsing = transitional && transitional.silentJSONParsing;
      const strictJSONParsing = !silentJSONParsing && JSONRequested;

      try {
        return JSON.parse(data);
      } catch (e) {
        if (strictJSONParsing) {
          if (e.name === 'SyntaxError') {
            throw AxiosError.from(e, AxiosError.ERR_BAD_RESPONSE, this, null, this.response);
          }
          throw e;
        }
      }
    }

    return data;
  }],

  /**
   * A timeout in milliseconds to abort a request. If set to 0 (default) a
   * timeout is not created.
   */
  timeout: 0,

  xsrfCookieName: 'XSRF-TOKEN',
  xsrfHeaderName: 'X-XSRF-TOKEN',

  maxContentLength: -1,
  maxBodyLength: -1,

  env: {
    FormData: platform.classes.FormData,
    Blob: platform.classes.Blob
  },

  validateStatus: function validateStatus(status) {
    return status >= 200 && status < 300;
  },

  headers: {
    common: {
      'Accept': 'application/json, text/plain, */*',
      'Content-Type': undefined
    }
  }
};

utils.forEach(['delete', 'get', 'head', 'post', 'put', 'patch'], (method) => {
  defaults.headers[method] = {};
});

var defaults$1 = defaults;

// RawAxiosHeaders whose duplicates are ignored by node
// c.f. https://nodejs.org/api/http.html#http_message_headers
const ignoreDuplicateOf = utils.toObjectSet([
  'age', 'authorization', 'content-length', 'content-type', 'etag',
  'expires', 'from', 'host', 'if-modified-since', 'if-unmodified-since',
  'last-modified', 'location', 'max-forwards', 'proxy-authorization',
  'referer', 'retry-after', 'user-agent'
]);

/**
 * Parse headers into an object
 *
 * ```
 * Date: Wed, 27 Aug 2014 08:58:49 GMT
 * Content-Type: application/json
 * Connection: keep-alive
 * Transfer-Encoding: chunked
 * ```
 *
 * @param {String} rawHeaders Headers needing to be parsed
 *
 * @returns {Object} Headers parsed into an object
 */
var parseHeaders = rawHeaders => {
  const parsed = {};
  let key;
  let val;
  let i;

  rawHeaders && rawHeaders.split('\n').forEach(function parser(line) {
    i = line.indexOf(':');
    key = line.substring(0, i).trim().toLowerCase();
    val = line.substring(i + 1).trim();

    if (!key || (parsed[key] && ignoreDuplicateOf[key])) {
      return;
    }

    if (key === 'set-cookie') {
      if (parsed[key]) {
        parsed[key].push(val);
      } else {
        parsed[key] = [val];
      }
    } else {
      parsed[key] = parsed[key] ? parsed[key] + ', ' + val : val;
    }
  });

  return parsed;
};

const $internals = Symbol('internals');

function normalizeHeader(header) {
  return header && String(header).trim().toLowerCase();
}

function normalizeValue(value) {
  if (value === false || value == null) {
    return value;
  }

  return utils.isArray(value) ? value.map(normalizeValue) : String(value);
}

function parseTokens(str) {
  const tokens = Object.create(null);
  const tokensRE = /([^\s,;=]+)\s*(?:=\s*([^,;]+))?/g;
  let match;

  while ((match = tokensRE.exec(str))) {
    tokens[match[1]] = match[2];
  }

  return tokens;
}

const isValidHeaderName = (str) => /^[-_a-zA-Z0-9^`|~,!#$%&'*+.]+$/.test(str.trim());

function matchHeaderValue(context, value, header, filter, isHeaderNameFilter) {
  if (utils.isFunction(filter)) {
    return filter.call(this, value, header);
  }

  if (isHeaderNameFilter) {
    value = header;
  }

  if (!utils.isString(value)) return;

  if (utils.isString(filter)) {
    return value.indexOf(filter) !== -1;
  }

  if (utils.isRegExp(filter)) {
    return filter.test(value);
  }
}

function formatHeader(header) {
  return header.trim()
    .toLowerCase().replace(/([a-z\d])(\w*)/g, (w, char, str) => {
      return char.toUpperCase() + str;
    });
}

function buildAccessors(obj, header) {
  const accessorName = utils.toCamelCase(' ' + header);

  ['get', 'set', 'has'].forEach(methodName => {
    Object.defineProperty(obj, methodName + accessorName, {
      value: function(arg1, arg2, arg3) {
        return this[methodName].call(this, header, arg1, arg2, arg3);
      },
      configurable: true
    });
  });
}

class AxiosHeaders {
  constructor(headers) {
    headers && this.set(headers);
  }

  set(header, valueOrRewrite, rewrite) {
    const self = this;

    function setHeader(_value, _header, _rewrite) {
      const lHeader = normalizeHeader(_header);

      if (!lHeader) {
        throw new Error('header name must be a non-empty string');
      }

      const key = utils.findKey(self, lHeader);

      if(!key || self[key] === undefined || _rewrite === true || (_rewrite === undefined && self[key] !== false)) {
        self[key || _header] = normalizeValue(_value);
      }
    }

    const setHeaders = (headers, _rewrite) =>
      utils.forEach(headers, (_value, _header) => setHeader(_value, _header, _rewrite));

    if (utils.isPlainObject(header) || header instanceof this.constructor) {
      setHeaders(header, valueOrRewrite);
    } else if(utils.isString(header) && (header = header.trim()) && !isValidHeaderName(header)) {
      setHeaders(parseHeaders(header), valueOrRewrite);
    } else {
      header != null && setHeader(valueOrRewrite, header, rewrite);
    }

    return this;
  }

  get(header, parser) {
    header = normalizeHeader(header);

    if (header) {
      const key = utils.findKey(this, header);

      if (key) {
        const value = this[key];

        if (!parser) {
          return value;
        }

        if (parser === true) {
          return parseTokens(value);
        }

        if (utils.isFunction(parser)) {
          return parser.call(this, value, key);
        }

        if (utils.isRegExp(parser)) {
          return parser.exec(value);
        }

        throw new TypeError('parser must be boolean|regexp|function');
      }
    }
  }

  has(header, matcher) {
    header = normalizeHeader(header);

    if (header) {
      const key = utils.findKey(this, header);

      return !!(key && this[key] !== undefined && (!matcher || matchHeaderValue(this, this[key], key, matcher)));
    }

    return false;
  }

  delete(header, matcher) {
    const self = this;
    let deleted = false;

    function deleteHeader(_header) {
      _header = normalizeHeader(_header);

      if (_header) {
        const key = utils.findKey(self, _header);

        if (key && (!matcher || matchHeaderValue(self, self[key], key, matcher))) {
          delete self[key];

          deleted = true;
        }
      }
    }

    if (utils.isArray(header)) {
      header.forEach(deleteHeader);
    } else {
      deleteHeader(header);
    }

    return deleted;
  }

  clear(matcher) {
    const keys = Object.keys(this);
    let i = keys.length;
    let deleted = false;

    while (i--) {
      const key = keys[i];
      if(!matcher || matchHeaderValue(this, this[key], key, matcher, true)) {
        delete this[key];
        deleted = true;
      }
    }

    return deleted;
  }

  normalize(format) {
    const self = this;
    const headers = {};

    utils.forEach(this, (value, header) => {
      const key = utils.findKey(headers, header);

      if (key) {
        self[key] = normalizeValue(value);
        delete self[header];
        return;
      }

      const normalized = format ? formatHeader(header) : String(header).trim();

      if (normalized !== header) {
        delete self[header];
      }

      self[normalized] = normalizeValue(value);

      headers[normalized] = true;
    });

    return this;
  }

  concat(...targets) {
    return this.constructor.concat(this, ...targets);
  }

  toJSON(asStrings) {
    const obj = Object.create(null);

    utils.forEach(this, (value, header) => {
      value != null && value !== false && (obj[header] = asStrings && utils.isArray(value) ? value.join(', ') : value);
    });

    return obj;
  }

  [Symbol.iterator]() {
    return Object.entries(this.toJSON())[Symbol.iterator]();
  }

  toString() {
    return Object.entries(this.toJSON()).map(([header, value]) => header + ': ' + value).join('\n');
  }

  get [Symbol.toStringTag]() {
    return 'AxiosHeaders';
  }

  static from(thing) {
    return thing instanceof this ? thing : new this(thing);
  }

  static concat(first, ...targets) {
    const computed = new this(first);

    targets.forEach((target) => computed.set(target));

    return computed;
  }

  static accessor(header) {
    const internals = this[$internals] = (this[$internals] = {
      accessors: {}
    });

    const accessors = internals.accessors;
    const prototype = this.prototype;

    function defineAccessor(_header) {
      const lHeader = normalizeHeader(_header);

      if (!accessors[lHeader]) {
        buildAccessors(prototype, _header);
        accessors[lHeader] = true;
      }
    }

    utils.isArray(header) ? header.forEach(defineAccessor) : defineAccessor(header);

    return this;
  }
}

AxiosHeaders.accessor(['Content-Type', 'Content-Length', 'Accept', 'Accept-Encoding', 'User-Agent', 'Authorization']);

// reserved names hotfix
utils.reduceDescriptors(AxiosHeaders.prototype, ({value}, key) => {
  let mapped = key[0].toUpperCase() + key.slice(1); // map `set` => `Set`
  return {
    get: () => value,
    set(headerValue) {
      this[mapped] = headerValue;
    }
  }
});

utils.freezeMethods(AxiosHeaders);

var AxiosHeaders$1 = AxiosHeaders;

/**
 * Transform the data for a request or a response
 *
 * @param {Array|Function} fns A single function or Array of functions
 * @param {?Object} response The response object
 *
 * @returns {*} The resulting transformed data
 */
function transformData(fns, response) {
  const config = this || defaults$1;
  const context = response || config;
  const headers = AxiosHeaders$1.from(context.headers);
  let data = context.data;

  utils.forEach(fns, function transform(fn) {
    data = fn.call(config, data, headers.normalize(), response ? response.status : undefined);
  });

  headers.normalize();

  return data;
}

function isCancel(value) {
  return !!(value && value.__CANCEL__);
}

/**
 * A `CanceledError` is an object that is thrown when an operation is canceled.
 *
 * @param {string=} message The message.
 * @param {Object=} config The config.
 * @param {Object=} request The request.
 *
 * @returns {CanceledError} The created error.
 */
function CanceledError(message, config, request) {
  // eslint-disable-next-line no-eq-null,eqeqeq
  AxiosError.call(this, message == null ? 'canceled' : message, AxiosError.ERR_CANCELED, config, request);
  this.name = 'CanceledError';
}

utils.inherits(CanceledError, AxiosError, {
  __CANCEL__: true
});

/**
 * Resolve or reject a Promise based on response status.
 *
 * @param {Function} resolve A function that resolves the promise.
 * @param {Function} reject A function that rejects the promise.
 * @param {object} response The response.
 *
 * @returns {object} The response.
 */
function settle(resolve, reject, response) {
  const validateStatus = response.config.validateStatus;
  if (!response.status || !validateStatus || validateStatus(response.status)) {
    resolve(response);
  } else {
    reject(new AxiosError(
      'Request failed with status code ' + response.status,
      [AxiosError.ERR_BAD_REQUEST, AxiosError.ERR_BAD_RESPONSE][Math.floor(response.status / 100) - 4],
      response.config,
      response.request,
      response
    ));
  }
}

var cookies = platform.isStandardBrowserEnv ?

// Standard browser envs support document.cookie
  (function standardBrowserEnv() {
    return {
      write: function write(name, value, expires, path, domain, secure) {
        const cookie = [];
        cookie.push(name + '=' + encodeURIComponent(value));

        if (utils.isNumber(expires)) {
          cookie.push('expires=' + new Date(expires).toGMTString());
        }

        if (utils.isString(path)) {
          cookie.push('path=' + path);
        }

        if (utils.isString(domain)) {
          cookie.push('domain=' + domain);
        }

        if (secure === true) {
          cookie.push('secure');
        }

        document.cookie = cookie.join('; ');
      },

      read: function read(name) {
        const match = document.cookie.match(new RegExp('(^|;\\s*)(' + name + ')=([^;]*)'));
        return (match ? decodeURIComponent(match[3]) : null);
      },

      remove: function remove(name) {
        this.write(name, '', Date.now() - 86400000);
      }
    };
  })() :

// Non standard browser env (web workers, react-native) lack needed support.
  (function nonStandardBrowserEnv() {
    return {
      write: function write() {},
      read: function read() { return null; },
      remove: function remove() {}
    };
  })();

/**
 * Determines whether the specified URL is absolute
 *
 * @param {string} url The URL to test
 *
 * @returns {boolean} True if the specified URL is absolute, otherwise false
 */
function isAbsoluteURL(url) {
  // A URL is considered absolute if it begins with "<scheme>://" or "//" (protocol-relative URL).
  // RFC 3986 defines scheme name as a sequence of characters beginning with a letter and followed
  // by any combination of letters, digits, plus, period, or hyphen.
  return /^([a-z][a-z\d+\-.]*:)?\/\//i.test(url);
}

/**
 * Creates a new URL by combining the specified URLs
 *
 * @param {string} baseURL The base URL
 * @param {string} relativeURL The relative URL
 *
 * @returns {string} The combined URL
 */
function combineURLs(baseURL, relativeURL) {
  return relativeURL
    ? baseURL.replace(/\/+$/, '') + '/' + relativeURL.replace(/^\/+/, '')
    : baseURL;
}

/**
 * Creates a new URL by combining the baseURL with the requestedURL,
 * only when the requestedURL is not already an absolute URL.
 * If the requestURL is absolute, this function returns the requestedURL untouched.
 *
 * @param {string} baseURL The base URL
 * @param {string} requestedURL Absolute or relative URL to combine
 *
 * @returns {string} The combined full path
 */
function buildFullPath(baseURL, requestedURL) {
  if (baseURL && !isAbsoluteURL(requestedURL)) {
    return combineURLs(baseURL, requestedURL);
  }
  return requestedURL;
}

var isURLSameOrigin = platform.isStandardBrowserEnv ?

// Standard browser envs have full support of the APIs needed to test
// whether the request URL is of the same origin as current location.
  (function standardBrowserEnv() {
    const msie = /(msie|trident)/i.test(navigator.userAgent);
    const urlParsingNode = document.createElement('a');
    let originURL;

    /**
    * Parse a URL to discover it's components
    *
    * @param {String} url The URL to be parsed
    * @returns {Object}
    */
    function resolveURL(url) {
      let href = url;

      if (msie) {
        // IE needs attribute set twice to normalize properties
        urlParsingNode.setAttribute('href', href);
        href = urlParsingNode.href;
      }

      urlParsingNode.setAttribute('href', href);

      // urlParsingNode provides the UrlUtils interface - http://url.spec.whatwg.org/#urlutils
      return {
        href: urlParsingNode.href,
        protocol: urlParsingNode.protocol ? urlParsingNode.protocol.replace(/:$/, '') : '',
        host: urlParsingNode.host,
        search: urlParsingNode.search ? urlParsingNode.search.replace(/^\?/, '') : '',
        hash: urlParsingNode.hash ? urlParsingNode.hash.replace(/^#/, '') : '',
        hostname: urlParsingNode.hostname,
        port: urlParsingNode.port,
        pathname: (urlParsingNode.pathname.charAt(0) === '/') ?
          urlParsingNode.pathname :
          '/' + urlParsingNode.pathname
      };
    }

    originURL = resolveURL(window.location.href);

    /**
    * Determine if a URL shares the same origin as the current location
    *
    * @param {String} requestURL The URL to test
    * @returns {boolean} True if URL shares the same origin, otherwise false
    */
    return function isURLSameOrigin(requestURL) {
      const parsed = (utils.isString(requestURL)) ? resolveURL(requestURL) : requestURL;
      return (parsed.protocol === originURL.protocol &&
          parsed.host === originURL.host);
    };
  })() :

  // Non standard browser envs (web workers, react-native) lack needed support.
  (function nonStandardBrowserEnv() {
    return function isURLSameOrigin() {
      return true;
    };
  })();

function parseProtocol(url) {
  const match = /^([-+\w]{1,25})(:?\/\/|:)/.exec(url);
  return match && match[1] || '';
}

/**
 * Calculate data maxRate
 * @param {Number} [samplesCount= 10]
 * @param {Number} [min= 1000]
 * @returns {Function}
 */
function speedometer(samplesCount, min) {
  samplesCount = samplesCount || 10;
  const bytes = new Array(samplesCount);
  const timestamps = new Array(samplesCount);
  let head = 0;
  let tail = 0;
  let firstSampleTS;

  min = min !== undefined ? min : 1000;

  return function push(chunkLength) {
    const now = Date.now();

    const startedAt = timestamps[tail];

    if (!firstSampleTS) {
      firstSampleTS = now;
    }

    bytes[head] = chunkLength;
    timestamps[head] = now;

    let i = tail;
    let bytesCount = 0;

    while (i !== head) {
      bytesCount += bytes[i++];
      i = i % samplesCount;
    }

    head = (head + 1) % samplesCount;

    if (head === tail) {
      tail = (tail + 1) % samplesCount;
    }

    if (now - firstSampleTS < min) {
      return;
    }

    const passed = startedAt && now - startedAt;

    return passed ? Math.round(bytesCount * 1000 / passed) : undefined;
  };
}

function progressEventReducer(listener, isDownloadStream) {
  let bytesNotified = 0;
  const _speedometer = speedometer(50, 250);

  return e => {
    const loaded = e.loaded;
    const total = e.lengthComputable ? e.total : undefined;
    const progressBytes = loaded - bytesNotified;
    const rate = _speedometer(progressBytes);
    const inRange = loaded <= total;

    bytesNotified = loaded;

    const data = {
      loaded,
      total,
      progress: total ? (loaded / total) : undefined,
      bytes: progressBytes,
      rate: rate ? rate : undefined,
      estimated: rate && total && inRange ? (total - loaded) / rate : undefined,
      event: e
    };

    data[isDownloadStream ? 'download' : 'upload'] = true;

    listener(data);
  };
}

const isXHRAdapterSupported = typeof XMLHttpRequest !== 'undefined';

var xhrAdapter = isXHRAdapterSupported && function (config) {
  return new Promise(function dispatchXhrRequest(resolve, reject) {
    let requestData = config.data;
    const requestHeaders = AxiosHeaders$1.from(config.headers).normalize();
    const responseType = config.responseType;
    let onCanceled;
    function done() {
      if (config.cancelToken) {
        config.cancelToken.unsubscribe(onCanceled);
      }

      if (config.signal) {
        config.signal.removeEventListener('abort', onCanceled);
      }
    }

    let contentType;

    if (utils.isFormData(requestData)) {
      if (platform.isStandardBrowserEnv || platform.isStandardBrowserWebWorkerEnv) {
        requestHeaders.setContentType(false); // Let the browser set it
      } else if(!requestHeaders.getContentType(/^\s*multipart\/form-data/)){
        requestHeaders.setContentType('multipart/form-data'); // mobile/desktop app frameworks
      } else if(utils.isString(contentType = requestHeaders.getContentType())){
        // fix semicolon duplication issue for ReactNative FormData implementation
        requestHeaders.setContentType(contentType.replace(/^\s*(multipart\/form-data);+/, '$1'));
      }
    }

    let request = new XMLHttpRequest();

    // HTTP basic authentication
    if (config.auth) {
      const username = config.auth.username || '';
      const password = config.auth.password ? unescape(encodeURIComponent(config.auth.password)) : '';
      requestHeaders.set('Authorization', 'Basic ' + btoa(username + ':' + password));
    }

    const fullPath = buildFullPath(config.baseURL, config.url);

    request.open(config.method.toUpperCase(), buildURL(fullPath, config.params, config.paramsSerializer), true);

    // Set the request timeout in MS
    request.timeout = config.timeout;

    function onloadend() {
      if (!request) {
        return;
      }
      // Prepare the response
      const responseHeaders = AxiosHeaders$1.from(
        'getAllResponseHeaders' in request && request.getAllResponseHeaders()
      );
      const responseData = !responseType || responseType === 'text' || responseType === 'json' ?
        request.responseText : request.response;
      const response = {
        data: responseData,
        status: request.status,
        statusText: request.statusText,
        headers: responseHeaders,
        config,
        request
      };

      settle(function _resolve(value) {
        resolve(value);
        done();
      }, function _reject(err) {
        reject(err);
        done();
      }, response);

      // Clean up request
      request = null;
    }

    if ('onloadend' in request) {
      // Use onloadend if available
      request.onloadend = onloadend;
    } else {
      // Listen for ready state to emulate onloadend
      request.onreadystatechange = function handleLoad() {
        if (!request || request.readyState !== 4) {
          return;
        }

        // The request errored out and we didn't get a response, this will be
        // handled by onerror instead
        // With one exception: request that using file: protocol, most browsers
        // will return status as 0 even though it's a successful request
        if (request.status === 0 && !(request.responseURL && request.responseURL.indexOf('file:') === 0)) {
          return;
        }
        // readystate handler is calling before onerror or ontimeout handlers,
        // so we should call onloadend on the next 'tick'
        setTimeout(onloadend);
      };
    }

    // Handle browser request cancellation (as opposed to a manual cancellation)
    request.onabort = function handleAbort() {
      if (!request) {
        return;
      }

      reject(new AxiosError('Request aborted', AxiosError.ECONNABORTED, config, request));

      // Clean up request
      request = null;
    };

    // Handle low level network errors
    request.onerror = function handleError() {
      // Real errors are hidden from us by the browser
      // onerror should only fire if it's a network error
      reject(new AxiosError('Network Error', AxiosError.ERR_NETWORK, config, request));

      // Clean up request
      request = null;
    };

    // Handle timeout
    request.ontimeout = function handleTimeout() {
      let timeoutErrorMessage = config.timeout ? 'timeout of ' + config.timeout + 'ms exceeded' : 'timeout exceeded';
      const transitional = config.transitional || transitionalDefaults;
      if (config.timeoutErrorMessage) {
        timeoutErrorMessage = config.timeoutErrorMessage;
      }
      reject(new AxiosError(
        timeoutErrorMessage,
        transitional.clarifyTimeoutError ? AxiosError.ETIMEDOUT : AxiosError.ECONNABORTED,
        config,
        request));

      // Clean up request
      request = null;
    };

    // Add xsrf header
    // This is only done if running in a standard browser environment.
    // Specifically not if we're in a web worker, or react-native.
    if (platform.isStandardBrowserEnv) {
      // Add xsrf header
      const xsrfValue = (config.withCredentials || isURLSameOrigin(fullPath))
        && config.xsrfCookieName && cookies.read(config.xsrfCookieName);

      if (xsrfValue) {
        requestHeaders.set(config.xsrfHeaderName, xsrfValue);
      }
    }

    // Remove Content-Type if data is undefined
    requestData === undefined && requestHeaders.setContentType(null);

    // Add headers to the request
    if ('setRequestHeader' in request) {
      utils.forEach(requestHeaders.toJSON(), function setRequestHeader(val, key) {
        request.setRequestHeader(key, val);
      });
    }

    // Add withCredentials to request if needed
    if (!utils.isUndefined(config.withCredentials)) {
      request.withCredentials = !!config.withCredentials;
    }

    // Add responseType to request if needed
    if (responseType && responseType !== 'json') {
      request.responseType = config.responseType;
    }

    // Handle progress if needed
    if (typeof config.onDownloadProgress === 'function') {
      request.addEventListener('progress', progressEventReducer(config.onDownloadProgress, true));
    }

    // Not all browsers support upload events
    if (typeof config.onUploadProgress === 'function' && request.upload) {
      request.upload.addEventListener('progress', progressEventReducer(config.onUploadProgress));
    }

    if (config.cancelToken || config.signal) {
      // Handle cancellation
      // eslint-disable-next-line func-names
      onCanceled = cancel => {
        if (!request) {
          return;
        }
        reject(!cancel || cancel.type ? new CanceledError(null, config, request) : cancel);
        request.abort();
        request = null;
      };

      config.cancelToken && config.cancelToken.subscribe(onCanceled);
      if (config.signal) {
        config.signal.aborted ? onCanceled() : config.signal.addEventListener('abort', onCanceled);
      }
    }

    const protocol = parseProtocol(fullPath);

    if (protocol && platform.protocols.indexOf(protocol) === -1) {
      reject(new AxiosError('Unsupported protocol ' + protocol + ':', AxiosError.ERR_BAD_REQUEST, config));
      return;
    }


    // Send the request
    request.send(requestData || null);
  });
};

const knownAdapters = {
  http: httpAdapter,
  xhr: xhrAdapter
};

utils.forEach(knownAdapters, (fn, value) => {
  if (fn) {
    try {
      Object.defineProperty(fn, 'name', {value});
    } catch (e) {
      // eslint-disable-next-line no-empty
    }
    Object.defineProperty(fn, 'adapterName', {value});
  }
});

const renderReason = (reason) => `- ${reason}`;

const isResolvedHandle = (adapter) => utils.isFunction(adapter) || adapter === null || adapter === false;

var adapters = {
  getAdapter: (adapters) => {
    adapters = utils.isArray(adapters) ? adapters : [adapters];

    const {length} = adapters;
    let nameOrAdapter;
    let adapter;

    const rejectedReasons = {};

    for (let i = 0; i < length; i++) {
      nameOrAdapter = adapters[i];
      let id;

      adapter = nameOrAdapter;

      if (!isResolvedHandle(nameOrAdapter)) {
        adapter = knownAdapters[(id = String(nameOrAdapter)).toLowerCase()];

        if (adapter === undefined) {
          throw new AxiosError(`Unknown adapter '${id}'`);
        }
      }

      if (adapter) {
        break;
      }

      rejectedReasons[id || '#' + i] = adapter;
    }

    if (!adapter) {

      const reasons = Object.entries(rejectedReasons)
        .map(([id, state]) => `adapter ${id} ` +
          (state === false ? 'is not supported by the environment' : 'is not available in the build')
        );

      let s = length ?
        (reasons.length > 1 ? 'since :\n' + reasons.map(renderReason).join('\n') : ' ' + renderReason(reasons[0])) :
        'as no adapter specified';

      throw new AxiosError(
        `There is no suitable adapter to dispatch the request ` + s,
        'ERR_NOT_SUPPORT'
      );
    }

    return adapter;
  },
  adapters: knownAdapters
};

/**
 * Throws a `CanceledError` if cancellation has been requested.
 *
 * @param {Object} config The config that is to be used for the request
 *
 * @returns {void}
 */
function throwIfCancellationRequested(config) {
  if (config.cancelToken) {
    config.cancelToken.throwIfRequested();
  }

  if (config.signal && config.signal.aborted) {
    throw new CanceledError(null, config);
  }
}

/**
 * Dispatch a request to the server using the configured adapter.
 *
 * @param {object} config The config that is to be used for the request
 *
 * @returns {Promise} The Promise to be fulfilled
 */
function dispatchRequest(config) {
  throwIfCancellationRequested(config);

  config.headers = AxiosHeaders$1.from(config.headers);

  // Transform request data
  config.data = transformData.call(
    config,
    config.transformRequest
  );

  if (['post', 'put', 'patch'].indexOf(config.method) !== -1) {
    config.headers.setContentType('application/x-www-form-urlencoded', false);
  }

  const adapter = adapters.getAdapter(config.adapter || defaults$1.adapter);

  return adapter(config).then(function onAdapterResolution(response) {
    throwIfCancellationRequested(config);

    // Transform response data
    response.data = transformData.call(
      config,
      config.transformResponse,
      response
    );

    response.headers = AxiosHeaders$1.from(response.headers);

    return response;
  }, function onAdapterRejection(reason) {
    if (!isCancel(reason)) {
      throwIfCancellationRequested(config);

      // Transform response data
      if (reason && reason.response) {
        reason.response.data = transformData.call(
          config,
          config.transformResponse,
          reason.response
        );
        reason.response.headers = AxiosHeaders$1.from(reason.response.headers);
      }
    }

    return Promise.reject(reason);
  });
}

const headersToObject = (thing) => thing instanceof AxiosHeaders$1 ? thing.toJSON() : thing;

/**
 * Config-specific merge-function which creates a new config-object
 * by merging two configuration objects together.
 *
 * @param {Object} config1
 * @param {Object} config2
 *
 * @returns {Object} New object resulting from merging config2 to config1
 */
function mergeConfig(config1, config2) {
  // eslint-disable-next-line no-param-reassign
  config2 = config2 || {};
  const config = {};

  function getMergedValue(target, source, caseless) {
    if (utils.isPlainObject(target) && utils.isPlainObject(source)) {
      return utils.merge.call({caseless}, target, source);
    } else if (utils.isPlainObject(source)) {
      return utils.merge({}, source);
    } else if (utils.isArray(source)) {
      return source.slice();
    }
    return source;
  }

  // eslint-disable-next-line consistent-return
  function mergeDeepProperties(a, b, caseless) {
    if (!utils.isUndefined(b)) {
      return getMergedValue(a, b, caseless);
    } else if (!utils.isUndefined(a)) {
      return getMergedValue(undefined, a, caseless);
    }
  }

  // eslint-disable-next-line consistent-return
  function valueFromConfig2(a, b) {
    if (!utils.isUndefined(b)) {
      return getMergedValue(undefined, b);
    }
  }

  // eslint-disable-next-line consistent-return
  function defaultToConfig2(a, b) {
    if (!utils.isUndefined(b)) {
      return getMergedValue(undefined, b);
    } else if (!utils.isUndefined(a)) {
      return getMergedValue(undefined, a);
    }
  }

  // eslint-disable-next-line consistent-return
  function mergeDirectKeys(a, b, prop) {
    if (prop in config2) {
      return getMergedValue(a, b);
    } else if (prop in config1) {
      return getMergedValue(undefined, a);
    }
  }

  const mergeMap = {
    url: valueFromConfig2,
    method: valueFromConfig2,
    data: valueFromConfig2,
    baseURL: defaultToConfig2,
    transformRequest: defaultToConfig2,
    transformResponse: defaultToConfig2,
    paramsSerializer: defaultToConfig2,
    timeout: defaultToConfig2,
    timeoutMessage: defaultToConfig2,
    withCredentials: defaultToConfig2,
    adapter: defaultToConfig2,
    responseType: defaultToConfig2,
    xsrfCookieName: defaultToConfig2,
    xsrfHeaderName: defaultToConfig2,
    onUploadProgress: defaultToConfig2,
    onDownloadProgress: defaultToConfig2,
    decompress: defaultToConfig2,
    maxContentLength: defaultToConfig2,
    maxBodyLength: defaultToConfig2,
    beforeRedirect: defaultToConfig2,
    transport: defaultToConfig2,
    httpAgent: defaultToConfig2,
    httpsAgent: defaultToConfig2,
    cancelToken: defaultToConfig2,
    socketPath: defaultToConfig2,
    responseEncoding: defaultToConfig2,
    validateStatus: mergeDirectKeys,
    headers: (a, b) => mergeDeepProperties(headersToObject(a), headersToObject(b), true)
  };

  utils.forEach(Object.keys(Object.assign({}, config1, config2)), function computeConfigValue(prop) {
    const merge = mergeMap[prop] || mergeDeepProperties;
    const configValue = merge(config1[prop], config2[prop], prop);
    (utils.isUndefined(configValue) && merge !== mergeDirectKeys) || (config[prop] = configValue);
  });

  return config;
}

const VERSION = "1.5.1";

const validators$1 = {};

// eslint-disable-next-line func-names
['object', 'boolean', 'number', 'function', 'string', 'symbol'].forEach((type, i) => {
  validators$1[type] = function validator(thing) {
    return typeof thing === type || 'a' + (i < 1 ? 'n ' : ' ') + type;
  };
});

const deprecatedWarnings = {};

/**
 * Transitional option validator
 *
 * @param {function|boolean?} validator - set to false if the transitional option has been removed
 * @param {string?} version - deprecated version / removed since version
 * @param {string?} message - some message with additional info
 *
 * @returns {function}
 */
validators$1.transitional = function transitional(validator, version, message) {
  function formatMessage(opt, desc) {
    return '[Axios v' + VERSION + '] Transitional option \'' + opt + '\'' + desc + (message ? '. ' + message : '');
  }

  // eslint-disable-next-line func-names
  return (value, opt, opts) => {
    if (validator === false) {
      throw new AxiosError(
        formatMessage(opt, ' has been removed' + (version ? ' in ' + version : '')),
        AxiosError.ERR_DEPRECATED
      );
    }

    if (version && !deprecatedWarnings[opt]) {
      deprecatedWarnings[opt] = true;
      // eslint-disable-next-line no-console
      console.warn(
        formatMessage(
          opt,
          ' has been deprecated since v' + version + ' and will be removed in the near future'
        )
      );
    }

    return validator ? validator(value, opt, opts) : true;
  };
};

/**
 * Assert object's properties type
 *
 * @param {object} options
 * @param {object} schema
 * @param {boolean?} allowUnknown
 *
 * @returns {object}
 */

function assertOptions(options, schema, allowUnknown) {
  if (typeof options !== 'object') {
    throw new AxiosError('options must be an object', AxiosError.ERR_BAD_OPTION_VALUE);
  }
  const keys = Object.keys(options);
  let i = keys.length;
  while (i-- > 0) {
    const opt = keys[i];
    const validator = schema[opt];
    if (validator) {
      const value = options[opt];
      const result = value === undefined || validator(value, opt, options);
      if (result !== true) {
        throw new AxiosError('option ' + opt + ' must be ' + result, AxiosError.ERR_BAD_OPTION_VALUE);
      }
      continue;
    }
    if (allowUnknown !== true) {
      throw new AxiosError('Unknown option ' + opt, AxiosError.ERR_BAD_OPTION);
    }
  }
}

var validator = {
  assertOptions,
  validators: validators$1
};

const validators = validator.validators;

/**
 * Create a new instance of Axios
 *
 * @param {Object} instanceConfig The default config for the instance
 *
 * @return {Axios} A new instance of Axios
 */
class Axios {
  constructor(instanceConfig) {
    this.defaults = instanceConfig;
    this.interceptors = {
      request: new InterceptorManager$1(),
      response: new InterceptorManager$1()
    };
  }

  /**
   * Dispatch a request
   *
   * @param {String|Object} configOrUrl The config specific for this request (merged with this.defaults)
   * @param {?Object} config
   *
   * @returns {Promise} The Promise to be fulfilled
   */
  request(configOrUrl, config) {
    /*eslint no-param-reassign:0*/
    // Allow for axios('example/url'[, config]) a la fetch API
    if (typeof configOrUrl === 'string') {
      config = config || {};
      config.url = configOrUrl;
    } else {
      config = configOrUrl || {};
    }

    config = mergeConfig(this.defaults, config);

    const {transitional, paramsSerializer, headers} = config;

    if (transitional !== undefined) {
      validator.assertOptions(transitional, {
        silentJSONParsing: validators.transitional(validators.boolean),
        forcedJSONParsing: validators.transitional(validators.boolean),
        clarifyTimeoutError: validators.transitional(validators.boolean)
      }, false);
    }

    if (paramsSerializer != null) {
      if (utils.isFunction(paramsSerializer)) {
        config.paramsSerializer = {
          serialize: paramsSerializer
        };
      } else {
        validator.assertOptions(paramsSerializer, {
          encode: validators.function,
          serialize: validators.function
        }, true);
      }
    }

    // Set config.method
    config.method = (config.method || this.defaults.method || 'get').toLowerCase();

    // Flatten headers
    let contextHeaders = headers && utils.merge(
      headers.common,
      headers[config.method]
    );

    headers && utils.forEach(
      ['delete', 'get', 'head', 'post', 'put', 'patch', 'common'],
      (method) => {
        delete headers[method];
      }
    );

    config.headers = AxiosHeaders$1.concat(contextHeaders, headers);

    // filter out skipped interceptors
    const requestInterceptorChain = [];
    let synchronousRequestInterceptors = true;
    this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
      if (typeof interceptor.runWhen === 'function' && interceptor.runWhen(config) === false) {
        return;
      }

      synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;

      requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);
    });

    const responseInterceptorChain = [];
    this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
      responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
    });

    let promise;
    let i = 0;
    let len;

    if (!synchronousRequestInterceptors) {
      const chain = [dispatchRequest.bind(this), undefined];
      chain.unshift.apply(chain, requestInterceptorChain);
      chain.push.apply(chain, responseInterceptorChain);
      len = chain.length;

      promise = Promise.resolve(config);

      while (i < len) {
        promise = promise.then(chain[i++], chain[i++]);
      }

      return promise;
    }

    len = requestInterceptorChain.length;

    let newConfig = config;

    i = 0;

    while (i < len) {
      const onFulfilled = requestInterceptorChain[i++];
      const onRejected = requestInterceptorChain[i++];
      try {
        newConfig = onFulfilled(newConfig);
      } catch (error) {
        onRejected.call(this, error);
        break;
      }
    }

    try {
      promise = dispatchRequest.call(this, newConfig);
    } catch (error) {
      return Promise.reject(error);
    }

    i = 0;
    len = responseInterceptorChain.length;

    while (i < len) {
      promise = promise.then(responseInterceptorChain[i++], responseInterceptorChain[i++]);
    }

    return promise;
  }

  getUri(config) {
    config = mergeConfig(this.defaults, config);
    const fullPath = buildFullPath(config.baseURL, config.url);
    return buildURL(fullPath, config.params, config.paramsSerializer);
  }
}

// Provide aliases for supported request methods
utils.forEach(['delete', 'get', 'head', 'options'], function forEachMethodNoData(method) {
  /*eslint func-names:0*/
  Axios.prototype[method] = function(url, config) {
    return this.request(mergeConfig(config || {}, {
      method,
      url,
      data: (config || {}).data
    }));
  };
});

utils.forEach(['post', 'put', 'patch'], function forEachMethodWithData(method) {
  /*eslint func-names:0*/

  function generateHTTPMethod(isForm) {
    return function httpMethod(url, data, config) {
      return this.request(mergeConfig(config || {}, {
        method,
        headers: isForm ? {
          'Content-Type': 'multipart/form-data'
        } : {},
        url,
        data
      }));
    };
  }

  Axios.prototype[method] = generateHTTPMethod();

  Axios.prototype[method + 'Form'] = generateHTTPMethod(true);
});

var Axios$1 = Axios;

/**
 * A `CancelToken` is an object that can be used to request cancellation of an operation.
 *
 * @param {Function} executor The executor function.
 *
 * @returns {CancelToken}
 */
class CancelToken {
  constructor(executor) {
    if (typeof executor !== 'function') {
      throw new TypeError('executor must be a function.');
    }

    let resolvePromise;

    this.promise = new Promise(function promiseExecutor(resolve) {
      resolvePromise = resolve;
    });

    const token = this;

    // eslint-disable-next-line func-names
    this.promise.then(cancel => {
      if (!token._listeners) return;

      let i = token._listeners.length;

      while (i-- > 0) {
        token._listeners[i](cancel);
      }
      token._listeners = null;
    });

    // eslint-disable-next-line func-names
    this.promise.then = onfulfilled => {
      let _resolve;
      // eslint-disable-next-line func-names
      const promise = new Promise(resolve => {
        token.subscribe(resolve);
        _resolve = resolve;
      }).then(onfulfilled);

      promise.cancel = function reject() {
        token.unsubscribe(_resolve);
      };

      return promise;
    };

    executor(function cancel(message, config, request) {
      if (token.reason) {
        // Cancellation has already been requested
        return;
      }

      token.reason = new CanceledError(message, config, request);
      resolvePromise(token.reason);
    });
  }

  /**
   * Throws a `CanceledError` if cancellation has been requested.
   */
  throwIfRequested() {
    if (this.reason) {
      throw this.reason;
    }
  }

  /**
   * Subscribe to the cancel signal
   */

  subscribe(listener) {
    if (this.reason) {
      listener(this.reason);
      return;
    }

    if (this._listeners) {
      this._listeners.push(listener);
    } else {
      this._listeners = [listener];
    }
  }

  /**
   * Unsubscribe from the cancel signal
   */

  unsubscribe(listener) {
    if (!this._listeners) {
      return;
    }
    const index = this._listeners.indexOf(listener);
    if (index !== -1) {
      this._listeners.splice(index, 1);
    }
  }

  /**
   * Returns an object that contains a new `CancelToken` and a function that, when called,
   * cancels the `CancelToken`.
   */
  static source() {
    let cancel;
    const token = new CancelToken(function executor(c) {
      cancel = c;
    });
    return {
      token,
      cancel
    };
  }
}

var CancelToken$1 = CancelToken;

/**
 * Syntactic sugar for invoking a function and expanding an array for arguments.
 *
 * Common use case would be to use `Function.prototype.apply`.
 *
 *  ```js
 *  function f(x, y, z) {}
 *  var args = [1, 2, 3];
 *  f.apply(null, args);
 *  ```
 *
 * With `spread` this example can be re-written.
 *
 *  ```js
 *  spread(function(x, y, z) {})([1, 2, 3]);
 *  ```
 *
 * @param {Function} callback
 *
 * @returns {Function}
 */
function spread(callback) {
  return function wrap(arr) {
    return callback.apply(null, arr);
  };
}

/**
 * Determines whether the payload is an error thrown by Axios
 *
 * @param {*} payload The value to test
 *
 * @returns {boolean} True if the payload is an error thrown by Axios, otherwise false
 */
function isAxiosError(payload) {
  return utils.isObject(payload) && (payload.isAxiosError === true);
}

const HttpStatusCode = {
  Continue: 100,
  SwitchingProtocols: 101,
  Processing: 102,
  EarlyHints: 103,
  Ok: 200,
  Created: 201,
  Accepted: 202,
  NonAuthoritativeInformation: 203,
  NoContent: 204,
  ResetContent: 205,
  PartialContent: 206,
  MultiStatus: 207,
  AlreadyReported: 208,
  ImUsed: 226,
  MultipleChoices: 300,
  MovedPermanently: 301,
  Found: 302,
  SeeOther: 303,
  NotModified: 304,
  UseProxy: 305,
  Unused: 306,
  TemporaryRedirect: 307,
  PermanentRedirect: 308,
  BadRequest: 400,
  Unauthorized: 401,
  PaymentRequired: 402,
  Forbidden: 403,
  NotFound: 404,
  MethodNotAllowed: 405,
  NotAcceptable: 406,
  ProxyAuthenticationRequired: 407,
  RequestTimeout: 408,
  Conflict: 409,
  Gone: 410,
  LengthRequired: 411,
  PreconditionFailed: 412,
  PayloadTooLarge: 413,
  UriTooLong: 414,
  UnsupportedMediaType: 415,
  RangeNotSatisfiable: 416,
  ExpectationFailed: 417,
  ImATeapot: 418,
  MisdirectedRequest: 421,
  UnprocessableEntity: 422,
  Locked: 423,
  FailedDependency: 424,
  TooEarly: 425,
  UpgradeRequired: 426,
  PreconditionRequired: 428,
  TooManyRequests: 429,
  RequestHeaderFieldsTooLarge: 431,
  UnavailableForLegalReasons: 451,
  InternalServerError: 500,
  NotImplemented: 501,
  BadGateway: 502,
  ServiceUnavailable: 503,
  GatewayTimeout: 504,
  HttpVersionNotSupported: 505,
  VariantAlsoNegotiates: 506,
  InsufficientStorage: 507,
  LoopDetected: 508,
  NotExtended: 510,
  NetworkAuthenticationRequired: 511,
};

Object.entries(HttpStatusCode).forEach(([key, value]) => {
  HttpStatusCode[value] = key;
});

var HttpStatusCode$1 = HttpStatusCode;

/**
 * Create an instance of Axios
 *
 * @param {Object} defaultConfig The default config for the instance
 *
 * @returns {Axios} A new instance of Axios
 */
function createInstance(defaultConfig) {
  const context = new Axios$1(defaultConfig);
  const instance = bind(Axios$1.prototype.request, context);

  // Copy axios.prototype to instance
  utils.extend(instance, Axios$1.prototype, context, {allOwnKeys: true});

  // Copy context to instance
  utils.extend(instance, context, null, {allOwnKeys: true});

  // Factory for creating new instances
  instance.create = function create(instanceConfig) {
    return createInstance(mergeConfig(defaultConfig, instanceConfig));
  };

  return instance;
}

// Create the default instance to be exported
const axios = createInstance(defaults$1);

// Expose Axios class to allow class inheritance
axios.Axios = Axios$1;

// Expose Cancel & CancelToken
axios.CanceledError = CanceledError;
axios.CancelToken = CancelToken$1;
axios.isCancel = isCancel;
axios.VERSION = VERSION;
axios.toFormData = toFormData;

// Expose AxiosError class
axios.AxiosError = AxiosError;

// alias for CanceledError for backward compatibility
axios.Cancel = axios.CanceledError;

// Expose all/spread
axios.all = function all(promises) {
  return Promise.all(promises);
};

axios.spread = spread;

// Expose isAxiosError
axios.isAxiosError = isAxiosError;

// Expose mergeConfig
axios.mergeConfig = mergeConfig;

axios.AxiosHeaders = AxiosHeaders$1;

axios.formToJSON = thing => formDataToJSON(utils.isHTMLForm(thing) ? new FormData(thing) : thing);

axios.getAdapter = adapters.getAdapter;

axios.HttpStatusCode = HttpStatusCode$1;

axios.default = axios;

module.exports = axios;
//# sourceMappingURL=axios.cjs.map


/***/ }),

/***/ 79574:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";
// Axios v1.6.5 Copyright (c) 2024 Matt Zabriskie and contributors


function bind(fn, thisArg) {
  return function wrap() {
    return fn.apply(thisArg, arguments);
  };
}

// utils is a library of generic helper functions non-specific to axios

const {toString} = Object.prototype;
const {getPrototypeOf} = Object;

const kindOf = (cache => thing => {
    const str = toString.call(thing);
    return cache[str] || (cache[str] = str.slice(8, -1).toLowerCase());
})(Object.create(null));

const kindOfTest = (type) => {
  type = type.toLowerCase();
  return (thing) => kindOf(thing) === type
};

const typeOfTest = type => thing => typeof thing === type;

/**
 * Determine if a value is an Array
 *
 * @param {Object} val The value to test
 *
 * @returns {boolean} True if value is an Array, otherwise false
 */
const {isArray} = Array;

/**
 * Determine if a value is undefined
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if the value is undefined, otherwise false
 */
const isUndefined = typeOfTest('undefined');

/**
 * Determine if a value is a Buffer
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Buffer, otherwise false
 */
function isBuffer(val) {
  return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor)
    && isFunction(val.constructor.isBuffer) && val.constructor.isBuffer(val);
}

/**
 * Determine if a value is an ArrayBuffer
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is an ArrayBuffer, otherwise false
 */
const isArrayBuffer = kindOfTest('ArrayBuffer');


/**
 * Determine if a value is a view on an ArrayBuffer
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a view on an ArrayBuffer, otherwise false
 */
function isArrayBufferView(val) {
  let result;
  if ((typeof ArrayBuffer !== 'undefined') && (ArrayBuffer.isView)) {
    result = ArrayBuffer.isView(val);
  } else {
    result = (val) && (val.buffer) && (isArrayBuffer(val.buffer));
  }
  return result;
}

/**
 * Determine if a value is a String
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a String, otherwise false
 */
const isString = typeOfTest('string');

/**
 * Determine if a value is a Function
 *
 * @param {*} val The value to test
 * @returns {boolean} True if value is a Function, otherwise false
 */
const isFunction = typeOfTest('function');

/**
 * Determine if a value is a Number
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Number, otherwise false
 */
const isNumber = typeOfTest('number');

/**
 * Determine if a value is an Object
 *
 * @param {*} thing The value to test
 *
 * @returns {boolean} True if value is an Object, otherwise false
 */
const isObject = (thing) => thing !== null && typeof thing === 'object';

/**
 * Determine if a value is a Boolean
 *
 * @param {*} thing The value to test
 * @returns {boolean} True if value is a Boolean, otherwise false
 */
const isBoolean = thing => thing === true || thing === false;

/**
 * Determine if a value is a plain Object
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a plain Object, otherwise false
 */
const isPlainObject = (val) => {
  if (kindOf(val) !== 'object') {
    return false;
  }

  const prototype = getPrototypeOf(val);
  return (prototype === null || prototype === Object.prototype || Object.getPrototypeOf(prototype) === null) && !(Symbol.toStringTag in val) && !(Symbol.iterator in val);
};

/**
 * Determine if a value is a Date
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Date, otherwise false
 */
const isDate = kindOfTest('Date');

/**
 * Determine if a value is a File
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a File, otherwise false
 */
const isFile = kindOfTest('File');

/**
 * Determine if a value is a Blob
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Blob, otherwise false
 */
const isBlob = kindOfTest('Blob');

/**
 * Determine if a value is a FileList
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a File, otherwise false
 */
const isFileList = kindOfTest('FileList');

/**
 * Determine if a value is a Stream
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a Stream, otherwise false
 */
const isStream = (val) => isObject(val) && isFunction(val.pipe);

/**
 * Determine if a value is a FormData
 *
 * @param {*} thing The value to test
 *
 * @returns {boolean} True if value is an FormData, otherwise false
 */
const isFormData = (thing) => {
  let kind;
  return thing && (
    (typeof FormData === 'function' && thing instanceof FormData) || (
      isFunction(thing.append) && (
        (kind = kindOf(thing)) === 'formdata' ||
        // detect form-data instance
        (kind === 'object' && isFunction(thing.toString) && thing.toString() === '[object FormData]')
      )
    )
  )
};

/**
 * Determine if a value is a URLSearchParams object
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a URLSearchParams object, otherwise false
 */
const isURLSearchParams = kindOfTest('URLSearchParams');

/**
 * Trim excess whitespace off the beginning and end of a string
 *
 * @param {String} str The String to trim
 *
 * @returns {String} The String freed of excess whitespace
 */
const trim = (str) => str.trim ?
  str.trim() : str.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, '');

/**
 * Iterate over an Array or an Object invoking a function for each item.
 *
 * If `obj` is an Array callback will be called passing
 * the value, index, and complete array for each item.
 *
 * If 'obj' is an Object callback will be called passing
 * the value, key, and complete object for each property.
 *
 * @param {Object|Array} obj The object to iterate
 * @param {Function} fn The callback to invoke for each item
 *
 * @param {Boolean} [allOwnKeys = false]
 * @returns {any}
 */
function forEach(obj, fn, {allOwnKeys = false} = {}) {
  // Don't bother if no value provided
  if (obj === null || typeof obj === 'undefined') {
    return;
  }

  let i;
  let l;

  // Force an array if not already something iterable
  if (typeof obj !== 'object') {
    /*eslint no-param-reassign:0*/
    obj = [obj];
  }

  if (isArray(obj)) {
    // Iterate over array values
    for (i = 0, l = obj.length; i < l; i++) {
      fn.call(null, obj[i], i, obj);
    }
  } else {
    // Iterate over object keys
    const keys = allOwnKeys ? Object.getOwnPropertyNames(obj) : Object.keys(obj);
    const len = keys.length;
    let key;

    for (i = 0; i < len; i++) {
      key = keys[i];
      fn.call(null, obj[key], key, obj);
    }
  }
}

function findKey(obj, key) {
  key = key.toLowerCase();
  const keys = Object.keys(obj);
  let i = keys.length;
  let _key;
  while (i-- > 0) {
    _key = keys[i];
    if (key === _key.toLowerCase()) {
      return _key;
    }
  }
  return null;
}

const _global = (() => {
  /*eslint no-undef:0*/
  if (typeof globalThis !== "undefined") return globalThis;
  return typeof self !== "undefined" ? self : (typeof window !== 'undefined' ? window : __webpack_require__.g)
})();

const isContextDefined = (context) => !isUndefined(context) && context !== _global;

/**
 * Accepts varargs expecting each argument to be an object, then
 * immutably merges the properties of each object and returns result.
 *
 * When multiple objects contain the same key the later object in
 * the arguments list will take precedence.
 *
 * Example:
 *
 * ```js
 * var result = merge({foo: 123}, {foo: 456});
 * console.log(result.foo); // outputs 456
 * ```
 *
 * @param {Object} obj1 Object to merge
 *
 * @returns {Object} Result of all merge properties
 */
function merge(/* obj1, obj2, obj3, ... */) {
  const {caseless} = isContextDefined(this) && this || {};
  const result = {};
  const assignValue = (val, key) => {
    const targetKey = caseless && findKey(result, key) || key;
    if (isPlainObject(result[targetKey]) && isPlainObject(val)) {
      result[targetKey] = merge(result[targetKey], val);
    } else if (isPlainObject(val)) {
      result[targetKey] = merge({}, val);
    } else if (isArray(val)) {
      result[targetKey] = val.slice();
    } else {
      result[targetKey] = val;
    }
  };

  for (let i = 0, l = arguments.length; i < l; i++) {
    arguments[i] && forEach(arguments[i], assignValue);
  }
  return result;
}

/**
 * Extends object a by mutably adding to it the properties of object b.
 *
 * @param {Object} a The object to be extended
 * @param {Object} b The object to copy properties from
 * @param {Object} thisArg The object to bind function to
 *
 * @param {Boolean} [allOwnKeys]
 * @returns {Object} The resulting value of object a
 */
const extend = (a, b, thisArg, {allOwnKeys}= {}) => {
  forEach(b, (val, key) => {
    if (thisArg && isFunction(val)) {
      a[key] = bind(val, thisArg);
    } else {
      a[key] = val;
    }
  }, {allOwnKeys});
  return a;
};

/**
 * Remove byte order marker. This catches EF BB BF (the UTF-8 BOM)
 *
 * @param {string} content with BOM
 *
 * @returns {string} content value without BOM
 */
const stripBOM = (content) => {
  if (content.charCodeAt(0) === 0xFEFF) {
    content = content.slice(1);
  }
  return content;
};

/**
 * Inherit the prototype methods from one constructor into another
 * @param {function} constructor
 * @param {function} superConstructor
 * @param {object} [props]
 * @param {object} [descriptors]
 *
 * @returns {void}
 */
const inherits = (constructor, superConstructor, props, descriptors) => {
  constructor.prototype = Object.create(superConstructor.prototype, descriptors);
  constructor.prototype.constructor = constructor;
  Object.defineProperty(constructor, 'super', {
    value: superConstructor.prototype
  });
  props && Object.assign(constructor.prototype, props);
};

/**
 * Resolve object with deep prototype chain to a flat object
 * @param {Object} sourceObj source object
 * @param {Object} [destObj]
 * @param {Function|Boolean} [filter]
 * @param {Function} [propFilter]
 *
 * @returns {Object}
 */
const toFlatObject = (sourceObj, destObj, filter, propFilter) => {
  let props;
  let i;
  let prop;
  const merged = {};

  destObj = destObj || {};
  // eslint-disable-next-line no-eq-null,eqeqeq
  if (sourceObj == null) return destObj;

  do {
    props = Object.getOwnPropertyNames(sourceObj);
    i = props.length;
    while (i-- > 0) {
      prop = props[i];
      if ((!propFilter || propFilter(prop, sourceObj, destObj)) && !merged[prop]) {
        destObj[prop] = sourceObj[prop];
        merged[prop] = true;
      }
    }
    sourceObj = filter !== false && getPrototypeOf(sourceObj);
  } while (sourceObj && (!filter || filter(sourceObj, destObj)) && sourceObj !== Object.prototype);

  return destObj;
};

/**
 * Determines whether a string ends with the characters of a specified string
 *
 * @param {String} str
 * @param {String} searchString
 * @param {Number} [position= 0]
 *
 * @returns {boolean}
 */
const endsWith = (str, searchString, position) => {
  str = String(str);
  if (position === undefined || position > str.length) {
    position = str.length;
  }
  position -= searchString.length;
  const lastIndex = str.indexOf(searchString, position);
  return lastIndex !== -1 && lastIndex === position;
};


/**
 * Returns new array from array like object or null if failed
 *
 * @param {*} [thing]
 *
 * @returns {?Array}
 */
const toArray = (thing) => {
  if (!thing) return null;
  if (isArray(thing)) return thing;
  let i = thing.length;
  if (!isNumber(i)) return null;
  const arr = new Array(i);
  while (i-- > 0) {
    arr[i] = thing[i];
  }
  return arr;
};

/**
 * Checking if the Uint8Array exists and if it does, it returns a function that checks if the
 * thing passed in is an instance of Uint8Array
 *
 * @param {TypedArray}
 *
 * @returns {Array}
 */
// eslint-disable-next-line func-names
const isTypedArray = (TypedArray => {
  // eslint-disable-next-line func-names
  return thing => {
    return TypedArray && thing instanceof TypedArray;
  };
})(typeof Uint8Array !== 'undefined' && getPrototypeOf(Uint8Array));

/**
 * For each entry in the object, call the function with the key and value.
 *
 * @param {Object<any, any>} obj - The object to iterate over.
 * @param {Function} fn - The function to call for each entry.
 *
 * @returns {void}
 */
const forEachEntry = (obj, fn) => {
  const generator = obj && obj[Symbol.iterator];

  const iterator = generator.call(obj);

  let result;

  while ((result = iterator.next()) && !result.done) {
    const pair = result.value;
    fn.call(obj, pair[0], pair[1]);
  }
};

/**
 * It takes a regular expression and a string, and returns an array of all the matches
 *
 * @param {string} regExp - The regular expression to match against.
 * @param {string} str - The string to search.
 *
 * @returns {Array<boolean>}
 */
const matchAll = (regExp, str) => {
  let matches;
  const arr = [];

  while ((matches = regExp.exec(str)) !== null) {
    arr.push(matches);
  }

  return arr;
};

/* Checking if the kindOfTest function returns true when passed an HTMLFormElement. */
const isHTMLForm = kindOfTest('HTMLFormElement');

const toCamelCase = str => {
  return str.toLowerCase().replace(/[-_\s]([a-z\d])(\w*)/g,
    function replacer(m, p1, p2) {
      return p1.toUpperCase() + p2;
    }
  );
};

/* Creating a function that will check if an object has a property. */
const hasOwnProperty = (({hasOwnProperty}) => (obj, prop) => hasOwnProperty.call(obj, prop))(Object.prototype);

/**
 * Determine if a value is a RegExp object
 *
 * @param {*} val The value to test
 *
 * @returns {boolean} True if value is a RegExp object, otherwise false
 */
const isRegExp = kindOfTest('RegExp');

const reduceDescriptors = (obj, reducer) => {
  const descriptors = Object.getOwnPropertyDescriptors(obj);
  const reducedDescriptors = {};

  forEach(descriptors, (descriptor, name) => {
    let ret;
    if ((ret = reducer(descriptor, name, obj)) !== false) {
      reducedDescriptors[name] = ret || descriptor;
    }
  });

  Object.defineProperties(obj, reducedDescriptors);
};

/**
 * Makes all methods read-only
 * @param {Object} obj
 */

const freezeMethods = (obj) => {
  reduceDescriptors(obj, (descriptor, name) => {
    // skip restricted props in strict mode
    if (isFunction(obj) && ['arguments', 'caller', 'callee'].indexOf(name) !== -1) {
      return false;
    }

    const value = obj[name];

    if (!isFunction(value)) return;

    descriptor.enumerable = false;

    if ('writable' in descriptor) {
      descriptor.writable = false;
      return;
    }

    if (!descriptor.set) {
      descriptor.set = () => {
        throw Error('Can not rewrite read-only method \'' + name + '\'');
      };
    }
  });
};

const toObjectSet = (arrayOrString, delimiter) => {
  const obj = {};

  const define = (arr) => {
    arr.forEach(value => {
      obj[value] = true;
    });
  };

  isArray(arrayOrString) ? define(arrayOrString) : define(String(arrayOrString).split(delimiter));

  return obj;
};

const noop = () => {};

const toFiniteNumber = (value, defaultValue) => {
  value = +value;
  return Number.isFinite(value) ? value : defaultValue;
};

const ALPHA = 'abcdefghijklmnopqrstuvwxyz';

const DIGIT = '0123456789';

const ALPHABET = {
  DIGIT,
  ALPHA,
  ALPHA_DIGIT: ALPHA + ALPHA.toUpperCase() + DIGIT
};

const generateString = (size = 16, alphabet = ALPHABET.ALPHA_DIGIT) => {
  let str = '';
  const {length} = alphabet;
  while (size--) {
    str += alphabet[Math.random() * length|0];
  }

  return str;
};

/**
 * If the thing is a FormData object, return true, otherwise return false.
 *
 * @param {unknown} thing - The thing to check.
 *
 * @returns {boolean}
 */
function isSpecCompliantForm(thing) {
  return !!(thing && isFunction(thing.append) && thing[Symbol.toStringTag] === 'FormData' && thing[Symbol.iterator]);
}

const toJSONObject = (obj) => {
  const stack = new Array(10);

  const visit = (source, i) => {

    if (isObject(source)) {
      if (stack.indexOf(source) >= 0) {
        return;
      }

      if(!('toJSON' in source)) {
        stack[i] = source;
        const target = isArray(source) ? [] : {};

        forEach(source, (value, key) => {
          const reducedValue = visit(value, i + 1);
          !isUndefined(reducedValue) && (target[key] = reducedValue);
        });

        stack[i] = undefined;

        return target;
      }
    }

    return source;
  };

  return visit(obj, 0);
};

const isAsyncFn = kindOfTest('AsyncFunction');

const isThenable = (thing) =>
  thing && (isObject(thing) || isFunction(thing)) && isFunction(thing.then) && isFunction(thing.catch);

var utils$1 = {
  isArray,
  isArrayBuffer,
  isBuffer,
  isFormData,
  isArrayBufferView,
  isString,
  isNumber,
  isBoolean,
  isObject,
  isPlainObject,
  isUndefined,
  isDate,
  isFile,
  isBlob,
  isRegExp,
  isFunction,
  isStream,
  isURLSearchParams,
  isTypedArray,
  isFileList,
  forEach,
  merge,
  extend,
  trim,
  stripBOM,
  inherits,
  toFlatObject,
  kindOf,
  kindOfTest,
  endsWith,
  toArray,
  forEachEntry,
  matchAll,
  isHTMLForm,
  hasOwnProperty,
  hasOwnProp: hasOwnProperty, // an alias to avoid ESLint no-prototype-builtins detection
  reduceDescriptors,
  freezeMethods,
  toObjectSet,
  toCamelCase,
  noop,
  toFiniteNumber,
  findKey,
  global: _global,
  isContextDefined,
  ALPHABET,
  generateString,
  isSpecCompliantForm,
  toJSONObject,
  isAsyncFn,
  isThenable
};

/**
 * Create an Error with the specified message, config, error code, request and response.
 *
 * @param {string} message The error message.
 * @param {string} [code] The error code (for example, 'ECONNABORTED').
 * @param {Object} [config] The config.
 * @param {Object} [request] The request.
 * @param {Object} [response] The response.
 *
 * @returns {Error} The created error.
 */
function AxiosError(message, code, config, request, response) {
  Error.call(this);

  if (Error.captureStackTrace) {
    Error.captureStackTrace(this, this.constructor);
  } else {
    this.stack = (new Error()).stack;
  }

  this.message = message;
  this.name = 'AxiosError';
  code && (this.code = code);
  config && (this.config = config);
  request && (this.request = request);
  response && (this.response = response);
}

utils$1.inherits(AxiosError, Error, {
  toJSON: function toJSON() {
    return {
      // Standard
      message: this.message,
      name: this.name,
      // Microsoft
      description: this.description,
      number: this.number,
      // Mozilla
      fileName: this.fileName,
      lineNumber: this.lineNumber,
      columnNumber: this.columnNumber,
      stack: this.stack,
      // Axios
      config: utils$1.toJSONObject(this.config),
      code: this.code,
      status: this.response && this.response.status ? this.response.status : null
    };
  }
});

const prototype$1 = AxiosError.prototype;
const descriptors = {};

[
  'ERR_BAD_OPTION_VALUE',
  'ERR_BAD_OPTION',
  'ECONNABORTED',
  'ETIMEDOUT',
  'ERR_NETWORK',
  'ERR_FR_TOO_MANY_REDIRECTS',
  'ERR_DEPRECATED',
  'ERR_BAD_RESPONSE',
  'ERR_BAD_REQUEST',
  'ERR_CANCELED',
  'ERR_NOT_SUPPORT',
  'ERR_INVALID_URL'
// eslint-disable-next-line func-names
].forEach(code => {
  descriptors[code] = {value: code};
});

Object.defineProperties(AxiosError, descriptors);
Object.defineProperty(prototype$1, 'isAxiosError', {value: true});

// eslint-disable-next-line func-names
AxiosError.from = (error, code, config, request, response, customProps) => {
  const axiosError = Object.create(prototype$1);

  utils$1.toFlatObject(error, axiosError, function filter(obj) {
    return obj !== Error.prototype;
  }, prop => {
    return prop !== 'isAxiosError';
  });

  AxiosError.call(axiosError, error.message, code, config, request, response);

  axiosError.cause = error;

  axiosError.name = error.name;

  customProps && Object.assign(axiosError, customProps);

  return axiosError;
};

// eslint-disable-next-line strict
var httpAdapter = null;

/**
 * Determines if the given thing is a array or js object.
 *
 * @param {string} thing - The object or array to be visited.
 *
 * @returns {boolean}
 */
function isVisitable(thing) {
  return utils$1.isPlainObject(thing) || utils$1.isArray(thing);
}

/**
 * It removes the brackets from the end of a string
 *
 * @param {string} key - The key of the parameter.
 *
 * @returns {string} the key without the brackets.
 */
function removeBrackets(key) {
  return utils$1.endsWith(key, '[]') ? key.slice(0, -2) : key;
}

/**
 * It takes a path, a key, and a boolean, and returns a string
 *
 * @param {string} path - The path to the current key.
 * @param {string} key - The key of the current object being iterated over.
 * @param {string} dots - If true, the key will be rendered with dots instead of brackets.
 *
 * @returns {string} The path to the current key.
 */
function renderKey(path, key, dots) {
  if (!path) return key;
  return path.concat(key).map(function each(token, i) {
    // eslint-disable-next-line no-param-reassign
    token = removeBrackets(token);
    return !dots && i ? '[' + token + ']' : token;
  }).join(dots ? '.' : '');
}

/**
 * If the array is an array and none of its elements are visitable, then it's a flat array.
 *
 * @param {Array<any>} arr - The array to check
 *
 * @returns {boolean}
 */
function isFlatArray(arr) {
  return utils$1.isArray(arr) && !arr.some(isVisitable);
}

const predicates = utils$1.toFlatObject(utils$1, {}, null, function filter(prop) {
  return /^is[A-Z]/.test(prop);
});

/**
 * Convert a data object to FormData
 *
 * @param {Object} obj
 * @param {?Object} [formData]
 * @param {?Object} [options]
 * @param {Function} [options.visitor]
 * @param {Boolean} [options.metaTokens = true]
 * @param {Boolean} [options.dots = false]
 * @param {?Boolean} [options.indexes = false]
 *
 * @returns {Object}
 **/

/**
 * It converts an object into a FormData object
 *
 * @param {Object<any, any>} obj - The object to convert to form data.
 * @param {string} formData - The FormData object to append to.
 * @param {Object<string, any>} options
 *
 * @returns
 */
function toFormData(obj, formData, options) {
  if (!utils$1.isObject(obj)) {
    throw new TypeError('target must be an object');
  }

  // eslint-disable-next-line no-param-reassign
  formData = formData || new (FormData)();

  // eslint-disable-next-line no-param-reassign
  options = utils$1.toFlatObject(options, {
    metaTokens: true,
    dots: false,
    indexes: false
  }, false, function defined(option, source) {
    // eslint-disable-next-line no-eq-null,eqeqeq
    return !utils$1.isUndefined(source[option]);
  });

  const metaTokens = options.metaTokens;
  // eslint-disable-next-line no-use-before-define
  const visitor = options.visitor || defaultVisitor;
  const dots = options.dots;
  const indexes = options.indexes;
  const _Blob = options.Blob || typeof Blob !== 'undefined' && Blob;
  const useBlob = _Blob && utils$1.isSpecCompliantForm(formData);

  if (!utils$1.isFunction(visitor)) {
    throw new TypeError('visitor must be a function');
  }

  function convertValue(value) {
    if (value === null) return '';

    if (utils$1.isDate(value)) {
      return value.toISOString();
    }

    if (!useBlob && utils$1.isBlob(value)) {
      throw new AxiosError('Blob is not supported. Use a Buffer instead.');
    }

    if (utils$1.isArrayBuffer(value) || utils$1.isTypedArray(value)) {
      return useBlob && typeof Blob === 'function' ? new Blob([value]) : Buffer.from(value);
    }

    return value;
  }

  /**
   * Default visitor.
   *
   * @param {*} value
   * @param {String|Number} key
   * @param {Array<String|Number>} path
   * @this {FormData}
   *
   * @returns {boolean} return true to visit the each prop of the value recursively
   */
  function defaultVisitor(value, key, path) {
    let arr = value;

    if (value && !path && typeof value === 'object') {
      if (utils$1.endsWith(key, '{}')) {
        // eslint-disable-next-line no-param-reassign
        key = metaTokens ? key : key.slice(0, -2);
        // eslint-disable-next-line no-param-reassign
        value = JSON.stringify(value);
      } else if (
        (utils$1.isArray(value) && isFlatArray(value)) ||
        ((utils$1.isFileList(value) || utils$1.endsWith(key, '[]')) && (arr = utils$1.toArray(value))
        )) {
        // eslint-disable-next-line no-param-reassign
        key = removeBrackets(key);

        arr.forEach(function each(el, index) {
          !(utils$1.isUndefined(el) || el === null) && formData.append(
            // eslint-disable-next-line no-nested-ternary
            indexes === true ? renderKey([key], index, dots) : (indexes === null ? key : key + '[]'),
            convertValue(el)
          );
        });
        return false;
      }
    }

    if (isVisitable(value)) {
      return true;
    }

    formData.append(renderKey(path, key, dots), convertValue(value));

    return false;
  }

  const stack = [];

  const exposedHelpers = Object.assign(predicates, {
    defaultVisitor,
    convertValue,
    isVisitable
  });

  function build(value, path) {
    if (utils$1.isUndefined(value)) return;

    if (stack.indexOf(value) !== -1) {
      throw Error('Circular reference detected in ' + path.join('.'));
    }

    stack.push(value);

    utils$1.forEach(value, function each(el, key) {
      const result = !(utils$1.isUndefined(el) || el === null) && visitor.call(
        formData, el, utils$1.isString(key) ? key.trim() : key, path, exposedHelpers
      );

      if (result === true) {
        build(el, path ? path.concat(key) : [key]);
      }
    });

    stack.pop();
  }

  if (!utils$1.isObject(obj)) {
    throw new TypeError('data must be an object');
  }

  build(obj);

  return formData;
}

/**
 * It encodes a string by replacing all characters that are not in the unreserved set with
 * their percent-encoded equivalents
 *
 * @param {string} str - The string to encode.
 *
 * @returns {string} The encoded string.
 */
function encode$1(str) {
  const charMap = {
    '!': '%21',
    "'": '%27',
    '(': '%28',
    ')': '%29',
    '~': '%7E',
    '%20': '+',
    '%00': '\x00'
  };
  return encodeURIComponent(str).replace(/[!'()~]|%20|%00/g, function replacer(match) {
    return charMap[match];
  });
}

/**
 * It takes a params object and converts it to a FormData object
 *
 * @param {Object<string, any>} params - The parameters to be converted to a FormData object.
 * @param {Object<string, any>} options - The options object passed to the Axios constructor.
 *
 * @returns {void}
 */
function AxiosURLSearchParams(params, options) {
  this._pairs = [];

  params && toFormData(params, this, options);
}

const prototype = AxiosURLSearchParams.prototype;

prototype.append = function append(name, value) {
  this._pairs.push([name, value]);
};

prototype.toString = function toString(encoder) {
  const _encode = encoder ? function(value) {
    return encoder.call(this, value, encode$1);
  } : encode$1;

  return this._pairs.map(function each(pair) {
    return _encode(pair[0]) + '=' + _encode(pair[1]);
  }, '').join('&');
};

/**
 * It replaces all instances of the characters `:`, `$`, `,`, `+`, `[`, and `]` with their
 * URI encoded counterparts
 *
 * @param {string} val The value to be encoded.
 *
 * @returns {string} The encoded value.
 */
function encode(val) {
  return encodeURIComponent(val).
    replace(/%3A/gi, ':').
    replace(/%24/g, '$').
    replace(/%2C/gi, ',').
    replace(/%20/g, '+').
    replace(/%5B/gi, '[').
    replace(/%5D/gi, ']');
}

/**
 * Build a URL by appending params to the end
 *
 * @param {string} url The base of the url (e.g., http://www.google.com)
 * @param {object} [params] The params to be appended
 * @param {?object} options
 *
 * @returns {string} The formatted url
 */
function buildURL(url, params, options) {
  /*eslint no-param-reassign:0*/
  if (!params) {
    return url;
  }
  
  const _encode = options && options.encode || encode;

  const serializeFn = options && options.serialize;

  let serializedParams;

  if (serializeFn) {
    serializedParams = serializeFn(params, options);
  } else {
    serializedParams = utils$1.isURLSearchParams(params) ?
      params.toString() :
      new AxiosURLSearchParams(params, options).toString(_encode);
  }

  if (serializedParams) {
    const hashmarkIndex = url.indexOf("#");

    if (hashmarkIndex !== -1) {
      url = url.slice(0, hashmarkIndex);
    }
    url += (url.indexOf('?') === -1 ? '?' : '&') + serializedParams;
  }

  return url;
}

class InterceptorManager {
  constructor() {
    this.handlers = [];
  }

  /**
   * Add a new interceptor to the stack
   *
   * @param {Function} fulfilled The function to handle `then` for a `Promise`
   * @param {Function} rejected The function to handle `reject` for a `Promise`
   *
   * @return {Number} An ID used to remove interceptor later
   */
  use(fulfilled, rejected, options) {
    this.handlers.push({
      fulfilled,
      rejected,
      synchronous: options ? options.synchronous : false,
      runWhen: options ? options.runWhen : null
    });
    return this.handlers.length - 1;
  }

  /**
   * Remove an interceptor from the stack
   *
   * @param {Number} id The ID that was returned by `use`
   *
   * @returns {Boolean} `true` if the interceptor was removed, `false` otherwise
   */
  eject(id) {
    if (this.handlers[id]) {
      this.handlers[id] = null;
    }
  }

  /**
   * Clear all interceptors from the stack
   *
   * @returns {void}
   */
  clear() {
    if (this.handlers) {
      this.handlers = [];
    }
  }

  /**
   * Iterate over all the registered interceptors
   *
   * This method is particularly useful for skipping over any
   * interceptors that may have become `null` calling `eject`.
   *
   * @param {Function} fn The function to call for each interceptor
   *
   * @returns {void}
   */
  forEach(fn) {
    utils$1.forEach(this.handlers, function forEachHandler(h) {
      if (h !== null) {
        fn(h);
      }
    });
  }
}

var InterceptorManager$1 = InterceptorManager;

var transitionalDefaults = {
  silentJSONParsing: true,
  forcedJSONParsing: true,
  clarifyTimeoutError: false
};

var URLSearchParams$1 = typeof URLSearchParams !== 'undefined' ? URLSearchParams : AxiosURLSearchParams;

var FormData$1 = typeof FormData !== 'undefined' ? FormData : null;

var Blob$1 = typeof Blob !== 'undefined' ? Blob : null;

var platform$1 = {
  isBrowser: true,
  classes: {
    URLSearchParams: URLSearchParams$1,
    FormData: FormData$1,
    Blob: Blob$1
  },
  protocols: ['http', 'https', 'file', 'blob', 'url', 'data']
};

const hasBrowserEnv = typeof window !== 'undefined' && typeof document !== 'undefined';

/**
 * Determine if we're running in a standard browser environment
 *
 * This allows axios to run in a web worker, and react-native.
 * Both environments support XMLHttpRequest, but not fully standard globals.
 *
 * web workers:
 *  typeof window -> undefined
 *  typeof document -> undefined
 *
 * react-native:
 *  navigator.product -> 'ReactNative'
 * nativescript
 *  navigator.product -> 'NativeScript' or 'NS'
 *
 * @returns {boolean}
 */
const hasStandardBrowserEnv = (
  (product) => {
    return hasBrowserEnv && ['ReactNative', 'NativeScript', 'NS'].indexOf(product) < 0
  })(typeof navigator !== 'undefined' && navigator.product);

/**
 * Determine if we're running in a standard browser webWorker environment
 *
 * Although the `isStandardBrowserEnv` method indicates that
 * `allows axios to run in a web worker`, the WebWorker will still be
 * filtered out due to its judgment standard
 * `typeof window !== 'undefined' && typeof document !== 'undefined'`.
 * This leads to a problem when axios post `FormData` in webWorker
 */
const hasStandardBrowserWebWorkerEnv = (() => {
  return (
    typeof WorkerGlobalScope !== 'undefined' &&
    // eslint-disable-next-line no-undef
    self instanceof WorkerGlobalScope &&
    typeof self.importScripts === 'function'
  );
})();

var utils = /*#__PURE__*/Object.freeze({
  __proto__: null,
  hasBrowserEnv: hasBrowserEnv,
  hasStandardBrowserWebWorkerEnv: hasStandardBrowserWebWorkerEnv,
  hasStandardBrowserEnv: hasStandardBrowserEnv
});

var platform = {
  ...utils,
  ...platform$1
};

function toURLEncodedForm(data, options) {
  return toFormData(data, new platform.classes.URLSearchParams(), Object.assign({
    visitor: function(value, key, path, helpers) {
      if (platform.isNode && utils$1.isBuffer(value)) {
        this.append(key, value.toString('base64'));
        return false;
      }

      return helpers.defaultVisitor.apply(this, arguments);
    }
  }, options));
}

/**
 * It takes a string like `foo[x][y][z]` and returns an array like `['foo', 'x', 'y', 'z']
 *
 * @param {string} name - The name of the property to get.
 *
 * @returns An array of strings.
 */
function parsePropPath(name) {
  // foo[x][y][z]
  // foo.x.y.z
  // foo-x-y-z
  // foo x y z
  return utils$1.matchAll(/\w+|\[(\w*)]/g, name).map(match => {
    return match[0] === '[]' ? '' : match[1] || match[0];
  });
}

/**
 * Convert an array to an object.
 *
 * @param {Array<any>} arr - The array to convert to an object.
 *
 * @returns An object with the same keys and values as the array.
 */
function arrayToObject(arr) {
  const obj = {};
  const keys = Object.keys(arr);
  let i;
  const len = keys.length;
  let key;
  for (i = 0; i < len; i++) {
    key = keys[i];
    obj[key] = arr[key];
  }
  return obj;
}

/**
 * It takes a FormData object and returns a JavaScript object
 *
 * @param {string} formData The FormData object to convert to JSON.
 *
 * @returns {Object<string, any> | null} The converted object.
 */
function formDataToJSON(formData) {
  function buildPath(path, value, target, index) {
    let name = path[index++];

    if (name === '__proto__') return true;

    const isNumericKey = Number.isFinite(+name);
    const isLast = index >= path.length;
    name = !name && utils$1.isArray(target) ? target.length : name;

    if (isLast) {
      if (utils$1.hasOwnProp(target, name)) {
        target[name] = [target[name], value];
      } else {
        target[name] = value;
      }

      return !isNumericKey;
    }

    if (!target[name] || !utils$1.isObject(target[name])) {
      target[name] = [];
    }

    const result = buildPath(path, value, target[name], index);

    if (result && utils$1.isArray(target[name])) {
      target[name] = arrayToObject(target[name]);
    }

    return !isNumericKey;
  }

  if (utils$1.isFormData(formData) && utils$1.isFunction(formData.entries)) {
    const obj = {};

    utils$1.forEachEntry(formData, (name, value) => {
      buildPath(parsePropPath(name), value, obj, 0);
    });

    return obj;
  }

  return null;
}

/**
 * It takes a string, tries to parse it, and if it fails, it returns the stringified version
 * of the input
 *
 * @param {any} rawValue - The value to be stringified.
 * @param {Function} parser - A function that parses a string into a JavaScript object.
 * @param {Function} encoder - A function that takes a value and returns a string.
 *
 * @returns {string} A stringified version of the rawValue.
 */
function stringifySafely(rawValue, parser, encoder) {
  if (utils$1.isString(rawValue)) {
    try {
      (parser || JSON.parse)(rawValue);
      return utils$1.trim(rawValue);
    } catch (e) {
      if (e.name !== 'SyntaxError') {
        throw e;
      }
    }
  }

  return (encoder || JSON.stringify)(rawValue);
}

const defaults = {

  transitional: transitionalDefaults,

  adapter: ['xhr', 'http'],

  transformRequest: [function transformRequest(data, headers) {
    const contentType = headers.getContentType() || '';
    const hasJSONContentType = contentType.indexOf('application/json') > -1;
    const isObjectPayload = utils$1.isObject(data);

    if (isObjectPayload && utils$1.isHTMLForm(data)) {
      data = new FormData(data);
    }

    const isFormData = utils$1.isFormData(data);

    if (isFormData) {
      if (!hasJSONContentType) {
        return data;
      }
      return hasJSONContentType ? JSON.stringify(formDataToJSON(data)) : data;
    }

    if (utils$1.isArrayBuffer(data) ||
      utils$1.isBuffer(data) ||
      utils$1.isStream(data) ||
      utils$1.isFile(data) ||
      utils$1.isBlob(data)
    ) {
      return data;
    }
    if (utils$1.isArrayBufferView(data)) {
      return data.buffer;
    }
    if (utils$1.isURLSearchParams(data)) {
      headers.setContentType('application/x-www-form-urlencoded;charset=utf-8', false);
      return data.toString();
    }

    let isFileList;

    if (isObjectPayload) {
      if (contentType.indexOf('application/x-www-form-urlencoded') > -1) {
        return toURLEncodedForm(data, this.formSerializer).toString();
      }

      if ((isFileList = utils$1.isFileList(data)) || contentType.indexOf('multipart/form-data') > -1) {
        const _FormData = this.env && this.env.FormData;

        return toFormData(
          isFileList ? {'files[]': data} : data,
          _FormData && new _FormData(),
          this.formSerializer
        );
      }
    }

    if (isObjectPayload || hasJSONContentType ) {
      headers.setContentType('application/json', false);
      return stringifySafely(data);
    }

    return data;
  }],

  transformResponse: [function transformResponse(data) {
    const transitional = this.transitional || defaults.transitional;
    const forcedJSONParsing = transitional && transitional.forcedJSONParsing;
    const JSONRequested = this.responseType === 'json';

    if (data && utils$1.isString(data) && ((forcedJSONParsing && !this.responseType) || JSONRequested)) {
      const silentJSONParsing = transitional && transitional.silentJSONParsing;
      const strictJSONParsing = !silentJSONParsing && JSONRequested;

      try {
        return JSON.parse(data);
      } catch (e) {
        if (strictJSONParsing) {
          if (e.name === 'SyntaxError') {
            throw AxiosError.from(e, AxiosError.ERR_BAD_RESPONSE, this, null, this.response);
          }
          throw e;
        }
      }
    }

    return data;
  }],

  /**
   * A timeout in milliseconds to abort a request. If set to 0 (default) a
   * timeout is not created.
   */
  timeout: 0,

  xsrfCookieName: 'XSRF-TOKEN',
  xsrfHeaderName: 'X-XSRF-TOKEN',

  maxContentLength: -1,
  maxBodyLength: -1,

  env: {
    FormData: platform.classes.FormData,
    Blob: platform.classes.Blob
  },

  validateStatus: function validateStatus(status) {
    return status >= 200 && status < 300;
  },

  headers: {
    common: {
      'Accept': 'application/json, text/plain, */*',
      'Content-Type': undefined
    }
  }
};

utils$1.forEach(['delete', 'get', 'head', 'post', 'put', 'patch'], (method) => {
  defaults.headers[method] = {};
});

var defaults$1 = defaults;

// RawAxiosHeaders whose duplicates are ignored by node
// c.f. https://nodejs.org/api/http.html#http_message_headers
const ignoreDuplicateOf = utils$1.toObjectSet([
  'age', 'authorization', 'content-length', 'content-type', 'etag',
  'expires', 'from', 'host', 'if-modified-since', 'if-unmodified-since',
  'last-modified', 'location', 'max-forwards', 'proxy-authorization',
  'referer', 'retry-after', 'user-agent'
]);

/**
 * Parse headers into an object
 *
 * ```
 * Date: Wed, 27 Aug 2014 08:58:49 GMT
 * Content-Type: application/json
 * Connection: keep-alive
 * Transfer-Encoding: chunked
 * ```
 *
 * @param {String} rawHeaders Headers needing to be parsed
 *
 * @returns {Object} Headers parsed into an object
 */
var parseHeaders = rawHeaders => {
  const parsed = {};
  let key;
  let val;
  let i;

  rawHeaders && rawHeaders.split('\n').forEach(function parser(line) {
    i = line.indexOf(':');
    key = line.substring(0, i).trim().toLowerCase();
    val = line.substring(i + 1).trim();

    if (!key || (parsed[key] && ignoreDuplicateOf[key])) {
      return;
    }

    if (key === 'set-cookie') {
      if (parsed[key]) {
        parsed[key].push(val);
      } else {
        parsed[key] = [val];
      }
    } else {
      parsed[key] = parsed[key] ? parsed[key] + ', ' + val : val;
    }
  });

  return parsed;
};

const $internals = Symbol('internals');

function normalizeHeader(header) {
  return header && String(header).trim().toLowerCase();
}

function normalizeValue(value) {
  if (value === false || value == null) {
    return value;
  }

  return utils$1.isArray(value) ? value.map(normalizeValue) : String(value);
}

function parseTokens(str) {
  const tokens = Object.create(null);
  const tokensRE = /([^\s,;=]+)\s*(?:=\s*([^,;]+))?/g;
  let match;

  while ((match = tokensRE.exec(str))) {
    tokens[match[1]] = match[2];
  }

  return tokens;
}

const isValidHeaderName = (str) => /^[-_a-zA-Z0-9^`|~,!#$%&'*+.]+$/.test(str.trim());

function matchHeaderValue(context, value, header, filter, isHeaderNameFilter) {
  if (utils$1.isFunction(filter)) {
    return filter.call(this, value, header);
  }

  if (isHeaderNameFilter) {
    value = header;
  }

  if (!utils$1.isString(value)) return;

  if (utils$1.isString(filter)) {
    return value.indexOf(filter) !== -1;
  }

  if (utils$1.isRegExp(filter)) {
    return filter.test(value);
  }
}

function formatHeader(header) {
  return header.trim()
    .toLowerCase().replace(/([a-z\d])(\w*)/g, (w, char, str) => {
      return char.toUpperCase() + str;
    });
}

function buildAccessors(obj, header) {
  const accessorName = utils$1.toCamelCase(' ' + header);

  ['get', 'set', 'has'].forEach(methodName => {
    Object.defineProperty(obj, methodName + accessorName, {
      value: function(arg1, arg2, arg3) {
        return this[methodName].call(this, header, arg1, arg2, arg3);
      },
      configurable: true
    });
  });
}

class AxiosHeaders {
  constructor(headers) {
    headers && this.set(headers);
  }

  set(header, valueOrRewrite, rewrite) {
    const self = this;

    function setHeader(_value, _header, _rewrite) {
      const lHeader = normalizeHeader(_header);

      if (!lHeader) {
        throw new Error('header name must be a non-empty string');
      }

      const key = utils$1.findKey(self, lHeader);

      if(!key || self[key] === undefined || _rewrite === true || (_rewrite === undefined && self[key] !== false)) {
        self[key || _header] = normalizeValue(_value);
      }
    }

    const setHeaders = (headers, _rewrite) =>
      utils$1.forEach(headers, (_value, _header) => setHeader(_value, _header, _rewrite));

    if (utils$1.isPlainObject(header) || header instanceof this.constructor) {
      setHeaders(header, valueOrRewrite);
    } else if(utils$1.isString(header) && (header = header.trim()) && !isValidHeaderName(header)) {
      setHeaders(parseHeaders(header), valueOrRewrite);
    } else {
      header != null && setHeader(valueOrRewrite, header, rewrite);
    }

    return this;
  }

  get(header, parser) {
    header = normalizeHeader(header);

    if (header) {
      const key = utils$1.findKey(this, header);

      if (key) {
        const value = this[key];

        if (!parser) {
          return value;
        }

        if (parser === true) {
          return parseTokens(value);
        }

        if (utils$1.isFunction(parser)) {
          return parser.call(this, value, key);
        }

        if (utils$1.isRegExp(parser)) {
          return parser.exec(value);
        }

        throw new TypeError('parser must be boolean|regexp|function');
      }
    }
  }

  has(header, matcher) {
    header = normalizeHeader(header);

    if (header) {
      const key = utils$1.findKey(this, header);

      return !!(key && this[key] !== undefined && (!matcher || matchHeaderValue(this, this[key], key, matcher)));
    }

    return false;
  }

  delete(header, matcher) {
    const self = this;
    let deleted = false;

    function deleteHeader(_header) {
      _header = normalizeHeader(_header);

      if (_header) {
        const key = utils$1.findKey(self, _header);

        if (key && (!matcher || matchHeaderValue(self, self[key], key, matcher))) {
          delete self[key];

          deleted = true;
        }
      }
    }

    if (utils$1.isArray(header)) {
      header.forEach(deleteHeader);
    } else {
      deleteHeader(header);
    }

    return deleted;
  }

  clear(matcher) {
    const keys = Object.keys(this);
    let i = keys.length;
    let deleted = false;

    while (i--) {
      const key = keys[i];
      if(!matcher || matchHeaderValue(this, this[key], key, matcher, true)) {
        delete this[key];
        deleted = true;
      }
    }

    return deleted;
  }

  normalize(format) {
    const self = this;
    const headers = {};

    utils$1.forEach(this, (value, header) => {
      const key = utils$1.findKey(headers, header);

      if (key) {
        self[key] = normalizeValue(value);
        delete self[header];
        return;
      }

      const normalized = format ? formatHeader(header) : String(header).trim();

      if (normalized !== header) {
        delete self[header];
      }

      self[normalized] = normalizeValue(value);

      headers[normalized] = true;
    });

    return this;
  }

  concat(...targets) {
    return this.constructor.concat(this, ...targets);
  }

  toJSON(asStrings) {
    const obj = Object.create(null);

    utils$1.forEach(this, (value, header) => {
      value != null && value !== false && (obj[header] = asStrings && utils$1.isArray(value) ? value.join(', ') : value);
    });

    return obj;
  }

  [Symbol.iterator]() {
    return Object.entries(this.toJSON())[Symbol.iterator]();
  }

  toString() {
    return Object.entries(this.toJSON()).map(([header, value]) => header + ': ' + value).join('\n');
  }

  get [Symbol.toStringTag]() {
    return 'AxiosHeaders';
  }

  static from(thing) {
    return thing instanceof this ? thing : new this(thing);
  }

  static concat(first, ...targets) {
    const computed = new this(first);

    targets.forEach((target) => computed.set(target));

    return computed;
  }

  static accessor(header) {
    const internals = this[$internals] = (this[$internals] = {
      accessors: {}
    });

    const accessors = internals.accessors;
    const prototype = this.prototype;

    function defineAccessor(_header) {
      const lHeader = normalizeHeader(_header);

      if (!accessors[lHeader]) {
        buildAccessors(prototype, _header);
        accessors[lHeader] = true;
      }
    }

    utils$1.isArray(header) ? header.forEach(defineAccessor) : defineAccessor(header);

    return this;
  }
}

AxiosHeaders.accessor(['Content-Type', 'Content-Length', 'Accept', 'Accept-Encoding', 'User-Agent', 'Authorization']);

// reserved names hotfix
utils$1.reduceDescriptors(AxiosHeaders.prototype, ({value}, key) => {
  let mapped = key[0].toUpperCase() + key.slice(1); // map `set` => `Set`
  return {
    get: () => value,
    set(headerValue) {
      this[mapped] = headerValue;
    }
  }
});

utils$1.freezeMethods(AxiosHeaders);

var AxiosHeaders$1 = AxiosHeaders;

/**
 * Transform the data for a request or a response
 *
 * @param {Array|Function} fns A single function or Array of functions
 * @param {?Object} response The response object
 *
 * @returns {*} The resulting transformed data
 */
function transformData(fns, response) {
  const config = this || defaults$1;
  const context = response || config;
  const headers = AxiosHeaders$1.from(context.headers);
  let data = context.data;

  utils$1.forEach(fns, function transform(fn) {
    data = fn.call(config, data, headers.normalize(), response ? response.status : undefined);
  });

  headers.normalize();

  return data;
}

function isCancel(value) {
  return !!(value && value.__CANCEL__);
}

/**
 * A `CanceledError` is an object that is thrown when an operation is canceled.
 *
 * @param {string=} message The message.
 * @param {Object=} config The config.
 * @param {Object=} request The request.
 *
 * @returns {CanceledError} The created error.
 */
function CanceledError(message, config, request) {
  // eslint-disable-next-line no-eq-null,eqeqeq
  AxiosError.call(this, message == null ? 'canceled' : message, AxiosError.ERR_CANCELED, config, request);
  this.name = 'CanceledError';
}

utils$1.inherits(CanceledError, AxiosError, {
  __CANCEL__: true
});

/**
 * Resolve or reject a Promise based on response status.
 *
 * @param {Function} resolve A function that resolves the promise.
 * @param {Function} reject A function that rejects the promise.
 * @param {object} response The response.
 *
 * @returns {object} The response.
 */
function settle(resolve, reject, response) {
  const validateStatus = response.config.validateStatus;
  if (!response.status || !validateStatus || validateStatus(response.status)) {
    resolve(response);
  } else {
    reject(new AxiosError(
      'Request failed with status code ' + response.status,
      [AxiosError.ERR_BAD_REQUEST, AxiosError.ERR_BAD_RESPONSE][Math.floor(response.status / 100) - 4],
      response.config,
      response.request,
      response
    ));
  }
}

var cookies = platform.hasStandardBrowserEnv ?

  // Standard browser envs support document.cookie
  {
    write(name, value, expires, path, domain, secure) {
      const cookie = [name + '=' + encodeURIComponent(value)];

      utils$1.isNumber(expires) && cookie.push('expires=' + new Date(expires).toGMTString());

      utils$1.isString(path) && cookie.push('path=' + path);

      utils$1.isString(domain) && cookie.push('domain=' + domain);

      secure === true && cookie.push('secure');

      document.cookie = cookie.join('; ');
    },

    read(name) {
      const match = document.cookie.match(new RegExp('(^|;\\s*)(' + name + ')=([^;]*)'));
      return (match ? decodeURIComponent(match[3]) : null);
    },

    remove(name) {
      this.write(name, '', Date.now() - 86400000);
    }
  }

  :

  // Non-standard browser env (web workers, react-native) lack needed support.
  {
    write() {},
    read() {
      return null;
    },
    remove() {}
  };

/**
 * Determines whether the specified URL is absolute
 *
 * @param {string} url The URL to test
 *
 * @returns {boolean} True if the specified URL is absolute, otherwise false
 */
function isAbsoluteURL(url) {
  // A URL is considered absolute if it begins with "<scheme>://" or "//" (protocol-relative URL).
  // RFC 3986 defines scheme name as a sequence of characters beginning with a letter and followed
  // by any combination of letters, digits, plus, period, or hyphen.
  return /^([a-z][a-z\d+\-.]*:)?\/\//i.test(url);
}

/**
 * Creates a new URL by combining the specified URLs
 *
 * @param {string} baseURL The base URL
 * @param {string} relativeURL The relative URL
 *
 * @returns {string} The combined URL
 */
function combineURLs(baseURL, relativeURL) {
  return relativeURL
    ? baseURL.replace(/\/?\/$/, '') + '/' + relativeURL.replace(/^\/+/, '')
    : baseURL;
}

/**
 * Creates a new URL by combining the baseURL with the requestedURL,
 * only when the requestedURL is not already an absolute URL.
 * If the requestURL is absolute, this function returns the requestedURL untouched.
 *
 * @param {string} baseURL The base URL
 * @param {string} requestedURL Absolute or relative URL to combine
 *
 * @returns {string} The combined full path
 */
function buildFullPath(baseURL, requestedURL) {
  if (baseURL && !isAbsoluteURL(requestedURL)) {
    return combineURLs(baseURL, requestedURL);
  }
  return requestedURL;
}

var isURLSameOrigin = platform.hasStandardBrowserEnv ?

// Standard browser envs have full support of the APIs needed to test
// whether the request URL is of the same origin as current location.
  (function standardBrowserEnv() {
    const msie = /(msie|trident)/i.test(navigator.userAgent);
    const urlParsingNode = document.createElement('a');
    let originURL;

    /**
    * Parse a URL to discover its components
    *
    * @param {String} url The URL to be parsed
    * @returns {Object}
    */
    function resolveURL(url) {
      let href = url;

      if (msie) {
        // IE needs attribute set twice to normalize properties
        urlParsingNode.setAttribute('href', href);
        href = urlParsingNode.href;
      }

      urlParsingNode.setAttribute('href', href);

      // urlParsingNode provides the UrlUtils interface - http://url.spec.whatwg.org/#urlutils
      return {
        href: urlParsingNode.href,
        protocol: urlParsingNode.protocol ? urlParsingNode.protocol.replace(/:$/, '') : '',
        host: urlParsingNode.host,
        search: urlParsingNode.search ? urlParsingNode.search.replace(/^\?/, '') : '',
        hash: urlParsingNode.hash ? urlParsingNode.hash.replace(/^#/, '') : '',
        hostname: urlParsingNode.hostname,
        port: urlParsingNode.port,
        pathname: (urlParsingNode.pathname.charAt(0) === '/') ?
          urlParsingNode.pathname :
          '/' + urlParsingNode.pathname
      };
    }

    originURL = resolveURL(window.location.href);

    /**
    * Determine if a URL shares the same origin as the current location
    *
    * @param {String} requestURL The URL to test
    * @returns {boolean} True if URL shares the same origin, otherwise false
    */
    return function isURLSameOrigin(requestURL) {
      const parsed = (utils$1.isString(requestURL)) ? resolveURL(requestURL) : requestURL;
      return (parsed.protocol === originURL.protocol &&
          parsed.host === originURL.host);
    };
  })() :

  // Non standard browser envs (web workers, react-native) lack needed support.
  (function nonStandardBrowserEnv() {
    return function isURLSameOrigin() {
      return true;
    };
  })();

function parseProtocol(url) {
  const match = /^([-+\w]{1,25})(:?\/\/|:)/.exec(url);
  return match && match[1] || '';
}

/**
 * Calculate data maxRate
 * @param {Number} [samplesCount= 10]
 * @param {Number} [min= 1000]
 * @returns {Function}
 */
function speedometer(samplesCount, min) {
  samplesCount = samplesCount || 10;
  const bytes = new Array(samplesCount);
  const timestamps = new Array(samplesCount);
  let head = 0;
  let tail = 0;
  let firstSampleTS;

  min = min !== undefined ? min : 1000;

  return function push(chunkLength) {
    const now = Date.now();

    const startedAt = timestamps[tail];

    if (!firstSampleTS) {
      firstSampleTS = now;
    }

    bytes[head] = chunkLength;
    timestamps[head] = now;

    let i = tail;
    let bytesCount = 0;

    while (i !== head) {
      bytesCount += bytes[i++];
      i = i % samplesCount;
    }

    head = (head + 1) % samplesCount;

    if (head === tail) {
      tail = (tail + 1) % samplesCount;
    }

    if (now - firstSampleTS < min) {
      return;
    }

    const passed = startedAt && now - startedAt;

    return passed ? Math.round(bytesCount * 1000 / passed) : undefined;
  };
}

function progressEventReducer(listener, isDownloadStream) {
  let bytesNotified = 0;
  const _speedometer = speedometer(50, 250);

  return e => {
    const loaded = e.loaded;
    const total = e.lengthComputable ? e.total : undefined;
    const progressBytes = loaded - bytesNotified;
    const rate = _speedometer(progressBytes);
    const inRange = loaded <= total;

    bytesNotified = loaded;

    const data = {
      loaded,
      total,
      progress: total ? (loaded / total) : undefined,
      bytes: progressBytes,
      rate: rate ? rate : undefined,
      estimated: rate && total && inRange ? (total - loaded) / rate : undefined,
      event: e
    };

    data[isDownloadStream ? 'download' : 'upload'] = true;

    listener(data);
  };
}

const isXHRAdapterSupported = typeof XMLHttpRequest !== 'undefined';

var xhrAdapter = isXHRAdapterSupported && function (config) {
  return new Promise(function dispatchXhrRequest(resolve, reject) {
    let requestData = config.data;
    const requestHeaders = AxiosHeaders$1.from(config.headers).normalize();
    let {responseType, withXSRFToken} = config;
    let onCanceled;
    function done() {
      if (config.cancelToken) {
        config.cancelToken.unsubscribe(onCanceled);
      }

      if (config.signal) {
        config.signal.removeEventListener('abort', onCanceled);
      }
    }

    let contentType;

    if (utils$1.isFormData(requestData)) {
      if (platform.hasStandardBrowserEnv || platform.hasStandardBrowserWebWorkerEnv) {
        requestHeaders.setContentType(false); // Let the browser set it
      } else if ((contentType = requestHeaders.getContentType()) !== false) {
        // fix semicolon duplication issue for ReactNative FormData implementation
        const [type, ...tokens] = contentType ? contentType.split(';').map(token => token.trim()).filter(Boolean) : [];
        requestHeaders.setContentType([type || 'multipart/form-data', ...tokens].join('; '));
      }
    }

    let request = new XMLHttpRequest();

    // HTTP basic authentication
    if (config.auth) {
      const username = config.auth.username || '';
      const password = config.auth.password ? unescape(encodeURIComponent(config.auth.password)) : '';
      requestHeaders.set('Authorization', 'Basic ' + btoa(username + ':' + password));
    }

    const fullPath = buildFullPath(config.baseURL, config.url);

    request.open(config.method.toUpperCase(), buildURL(fullPath, config.params, config.paramsSerializer), true);

    // Set the request timeout in MS
    request.timeout = config.timeout;

    function onloadend() {
      if (!request) {
        return;
      }
      // Prepare the response
      const responseHeaders = AxiosHeaders$1.from(
        'getAllResponseHeaders' in request && request.getAllResponseHeaders()
      );
      const responseData = !responseType || responseType === 'text' || responseType === 'json' ?
        request.responseText : request.response;
      const response = {
        data: responseData,
        status: request.status,
        statusText: request.statusText,
        headers: responseHeaders,
        config,
        request
      };

      settle(function _resolve(value) {
        resolve(value);
        done();
      }, function _reject(err) {
        reject(err);
        done();
      }, response);

      // Clean up request
      request = null;
    }

    if ('onloadend' in request) {
      // Use onloadend if available
      request.onloadend = onloadend;
    } else {
      // Listen for ready state to emulate onloadend
      request.onreadystatechange = function handleLoad() {
        if (!request || request.readyState !== 4) {
          return;
        }

        // The request errored out and we didn't get a response, this will be
        // handled by onerror instead
        // With one exception: request that using file: protocol, most browsers
        // will return status as 0 even though it's a successful request
        if (request.status === 0 && !(request.responseURL && request.responseURL.indexOf('file:') === 0)) {
          return;
        }
        // readystate handler is calling before onerror or ontimeout handlers,
        // so we should call onloadend on the next 'tick'
        setTimeout(onloadend);
      };
    }

    // Handle browser request cancellation (as opposed to a manual cancellation)
    request.onabort = function handleAbort() {
      if (!request) {
        return;
      }

      reject(new AxiosError('Request aborted', AxiosError.ECONNABORTED, config, request));

      // Clean up request
      request = null;
    };

    // Handle low level network errors
    request.onerror = function handleError() {
      // Real errors are hidden from us by the browser
      // onerror should only fire if it's a network error
      reject(new AxiosError('Network Error', AxiosError.ERR_NETWORK, config, request));

      // Clean up request
      request = null;
    };

    // Handle timeout
    request.ontimeout = function handleTimeout() {
      let timeoutErrorMessage = config.timeout ? 'timeout of ' + config.timeout + 'ms exceeded' : 'timeout exceeded';
      const transitional = config.transitional || transitionalDefaults;
      if (config.timeoutErrorMessage) {
        timeoutErrorMessage = config.timeoutErrorMessage;
      }
      reject(new AxiosError(
        timeoutErrorMessage,
        transitional.clarifyTimeoutError ? AxiosError.ETIMEDOUT : AxiosError.ECONNABORTED,
        config,
        request));

      // Clean up request
      request = null;
    };

    // Add xsrf header
    // This is only done if running in a standard browser environment.
    // Specifically not if we're in a web worker, or react-native.
    if(platform.hasStandardBrowserEnv) {
      withXSRFToken && utils$1.isFunction(withXSRFToken) && (withXSRFToken = withXSRFToken(config));

      if (withXSRFToken || (withXSRFToken !== false && isURLSameOrigin(fullPath))) {
        // Add xsrf header
        const xsrfValue = config.xsrfHeaderName && config.xsrfCookieName && cookies.read(config.xsrfCookieName);

        if (xsrfValue) {
          requestHeaders.set(config.xsrfHeaderName, xsrfValue);
        }
      }
    }

    // Remove Content-Type if data is undefined
    requestData === undefined && requestHeaders.setContentType(null);

    // Add headers to the request
    if ('setRequestHeader' in request) {
      utils$1.forEach(requestHeaders.toJSON(), function setRequestHeader(val, key) {
        request.setRequestHeader(key, val);
      });
    }

    // Add withCredentials to request if needed
    if (!utils$1.isUndefined(config.withCredentials)) {
      request.withCredentials = !!config.withCredentials;
    }

    // Add responseType to request if needed
    if (responseType && responseType !== 'json') {
      request.responseType = config.responseType;
    }

    // Handle progress if needed
    if (typeof config.onDownloadProgress === 'function') {
      request.addEventListener('progress', progressEventReducer(config.onDownloadProgress, true));
    }

    // Not all browsers support upload events
    if (typeof config.onUploadProgress === 'function' && request.upload) {
      request.upload.addEventListener('progress', progressEventReducer(config.onUploadProgress));
    }

    if (config.cancelToken || config.signal) {
      // Handle cancellation
      // eslint-disable-next-line func-names
      onCanceled = cancel => {
        if (!request) {
          return;
        }
        reject(!cancel || cancel.type ? new CanceledError(null, config, request) : cancel);
        request.abort();
        request = null;
      };

      config.cancelToken && config.cancelToken.subscribe(onCanceled);
      if (config.signal) {
        config.signal.aborted ? onCanceled() : config.signal.addEventListener('abort', onCanceled);
      }
    }

    const protocol = parseProtocol(fullPath);

    if (protocol && platform.protocols.indexOf(protocol) === -1) {
      reject(new AxiosError('Unsupported protocol ' + protocol + ':', AxiosError.ERR_BAD_REQUEST, config));
      return;
    }


    // Send the request
    request.send(requestData || null);
  });
};

const knownAdapters = {
  http: httpAdapter,
  xhr: xhrAdapter
};

utils$1.forEach(knownAdapters, (fn, value) => {
  if (fn) {
    try {
      Object.defineProperty(fn, 'name', {value});
    } catch (e) {
      // eslint-disable-next-line no-empty
    }
    Object.defineProperty(fn, 'adapterName', {value});
  }
});

const renderReason = (reason) => `- ${reason}`;

const isResolvedHandle = (adapter) => utils$1.isFunction(adapter) || adapter === null || adapter === false;

var adapters = {
  getAdapter: (adapters) => {
    adapters = utils$1.isArray(adapters) ? adapters : [adapters];

    const {length} = adapters;
    let nameOrAdapter;
    let adapter;

    const rejectedReasons = {};

    for (let i = 0; i < length; i++) {
      nameOrAdapter = adapters[i];
      let id;

      adapter = nameOrAdapter;

      if (!isResolvedHandle(nameOrAdapter)) {
        adapter = knownAdapters[(id = String(nameOrAdapter)).toLowerCase()];

        if (adapter === undefined) {
          throw new AxiosError(`Unknown adapter '${id}'`);
        }
      }

      if (adapter) {
        break;
      }

      rejectedReasons[id || '#' + i] = adapter;
    }

    if (!adapter) {

      const reasons = Object.entries(rejectedReasons)
        .map(([id, state]) => `adapter ${id} ` +
          (state === false ? 'is not supported by the environment' : 'is not available in the build')
        );

      let s = length ?
        (reasons.length > 1 ? 'since :\n' + reasons.map(renderReason).join('\n') : ' ' + renderReason(reasons[0])) :
        'as no adapter specified';

      throw new AxiosError(
        `There is no suitable adapter to dispatch the request ` + s,
        'ERR_NOT_SUPPORT'
      );
    }

    return adapter;
  },
  adapters: knownAdapters
};

/**
 * Throws a `CanceledError` if cancellation has been requested.
 *
 * @param {Object} config The config that is to be used for the request
 *
 * @returns {void}
 */
function throwIfCancellationRequested(config) {
  if (config.cancelToken) {
    config.cancelToken.throwIfRequested();
  }

  if (config.signal && config.signal.aborted) {
    throw new CanceledError(null, config);
  }
}

/**
 * Dispatch a request to the server using the configured adapter.
 *
 * @param {object} config The config that is to be used for the request
 *
 * @returns {Promise} The Promise to be fulfilled
 */
function dispatchRequest(config) {
  throwIfCancellationRequested(config);

  config.headers = AxiosHeaders$1.from(config.headers);

  // Transform request data
  config.data = transformData.call(
    config,
    config.transformRequest
  );

  if (['post', 'put', 'patch'].indexOf(config.method) !== -1) {
    config.headers.setContentType('application/x-www-form-urlencoded', false);
  }

  const adapter = adapters.getAdapter(config.adapter || defaults$1.adapter);

  return adapter(config).then(function onAdapterResolution(response) {
    throwIfCancellationRequested(config);

    // Transform response data
    response.data = transformData.call(
      config,
      config.transformResponse,
      response
    );

    response.headers = AxiosHeaders$1.from(response.headers);

    return response;
  }, function onAdapterRejection(reason) {
    if (!isCancel(reason)) {
      throwIfCancellationRequested(config);

      // Transform response data
      if (reason && reason.response) {
        reason.response.data = transformData.call(
          config,
          config.transformResponse,
          reason.response
        );
        reason.response.headers = AxiosHeaders$1.from(reason.response.headers);
      }
    }

    return Promise.reject(reason);
  });
}

const headersToObject = (thing) => thing instanceof AxiosHeaders$1 ? thing.toJSON() : thing;

/**
 * Config-specific merge-function which creates a new config-object
 * by merging two configuration objects together.
 *
 * @param {Object} config1
 * @param {Object} config2
 *
 * @returns {Object} New object resulting from merging config2 to config1
 */
function mergeConfig(config1, config2) {
  // eslint-disable-next-line no-param-reassign
  config2 = config2 || {};
  const config = {};

  function getMergedValue(target, source, caseless) {
    if (utils$1.isPlainObject(target) && utils$1.isPlainObject(source)) {
      return utils$1.merge.call({caseless}, target, source);
    } else if (utils$1.isPlainObject(source)) {
      return utils$1.merge({}, source);
    } else if (utils$1.isArray(source)) {
      return source.slice();
    }
    return source;
  }

  // eslint-disable-next-line consistent-return
  function mergeDeepProperties(a, b, caseless) {
    if (!utils$1.isUndefined(b)) {
      return getMergedValue(a, b, caseless);
    } else if (!utils$1.isUndefined(a)) {
      return getMergedValue(undefined, a, caseless);
    }
  }

  // eslint-disable-next-line consistent-return
  function valueFromConfig2(a, b) {
    if (!utils$1.isUndefined(b)) {
      return getMergedValue(undefined, b);
    }
  }

  // eslint-disable-next-line consistent-return
  function defaultToConfig2(a, b) {
    if (!utils$1.isUndefined(b)) {
      return getMergedValue(undefined, b);
    } else if (!utils$1.isUndefined(a)) {
      return getMergedValue(undefined, a);
    }
  }

  // eslint-disable-next-line consistent-return
  function mergeDirectKeys(a, b, prop) {
    if (prop in config2) {
      return getMergedValue(a, b);
    } else if (prop in config1) {
      return getMergedValue(undefined, a);
    }
  }

  const mergeMap = {
    url: valueFromConfig2,
    method: valueFromConfig2,
    data: valueFromConfig2,
    baseURL: defaultToConfig2,
    transformRequest: defaultToConfig2,
    transformResponse: defaultToConfig2,
    paramsSerializer: defaultToConfig2,
    timeout: defaultToConfig2,
    timeoutMessage: defaultToConfig2,
    withCredentials: defaultToConfig2,
    withXSRFToken: defaultToConfig2,
    adapter: defaultToConfig2,
    responseType: defaultToConfig2,
    xsrfCookieName: defaultToConfig2,
    xsrfHeaderName: defaultToConfig2,
    onUploadProgress: defaultToConfig2,
    onDownloadProgress: defaultToConfig2,
    decompress: defaultToConfig2,
    maxContentLength: defaultToConfig2,
    maxBodyLength: defaultToConfig2,
    beforeRedirect: defaultToConfig2,
    transport: defaultToConfig2,
    httpAgent: defaultToConfig2,
    httpsAgent: defaultToConfig2,
    cancelToken: defaultToConfig2,
    socketPath: defaultToConfig2,
    responseEncoding: defaultToConfig2,
    validateStatus: mergeDirectKeys,
    headers: (a, b) => mergeDeepProperties(headersToObject(a), headersToObject(b), true)
  };

  utils$1.forEach(Object.keys(Object.assign({}, config1, config2)), function computeConfigValue(prop) {
    const merge = mergeMap[prop] || mergeDeepProperties;
    const configValue = merge(config1[prop], config2[prop], prop);
    (utils$1.isUndefined(configValue) && merge !== mergeDirectKeys) || (config[prop] = configValue);
  });

  return config;
}

const VERSION = "1.6.5";

const validators$1 = {};

// eslint-disable-next-line func-names
['object', 'boolean', 'number', 'function', 'string', 'symbol'].forEach((type, i) => {
  validators$1[type] = function validator(thing) {
    return typeof thing === type || 'a' + (i < 1 ? 'n ' : ' ') + type;
  };
});

const deprecatedWarnings = {};

/**
 * Transitional option validator
 *
 * @param {function|boolean?} validator - set to false if the transitional option has been removed
 * @param {string?} version - deprecated version / removed since version
 * @param {string?} message - some message with additional info
 *
 * @returns {function}
 */
validators$1.transitional = function transitional(validator, version, message) {
  function formatMessage(opt, desc) {
    return '[Axios v' + VERSION + '] Transitional option \'' + opt + '\'' + desc + (message ? '. ' + message : '');
  }

  // eslint-disable-next-line func-names
  return (value, opt, opts) => {
    if (validator === false) {
      throw new AxiosError(
        formatMessage(opt, ' has been removed' + (version ? ' in ' + version : '')),
        AxiosError.ERR_DEPRECATED
      );
    }

    if (version && !deprecatedWarnings[opt]) {
      deprecatedWarnings[opt] = true;
      // eslint-disable-next-line no-console
      console.warn(
        formatMessage(
          opt,
          ' has been deprecated since v' + version + ' and will be removed in the near future'
        )
      );
    }

    return validator ? validator(value, opt, opts) : true;
  };
};

/**
 * Assert object's properties type
 *
 * @param {object} options
 * @param {object} schema
 * @param {boolean?} allowUnknown
 *
 * @returns {object}
 */

function assertOptions(options, schema, allowUnknown) {
  if (typeof options !== 'object') {
    throw new AxiosError('options must be an object', AxiosError.ERR_BAD_OPTION_VALUE);
  }
  const keys = Object.keys(options);
  let i = keys.length;
  while (i-- > 0) {
    const opt = keys[i];
    const validator = schema[opt];
    if (validator) {
      const value = options[opt];
      const result = value === undefined || validator(value, opt, options);
      if (result !== true) {
        throw new AxiosError('option ' + opt + ' must be ' + result, AxiosError.ERR_BAD_OPTION_VALUE);
      }
      continue;
    }
    if (allowUnknown !== true) {
      throw new AxiosError('Unknown option ' + opt, AxiosError.ERR_BAD_OPTION);
    }
  }
}

var validator = {
  assertOptions,
  validators: validators$1
};

const validators = validator.validators;

/**
 * Create a new instance of Axios
 *
 * @param {Object} instanceConfig The default config for the instance
 *
 * @return {Axios} A new instance of Axios
 */
class Axios {
  constructor(instanceConfig) {
    this.defaults = instanceConfig;
    this.interceptors = {
      request: new InterceptorManager$1(),
      response: new InterceptorManager$1()
    };
  }

  /**
   * Dispatch a request
   *
   * @param {String|Object} configOrUrl The config specific for this request (merged with this.defaults)
   * @param {?Object} config
   *
   * @returns {Promise} The Promise to be fulfilled
   */
  request(configOrUrl, config) {
    /*eslint no-param-reassign:0*/
    // Allow for axios('example/url'[, config]) a la fetch API
    if (typeof configOrUrl === 'string') {
      config = config || {};
      config.url = configOrUrl;
    } else {
      config = configOrUrl || {};
    }

    config = mergeConfig(this.defaults, config);

    const {transitional, paramsSerializer, headers} = config;

    if (transitional !== undefined) {
      validator.assertOptions(transitional, {
        silentJSONParsing: validators.transitional(validators.boolean),
        forcedJSONParsing: validators.transitional(validators.boolean),
        clarifyTimeoutError: validators.transitional(validators.boolean)
      }, false);
    }

    if (paramsSerializer != null) {
      if (utils$1.isFunction(paramsSerializer)) {
        config.paramsSerializer = {
          serialize: paramsSerializer
        };
      } else {
        validator.assertOptions(paramsSerializer, {
          encode: validators.function,
          serialize: validators.function
        }, true);
      }
    }

    // Set config.method
    config.method = (config.method || this.defaults.method || 'get').toLowerCase();

    // Flatten headers
    let contextHeaders = headers && utils$1.merge(
      headers.common,
      headers[config.method]
    );

    headers && utils$1.forEach(
      ['delete', 'get', 'head', 'post', 'put', 'patch', 'common'],
      (method) => {
        delete headers[method];
      }
    );

    config.headers = AxiosHeaders$1.concat(contextHeaders, headers);

    // filter out skipped interceptors
    const requestInterceptorChain = [];
    let synchronousRequestInterceptors = true;
    this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
      if (typeof interceptor.runWhen === 'function' && interceptor.runWhen(config) === false) {
        return;
      }

      synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;

      requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);
    });

    const responseInterceptorChain = [];
    this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
      responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
    });

    let promise;
    let i = 0;
    let len;

    if (!synchronousRequestInterceptors) {
      const chain = [dispatchRequest.bind(this), undefined];
      chain.unshift.apply(chain, requestInterceptorChain);
      chain.push.apply(chain, responseInterceptorChain);
      len = chain.length;

      promise = Promise.resolve(config);

      while (i < len) {
        promise = promise.then(chain[i++], chain[i++]);
      }

      return promise;
    }

    len = requestInterceptorChain.length;

    let newConfig = config;

    i = 0;

    while (i < len) {
      const onFulfilled = requestInterceptorChain[i++];
      const onRejected = requestInterceptorChain[i++];
      try {
        newConfig = onFulfilled(newConfig);
      } catch (error) {
        onRejected.call(this, error);
        break;
      }
    }

    try {
      promise = dispatchRequest.call(this, newConfig);
    } catch (error) {
      return Promise.reject(error);
    }

    i = 0;
    len = responseInterceptorChain.length;

    while (i < len) {
      promise = promise.then(responseInterceptorChain[i++], responseInterceptorChain[i++]);
    }

    return promise;
  }

  getUri(config) {
    config = mergeConfig(this.defaults, config);
    const fullPath = buildFullPath(config.baseURL, config.url);
    return buildURL(fullPath, config.params, config.paramsSerializer);
  }
}

// Provide aliases for supported request methods
utils$1.forEach(['delete', 'get', 'head', 'options'], function forEachMethodNoData(method) {
  /*eslint func-names:0*/
  Axios.prototype[method] = function(url, config) {
    return this.request(mergeConfig(config || {}, {
      method,
      url,
      data: (config || {}).data
    }));
  };
});

utils$1.forEach(['post', 'put', 'patch'], function forEachMethodWithData(method) {
  /*eslint func-names:0*/

  function generateHTTPMethod(isForm) {
    return function httpMethod(url, data, config) {
      return this.request(mergeConfig(config || {}, {
        method,
        headers: isForm ? {
          'Content-Type': 'multipart/form-data'
        } : {},
        url,
        data
      }));
    };
  }

  Axios.prototype[method] = generateHTTPMethod();

  Axios.prototype[method + 'Form'] = generateHTTPMethod(true);
});

var Axios$1 = Axios;

/**
 * A `CancelToken` is an object that can be used to request cancellation of an operation.
 *
 * @param {Function} executor The executor function.
 *
 * @returns {CancelToken}
 */
class CancelToken {
  constructor(executor) {
    if (typeof executor !== 'function') {
      throw new TypeError('executor must be a function.');
    }

    let resolvePromise;

    this.promise = new Promise(function promiseExecutor(resolve) {
      resolvePromise = resolve;
    });

    const token = this;

    // eslint-disable-next-line func-names
    this.promise.then(cancel => {
      if (!token._listeners) return;

      let i = token._listeners.length;

      while (i-- > 0) {
        token._listeners[i](cancel);
      }
      token._listeners = null;
    });

    // eslint-disable-next-line func-names
    this.promise.then = onfulfilled => {
      let _resolve;
      // eslint-disable-next-line func-names
      const promise = new Promise(resolve => {
        token.subscribe(resolve);
        _resolve = resolve;
      }).then(onfulfilled);

      promise.cancel = function reject() {
        token.unsubscribe(_resolve);
      };

      return promise;
    };

    executor(function cancel(message, config, request) {
      if (token.reason) {
        // Cancellation has already been requested
        return;
      }

      token.reason = new CanceledError(message, config, request);
      resolvePromise(token.reason);
    });
  }

  /**
   * Throws a `CanceledError` if cancellation has been requested.
   */
  throwIfRequested() {
    if (this.reason) {
      throw this.reason;
    }
  }

  /**
   * Subscribe to the cancel signal
   */

  subscribe(listener) {
    if (this.reason) {
      listener(this.reason);
      return;
    }

    if (this._listeners) {
      this._listeners.push(listener);
    } else {
      this._listeners = [listener];
    }
  }

  /**
   * Unsubscribe from the cancel signal
   */

  unsubscribe(listener) {
    if (!this._listeners) {
      return;
    }
    const index = this._listeners.indexOf(listener);
    if (index !== -1) {
      this._listeners.splice(index, 1);
    }
  }

  /**
   * Returns an object that contains a new `CancelToken` and a function that, when called,
   * cancels the `CancelToken`.
   */
  static source() {
    let cancel;
    const token = new CancelToken(function executor(c) {
      cancel = c;
    });
    return {
      token,
      cancel
    };
  }
}

var CancelToken$1 = CancelToken;

/**
 * Syntactic sugar for invoking a function and expanding an array for arguments.
 *
 * Common use case would be to use `Function.prototype.apply`.
 *
 *  ```js
 *  function f(x, y, z) {}
 *  var args = [1, 2, 3];
 *  f.apply(null, args);
 *  ```
 *
 * With `spread` this example can be re-written.
 *
 *  ```js
 *  spread(function(x, y, z) {})([1, 2, 3]);
 *  ```
 *
 * @param {Function} callback
 *
 * @returns {Function}
 */
function spread(callback) {
  return function wrap(arr) {
    return callback.apply(null, arr);
  };
}

/**
 * Determines whether the payload is an error thrown by Axios
 *
 * @param {*} payload The value to test
 *
 * @returns {boolean} True if the payload is an error thrown by Axios, otherwise false
 */
function isAxiosError(payload) {
  return utils$1.isObject(payload) && (payload.isAxiosError === true);
}

const HttpStatusCode = {
  Continue: 100,
  SwitchingProtocols: 101,
  Processing: 102,
  EarlyHints: 103,
  Ok: 200,
  Created: 201,
  Accepted: 202,
  NonAuthoritativeInformation: 203,
  NoContent: 204,
  ResetContent: 205,
  PartialContent: 206,
  MultiStatus: 207,
  AlreadyReported: 208,
  ImUsed: 226,
  MultipleChoices: 300,
  MovedPermanently: 301,
  Found: 302,
  SeeOther: 303,
  NotModified: 304,
  UseProxy: 305,
  Unused: 306,
  TemporaryRedirect: 307,
  PermanentRedirect: 308,
  BadRequest: 400,
  Unauthorized: 401,
  PaymentRequired: 402,
  Forbidden: 403,
  NotFound: 404,
  MethodNotAllowed: 405,
  NotAcceptable: 406,
  ProxyAuthenticationRequired: 407,
  RequestTimeout: 408,
  Conflict: 409,
  Gone: 410,
  LengthRequired: 411,
  PreconditionFailed: 412,
  PayloadTooLarge: 413,
  UriTooLong: 414,
  UnsupportedMediaType: 415,
  RangeNotSatisfiable: 416,
  ExpectationFailed: 417,
  ImATeapot: 418,
  MisdirectedRequest: 421,
  UnprocessableEntity: 422,
  Locked: 423,
  FailedDependency: 424,
  TooEarly: 425,
  UpgradeRequired: 426,
  PreconditionRequired: 428,
  TooManyRequests: 429,
  RequestHeaderFieldsTooLarge: 431,
  UnavailableForLegalReasons: 451,
  InternalServerError: 500,
  NotImplemented: 501,
  BadGateway: 502,
  ServiceUnavailable: 503,
  GatewayTimeout: 504,
  HttpVersionNotSupported: 505,
  VariantAlsoNegotiates: 506,
  InsufficientStorage: 507,
  LoopDetected: 508,
  NotExtended: 510,
  NetworkAuthenticationRequired: 511,
};

Object.entries(HttpStatusCode).forEach(([key, value]) => {
  HttpStatusCode[value] = key;
});

var HttpStatusCode$1 = HttpStatusCode;

/**
 * Create an instance of Axios
 *
 * @param {Object} defaultConfig The default config for the instance
 *
 * @returns {Axios} A new instance of Axios
 */
function createInstance(defaultConfig) {
  const context = new Axios$1(defaultConfig);
  const instance = bind(Axios$1.prototype.request, context);

  // Copy axios.prototype to instance
  utils$1.extend(instance, Axios$1.prototype, context, {allOwnKeys: true});

  // Copy context to instance
  utils$1.extend(instance, context, null, {allOwnKeys: true});

  // Factory for creating new instances
  instance.create = function create(instanceConfig) {
    return createInstance(mergeConfig(defaultConfig, instanceConfig));
  };

  return instance;
}

// Create the default instance to be exported
const axios = createInstance(defaults$1);

// Expose Axios class to allow class inheritance
axios.Axios = Axios$1;

// Expose Cancel & CancelToken
axios.CanceledError = CanceledError;
axios.CancelToken = CancelToken$1;
axios.isCancel = isCancel;
axios.VERSION = VERSION;
axios.toFormData = toFormData;

// Expose AxiosError class
axios.AxiosError = AxiosError;

// alias for CanceledError for backward compatibility
axios.Cancel = axios.CanceledError;

// Expose all/spread
axios.all = function all(promises) {
  return Promise.all(promises);
};

axios.spread = spread;

// Expose isAxiosError
axios.isAxiosError = isAxiosError;

// Expose mergeConfig
axios.mergeConfig = mergeConfig;

axios.AxiosHeaders = AxiosHeaders$1;

axios.formToJSON = thing => formDataToJSON(utils$1.isHTMLForm(thing) ? new FormData(thing) : thing);

axios.getAdapter = adapters.getAdapter;

axios.HttpStatusCode = HttpStatusCode$1;

axios.default = axios;

module.exports = axios;
//# sourceMappingURL=axios.cjs.map


/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			id: moduleId,
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = __webpack_modules__;
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/create fake namespace object */
/******/ 	!function() {
/******/ 		var getProto = Object.getPrototypeOf ? function(obj) { return Object.getPrototypeOf(obj); } : function(obj) { return obj.__proto__; };
/******/ 		var leafPrototypes;
/******/ 		// create a fake namespace object
/******/ 		// mode & 1: value is a module id, require it
/******/ 		// mode & 2: merge all properties of value into the ns
/******/ 		// mode & 4: return value when already ns object
/******/ 		// mode & 16: return value when it's Promise-like
/******/ 		// mode & 8|1: behave like require
/******/ 		__webpack_require__.t = function(value, mode) {
/******/ 			if(mode & 1) value = this(value);
/******/ 			if(mode & 8) return value;
/******/ 			if(typeof value === 'object' && value) {
/******/ 				if((mode & 4) && value.__esModule) return value;
/******/ 				if((mode & 16) && typeof value.then === 'function') return value;
/******/ 			}
/******/ 			var ns = Object.create(null);
/******/ 			__webpack_require__.r(ns);
/******/ 			var def = {};
/******/ 			leafPrototypes = leafPrototypes || [null, getProto({}), getProto([]), getProto(getProto)];
/******/ 			for(var current = mode & 2 && value; typeof current == 'object' && !~leafPrototypes.indexOf(current); current = getProto(current)) {
/******/ 				Object.getOwnPropertyNames(current).forEach(function(key) { def[key] = function() { return value[key]; }; });
/******/ 			}
/******/ 			def['default'] = function() { return value; };
/******/ 			__webpack_require__.d(ns, def);
/******/ 			return ns;
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	!function() {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = function(exports, definition) {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/ensure chunk */
/******/ 	!function() {
/******/ 		__webpack_require__.f = {};
/******/ 		// This file contains only the entry chunk.
/******/ 		// The chunk loading function for additional chunks
/******/ 		__webpack_require__.e = function(chunkId) {
/******/ 			return Promise.all(Object.keys(__webpack_require__.f).reduce(function(promises, key) {
/******/ 				__webpack_require__.f[key](chunkId, promises);
/******/ 				return promises;
/******/ 			}, []));
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/get javascript chunk filename */
/******/ 	!function() {
/******/ 		// This function allow to reference async chunks
/******/ 		__webpack_require__.u = function(chunkId) {
/******/ 			// return url for filenames based on template
/******/ 			return "" + chunkId + ".bundle.js";
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/global */
/******/ 	!function() {
/******/ 		__webpack_require__.g = (function() {
/******/ 			if (typeof globalThis === 'object') return globalThis;
/******/ 			try {
/******/ 				return this || new Function('return this')();
/******/ 			} catch (e) {
/******/ 				if (typeof window === 'object') return window;
/******/ 			}
/******/ 		})();
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	!function() {
/******/ 		__webpack_require__.o = function(obj, prop) { return Object.prototype.hasOwnProperty.call(obj, prop); }
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/load script */
/******/ 	!function() {
/******/ 		var inProgress = {};
/******/ 		var dataWebpackPrefix = "star-citizen-universe-client:";
/******/ 		// loadScript function to load a script via script tag
/******/ 		__webpack_require__.l = function(url, done, key, chunkId) {
/******/ 			if(inProgress[url]) { inProgress[url].push(done); return; }
/******/ 			var script, needAttach;
/******/ 			if(key !== undefined) {
/******/ 				var scripts = document.getElementsByTagName("script");
/******/ 				for(var i = 0; i < scripts.length; i++) {
/******/ 					var s = scripts[i];
/******/ 					if(s.getAttribute("src") == url || s.getAttribute("data-webpack") == dataWebpackPrefix + key) { script = s; break; }
/******/ 				}
/******/ 			}
/******/ 			if(!script) {
/******/ 				needAttach = true;
/******/ 				script = document.createElement('script');
/******/ 		
/******/ 				script.charset = 'utf-8';
/******/ 				script.timeout = 120;
/******/ 				if (__webpack_require__.nc) {
/******/ 					script.setAttribute("nonce", __webpack_require__.nc);
/******/ 				}
/******/ 				script.setAttribute("data-webpack", dataWebpackPrefix + key);
/******/ 		
/******/ 				script.src = url;
/******/ 			}
/******/ 			inProgress[url] = [done];
/******/ 			var onScriptComplete = function(prev, event) {
/******/ 				// avoid mem leaks in IE.
/******/ 				script.onerror = script.onload = null;
/******/ 				clearTimeout(timeout);
/******/ 				var doneFns = inProgress[url];
/******/ 				delete inProgress[url];
/******/ 				script.parentNode && script.parentNode.removeChild(script);
/******/ 				doneFns && doneFns.forEach(function(fn) { return fn(event); });
/******/ 				if(prev) return prev(event);
/******/ 			}
/******/ 			var timeout = setTimeout(onScriptComplete.bind(null, undefined, { type: 'timeout', target: script }), 120000);
/******/ 			script.onerror = onScriptComplete.bind(null, script.onerror);
/******/ 			script.onload = onScriptComplete.bind(null, script.onload);
/******/ 			needAttach && document.head.appendChild(script);
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	!function() {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = function(exports) {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/publicPath */
/******/ 	!function() {
/******/ 		var scriptUrl;
/******/ 		if (__webpack_require__.g.importScripts) scriptUrl = __webpack_require__.g.location + "";
/******/ 		var document = __webpack_require__.g.document;
/******/ 		if (!scriptUrl && document) {
/******/ 			if (document.currentScript)
/******/ 				scriptUrl = document.currentScript.src;
/******/ 			if (!scriptUrl) {
/******/ 				var scripts = document.getElementsByTagName("script");
/******/ 				if(scripts.length) {
/******/ 					var i = scripts.length - 1;
/******/ 					while (i > -1 && !scriptUrl) scriptUrl = scripts[i--].src;
/******/ 				}
/******/ 			}
/******/ 		}
/******/ 		// When supporting browsers where an automatic publicPath is not supported you must specify an output.publicPath manually via configuration
/******/ 		// or pass an empty string ("") and set the __webpack_public_path__ variable from your code to use your own logic.
/******/ 		if (!scriptUrl) throw new Error("Automatic publicPath is not supported in this browser");
/******/ 		scriptUrl = scriptUrl.replace(/#.*$/, "").replace(/\?.*$/, "").replace(/\/[^\/]+$/, "/");
/******/ 		__webpack_require__.p = scriptUrl;
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/jsonp chunk loading */
/******/ 	!function() {
/******/ 		// no baseURI
/******/ 		
/******/ 		// object to store loaded and loading chunks
/******/ 		// undefined = chunk not loaded, null = chunk preloaded/prefetched
/******/ 		// [resolve, reject, Promise] = chunk loading, 0 = chunk loaded
/******/ 		var installedChunks = {
/******/ 			179: 0
/******/ 		};
/******/ 		
/******/ 		__webpack_require__.f.j = function(chunkId, promises) {
/******/ 				// JSONP chunk loading for javascript
/******/ 				var installedChunkData = __webpack_require__.o(installedChunks, chunkId) ? installedChunks[chunkId] : undefined;
/******/ 				if(installedChunkData !== 0) { // 0 means "already installed".
/******/ 		
/******/ 					// a Promise means "currently loading".
/******/ 					if(installedChunkData) {
/******/ 						promises.push(installedChunkData[2]);
/******/ 					} else {
/******/ 						if(true) { // all chunks have JS
/******/ 							// setup Promise in chunk cache
/******/ 							var promise = new Promise(function(resolve, reject) { installedChunkData = installedChunks[chunkId] = [resolve, reject]; });
/******/ 							promises.push(installedChunkData[2] = promise);
/******/ 		
/******/ 							// start chunk loading
/******/ 							var url = __webpack_require__.p + __webpack_require__.u(chunkId);
/******/ 							// create error before stack unwound to get useful stacktrace later
/******/ 							var error = new Error();
/******/ 							var loadingEnded = function(event) {
/******/ 								if(__webpack_require__.o(installedChunks, chunkId)) {
/******/ 									installedChunkData = installedChunks[chunkId];
/******/ 									if(installedChunkData !== 0) installedChunks[chunkId] = undefined;
/******/ 									if(installedChunkData) {
/******/ 										var errorType = event && (event.type === 'load' ? 'missing' : event.type);
/******/ 										var realSrc = event && event.target && event.target.src;
/******/ 										error.message = 'Loading chunk ' + chunkId + ' failed.\n(' + errorType + ': ' + realSrc + ')';
/******/ 										error.name = 'ChunkLoadError';
/******/ 										error.type = errorType;
/******/ 										error.request = realSrc;
/******/ 										installedChunkData[1](error);
/******/ 									}
/******/ 								}
/******/ 							};
/******/ 							__webpack_require__.l(url, loadingEnded, "chunk-" + chunkId, chunkId);
/******/ 						}
/******/ 					}
/******/ 				}
/******/ 		};
/******/ 		
/******/ 		// no prefetching
/******/ 		
/******/ 		// no preloaded
/******/ 		
/******/ 		// no HMR
/******/ 		
/******/ 		// no HMR manifest
/******/ 		
/******/ 		// no on chunks loaded
/******/ 		
/******/ 		// install a JSONP callback for chunk loading
/******/ 		var webpackJsonpCallback = function(parentChunkLoadingFunction, data) {
/******/ 			var chunkIds = data[0];
/******/ 			var moreModules = data[1];
/******/ 			var runtime = data[2];
/******/ 			// add "moreModules" to the modules object,
/******/ 			// then flag all "chunkIds" as loaded and fire callback
/******/ 			var moduleId, chunkId, i = 0;
/******/ 			if(chunkIds.some(function(id) { return installedChunks[id] !== 0; })) {
/******/ 				for(moduleId in moreModules) {
/******/ 					if(__webpack_require__.o(moreModules, moduleId)) {
/******/ 						__webpack_require__.m[moduleId] = moreModules[moduleId];
/******/ 					}
/******/ 				}
/******/ 				if(runtime) var result = runtime(__webpack_require__);
/******/ 			}
/******/ 			if(parentChunkLoadingFunction) parentChunkLoadingFunction(data);
/******/ 			for(;i < chunkIds.length; i++) {
/******/ 				chunkId = chunkIds[i];
/******/ 				if(__webpack_require__.o(installedChunks, chunkId) && installedChunks[chunkId]) {
/******/ 					installedChunks[chunkId][0]();
/******/ 				}
/******/ 				installedChunks[chunkId] = 0;
/******/ 			}
/******/ 		
/******/ 		}
/******/ 		
/******/ 		var chunkLoadingGlobal = self["webpackChunkstar_citizen_universe_client"] = self["webpackChunkstar_citizen_universe_client"] || [];
/******/ 		chunkLoadingGlobal.forEach(webpackJsonpCallback.bind(null, 0));
/******/ 		chunkLoadingGlobal.push = webpackJsonpCallback.bind(null, chunkLoadingGlobal.push.bind(chunkLoadingGlobal));
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/nonce */
/******/ 	!function() {
/******/ 		__webpack_require__.nc = undefined;
/******/ 	}();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be isolated against other modules in the chunk.
!function() {
__webpack_require__(39227)
__webpack_require__(58790)
const App = __webpack_require__(18739)
const { auth } = __webpack_require__(14498)
const { navigator } = __webpack_require__(14498)
const { emptyLayoutRouter, mainLayoutRouter } = __webpack_require__(58790)

const start = async () => {
  console.info('Starting app')
  const app = new App()
  document.body.appendChild(app)

  app.router.use(emptyLayoutRouter)
  app.router.use(mainLayoutRouter)
  app.router.use((req, res)=>{
    res.navigate('/code-30000', true)
  })
  navigator.use(app.router)
  await auth.getMe()
  await app.start()
  await navigator.start()
}

start()
}();
/******/ })()
;
//# sourceMappingURL=main.bundle.js.map
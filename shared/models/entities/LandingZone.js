const mixer = require('core/mixer')
const Entity = require('./Entity')

module.exports = class LandingZone extends Entity {

}
  .define({
    name: 'landingZone',
    pluralName: 'landingZones'
  })
  .properties()